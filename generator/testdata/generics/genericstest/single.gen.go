// Code generated by testkit builder. DO NOT EDIT.
// Source: testkit builder -o genericstest/single.gen.go Container

// This file holds fluent builders for test fixtures. Each builder
// follows the same contract:
//
//   - New<Type>()       creates a builder seeded with defaults (when
//     a <Type>Defaults factory or //testkit:default
//     directives are present) or zero values.
//   - New<Type>From(v)  creates a builder seeded with v.
//   - With<Field>(v)    sets a field; chains.
//   - Append<Field>(v)  appends to a slice field (slice fields only).
//   - With<Field>Entry(k, v) adds a map entry (map fields only).
//   - Mutate(fn)        applies fn to the value under construction.
//   - Clone()           returns a deep copy (slice/map/bytes deep-copied).
//   - Build()           returns the constructed value.
package genericstest

import (
	"go.thesmos.sh/testkit/generator/testdata/generics"
)

// ContainerBuilder constructs [generics.Container[T]] values for tests.
// Start with [NewContainer] or [NewContainerFrom], chain With*
// setters, then call [ContainerBuilder.Build].
type ContainerBuilder[T any] struct {
	v generics.Container[T]
}

// NewContainer creates a builder with zero
// values.
func NewContainer[T any]() *ContainerBuilder[T] {
	return &ContainerBuilder[T]{}
}

// NewContainerFrom creates a builder seeded with v. Use when the
// test starts from a known value rather than defaults — e.g. for
// round-trip / clone scenarios.
func NewContainerFrom[T any](v generics.Container[T]) *ContainerBuilder[T] {
	return &ContainerBuilder[T]{v: v}
}

// WithItems replaces the Items slice (variadic). The
// second call overwrites the first — use AppendItems to extend.
func (b *ContainerBuilder[T]) WithItems(v ...T) *ContainerBuilder[T] {
	b.v.Items = v
	return b
}

// AppendItems appends to the Items slice, preserving
// any previously-set elements.
func (b *ContainerBuilder[T]) AppendItems(v ...T) *ContainerBuilder[T] {
	b.v.Items = append(b.v.Items, v...)
	return b
}

// WithLabel sets the Label field.
func (b *ContainerBuilder[T]) WithLabel(v string) *ContainerBuilder[T] {
	b.v.Label = v
	return b
}

// WithLimit sets the Limit field.
func (b *ContainerBuilder[T]) WithLimit(v int) *ContainerBuilder[T] {
	b.v.Limit = v
	return b
}

// Mutate applies fn to the value under construction. Use for
// deeply-nested fields or mutations that don't warrant a dedicated
// setter.
func (b *ContainerBuilder[T]) Mutate(fn func(*generics.Container[T])) *ContainerBuilder[T] {
	fn(&b.v)
	return b
}

// Clone returns a deep copy of the builder. Slice, map, and []byte
// fields are copied so mutations to the clone don't bleed into the
// original — scalar and struct-value fields use Go's default copy
// semantics.
func (b *ContainerBuilder[T]) Clone() *ContainerBuilder[T] {
	out := &ContainerBuilder[T]{v: b.v}
	if b.v.Items != nil {
		out.v.Items = append([]T(nil), b.v.Items...)
	}
	return out
}

// Build returns the constructed [generics.Container[T]].
func (b *ContainerBuilder[T]) Build() generics.Container[T] {
	return b.v
}
