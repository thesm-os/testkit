// Code generated by testkit builder. DO NOT EDIT.
// Source: testkit builder -o structstest/nested.gen.go Order Holder

// This file holds fluent builders for test fixtures. Each builder
// follows the same contract:
//
//   - New<Type>()       creates a builder seeded with defaults (when
//     a <Type>Defaults factory or //testkit:default
//     directives are present) or zero values.
//   - New<Type>From(v)  creates a builder seeded with v.
//   - With<Field>(v)    sets a field; chains.
//   - Append<Field>(v)  appends to a slice field (slice fields only).
//   - With<Field>Entry(k, v) adds a map entry (map fields only).
//   - Mutate(fn)        applies fn to the value under construction.
//   - Clone()           returns a deep copy (slice/map/bytes deep-copied).
//   - Build()           returns the constructed value.
package structstest_test

import (
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/generator/testdata/structs"
	"go.thesmos.sh/testkit/generator/testdata/structs/structstest"
)

// TestOrderBuilder verifies the OrderBuilder contract
// against the generated builder: constructor seeding and every
// per-field setter, plus Mutate / Clone independence
// asserted via ID, plus a
// zero-value round-trip via [NewOrderFrom].
func TestOrderBuilder(t *testing.T) {
	t.Parallel()

	t.Run("NewOrder builds without panic", func(t *testing.T) {
		t.Parallel()
		_ = structstest.NewOrder().Build()
	})

	t.Run("NewOrderFrom zero value round-trips", func(t *testing.T) {
		t.Parallel()
		got := structstest.NewOrderFrom(structs.Order{}).Build()
		testkit.Equal(t, got, structs.Order{}, "must return zero value")
	})

	t.Run("WithBilling sets field", func(t *testing.T) {
		t.Parallel()
		got := structstest.NewOrderFrom(structs.Order{}).
			WithBilling(structs.Address{Street: "test-street"}).
			Build()
		testkit.Equal(t, got.Billing, structs.Address{Street: "test-street"}, "must set Billing")
	})

	t.Run("WithCustomer sets field", func(t *testing.T) {
		t.Parallel()
		got := structstest.NewOrderFrom(structs.Order{}).
			WithCustomer(&structs.Customer{Name: "test-name"}).
			Build()
		testkit.Equal(t, got.Customer, &structs.Customer{Name: "test-name"}, "must set Customer")
	})

	t.Run("WithID sets field", func(t *testing.T) {
		t.Parallel()
		got := structstest.NewOrderFrom(structs.Order{}).
			WithID("test-id").
			Build()
		testkit.Equal(t, got.ID, "test-id", "must set ID")
	})

	t.Run("WithMetadata sets field", func(t *testing.T) {
		t.Parallel()
		got := structstest.NewOrderFrom(structs.Order{}).
			WithMetadata(structs.Metadata{Source: "test-source"}).
			Build()
		testkit.Equal(t, got.Metadata, structs.Metadata{Source: "test-source"}, "must set Metadata")
	})

	t.Run("WithPriority sets field", func(t *testing.T) {
		t.Parallel()
		got := structstest.NewOrderFrom(structs.Order{}).
			WithPriority(42).
			Build()
		testkit.Equal(t, got.Priority, 42, "must set Priority")
	})

	t.Run("WithShipping sets field", func(t *testing.T) {
		t.Parallel()
		got := structstest.NewOrderFrom(structs.Order{}).
			WithShipping(structs.Address{Street: "test-street"}).
			Build()
		testkit.Equal(t, got.Shipping, structs.Address{Street: "test-street"}, "must set Shipping")
	})

	t.Run("Clone forks independent scalar", func(t *testing.T) {
		t.Parallel()
		base := structstest.NewOrderFrom(structs.Order{})
		clone := base.Clone().WithID("test-id")
		original := base.Build()
		modified := clone.Build()
		testkit.NotEqual(t, original.ID, modified.ID,
			"Clone must produce independent copy")
	})

	t.Run("Mutate modifies value", func(t *testing.T) {
		t.Parallel()
		got := structstest.NewOrderFrom(structs.Order{}).
			Mutate(func(v *structs.Order) {
				v.ID = "test-id"
			}).
			Build()
		testkit.Equal(t, got.ID, "test-id", "Mutate must modify")
	})
}

// TestHolderBuilder verifies the HolderBuilder contract
// against the generated builder: constructor seeding and every
// per-field setter, plus a
// zero-value round-trip via [NewHolderFrom].
func TestHolderBuilder(t *testing.T) {
	t.Parallel()

	t.Run("NewHolder builds without panic", func(t *testing.T) {
		t.Parallel()
		_ = structstest.NewHolder().Build()
	})

	t.Run("NewHolderFrom zero value round-trips", func(t *testing.T) {
		t.Parallel()
		got := structstest.NewHolderFrom(structs.Holder{}).Build()
		testkit.Equal(t, got, structs.Holder{}, "must return zero value")
	})

	t.Run("WithDone sets field", func(t *testing.T) {
		t.Parallel()
		got := structstest.NewHolderFrom(structs.Holder{}).
			WithDone(nil).
			Build()
		testkit.Equal(t, got.Done, nil, "must set Done")
	})

	t.Run("WithOnFail sets field", func(t *testing.T) {
		t.Parallel()
		got := structstest.NewHolderFrom(structs.Holder{}).
			WithOnFail(nil).
			Build()
		testkit.Equal(t, got.OnFail, nil, "must set OnFail")
	})

	t.Run("WithStringer sets field", func(t *testing.T) {
		t.Parallel()
		got := structstest.NewHolderFrom(structs.Holder{}).
			WithStringer(nil).
			Build()
		testkit.Equal(t, got.Stringer, nil, "must set Stringer")
	})

	// Smoke coverage when no basic-comparable field is available
	// for an independence assertion. Clone and Mutate still execute
	// their bodies — any panic in the deep-copy / callback machinery
	// surfaces here.
	t.Run("Clone returns a usable builder", func(t *testing.T) {
		t.Parallel()
		_ = structstest.NewHolderFrom(structs.Holder{}).Clone().Build()
	})

	t.Run("Mutate is callable", func(t *testing.T) {
		t.Parallel()
		_ = structstest.NewHolderFrom(structs.Holder{}).
			Mutate(func(*structs.Holder) {}).Build()
	})
}
