// Code generated by testkit builder. DO NOT EDIT.
// Source: testkit builder -o defaultstest/field.gen.go Config

// This file holds fluent builders for test fixtures. Each builder
// follows the same contract:
//
//   - New<Type>()       creates a builder seeded with defaults (when
//     a <Type>Defaults factory or //testkit:default
//     directives are present) or zero values.
//   - New<Type>From(v)  creates a builder seeded with v.
//   - With<Field>(v)    sets a field; chains.
//   - Append<Field>(v)  appends to a slice field (slice fields only).
//   - With<Field>Entry(k, v) adds a map entry (map fields only).
//   - Mutate(fn)        applies fn to the value under construction.
//   - Clone()           returns a deep copy (slice/map/bytes deep-copied).
//   - Build()           returns the constructed value.
package defaultstest_test

import (
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/generator/testdata/defaults"
	"go.thesmos.sh/testkit/generator/testdata/defaults/defaultstest"
)

// TestConfigBuilder verifies the ConfigBuilder contract
// against the generated builder: constructor seeding and every
// per-field setter, plus Mutate / Clone independence
// asserted via Host, plus a
// zero-value round-trip via [NewConfigFrom].
func TestConfigBuilder(t *testing.T) {
	t.Parallel()

	t.Run("NewConfig builds without panic", func(t *testing.T) {
		t.Parallel()
		_ = defaultstest.NewConfig().Build()
	})

	t.Run("NewConfigFrom zero value round-trips", func(t *testing.T) {
		t.Parallel()
		got := defaultstest.NewConfigFrom(defaults.Config{}).Build()
		testkit.Equal(t, got, defaults.Config{}, "must return zero value")
	})

	t.Run("NewConfig uses field defaults", func(t *testing.T) {
		t.Parallel()
		got := defaultstest.NewConfig().Build()
		testkit.Equal(t, got.Host, "localhost",
			"Host default must match directive")
		testkit.Equal(t, got.Port, 8080,
			"Port default must match directive")
		testkit.Equal(t, got.Verbose, true,
			"Verbose default must match directive")
	})

	t.Run("WithHost sets field", func(t *testing.T) {
		t.Parallel()
		got := defaultstest.NewConfigFrom(defaults.Config{}).
			WithHost("test-host").
			Build()
		testkit.Equal(t, got.Host, "test-host", "must set Host")
	})

	t.Run("WithName sets field", func(t *testing.T) {
		t.Parallel()
		got := defaultstest.NewConfigFrom(defaults.Config{}).
			WithName("test-name").
			Build()
		testkit.Equal(t, got.Name, "test-name", "must set Name")
	})

	t.Run("WithPort sets field", func(t *testing.T) {
		t.Parallel()
		got := defaultstest.NewConfigFrom(defaults.Config{}).
			WithPort(42).
			Build()
		testkit.Equal(t, got.Port, 42, "must set Port")
	})

	t.Run("WithVerbose sets field", func(t *testing.T) {
		t.Parallel()
		got := defaultstest.NewConfigFrom(defaults.Config{}).
			WithVerbose(true).
			Build()
		testkit.Equal(t, got.Verbose, true, "must set Verbose")
	})

	t.Run("Clone forks independent scalar", func(t *testing.T) {
		t.Parallel()
		base := defaultstest.NewConfigFrom(defaults.Config{})
		clone := base.Clone().WithHost("test-host")
		original := base.Build()
		modified := clone.Build()
		testkit.NotEqual(t, original.Host, modified.Host,
			"Clone must produce independent copy")
	})

	t.Run("Mutate modifies value", func(t *testing.T) {
		t.Parallel()
		got := defaultstest.NewConfigFrom(defaults.Config{}).
			Mutate(func(v *defaults.Config) {
				v.Host = "test-host"
			}).
			Build()
		testkit.Equal(t, got.Host, "test-host", "Mutate must modify")
	})
}
