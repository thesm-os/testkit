// Code generated by testkit builder. DO NOT EDIT.
// Source: testkit builder -o defaultstest/convention.gen.go Request

// This file holds fluent builders for test fixtures. Each builder
// follows the same contract:
//
//   - New<Type>()       creates a builder seeded with defaults (when
//     a <Type>Defaults factory or //testkit:default
//     directives are present) or zero values.
//   - New<Type>From(v)  creates a builder seeded with v.
//   - With<Field>(v)    sets a field; chains.
//   - Append<Field>(v)  appends to a slice field (slice fields only).
//   - With<Field>Entry(k, v) adds a map entry (map fields only).
//   - Mutate(fn)        applies fn to the value under construction.
//   - Clone()           returns a deep copy (slice/map/bytes deep-copied).
//   - Build()           returns the constructed value.
package defaultstest_test

import (
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/generator/testdata/defaults"
	"go.thesmos.sh/testkit/generator/testdata/defaults/defaultstest"
)

// TestRequestBuilder verifies the RequestBuilder contract
// against the generated builder: constructor seeding and every
// per-field setter, plus Mutate / Clone independence
// asserted via RunID, plus a
// zero-value round-trip via [NewRequestFrom].
func TestRequestBuilder(t *testing.T) {
	t.Parallel()

	t.Run("NewRequest builds without panic", func(t *testing.T) {
		t.Parallel()
		_ = defaultstest.NewRequest().Build()
	})

	t.Run("NewRequestFrom zero value round-trips", func(t *testing.T) {
		t.Parallel()
		got := defaultstest.NewRequestFrom(defaults.Request{}).Build()
		testkit.Equal(t, got, defaults.Request{}, "must return zero value")
	})

	t.Run("NewRequest uses defaults", func(t *testing.T) {
		t.Parallel()
		got := defaultstest.NewRequest().Build()
		want := defaultstest.RequestDefaults()
		testkit.Equal(t, got, want, "NewRequest must use defaultstest.RequestDefaults")
	})

	t.Run("WithData sets field", func(t *testing.T) {
		t.Parallel()
		sample := []byte{42}
		got := defaultstest.NewRequestFrom(defaults.Request{}).
			WithData(sample).
			Build()
		testkit.Equal(t, got.Data, sample, "must set Data")
	})

	t.Run("WithDataString sets from string", func(t *testing.T) {
		t.Parallel()
		got := defaultstest.NewRequestFrom(defaults.Request{}).
			WithDataString("hello").
			Build()
		testkit.Equal(t, string(got.Data), "hello",
			"WithDataString must convert")
	})

	t.Run("WithRunID sets field", func(t *testing.T) {
		t.Parallel()
		got := defaultstest.NewRequestFrom(defaults.Request{}).
			WithRunID("test-runid").
			Build()
		testkit.Equal(t, got.RunID, "test-runid", "must set RunID")
	})

	t.Run("WithToken sets field", func(t *testing.T) {
		t.Parallel()
		got := defaultstest.NewRequestFrom(defaults.Request{}).
			WithToken(42).
			Build()
		testkit.Equal(t, got.Token, 42, "must set Token")
	})

	t.Run("Clone deep-copies Data bytes", func(t *testing.T) {
		t.Parallel()
		base := defaultstest.NewRequestFrom(defaults.Request{}).
			WithDataString("original")
		clone := base.Clone()
		clone.WithDataString("modified")
		original := base.Build()
		modified := clone.Build()
		testkit.Equal(t, string(original.Data), "original",
			"original bytes must be unchanged after clone")
		testkit.Equal(t, string(modified.Data), "modified",
			"clone must have new bytes")
	})

	t.Run("Clone forks independent scalar", func(t *testing.T) {
		t.Parallel()
		base := defaultstest.NewRequestFrom(defaults.Request{})
		clone := base.Clone().WithRunID("test-runid")
		original := base.Build()
		modified := clone.Build()
		testkit.NotEqual(t, original.RunID, modified.RunID,
			"Clone must produce independent copy")
	})

	t.Run("Mutate modifies value", func(t *testing.T) {
		t.Parallel()
		got := defaultstest.NewRequestFrom(defaults.Request{}).
			Mutate(func(v *defaults.Request) {
				v.RunID = "test-runid"
			}).
			Build()
		testkit.Equal(t, got.RunID, "test-runid", "Mutate must modify")
	})
}
