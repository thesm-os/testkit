// Code generated by testkit builder. DO NOT EDIT.
// Source: testkit builder -o defaultstest/inline.gen.go Profile

// This file holds fluent builders for test fixtures. Each builder
// follows the same contract:
//
//   - New<Type>()       creates a builder seeded with defaults (when
//     a <Type>Defaults factory or //testkit:default
//     directives are present) or zero values.
//   - New<Type>From(v)  creates a builder seeded with v.
//   - With<Field>(v)    sets a field; chains.
//   - Append<Field>(v)  appends to a slice field (slice fields only).
//   - With<Field>Entry(k, v) adds a map entry (map fields only).
//   - Mutate(fn)        applies fn to the value under construction.
//   - Clone()           returns a deep copy (slice/map/bytes deep-copied).
//   - Build()           returns the constructed value.
package defaultstest_test

import (
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/generator/testdata/defaults"
	"go.thesmos.sh/testkit/generator/testdata/defaults/defaultstest"
)

// TestProfileBuilder verifies the ProfileBuilder contract
// against the generated builder: constructor seeding and every
// per-field setter, plus Mutate / Clone independence
// asserted via Email, plus a
// zero-value round-trip via [NewProfileFrom].
func TestProfileBuilder(t *testing.T) {
	t.Parallel()

	t.Run("NewProfile builds without panic", func(t *testing.T) {
		t.Parallel()
		_ = defaultstest.NewProfile().Build()
	})

	t.Run("NewProfileFrom zero value round-trips", func(t *testing.T) {
		t.Parallel()
		got := defaultstest.NewProfileFrom(defaults.Profile{}).Build()
		testkit.Equal(t, got, defaults.Profile{}, "must return zero value")
	})

	t.Run("NewProfile uses defaults", func(t *testing.T) {
		t.Parallel()
		got := defaultstest.NewProfile().Build()
		want := defaults.ProfileDefaults()
		testkit.Equal(t, got, want, "NewProfile must use defaults.ProfileDefaults")
	})

	t.Run("WithEmail sets field", func(t *testing.T) {
		t.Parallel()
		got := defaultstest.NewProfileFrom(defaults.Profile{}).
			WithEmail("test-email").
			Build()
		testkit.Equal(t, got.Email, "test-email", "must set Email")
	})

	t.Run("WithName sets field", func(t *testing.T) {
		t.Parallel()
		got := defaultstest.NewProfileFrom(defaults.Profile{}).
			WithName("test-name").
			Build()
		testkit.Equal(t, got.Name, "test-name", "must set Name")
	})

	t.Run("Clone forks independent scalar", func(t *testing.T) {
		t.Parallel()
		base := defaultstest.NewProfileFrom(defaults.Profile{})
		clone := base.Clone().WithEmail("test-email")
		original := base.Build()
		modified := clone.Build()
		testkit.NotEqual(t, original.Email, modified.Email,
			"Clone must produce independent copy")
	})

	t.Run("Mutate modifies value", func(t *testing.T) {
		t.Parallel()
		got := defaultstest.NewProfileFrom(defaults.Profile{}).
			Mutate(func(v *defaults.Profile) {
				v.Email = "test-email"
			}).
			Build()
		testkit.Equal(t, got.Email, "test-email", "Mutate must modify")
	})
}
