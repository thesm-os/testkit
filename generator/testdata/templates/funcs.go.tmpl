{{- /* Exercises Pipeline.TemplateFuncs by calling a data-aware func
       (upperType) registered per-run. The output carries the func's
       return value so tests can assert it ran. */ -}}
package funcs

// upper: {{upperType}}
type {{.Type}} struct{}
