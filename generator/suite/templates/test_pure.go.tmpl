{{- /* Pure-shape baseline body. Body-only partial. */ -}}
{{- define "test-method-pure" -}}
{{- $m := .M -}}
{{- $iface := .Iface -}}
{{- $tracker := .Tracker -}}
{{- $R := $m.ShapeValType -}}
		sctx := suite.PureContextFor[{{ $iface }}, {{ $R }}](t, factory,
			func(impl {{ $iface }}) {{ $R }} {
				return impl.{{ $m.Name }}()
			})
		suite.AssertPureBaseline[{{ $iface }}, {{ $R }}]({{ $m.SampleResultAt 0 $tracker }})(sctx)
{{- end -}}
