// Code generated by eidos. DO NOT EDIT.
//
// Source:    storepkg/store.go
// Plugins:   testpipe-synthetic, stub 1.2.0, backend.golang 1.0.0
// Command:   testkit run ./storepkg

package storepkg_test

import (
	"context"
	"testing"
	"time"

	"example.com/storepkg"
	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/rand"
	"go.thesmos.sh/testkit/stub"
)

// storeStubGetSubject binds Get into the shape
// [stub.Behaviour] drives: how to call it, what it answers with, and how to
// override it.
//
// Each call builds a fresh double, because several of the checks assert on
// failure and need one bound to a failable TB rather than to the running
// test.
func storeStubGetSubject(tb testing.TB) stub.Subject[storepkg.StoreGetCall, storepkg.StoreGetReturn] {
	tb.Helper()
	s := storepkg.NewStoreStub(tb)
	return stub.Subject[storepkg.StoreGetCall, storepkg.StoreGetReturn]{
		Stub: s.OnGet.MethodStub,
		Call: func() {
			var a0 context.Context
			var a1 string
			_, _ = s.Get(a0, a1)
		},
		Result: func() storepkg.StoreGetReturn {
			var a0 context.Context
			var a1 string
			got0, got1 := s.Get(a0, a1)
			return storepkg.StoreGetReturn{Item: got0, Err: got1}
		},
		Override: func(mark func()) {
			s.OnGet.Func(func(_ context.Context, _ string) (string, error) {
				mark()
				var z0 string
				var z1 error
				return z0, z1
			})
		},
		Fails: func() error {
			var a0 context.Context
			var a1 string
			_, err := s.Get(a0, a1)
			return err
		},
	}
}

// TestStoreStubGet pins how Get answers.
//
// Recording, resetting, call-count expectations, strict mode, fault injection
// and zero-value dispatch are the same contract for every method, so they are
// asserted once in [stub.Behaviour] rather than restated here. What remains
// below needs a value this method's signature can tell apart from a zero one.
func TestStoreStubGet(t *testing.T) {
	t.Parallel()

	stub.Behaviour(t, "Get", storeStubGetSubject)

	t.Run("answers with the value pinned by Returns", func(t *testing.T) {
		t.Parallel()
		s := storepkg.NewStoreStub(t)
		// Distinguishable from the zero wherever a literal can be written.
		// Pinning the zero and asserting the call answers it is what an
		// unconfigured double does by design, so the check passed for a
		// double that ignored Returns entirely.
		var want0 string = "test-get"
		var want1 error
		s.OnGet.Returns(want0, want1)
		var a0 context.Context
		var a1 string
		got0, got1 := s.Get(a0, a1)
		testkit.Equal(t, got0, want0, "Get must answer with what Returns pinned")
		testkit.Equal(t, got1, want1, "Get must answer with what Returns pinned")
	})
	t.Run("records what it was called with", func(t *testing.T) {
		t.Parallel()
		// Asserting only that a call happened would pass against a double
		// that recorded the wrong arguments, which is the failure a reader
		// most needs the log to surface.
		s := storepkg.NewStoreStub(t)
		var a0 context.Context
		var a1 string
		_, _ = s.Get(a0, a1)
		got := s.OnGet.AssertCalledOnce(t, "Get must record the call")
		testkit.Equal(t, got.Ctx, a0, "the recorded call carries Ctx")
		testkit.Equal(t, got.ID, a1, "the recorded call carries ID")
	})

	t.Run("fires the OnRecord hook for every call", func(t *testing.T) {
		t.Parallel()
		// The hook fires synchronously as each call lands, which is what a
		// concurrency test observes progress with — polling the log instead
		// races the thing under test.
		s := storepkg.NewStoreStub(t)
		var seen []storepkg.StoreGetCall
		s.OnGet.OnRecord(func(c storepkg.StoreGetCall) { seen = append(seen, c) })
		var a0 context.Context
		var a1 string
		_, _ = s.Get(a0, a1)
		_, _ = s.Get(a0, a1)
		testkit.Len(t, seen, 2, "OnRecord must fire once per Get call")
	})

	t.Run("wires WithStoreGet at construction", func(t *testing.T) {
		t.Parallel()
		called := false
		s := storepkg.NewStoreStub(t, storepkg.WithStoreGet(func(_ context.Context, _ string) (string, error) {
			called = true
			var z0 string
			var z1 error
			return z0, z1
		}))
		var a0 context.Context
		var a1 string
		_, _ = s.Get(a0, a1)
		testkit.True(t, called, "WithStoreGet must install the override")
	})

	t.Run("keeps the Returns configuration across a reset", func(t *testing.T) {
		t.Parallel()
		// A reset clears what happened, not what was configured. Losing the
		// configuration would make a double answer differently in the second
		// half of a test than in the first, for no reason the reader can see.
		s := storepkg.NewStoreStub(t)
		var want0 string
		var want1 error
		s.OnGet.Returns(want0, want1)
		var a0 context.Context
		var a1 string
		_, _ = s.Get(a0, a1)
		s.ResetCalls()
		got0, got1 := s.Get(a0, a1)
		testkit.Equal(t, got0, want0, "a reset must keep what Returns pinned")
		testkit.Equal(t, got1, want1, "a reset must keep what Returns pinned")
	})
}

// storeStubListSubject binds List into the shape
// [stub.Behaviour] drives: how to call it, what it answers with, and how to
// override it.
//
// Each call builds a fresh double, because several of the checks assert on
// failure and need one bound to a failable TB rather than to the running
// test.
func storeStubListSubject(tb testing.TB) stub.Subject[storepkg.StoreListCall, storepkg.StoreListReturn] {
	tb.Helper()
	s := storepkg.NewStoreStub(tb)
	return stub.Subject[storepkg.StoreListCall, storepkg.StoreListReturn]{
		Stub: s.OnList.MethodStub,
		Call: func() {
			_, _ = s.List()
		},
		Result: func() storepkg.StoreListReturn {
			got0, got1 := s.List()
			return storepkg.StoreListReturn{Result: got0, Err: got1}
		},
		Override: func(mark func()) {
			s.OnList.Func(func() ([]string, error) {
				mark()
				var z0 []string
				var z1 error
				return z0, z1
			})
		},
		Fails: func() error {
			_, r1 := s.List()
			return r1
		},
	}
}

// TestStoreStubList pins how List answers.
//
// Recording, resetting, call-count expectations, strict mode, fault injection
// and zero-value dispatch are the same contract for every method, so they are
// asserted once in [stub.Behaviour] rather than restated here. What remains
// below needs a value this method's signature can tell apart from a zero one.
func TestStoreStubList(t *testing.T) {
	t.Parallel()

	stub.Behaviour(t, "List", storeStubListSubject)

	// No check that List answers what Returns pinned: no result of
	// its signature admits a literal this generator can write, so the only
	// value available to pin is the zero — which an unconfigured double
	// answers anyway. stub.Behaviour asserts that half honestly.
	t.Run("records what it was called with", func(t *testing.T) {
		t.Parallel()
		// Asserting only that a call happened would pass against a double
		// that recorded the wrong arguments, which is the failure a reader
		// most needs the log to surface.
		s := storepkg.NewStoreStub(t)
		_, _ = s.List()
		s.OnList.AssertCalledOnce(t, "List must record the call")
	})

	t.Run("fires the OnRecord hook for every call", func(t *testing.T) {
		t.Parallel()
		// The hook fires synchronously as each call lands, which is what a
		// concurrency test observes progress with — polling the log instead
		// races the thing under test.
		s := storepkg.NewStoreStub(t)
		var seen []storepkg.StoreListCall
		s.OnList.OnRecord(func(c storepkg.StoreListCall) { seen = append(seen, c) })
		_, _ = s.List()
		_, _ = s.List()
		testkit.Len(t, seen, 2, "OnRecord must fire once per List call")
	})

	t.Run("wires WithStoreList at construction", func(t *testing.T) {
		t.Parallel()
		called := false
		s := storepkg.NewStoreStub(t, storepkg.WithStoreList(func() ([]string, error) {
			called = true
			var z0 []string
			var z1 error
			return z0, z1
		}))
		_, _ = s.List()
		testkit.True(t, called, "WithStoreList must install the override")
	})

	t.Run("keeps the Returns configuration across a reset", func(t *testing.T) {
		t.Parallel()
		// A reset clears what happened, not what was configured. Losing the
		// configuration would make a double answer differently in the second
		// half of a test than in the first, for no reason the reader can see.
		s := storepkg.NewStoreStub(t)
		var want0 []string
		var want1 error
		s.OnList.Returns(want0, want1)
		_, _ = s.List()
		s.ResetCalls()
		got0, got1 := s.List()
		testkit.Equal(t, got0, want0, "a reset must keep what Returns pinned")
		testkit.Equal(t, got1, want1, "a reset must keep what Returns pinned")
	})
}

// storeStubCloseSubject binds Close into the shape
// [stub.Behaviour] drives: how to call it, what it answers with, and how to
// override it.
//
// Each call builds a fresh double, because several of the checks assert on
// failure and need one bound to a failable TB rather than to the running
// test.
func storeStubCloseSubject(tb testing.TB) stub.Subject[storepkg.StoreCloseCall, struct{}] {
	tb.Helper()
	s := storepkg.NewStoreStub(tb)
	return stub.Subject[storepkg.StoreCloseCall, struct{}]{
		Stub: s.OnClose.MethodStub,
		Call: func() {
			s.Close()
		},
		Override: func(mark func()) {
			s.OnClose.Func(func() {
				mark()
			})
		},
	}
}

// TestStoreStubClose pins how Close answers.
//
// Recording, resetting, call-count expectations, strict mode, fault injection
// and zero-value dispatch are the same contract for every method, so they are
// asserted once in [stub.Behaviour] rather than restated here. What remains
// below needs a value this method's signature can tell apart from a zero one.
func TestStoreStubClose(t *testing.T) {
	t.Parallel()

	stub.Behaviour(t, "Close", storeStubCloseSubject)
	t.Run("records what it was called with", func(t *testing.T) {
		t.Parallel()
		// Asserting only that a call happened would pass against a double
		// that recorded the wrong arguments, which is the failure a reader
		// most needs the log to surface.
		s := storepkg.NewStoreStub(t)
		s.Close()
		s.OnClose.AssertCalledOnce(t, "Close must record the call")
	})

	t.Run("fires the OnRecord hook for every call", func(t *testing.T) {
		t.Parallel()
		// The hook fires synchronously as each call lands, which is what a
		// concurrency test observes progress with — polling the log instead
		// races the thing under test.
		s := storepkg.NewStoreStub(t)
		var seen []storepkg.StoreCloseCall
		s.OnClose.OnRecord(func(c storepkg.StoreCloseCall) { seen = append(seen, c) })
		s.Close()
		s.Close()
		testkit.Len(t, seen, 2, "OnRecord must fire once per Close call")
	})

	t.Run("wires WithStoreClose at construction", func(t *testing.T) {
		t.Parallel()
		called := false
		s := storepkg.NewStoreStub(t, storepkg.WithStoreClose(func() {
			called = true
		}))
		s.Close()
		testkit.True(t, called, "WithStoreClose must install the override")
	})
}

// storeStubDouble describes how to build a StoreStub under each
// option whose effect is the same whatever a method's signature.
//
// Get stands in for the double as a whole: what these checks assert
// is that an option reached it at all, and the first method witnesses that as
// well as any other would.
func storeStubDouble() stub.Double[storepkg.StoreGetCall] {
	instance := func(s *storepkg.StoreStub) stub.Instance[storepkg.StoreGetCall] {
		return stub.Instance[storepkg.StoreGetCall]{
			Stub: s.OnGet.MethodStub,
			Call: func() {
				var a0 context.Context
				var a1 string
				_, _ = s.Get(a0, a1)
			},
			Reset: s.ResetCalls,
		}
	}
	return stub.Double[storepkg.StoreGetCall]{
		New: func(tb testing.TB) stub.Instance[storepkg.StoreGetCall] {
			return instance(storepkg.NewStoreStub(tb))
		},
		WithClock: func(tb testing.TB, clk clock.Clock) stub.Instance[storepkg.StoreGetCall] {
			return instance(storepkg.NewStoreStub(tb, storepkg.StoreStubWithClock(clk)))
		},
		WithRandSource: func(tb testing.TB, src rand.Source) stub.Instance[storepkg.StoreGetCall] {
			return instance(storepkg.NewStoreStub(tb, storepkg.StoreStubWithRandSource(src)))
		},
		BenchMode: func(tb testing.TB) stub.Instance[storepkg.StoreGetCall] {
			return instance(storepkg.NewStoreStub(tb, storepkg.StoreStubBenchMode()))
		},
		Strict: func(tb testing.TB) stub.Instance[storepkg.StoreGetCall] {
			return instance(storepkg.NewStoreStub(tb, storepkg.StoreStubStrict()))
		},
	}
}

// TestStoreStubOptions pins the settings that apply to the double rather
// than to one method: bench mode, the injected clock and random source, and
// what a reset clears.
func TestStoreStubOptions(t *testing.T) {
	t.Parallel()

	stub.DoubleBehaviour(t, storeStubDouble())
}

// TestStoreStubDelegateTo pins that a wrapped implementation is called
// through, and recorded on the way past.
//
// This is what lets one generated suite run against both the double and a
// production type, which is the point of conformance testing.
func TestStoreStubDelegateTo(t *testing.T) {
	t.Parallel()

	inner := storepkg.NewStoreStub(t)
	s := storepkg.NewStoreStub(t, storepkg.StoreStubDelegateTo(inner))

	t.Run("forwards Get to the wrapped implementation", func(t *testing.T) {
		var a0 context.Context
		var a1 string
		_, _ = s.Get(a0, a1)
		inner.OnGet.AssertCalledOnce(t, "Get must reach the wrapped implementation")
	})

	t.Run("surfaces what Get answered", func(t *testing.T) {
		// Reaching the wrapped implementation is not enough: a double that
		// called through and then discarded the answer would pass the check
		// above while telling its caller nothing true.
		want := testkit.TestError("Get-delegate")
		inner.OnGet.FaultsFor(time.Hour, want)
		var a0 context.Context
		var a1 string
		_, err := s.Get(a0, a1)
		testkit.ErrorIs(t, err, want, "Get must surface the wrapped answer")
	})

	t.Run("forwards List to the wrapped implementation", func(t *testing.T) {
		_, _ = s.List()
		inner.OnList.AssertCalledOnce(t, "List must reach the wrapped implementation")
	})

	t.Run("surfaces what List answered", func(t *testing.T) {
		// Reaching the wrapped implementation is not enough: a double that
		// called through and then discarded the answer would pass the check
		// above while telling its caller nothing true.
		want := testkit.TestError("List-delegate")
		inner.OnList.FaultsFor(time.Hour, want)
		_, r1 := s.List()
		testkit.ErrorIs(t, r1, want, "List must surface the wrapped answer")
	})

	t.Run("forwards Close to the wrapped implementation", func(t *testing.T) {
		s.Close()
		inner.OnClose.AssertCalledOnce(t, "Close must reach the wrapped implementation")
	})
}

// eidos: end of generated content.
// eidos:provenance e82dea3194d251fba56f409818c5f3e3c214da8bda41e0d245061ff961126049
