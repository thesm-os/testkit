{{- /*
Values reuses the shared pool, never a generator of its own: a law drawing
values the sequences never wrote is a law checked over an empty history.
*/ -}}
{{- define "model.lawfield.Values" -}}
{{ .Name }}: {{ .Pool }}
{{- end -}}
