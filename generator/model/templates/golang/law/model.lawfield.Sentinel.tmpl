{{- /*
Sentinel is the constant the declaration stamped — the error a miss reports,
read from the classification's own parameter rather than invented.
*/ -}}
{{- define "model.lawfield.Sentinel" -}}
Sentinel: {{ renderExpr .Const }}
{{- end -}}
