{{- /*
The drain every publisher law shares: the property local, which is the
consumer's supplied closure or the derived synchronous sweep.
*/ -}}
{{- define "model.lawfield.DrainSub" -}}
{{ .Name }}: {{ .KeyOfName }}
{{- end -}}
