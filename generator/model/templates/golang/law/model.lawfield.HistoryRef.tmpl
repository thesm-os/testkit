{{- /*
The append history: the property local the recording action fills and the
runner clears each iteration — shared by reference so the law reads what
the sequences attempted.
*/ -}}
{{- define "model.lawfield.HistoryRef" -}}
{{ .Name }}: appendHist
{{- end -}}
