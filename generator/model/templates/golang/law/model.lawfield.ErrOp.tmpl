{{- /*
A nullary operation whose answer is its error: a probe, a verify, one half of
an open/close cycle.
*/ -}}
{{- define "model.lawfield.ErrOp" -}}
{{ .Name }}: func(rt *{{ renderExpr (external .ModelPkg "T") }}, s {{ renderType .Iface }}) error {
			return s.{{ .Method }}({{ if .TakesCtx }}rt.Context(){{ end }})
		}
{{- end -}}
