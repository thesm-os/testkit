// Code generated by testkit stub. DO NOT EDIT.
// Source: finder.go:19-21 (testkit stub Finder)

package findertest

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/gen/stub/testdata/variadic"
	"go.thesmos.sh/testkit/rand"
	"go.thesmos.sh/testkit/stub"
)

// Compile-time interface check.
var _ variadic.Finder = (*FinderStub)(nil)

// --- Call types ---

// FinderFindCall records one invocation of variadic.Finder.Find.
// Result fields use the return-value names from the source interface when
// available; unnamed returns produce positional names (Result0, Result1, ...).
type FinderFindCall struct {
	Ctx    context.Context
	Ids    []string
	Result []string
	Err    error
}

// FinderMergeCall records one invocation of variadic.Finder.Merge.
// Result fields use the return-value names from the source interface when
// available; unnamed returns produce positional names (Result0, Result1, ...).
type FinderMergeCall struct {
	Ctx    context.Context
	Values []int
	Result int
	Err    error
}

// --- Per-method stubs ---

// FinderFindStub controls behavior and records calls for Find.
//
// Concurrent-test primitives inherited from [stub.MethodStub]:
//   - [stub.Recorder.WaitForN] / [stub.Recorder.WaitFor] — block until calls arrive (cond-variable, no polling)
//   - [stub.Recorder.NewGate] / [stub.Gate.Release] / [stub.Gate.ReleaseOne] — deterministic race control
//   - [stub.Recorder.OnRecord] — streaming hook fired synchronously on every call
type FinderFindStub struct {
	*stub.MethodStub[FinderFindCall]
	fn       func(context.Context, ...string) ([]string, error)
	fallback *finderFindReturn
}

type finderFindReturn struct {
	Result []string
	Err    error
}

// Returns sets a fixed return value for Find.
func (s *FinderFindStub) Returns(result []string, err error) *FinderFindStub {
	s.fallback = &finderFindReturn{Result: result, Err: err}
	return s
}

// Func sets a function override for Find.
func (s *FinderFindStub) Func(fn func(context.Context, ...string) ([]string, error)) *FinderFindStub {
	s.fn = fn
	return s
}

// FinderMergeStub controls behavior and records calls for Merge.
//
// Concurrent-test primitives inherited from [stub.MethodStub]:
//   - [stub.Recorder.WaitForN] / [stub.Recorder.WaitFor] — block until calls arrive (cond-variable, no polling)
//   - [stub.Recorder.NewGate] / [stub.Gate.Release] / [stub.Gate.ReleaseOne] — deterministic race control
//   - [stub.Recorder.OnRecord] — streaming hook fired synchronously on every call
type FinderMergeStub struct {
	*stub.MethodStub[FinderMergeCall]
	fn       func(context.Context, ...int) (int, error)
	fallback *finderMergeReturn
}

type finderMergeReturn struct {
	Result int
	Err    error
}

// Returns sets a fixed return value for Merge.
func (s *FinderMergeStub) Returns(result int, err error) *FinderMergeStub {
	s.fallback = &finderMergeReturn{Result: result, Err: err}
	return s
}

// Func sets a function override for Merge.
func (s *FinderMergeStub) Func(fn func(context.Context, ...int) (int, error)) *FinderMergeStub {
	s.fn = fn
	return s
}

// --- FinderStub ---

// FinderStubOption configures a [FinderStub] at construction time.
type FinderStubOption func(*FinderStub)

// FinderStubStrict enables strict mode — unconfigured methods fail the test.
func FinderStubStrict() FinderStubOption {
	return func(s *FinderStub) { s.strict = true }
}

// FinderStubDelegateTo wraps a real implementation — all calls delegate to
// impl and are recorded on the outer stub.
func FinderStubDelegateTo(impl variadic.Finder) FinderStubOption {
	return func(s *FinderStub) {
		s.OnFind.Func(impl.Find)
		s.OnMerge.Func(impl.Merge)
	}
}

// FinderStubWithClock injects a [clock.Clock] into all per-method stubs.
// Use [clock.NewTestClock] for deterministic virtual time or a consumer's
// own clock implementation. Default is real wall-clock time.
func FinderStubWithClock(clk clock.Clock) FinderStubOption {
	return func(s *FinderStub) {
		s.OnFind.WithClock(clk)
		s.OnMerge.WithClock(clk)
	}
}

// FinderStubWithRandSource injects a [rand.Source] into all per-method
// stubs. Use [rand.FixedRandSource] for deterministic testing or a consumer's
// own seeded RNG. Default is [rand.DefaultRandSource] (math/rand/v2).
func FinderStubWithRandSource(src rand.Source) FinderStubOption {
	return func(s *FinderStub) {
		s.OnFind.WithRandSource(src)
		s.OnMerge.WithRandSource(src)
	}
}

// FinderStubBenchMode disables call recording on all per-method stubs. Use
// this in benchmarks to avoid allocation overhead from accumulated call logs.
// Dispatch (Func, Returns, Faults) still works normally.
func FinderStubBenchMode() FinderStubOption {
	return func(s *FinderStub) {
		s.OnFind.BenchMode()
		s.OnMerge.BenchMode()
	}
}

// WithFinderFind sets a function override at construction time.
func WithFinderFind(fn func(context.Context, ...string) ([]string, error)) FinderStubOption {
	return func(s *FinderStub) { s.OnFind.Func(fn) }
}

// WithFinderMerge sets a function override at construction time.
func WithFinderMerge(fn func(context.Context, ...int) (int, error)) FinderStubOption {
	return func(s *FinderStub) { s.OnMerge.Func(fn) }
}

// FinderStub is a configurable test double for [variadic.Finder].
type FinderStub struct {
	OnFind  *FinderFindStub
	OnMerge *FinderMergeStub
	strict  bool
}

// NewFinderStub creates a stub. Pass tb for auto-verification of
// expectations at test cleanup. Pass nil for a pure stub.
func NewFinderStub(tb testing.TB, opts ...FinderStubOption) *FinderStub {
	s := &FinderStub{
		OnFind:  &FinderFindStub{MethodStub: stub.NewMethodStub[FinderFindCall](tb, "Finder.Find")},
		OnMerge: &FinderMergeStub{MethodStub: stub.NewMethodStub[FinderMergeCall](tb, "Finder.Merge")},
	}
	for _, opt := range opts {
		opt(s)
	}
	if s.strict {
		s.OnFind.Strict()
		s.OnMerge.Strict()
	}
	if tb != nil {
		tb.Cleanup(func() {
			s.OnFind.Verify()
			s.OnMerge.Verify()
		})
	}
	return s
}

// ResetCalls clears recorded calls, fault counters, and call-count expectations.
// Func, Returns, and Faults configuration is preserved.
func (s *FinderStub) ResetCalls() {
	s.OnFind.Reset()
	s.OnMerge.Reset()
}

// --- Interface method implementations ---

func (s *FinderStub) Find(ctx context.Context, ids ...string) ([]string, error) {
	s.OnFind.SleepLatency()
	call := FinderFindCall{Ctx: ctx, Ids: ids}
	if fired, faultErr := s.OnFind.ShouldFaultFor(call); fired {
		call.Err = faultErr
		s.OnFind.Record(call)
		return nil, faultErr
	}
	if s.OnFind.fn != nil {
		r0, r1 := s.OnFind.fn(ctx, ids...)
		call.Result = r0
		call.Err = r1
		s.OnFind.Record(call)
		return r0, r1
	}
	if s.OnFind.fallback != nil {
		f := s.OnFind.fallback
		call.Result = f.Result
		call.Err = f.Err
		s.OnFind.Record(call)
		return f.Result, f.Err
	}
	s.OnFind.FailUnexpectedCall(call)
	s.OnFind.Record(call)
	return nil, nil
}

func (s *FinderStub) Merge(ctx context.Context, values ...int) (int, error) {
	s.OnMerge.SleepLatency()
	call := FinderMergeCall{Ctx: ctx, Values: values}
	if fired, faultErr := s.OnMerge.ShouldFaultFor(call); fired {
		call.Err = faultErr
		s.OnMerge.Record(call)
		return 0, faultErr
	}
	if s.OnMerge.fn != nil {
		r0, r1 := s.OnMerge.fn(ctx, values...)
		call.Result = r0
		call.Err = r1
		s.OnMerge.Record(call)
		return r0, r1
	}
	if s.OnMerge.fallback != nil {
		f := s.OnMerge.fallback
		call.Result = f.Result
		call.Err = f.Err
		s.OnMerge.Record(call)
		return f.Result, f.Err
	}
	s.OnMerge.FailUnexpectedCall(call)
	s.OnMerge.Record(call)
	return 0, nil
}
