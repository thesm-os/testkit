// Code generated by testkit builder. DO NOT EDIT.
// Source: testkit builder Account Address Minimal

package builder_test

import (
	"testing"
	"time"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/gen/testdata/builder"
)

func TestAccountBuilder(t *testing.T) {
	t.Parallel()

	t.Run("Build without setters does not panic", func(t *testing.T) {
		t.Parallel()
		defaults := builder.Account{}
		_ = builder.NewAccountBuilder(defaults).Build()
	})

	t.Run("NewDefault does not panic", func(t *testing.T) {
		t.Parallel()
		_ = builder.NewDefaultAccountBuilder().Build()
	})

	t.Run("WithName sets the Name field", func(t *testing.T) {
		t.Parallel()
		sample := "test-name"
		got := builder.NewDefaultAccountBuilder().WithName(sample).Build()
		testkit.Equal(t, got.Name, sample, "WithName must set Name")
	})

	t.Run("WithAge sets the Age field", func(t *testing.T) {
		t.Parallel()
		sample := 42
		got := builder.NewDefaultAccountBuilder().WithAge(sample).Build()
		testkit.Equal(t, got.Age, sample, "WithAge must set Age")
	})

	t.Run("WithBalance sets the Balance field", func(t *testing.T) {
		t.Parallel()
		sample := 3.14
		got := builder.NewDefaultAccountBuilder().WithBalance(sample).Build()
		testkit.Equal(t, got.Balance, sample, "WithBalance must set Balance")
	})

	t.Run("WithActive sets the Active field", func(t *testing.T) {
		t.Parallel()
		sample := true
		got := builder.NewDefaultAccountBuilder().WithActive(sample).Build()
		testkit.Equal(t, got.Active, sample, "WithActive must set Active")
	})

	t.Run("WithAccountID sets the AccountID field", func(t *testing.T) {
		t.Parallel()
		sample := builder.ID("test-accountid")
		got := builder.NewDefaultAccountBuilder().WithAccountID(sample).Build()
		testkit.Equal(t, got.AccountID, sample, "WithAccountID must set AccountID")
	})

	t.Run("WithStatus sets the Status field", func(t *testing.T) {
		t.Parallel()
		sample := builder.Status(42)
		got := builder.NewDefaultAccountBuilder().WithStatus(sample).Build()
		testkit.Equal(t, got.Status, sample, "WithStatus must set Status")
	})

	t.Run("WithNickname sets the Nickname field", func(t *testing.T) {
		t.Parallel()
		sample := "test-nickname"
		got := builder.NewDefaultAccountBuilder().WithNickname(&sample).Build()
		if got.Nickname == nil {
			t.Fatal("WithNickname must set non-nil pointer")
		}
		testkit.Equal(t, *got.Nickname, sample, "WithNickname must set Nickname")
	})

	t.Run("WithManager compiles and sets field", func(t *testing.T) {
		t.Parallel()
		got := builder.NewDefaultAccountBuilder().WithManager(nil).Build()
		_ = got
	})

	t.Run("WithScore sets the Score field", func(t *testing.T) {
		t.Parallel()
		sample := 42
		got := builder.NewDefaultAccountBuilder().WithScore(&sample).Build()
		if got.Score == nil {
			t.Fatal("WithScore must set non-nil pointer")
		}
		testkit.Equal(t, *got.Score, sample, "WithScore must set Score")
	})

	t.Run("WithAddress sets the Address field", func(t *testing.T) {
		t.Parallel()
		sample := builder.Address{}
		got := builder.NewDefaultAccountBuilder().WithAddress(sample).Build()
		testkit.Equal(t, got.Address, sample, "WithAddress must set Address")
	})

	t.Run("WithCreatedAt sets the CreatedAt field", func(t *testing.T) {
		t.Parallel()
		sample := time.Time{}
		got := builder.NewDefaultAccountBuilder().WithCreatedAt(sample).Build()
		testkit.Equal(t, got.CreatedAt, sample, "WithCreatedAt must set CreatedAt")
	})

	t.Run("WithTTL sets the TTL field", func(t *testing.T) {
		t.Parallel()
		sample := time.Duration(42)
		got := builder.NewDefaultAccountBuilder().WithTTL(sample).Build()
		testkit.Equal(t, got.TTL, sample, "WithTTL must set TTL")
	})

	t.Run("WithTags sets the Tags field", func(t *testing.T) {
		t.Parallel()
		sample := []string{"test-tags"}
		got := builder.NewDefaultAccountBuilder().WithTags(sample).Build()
		testkit.Equal(t, got.Tags, sample, "WithTags must set Tags")
	})

	t.Run("WithScores sets the Scores field", func(t *testing.T) {
		t.Parallel()
		sample := []int{42}
		got := builder.NewDefaultAccountBuilder().WithScores(sample).Build()
		testkit.Equal(t, got.Scores, sample, "WithScores must set Scores")
	})

	t.Run("WithChildren sets the Children field", func(t *testing.T) {
		t.Parallel()
		sample := []*builder.Account{nil}
		got := builder.NewDefaultAccountBuilder().WithChildren(sample).Build()
		testkit.Equal(t, got.Children, sample, "WithChildren must set Children")
	})

	t.Run("WithMetadata sets the Metadata field", func(t *testing.T) {
		t.Parallel()
		sample := map[string]string{"test-metadata": "test-metadata"}
		got := builder.NewDefaultAccountBuilder().WithMetadata(sample).Build()
		testkit.Equal(t, got.Metadata, sample, "WithMetadata must set Metadata")
	})

	t.Run("WithCounts sets the Counts field", func(t *testing.T) {
		t.Parallel()
		sample := map[string]int{"test-counts": 42}
		got := builder.NewDefaultAccountBuilder().WithCounts(sample).Build()
		testkit.Equal(t, got.Counts, sample, "WithCounts must set Counts")
	})

	t.Run("WithChecksum sets the Checksum field", func(t *testing.T) {
		t.Parallel()
		sample := [32]byte{1}
		got := builder.NewDefaultAccountBuilder().WithChecksum(sample).Build()
		testkit.Equal(t, got.Checksum, sample, "WithChecksum must set Checksum")
	})

	t.Run("WithLogger compiles and sets field", func(t *testing.T) {
		t.Parallel()
		got := builder.NewDefaultAccountBuilder().WithLogger(nil).Build()
		_ = got
	})

	t.Run("WithOnChange compiles and sets field", func(t *testing.T) {
		t.Parallel()
		got := builder.NewDefaultAccountBuilder().WithOnChange(nil).Build()
		_ = got
	})

	t.Run("WithEvents compiles and sets field", func(t *testing.T) {
		t.Parallel()
		got := builder.NewDefaultAccountBuilder().WithEvents(nil).Build()
		_ = got
	})

	t.Run("WithMarker sets the Marker field", func(t *testing.T) {
		t.Parallel()
		sample := struct{}{}
		got := builder.NewDefaultAccountBuilder().WithMarker(sample).Build()
		testkit.Equal(t, got.Marker, sample, "WithMarker must set Marker")
	})

}

func TestAddressBuilder(t *testing.T) {
	t.Parallel()

	t.Run("Build without setters returns defaults", func(t *testing.T) {
		t.Parallel()
		defaults := builder.Address{}
		got := builder.NewAddressBuilder(defaults).Build()
		testkit.Equal(t, got, defaults, "must return defaults unchanged")
	})

	t.Run("NewDefault returns zero value", func(t *testing.T) {
		t.Parallel()
		got := builder.NewDefaultAddressBuilder().Build()
		testkit.Equal(t, got, builder.Address{}, "must return zero value")
	})

	t.Run("WithStreet sets the Street field", func(t *testing.T) {
		t.Parallel()
		sample := "test-street"
		got := builder.NewDefaultAddressBuilder().WithStreet(sample).Build()
		testkit.Equal(t, got.Street, sample, "WithStreet must set Street")
	})

	t.Run("WithCity sets the City field", func(t *testing.T) {
		t.Parallel()
		sample := "test-city"
		got := builder.NewDefaultAddressBuilder().WithCity(sample).Build()
		testkit.Equal(t, got.City, sample, "WithCity must set City")
	})

	t.Run("WithZip sets the Zip field", func(t *testing.T) {
		t.Parallel()
		sample := "test-zip"
		got := builder.NewDefaultAddressBuilder().WithZip(sample).Build()
		testkit.Equal(t, got.Zip, sample, "WithZip must set Zip")
	})

}

func TestMinimalBuilder(t *testing.T) {
	t.Parallel()

	t.Run("Build without setters returns defaults", func(t *testing.T) {
		t.Parallel()
		defaults := builder.Minimal{}
		got := builder.NewMinimalBuilder(defaults).Build()
		testkit.Equal(t, got, defaults, "must return defaults unchanged")
	})

	t.Run("NewDefault returns zero value", func(t *testing.T) {
		t.Parallel()
		got := builder.NewDefaultMinimalBuilder().Build()
		testkit.Equal(t, got, builder.Minimal{}, "must return zero value")
	})

	t.Run("WithValue sets the Value field", func(t *testing.T) {
		t.Parallel()
		sample := "test-value"
		got := builder.NewDefaultMinimalBuilder().WithValue(sample).Build()
		testkit.Equal(t, got.Value, sample, "WithValue must set Value")
	})

}
