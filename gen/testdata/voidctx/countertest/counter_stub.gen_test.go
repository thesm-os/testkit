// Code generated by testkit stub. DO NOT EDIT.
// Source: counter.go:19-22 (testkit stub Counter)

package countertest_test

import (
	"context"
	"testing"
	"time"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/gen/testdata/voidctx/countertest"
	"go.thesmos.sh/testkit/rand"
)

func TestCounterStub(t *testing.T) {
	t.Parallel()

	t.Run("Add default returns zero", func(t *testing.T) {
		t.Parallel()
		s := countertest.NewCounterStub(t)
		s.Add(t.Context(), 0)
	})

	t.Run("Add Func override", func(t *testing.T) {
		t.Parallel()
		s := countertest.NewCounterStub(t)
		called := false
		s.OnAdd.Func(func(context.Context, int64) {
			called = true
		})
		s.Add(t.Context(), 0)
		testkit.True(t, called, "Func must be called")
	})

	t.Run("Add records calls", func(t *testing.T) {
		t.Parallel()
		s := countertest.NewCounterStub(t)
		s.Add(t.Context(), 0)
		s.OnAdd.AssertCalledOnce(t, "must record Add call")
	})

	t.Run("WithCounterAdd constructor option", func(t *testing.T) {
		t.Parallel()
		called := false
		s := countertest.NewCounterStub(t, countertest.WithCounterAdd(func(context.Context, int64) {
			called = true
		}))
		s.Add(t.Context(), 0)
		testkit.True(t, called, "WithCounterAdd must be called")
	})

	t.Run("Add records call args", func(t *testing.T) {
		t.Parallel()
		s := countertest.NewCounterStub(t)
		s.Add(t.Context(), 42)
		call := s.OnAdd.LastCall(t)
		testkit.Equal(t, call.Value, 42, "Value must be recorded")
	})

	t.Run("Add OnRecord hook fires", func(t *testing.T) {
		t.Parallel()
		s := countertest.NewCounterStub(t)
		var observed []countertest.CounterAddCall
		s.OnAdd.OnRecord(func(c countertest.CounterAddCall) {
			observed = append(observed, c)
		})
		s.Add(t.Context(), 0)
		s.Add(t.Context(), 0)
		testkit.Len(t, observed, 2, "OnRecord hook must fire for every Add call")
	})

	t.Run("Name default returns zero", func(t *testing.T) {
		t.Parallel()
		s := countertest.NewCounterStub(t)
		result := s.Name()
		testkit.Equal(t, result, "", "Name must return zero")
	})

	t.Run("Name Func override", func(t *testing.T) {
		t.Parallel()
		s := countertest.NewCounterStub(t)
		called := false
		s.OnName.Func(func() string {
			called = true
			return "test-result"
		})
		result := s.Name()
		testkit.True(t, called, "Func must be called")
		testkit.Equal(t, result, "test-result", "Func must return set value")
	})

	t.Run("Name Returns fixed value", func(t *testing.T) {
		t.Parallel()
		s := countertest.NewCounterStub(t)
		s.OnName.Returns("test-result")
		result := s.Name()
		testkit.Equal(t, result, "test-result", "Returns must return set value")
	})

	t.Run("Name records calls", func(t *testing.T) {
		t.Parallel()
		s := countertest.NewCounterStub(t)
		_ = s.Name()
		s.OnName.AssertCalledOnce(t, "must record Name call")
	})

	t.Run("WithCounterName constructor option", func(t *testing.T) {
		t.Parallel()
		called := false
		s := countertest.NewCounterStub(t, countertest.WithCounterName(func() string {
			called = true
			return ""
		}))
		_ = s.Name()
		testkit.True(t, called, "WithCounterName must be called")
	})

	t.Run("Name OnRecord hook fires", func(t *testing.T) {
		t.Parallel()
		s := countertest.NewCounterStub(t)
		var observed []countertest.CounterNameCall
		s.OnName.OnRecord(func(c countertest.CounterNameCall) {
			observed = append(observed, c)
		})
		_ = s.Name()
		_ = s.Name()
		testkit.Len(t, observed, 2, "OnRecord hook must fire for every Name call")
	})

	t.Run("BenchMode disables recording", func(t *testing.T) {
		t.Parallel()
		s := countertest.NewCounterStub(t, countertest.CounterStubBenchMode())
		s.Add(t.Context(), 0)
		testkit.Equal(t, s.OnAdd.CallCount(), 0,
			"BenchMode must disable recording")
	})

	t.Run("WithRandSource propagates to all methods", func(t *testing.T) {
		t.Parallel()
		s := countertest.NewCounterStub(t, countertest.CounterStubWithRandSource(rand.FixedRandSource(0.5)))
		_ = s // verify construction succeeds with RandSource
	})

	t.Run("Times mismatch fails", func(t *testing.T) {
		t.Parallel()
		f := testkit.NewFailableTB()
		s := countertest.NewCounterStub(f)
		s.OnAdd.Times(2)
		s.Add(t.Context(), 0)
		f.RunCleanups()
		testkit.True(t, f.Failed(), "Times mismatch must fail")
	})

	t.Run("TimesAtLeast mismatch fails", func(t *testing.T) {
		t.Parallel()
		f := testkit.NewFailableTB()
		s := countertest.NewCounterStub(f)
		s.OnAdd.TimesAtLeast(3)
		s.Add(t.Context(), 0)
		f.RunCleanups()
		testkit.True(t, f.Failed(), "TimesAtLeast mismatch must fail")
	})

	t.Run("ResetCalls clears all recordings", func(t *testing.T) {
		t.Parallel()
		s := countertest.NewCounterStub(t)
		s.Add(t.Context(), 0)
		_ = s.Name()
		s.ResetCalls()
		s.OnAdd.AssertNotCalled(t, "Add must be cleared after ResetCalls")
		s.OnName.AssertNotCalled(t, "Name must be cleared after ResetCalls")
	})

	t.Run("Strict mode fails on unconfigured Add", func(t *testing.T) {
		t.Parallel()
		f := testkit.NewFailableTB()
		s := countertest.NewCounterStub(f, countertest.CounterStubStrict())
		s.Add(t.Context(), 0)
		testkit.True(t, f.Failed(), "strict mode must fail on unconfigured Add")
	})

	t.Run("Strict mode fails on unconfigured Name", func(t *testing.T) {
		t.Parallel()
		f := testkit.NewFailableTB()
		s := countertest.NewCounterStub(f, countertest.CounterStubStrict())
		_ = s.Name()
		testkit.True(t, f.Failed(), "strict mode must fail on unconfigured Name")
	})

	t.Run("DelegateTo forwards to implementation", func(t *testing.T) {
		t.Parallel()
		inner := countertest.NewCounterStub(t)
		inner.OnName.Returns("")
		s := countertest.NewCounterStub(t, countertest.CounterStubDelegateTo(inner))
		s.Add(t.Context(), 0)
		_ = s.Name()
		s.OnAdd.AssertCalledOnce(t, "Add must delegate")
		s.OnName.AssertCalledOnce(t, "Name must delegate")
	})

	t.Run("DelegateTo surfaces Name return values", func(t *testing.T) {
		t.Parallel()
		inner := countertest.NewCounterStub(t)
		inner.OnName.Returns("test-result")
		s := countertest.NewCounterStub(t, countertest.CounterStubDelegateTo(inner))
		result := s.Name()
		testkit.Equal(t, result, "test-result", "Name must return configured value")
	})

	t.Run("ResetCalls preserves Name Returns config", func(t *testing.T) {
		t.Parallel()
		s := countertest.NewCounterStub(t)
		s.OnName.Returns("test-result")
		_ = s.Name()
		s.ResetCalls()
		s.OnName.AssertNotCalled(t, "ResetCalls must clear recordings")
		result := s.Name()
		testkit.Equal(t, result, "test-result", "Name must return configured value")
	})

	t.Run("ResetCalls clears timestamps", func(t *testing.T) {
		t.Parallel()
		s := countertest.NewCounterStub(t)
		s.Add(t.Context(), 0)
		testkit.Len(t, s.OnAdd.Timestamped(), 1, "must record")
		s.ResetCalls()
		testkit.Len(t, s.OnAdd.Timestamped(), 0, "ResetCalls must clear timestamps")
	})

	t.Run("Add Latency honors virtual clock", func(t *testing.T) {
		t.Parallel()
		clk := clock.NewTestClock(time.Unix(0, 0))
		s := countertest.NewCounterStub(t, countertest.CounterStubWithClock(clk))
		s.OnAdd.Latency(5 * time.Second)

		done := make(chan struct{})
		go func() {
			s.Add(t.Context(), 0)
			close(done)
		}()

		clk.AwaitWaiters(1)
		start := time.Now()
		clk.Advance(6 * time.Second)
		<-done
		testkit.True(t, time.Since(start) < 100*time.Millisecond,
			"Latency must use virtual clock, not real time")
	})

	t.Run("Add Latency happens before Record", func(t *testing.T) {
		t.Parallel()
		clk := clock.NewTestClock(time.Unix(0, 0))
		s := countertest.NewCounterStub(t, countertest.CounterStubWithClock(clk))
		s.OnAdd.Latency(5 * time.Second)

		done := make(chan struct{})
		go func() {
			s.Add(t.Context(), 0)
			close(done)
		}()

		clk.AwaitWaiters(1)
		testkit.Equal(t, s.OnAdd.CallCount(), 0,
			"call must not record until latency completes")
		clk.Advance(6 * time.Second)
		<-done
		testkit.Equal(t, s.OnAdd.CallCount(), 1,
			"call records after latency")
	})

	t.Run("Add Latency applies on DelegateTo path", func(t *testing.T) {
		t.Parallel()
		clk := clock.NewTestClock(time.Unix(0, 0))
		inner := countertest.NewCounterStub(t)
		inner.OnName.Returns("")
		s := countertest.NewCounterStub(t,
			countertest.CounterStubWithClock(clk),
			countertest.CounterStubDelegateTo(inner))
		s.OnAdd.Latency(5 * time.Second)

		done := make(chan struct{})
		go func() {
			s.Add(t.Context(), 0)
			close(done)
		}()

		clk.AwaitWaiters(1)
		start := time.Now()
		clk.Advance(6 * time.Second)
		<-done
		testkit.True(t, time.Since(start) < 100*time.Millisecond,
			"DelegateTo path must honor Latency with virtual clock")
	})
}

func TestCounterStubBenchModeZeroAlloc(t *testing.T) {
	s := countertest.NewCounterStub(nil, countertest.CounterStubBenchMode())
	s.OnName.Returns("")

	t.Run("Add", func(t *testing.T) {
		const iterations = 100
		avgAllocs := testing.AllocsPerRun(iterations, func() {
			s.Add(t.Context(), 0)
		})
		testkit.Equal(t, int(avgAllocs), 0, "Add in BenchMode must be 0 alloc")
	})

	t.Run("Name", func(t *testing.T) {
		const iterations = 100
		avgAllocs := testing.AllocsPerRun(iterations, func() {
			_ = s.Name()
		})
		testkit.Equal(t, int(avgAllocs), 0, "Name in BenchMode must be 0 alloc")
	})

}
