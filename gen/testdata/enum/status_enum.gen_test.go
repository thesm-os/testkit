// Code generated by testkit enum. DO NOT EDIT.
// Source: testkit enum Status Priority Region

package enum_test

import (
	"fmt"
	"testing"

	"go.thesmos.sh/testkit/gen/testdata/enum"

	"go.thesmos.sh/testkit"
)

func TestStatus_Enum(t *testing.T) {
	t.Parallel()

	type enumEntry struct {
		value    enum.Status
		wantName string
	}
	all := []enumEntry{
		{enum.StatusActive, "Active"},
		{enum.StatusClosed, "Closed"},
		{enum.StatusPending, "Pending"},
		{enum.StatusUnspecified, "Unspecified"},
	}

	t.Run("stringer", func(t *testing.T) {
		t.Parallel()
		for _, tt := range all {
			t.Run(tt.wantName, func(t *testing.T) {
				t.Parallel()
				testkit.Equal(t, tt.value.String(), tt.wantName,
					fmt.Sprintf("%s.String() must match expected", tt.wantName))
			})
		}
	})

	t.Run("exhaustive", func(t *testing.T) {
		t.Parallel()
		testkit.Equal(t, len(all), 4,
			"must have exactly 4 values")
	})

	t.Run("boundary max+1 uses fallback format", func(t *testing.T) {
		t.Parallel()
		outOfRange := enum.Status(3 + 1)
		expected := fmt.Sprintf("Status(%d)", 3+1)
		testkit.Equal(t, outOfRange.String(), expected,
			"out-of-range value must use fallback format")
	})

	t.Run("all values are distinct", func(t *testing.T) {
		t.Parallel()
		seen := make(map[enum.Status]string)
		for _, tt := range all {
			if prev, ok := seen[tt.value]; ok {
				t.Errorf("duplicate: %s and %s both equal %d",
					prev, tt.wantName, int(tt.value))
			}
			seen[tt.value] = tt.wantName
		}
	})
}

func TestPriority_Enum(t *testing.T) {
	t.Parallel()

	type enumEntry struct {
		value    enum.Priority
		wantName string
	}
	all := []enumEntry{
		{enum.PriorityHigh, "High"},
		{enum.PriorityLow, "Low"},
		{enum.PriorityMedium, "Medium"},
	}

	t.Run("stringer", func(t *testing.T) {
		t.Parallel()
		for _, tt := range all {
			t.Run(tt.wantName, func(t *testing.T) {
				t.Parallel()
				testkit.Equal(t, tt.value.String(), tt.wantName,
					fmt.Sprintf("%s.String() must match expected", tt.wantName))
			})
		}
	})

	t.Run("exhaustive", func(t *testing.T) {
		t.Parallel()
		testkit.Equal(t, len(all), 3,
			"must have exactly 3 values")
	})

	t.Run("boundary max+1 uses fallback format", func(t *testing.T) {
		t.Parallel()
		outOfRange := enum.Priority(2 + 1)
		expected := fmt.Sprintf("Priority(%d)", 2+1)
		testkit.Equal(t, outOfRange.String(), expected,
			"out-of-range value must use fallback format")
	})

	t.Run("all values are distinct", func(t *testing.T) {
		t.Parallel()
		seen := make(map[enum.Priority]string)
		for _, tt := range all {
			if prev, ok := seen[tt.value]; ok {
				t.Errorf("duplicate: %s and %s both equal %d",
					prev, tt.wantName, int(tt.value))
			}
			seen[tt.value] = tt.wantName
		}
	})
}

func TestRegion_Enum(t *testing.T) {
	t.Parallel()

	type enumEntry struct {
		value    enum.Region
		wantName string
	}
	all := []enumEntry{
		{enum.RegionAP, "RegionAP"},
		{enum.RegionEU, "RegionEU"},
		{enum.RegionUS, "RegionUS"},
	}

	t.Run("stringer", func(t *testing.T) {
		t.Parallel()
		for _, tt := range all {
			t.Run(tt.wantName, func(t *testing.T) {
				t.Parallel()
				testkit.Equal(t, tt.value.String(), tt.wantName,
					fmt.Sprintf("%s.String() must match expected", tt.wantName))
			})
		}
	})

	t.Run("exhaustive", func(t *testing.T) {
		t.Parallel()
		testkit.Equal(t, len(all), 3,
			"must have exactly 3 values")
	})

	t.Run("boundary max+1 uses fallback format", func(t *testing.T) {
		t.Parallel()
		outOfRange := enum.Region(2 + 1)
		expected := fmt.Sprintf("Region(%d)", 2+1)
		testkit.Equal(t, outOfRange.String(), expected,
			"out-of-range value must use fallback format")
	})

	t.Run("all values are distinct", func(t *testing.T) {
		t.Parallel()
		seen := make(map[enum.Region]string)
		for _, tt := range all {
			if prev, ok := seen[tt.value]; ok {
				t.Errorf("duplicate: %s and %s both equal %d",
					prev, tt.wantName, int(tt.value))
			}
			seen[tt.value] = tt.wantName
		}
	})
}
