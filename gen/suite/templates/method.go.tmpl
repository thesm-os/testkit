{{define "method"}}
func run{{.InterfaceName}}{{.Name}}(t *testing.T, factory func() {{.QualifiedType}}, cfg *{{.LowerInterfaceName}}Config) {
	t.Helper()

{{template "subtests" .}}

{{- /* Plug-in dispatch — single method-named wrapper, primitives nest inside */}}
{{- if .IsReader}}
	if len(cfg.on{{.Name}}) > 0 {
		t.Run("{{.Name}}", func(t *testing.T) {
			prePopFactory := func() {{.QualifiedType}} {
				impl := factory()
				if cfg.prePopulate != nil {
					cfg.prePopulate(t.Context(), impl)
				}
				return impl
			}
			rctx := suite.ReaderContext[{{.QualifiedType}}, {{.Shape.KeyType}}, {{.Shape.ValType}}]{
				T: t,
				ReaderBindings: bindings.ReaderBindings[{{.QualifiedType}}, {{.Shape.KeyType}}, {{.Shape.ValType}}]{
					Factory: prePopFactory,
					Call: func(ctx context.Context, impl {{.QualifiedType}}, k {{.Shape.KeyType}}) ({{.Shape.ValType}}, error) {
						return impl.{{.Name}}(ctx, k)
					},
				},
			}
			for _, a := range cfg.on{{.Name}} {
				a(rctx)
			}
		})
	}
{{- else if .IsWriter}}
	if len(cfg.on{{.Name}}) > 0 {
		t.Run("{{.Name}}", func(t *testing.T) {
			prePopFactory := func() {{.QualifiedType}} {
				impl := factory()
				if cfg.prePopulate != nil {
					cfg.prePopulate(t.Context(), impl)
				}
				return impl
			}
			wctx := suite.WriterContext[{{.QualifiedType}}, {{.Shape.ValType}}]{
				T: t,
				WriterBindings: bindings.WriterBindings[{{.QualifiedType}}, {{.Shape.ValType}}]{
					Factory: prePopFactory,
					Call: func(ctx context.Context, impl {{.QualifiedType}}, v {{.Shape.ValType}}) error {
						return impl.{{.Name}}(ctx, v)
					},
				},
			}
			for _, a := range cfg.on{{.Name}} {
				a(wctx)
			}
		})
	}
{{- else if .IsStreamReader}}
	if len(cfg.on{{.Name}}) > 0 {
		t.Run("{{.Name}}", func(t *testing.T) {
			prePopFactory := func() {{.QualifiedType}} {
				impl := factory()
				if cfg.prePopulate != nil {
					cfg.prePopulate(t.Context(), impl)
				}
				return impl
			}
			sctx := suite.StreamContext[{{.QualifiedType}}, {{.Shape.IterInfo.ElemType}}]{
				T: t,
				StreamBindings: bindings.StreamBindings[{{.QualifiedType}}, {{.Shape.IterInfo.ElemType}}]{
					Factory: prePopFactory,
					Call: func(ctx context.Context, impl {{.QualifiedType}}) iter.Seq2[{{.Shape.IterInfo.ElemType}}, error] {
{{- if .Shape.IterInfo.Seq2Error}}
						return impl.{{.Name}}({{.StreamCallArgs "impl"}})
{{- else}}
						return func(yield func({{.Shape.IterInfo.ElemType}}, error) bool) {
							for v := range impl.{{.Name}}({{.StreamCallArgs "impl"}}) {
								if !yield(v, nil) { return }
							}
						}
{{- end}}
					},
				},
			}
			for _, a := range cfg.on{{.Name}} {
				a(sctx)
			}
		})
	}
{{- else if .IsDeleter}}
	if len(cfg.on{{.Name}}) > 0 {
		t.Run("{{.Name}}", func(t *testing.T) {
			prePopFactory := func() {{.QualifiedType}} {
				impl := factory()
				if cfg.prePopulate != nil {
					cfg.prePopulate(t.Context(), impl)
				}
				return impl
			}
			dctx := suite.DeleterContext[{{.QualifiedType}}, {{.Shape.KeyType}}]{
				T: t,
				DeleterBindings: bindings.DeleterBindings[{{.QualifiedType}}, {{.Shape.KeyType}}]{
					Factory: prePopFactory,
					Call: func(ctx context.Context, impl {{.QualifiedType}}, k {{.Shape.KeyType}}) error {
						return impl.{{.Name}}(ctx, k)
					},
				},
			}
			for _, a := range cfg.on{{.Name}} {
				a(dctx)
			}
		})
	}
{{- else if .IsAggregator}}
	if len(cfg.on{{.Name}}) > 0 {
		t.Run("{{.Name}}", func(t *testing.T) {
			prePopFactory := func() {{.QualifiedType}} {
				impl := factory()
				if cfg.prePopulate != nil {
					cfg.prePopulate(t.Context(), impl)
				}
				return impl
			}
			actx := suite.AggregatorContext[{{.QualifiedType}}, {{.Shape.ValType}}]{
				T: t,
				AggregatorBindings: bindings.AggregatorBindings[{{.QualifiedType}}, {{.Shape.ValType}}]{
					Factory: prePopFactory,
					Call: func(ctx context.Context, impl {{.QualifiedType}}) ({{.Shape.ValType}}, error) {
						return impl.{{.Name}}(ctx)
					},
				},
			}
			for _, a := range cfg.on{{.Name}} {
				a(actx)
			}
		})
	}
{{- else if .IsLifecycle}}
	if len(cfg.on{{.Name}}) > 0 {
		t.Run("{{.Name}}", func(t *testing.T) {
			prePopFactory := func() {{.QualifiedType}} {
				impl := factory()
				if cfg.prePopulate != nil {
					cfg.prePopulate(t.Context(), impl)
				}
				return impl
			}
			lctx := suite.LifecycleContext[{{.QualifiedType}}]{
				T: t,
				LifecycleBindings: bindings.LifecycleBindings[{{.QualifiedType}}]{
					Factory: prePopFactory,
					Call: func(ctx context.Context, impl {{.QualifiedType}}) error {
						return impl.{{.Name}}(ctx)
					},
				},
			}
			for _, a := range cfg.on{{.Name}} {
				a(lctx)
			}
		})
	}
{{- else if .IsPure}}
	if len(cfg.on{{.Name}}) > 0 {
		t.Run("{{.Name}}", func(t *testing.T) {
			prePopFactory := func() {{.QualifiedType}} {
				impl := factory()
				if cfg.prePopulate != nil {
					cfg.prePopulate(t.Context(), impl)
				}
				return impl
			}
			pctx := suite.PureContext[{{.QualifiedType}}, {{.Shape.ValType}}]{
				T: t,
				PureBindings: bindings.PureBindings[{{.QualifiedType}}, {{.Shape.ValType}}]{
					Factory: prePopFactory,
					Call: func(impl {{.QualifiedType}}) {{.Shape.ValType}} {
						return impl.{{.Name}}({{.PureCallArgs "impl"}})
					},
				},
			}
			for _, a := range cfg.on{{.Name}} {
				a(pctx)
			}
		})
	}
{{- else if .IsPredicate}}
	if len(cfg.on{{.Name}}) > 0 {
		t.Run("{{.Name}}", func(t *testing.T) {
			prePopFactory := func() {{.QualifiedType}} {
				impl := factory()
				if cfg.prePopulate != nil {
					cfg.prePopulate(t.Context(), impl)
				}
				return impl
			}
			pctx := suite.PredicateContext[{{.QualifiedType}}]{
				T: t,
				PredicateBindings: bindings.PredicateBindings[{{.QualifiedType}}]{
					Factory: prePopFactory,
					Call: func(impl {{.QualifiedType}}) bool {
						return impl.{{.Name}}({{.PureCallArgs "impl"}})
					},
				},
			}
			for _, a := range cfg.on{{.Name}} {
				a(pctx)
			}
		})
	}
{{- else if .IsMutator}}
	if len(cfg.on{{.Name}}) > 0 {
		t.Run("{{.Name}}", func(t *testing.T) {
			prePopFactory := func() {{.QualifiedType}} {
				impl := factory()
				if cfg.prePopulate != nil {
					cfg.prePopulate(t.Context(), impl)
				}
				return impl
			}
			mctx := suite.MutatorContext[{{.QualifiedType}}, {{.Shape.ValType}}]{
				T: t,
				MutatorBindings: bindings.MutatorBindings[{{.QualifiedType}}, {{.Shape.ValType}}]{
					Factory: prePopFactory,
					Call: func(ctx context.Context, impl {{.QualifiedType}}, v {{.Shape.ValType}}) {
						impl.{{.Name}}(ctx, v)
					},
				},
			}
			for _, a := range cfg.on{{.Name}} {
				a(mctx)
			}
		})
	}
{{- else if .IsReaderWithBool}}
	if len(cfg.on{{.Name}}) > 0 {
		t.Run("{{.Name}}", func(t *testing.T) {
			prePopFactory := func() {{.QualifiedType}} {
				impl := factory()
				if cfg.prePopulate != nil {
					cfg.prePopulate(t.Context(), impl)
				}
				return impl
			}
			rctx := suite.ReaderWithBoolContext[{{.QualifiedType}}, {{.Shape.KeyType}}, {{.Shape.ValType}}]{
				T: t,
				ReaderWithBoolBindings: bindings.ReaderWithBoolBindings[{{.QualifiedType}}, {{.Shape.KeyType}}, {{.Shape.ValType}}]{
					Factory: prePopFactory,
					Call: func(ctx context.Context, impl {{.QualifiedType}}, k {{.Shape.KeyType}}) ({{.Shape.ValType}}, bool) {
{{- if .HasCtx}}
						return impl.{{.Name}}(ctx, k)
{{- else}}
						return impl.{{.Name}}(k)
{{- end}}
					},
				},
			}
			for _, a := range cfg.on{{.Name}} {
				a(rctx)
			}
		})
	}
{{- else if .IsLookup}}
	if len(cfg.on{{.Name}}) > 0 {
		t.Run("{{.Name}}", func(t *testing.T) {
			prePopFactory := func() {{.QualifiedType}} {
				impl := factory()
				if cfg.prePopulate != nil {
					cfg.prePopulate(t.Context(), impl)
				}
				return impl
			}
			lctx := suite.LookupContext[{{.QualifiedType}}, {{.Shape.KeyType}}, {{.Shape.ValType}}, {{.Shape.RetType}}]{
				T: t,
				LookupBindings: bindings.LookupBindings[{{.QualifiedType}}, {{.Shape.KeyType}}, {{.Shape.ValType}}, {{.Shape.RetType}}]{
					Factory: prePopFactory,
					Call: func(ctx context.Context, impl {{.QualifiedType}}, k {{.Shape.KeyType}}) ({{.Shape.ValType}}, {{.Shape.RetType}}, bool) {
{{- if .HasCtx}}
						return impl.{{.Name}}(ctx, k)
{{- else}}
						return impl.{{.Name}}(k)
{{- end}}
					},
				},
			}
			for _, a := range cfg.on{{.Name}} {
				a(lctx)
			}
		})
	}
{{- else if .IsPoisonAccessor}}
	if len(cfg.on{{.Name}}) > 0 {
		t.Run("{{.Name}}", func(t *testing.T) {
			prePopFactory := func() {{.QualifiedType}} {
				impl := factory()
				if cfg.prePopulate != nil {
					cfg.prePopulate(t.Context(), impl)
				}
				return impl
			}
			pctx := suite.PoisonAccessorContext[{{.QualifiedType}}]{
				T: t,
				PoisonAccessorBindings: bindings.PoisonAccessorBindings[{{.QualifiedType}}]{
					Factory: prePopFactory,
					Call: func(impl {{.QualifiedType}}) error {
						return impl.{{.Name}}()
					},
				},
			}
			for _, a := range cfg.on{{.Name}} {
				a(pctx)
			}
		})
	}
{{- else}}
	if len(cfg.on{{.Name}}) > 0 {
		t.Run("{{.Name}}", func(t *testing.T) {
			for _, fn := range cfg.on{{.Name}} {
				impl := factory()
				if cfg.prePopulate != nil {
					cfg.prePopulate(t.Context(), impl)
				}
				fn(t, impl)
			}
		})
	}
{{- end}}
}
{{end}}
