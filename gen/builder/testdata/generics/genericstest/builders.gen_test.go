// Code generated by testkit builder. DO NOT EDIT.
// Source: testkit builder Container Pair

package genericstest_test

import (
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/gen/builder/testdata/generics"
	"go.thesmos.sh/testkit/gen/builder/testdata/generics/genericstest"
)

func TestContainerBuilder(t *testing.T) {
	t.Parallel()

	t.Run("Build does not panic", func(t *testing.T) {
		t.Parallel()
		_ = genericstest.NewContainer[string]().Build()
	})

	t.Run("NewContainerFrom round-trips", func(t *testing.T) {
		t.Parallel()
		v := generics.Container[string]{}
		got := genericstest.NewContainerFrom(v).Build()
		testkit.Equal(t, got, v, "must round-trip zero value")
	})

	t.Run("Clone forks independent copy", func(t *testing.T) {
		t.Parallel()
		base := genericstest.NewContainer[string]()
		clone := base.Clone().WithLabel("test-label")
		original := base.Build()
		modified := clone.Build()
		testkit.True(t, original.Label != modified.Label,
			"Clone must produce independent copy")
	})

	t.Run("Mutate modifies value", func(t *testing.T) {
		t.Parallel()
		sample := "test-label"
		got := genericstest.NewContainer[string]().
			Mutate(func(v *generics.Container[string]) {
				v.Label = sample
			}).Build()
		testkit.Equal(t, got.Label, sample, "Mutate must modify")
	})

	t.Run("WithItems replaces existing slice", func(t *testing.T) {
		t.Parallel()
		sample := []string{"test-items"}
		got := genericstest.NewContainer[string]().
			WithItems(sample...).
			WithItems(sample...).
			Build()
		testkit.Len(t, got.Items, len(sample),
			"With must replace, not append — second call overwrites first")
	})

	t.Run("AppendItems preserves existing", func(t *testing.T) {
		t.Parallel()
		sample := []string{"test-items"}
		got := genericstest.NewContainer[string]().
			WithItems(sample...).
			AppendItems(sample...).
			Build()
		testkit.Len(t, got.Items, len(sample)*2,
			"Append must preserve existing + add new")
	})

	t.Run("Clone deep-copies Items slice", func(t *testing.T) {
		t.Parallel()
		sample := []string{"test-items"}
		base := genericstest.NewContainer[string]().WithItems(sample...)
		clone := base.Clone()
		clone.AppendItems(sample...)
		original := base.Build()
		modified := clone.Build()
		testkit.Len(t, original.Items, len(sample),
			"original slice must be unchanged")
		testkit.Len(t, modified.Items, len(sample)*2,
			"clone must have appended")
	})

	t.Run("WithLabel sets field", func(t *testing.T) {
		t.Parallel()
		sample := "test-label"
		got := genericstest.NewContainer[string]().
			WithLabel(sample).Build()
		testkit.Equal(t, got.Label, sample, "must set Label")
	})

	t.Run("WithLimit sets field", func(t *testing.T) {
		t.Parallel()
		sample := 42
		got := genericstest.NewContainer[string]().
			WithLimit(sample).Build()
		testkit.Equal(t, got.Limit, sample, "must set Limit")
	})

}

func TestPairBuilder(t *testing.T) {
	t.Parallel()

	t.Run("Build does not panic", func(t *testing.T) {
		t.Parallel()
		_ = genericstest.NewPair[string, int]().Build()
	})

	t.Run("NewPairFrom round-trips", func(t *testing.T) {
		t.Parallel()
		v := generics.Pair[string, int]{}
		got := genericstest.NewPairFrom(v).Build()
		testkit.Equal(t, got, v, "must round-trip zero value")
	})

	t.Run("Clone forks independent copy", func(t *testing.T) {
		t.Parallel()
		base := genericstest.NewPair[string, int]()
		clone := base.Clone().WithFirst("test-first")
		original := base.Build()
		modified := clone.Build()
		testkit.True(t, original.First != modified.First,
			"Clone must produce independent copy")
	})

	t.Run("Mutate modifies value", func(t *testing.T) {
		t.Parallel()
		sample := "test-first"
		got := genericstest.NewPair[string, int]().
			Mutate(func(v *generics.Pair[string, int]) {
				v.First = sample
			}).Build()
		testkit.Equal(t, got.First, sample, "Mutate must modify")
	})

	t.Run("WithFirst sets field", func(t *testing.T) {
		t.Parallel()
		sample := "test-first"
		got := genericstest.NewPair[string, int]().
			WithFirst(sample).Build()
		testkit.Equal(t, got.First, sample, "must set First")
	})

	t.Run("WithSecond sets field", func(t *testing.T) {
		t.Parallel()
		sample := 42
		got := genericstest.NewPair[string, int]().
			WithSecond(sample).Build()
		testkit.Equal(t, got.Second, sample, "must set Second")
	})

}
