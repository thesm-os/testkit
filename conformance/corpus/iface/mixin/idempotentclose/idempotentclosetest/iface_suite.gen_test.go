// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/mixin/idempotentclose/iface.go
// Plugins:   golang 1.0.0, suite 1.3.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package idempotentclosetest_test

import (
	"context"
	"errors"
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/iface/mixin/idempotentclose/idempotentclosetest"
)

// Every generated check for Closer, driven against a stand-in built
// to violate it.
//
//	Proved:   9 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertCloserCloseSmokeCanFail holds AssertCloserCloseSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertCloserCloseSmokeCanFail(t *testing.T) {
	t.Parallel()

	subject := idempotentclosetest.NewCloserStub(t,
		idempotentclosetest.WithCloserClose(func(context.Context) error {
			panic("Closer.Close: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			idempotentclosetest.AssertCloserCloseSmoke(tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertCloserCloseCancelsCanFail holds AssertCloserCloseCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertCloserCloseCancelsCanFail(t *testing.T) {
	t.Parallel()

	subject := idempotentclosetest.NewCloserStub(t,
		idempotentclosetest.WithCloserClose(func(context.Context) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			idempotentclosetest.AssertCloserCloseCancels(tb, subject)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertCloserCloseHonoursDeadlineCanFail holds AssertCloserCloseHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertCloserCloseHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	subject := idempotentclosetest.NewCloserStub(t,
		idempotentclosetest.WithCloserClose(func(context.Context) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			idempotentclosetest.AssertCloserCloseHonoursDeadline(tb, subject)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertCloserCloseToleratesNilContextCanFail holds AssertCloserCloseToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertCloserCloseToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	subject := idempotentclosetest.NewCloserStub(t,
		idempotentclosetest.WithCloserClose(func(context.Context) error {
			panic("Closer.Close: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			idempotentclosetest.AssertCloserCloseToleratesNilContext(tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertCloserCloseCloseTwiceCanFail holds AssertCloserCloseCloseTwice to being able to fail.
//
// The stand-in closes cleanly once and refuses every call after, which is the
// subject the second-call claim exists to reject.
func TestAssertCloserCloseCloseTwiceCanFail(t *testing.T) {
	t.Parallel()

	closes := 0
	subject := idempotentclosetest.NewCloserStub(t,
		idempotentclosetest.WithCloserClose(func(context.Context) error {
			closes++
			if closes > 1 {
				return errors.New("violate: already closed")
			}
			return nil
		}))

	got := testkit.Rejects(t, "a teardown that refuses its second call",
		func(tb testing.TB) {
			tb.Helper()
			idempotentclosetest.AssertCloserCloseCloseTwice(tb, subject)
		})
	testkit.Assert(t, got).Contains("must be a no-op",
		"and rejects it for the reason the check is about")
}

// TestAssertCloserStatsSmokeCanFail holds AssertCloserStatsSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertCloserStatsSmokeCanFail(t *testing.T) {
	t.Parallel()

	subject := idempotentclosetest.NewCloserStub(t,
		idempotentclosetest.WithCloserStats(func(context.Context) (int, error) {
			panic("Closer.Stats: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			idempotentclosetest.AssertCloserStatsSmoke(tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertCloserStatsCancelsCanFail holds AssertCloserStatsCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertCloserStatsCancelsCanFail(t *testing.T) {
	t.Parallel()

	subject := idempotentclosetest.NewCloserStub(t,
		idempotentclosetest.WithCloserStats(func(context.Context) (int, error) {
			var r0 int
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			idempotentclosetest.AssertCloserStatsCancels(tb, subject)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertCloserStatsHonoursDeadlineCanFail holds AssertCloserStatsHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertCloserStatsHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	subject := idempotentclosetest.NewCloserStub(t,
		idempotentclosetest.WithCloserStats(func(context.Context) (int, error) {
			var r0 int
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			idempotentclosetest.AssertCloserStatsHonoursDeadline(tb, subject)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertCloserStatsToleratesNilContextCanFail holds AssertCloserStatsToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertCloserStatsToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	subject := idempotentclosetest.NewCloserStub(t,
		idempotentclosetest.WithCloserStats(func(context.Context) (int, error) {
			panic("Closer.Stats: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			idempotentclosetest.AssertCloserStatsToleratesNilContext(tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance 2cbb963227e85e92723f86e4b632f70c5d46c544e66b9142e970b184bc840e56
