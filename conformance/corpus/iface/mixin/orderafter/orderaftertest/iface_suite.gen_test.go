// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/mixin/orderafter/iface.go
// Plugins:   golang 1.0.0, suite 1.0.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/iface/mixin/orderafter/... ./corpus/iface/mixin/nilsafe/... ./corpus/iface/mixin/sideeffect/... ./corpus/iface/mixin/hooks/...

package orderaftertest_test

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/iface/mixin/orderafter/orderaftertest"
)

// Every generated check for Mixed, driven against a stand-in built
// to violate it.
//
//	Proved:   9 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertMixedCommitSmokeCanFail holds AssertMixedCommitSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertMixedCommitSmokeCanFail(t *testing.T) {
	t.Parallel()

	subject := orderaftertest.NewMixedStub(t,
		orderaftertest.WithMixedCommit(func(context.Context) error {
			panic("Mixed.Commit: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			orderaftertest.AssertMixedCommitSmoke(tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedCommitCancelsCanFail holds AssertMixedCommitCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertMixedCommitCancelsCanFail(t *testing.T) {
	t.Parallel()

	subject := orderaftertest.NewMixedStub(t,
		orderaftertest.WithMixedCommit(func(context.Context) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			orderaftertest.AssertMixedCommitCancels(tb, subject)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedCommitHonoursDeadlineCanFail holds AssertMixedCommitHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertMixedCommitHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	subject := orderaftertest.NewMixedStub(t,
		orderaftertest.WithMixedCommit(func(context.Context) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			orderaftertest.AssertMixedCommitHonoursDeadline(tb, subject)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedCommitToleratesNilContextCanFail holds AssertMixedCommitToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertMixedCommitToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	subject := orderaftertest.NewMixedStub(t,
		orderaftertest.WithMixedCommit(func(context.Context) error {
			panic("Mixed.Commit: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			orderaftertest.AssertMixedCommitToleratesNilContext(tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedCommitRequiresItsPrerequisiteCanFail holds AssertMixedCommitRequiresItsPrerequisite to being able to fail.
//
// The stand-in answers before the prerequisite has run. A check that let that
// pass would report an ordering constraint for a method that has none, and the
// caller who skipped a step would find out in production.
func TestAssertMixedCommitRequiresItsPrerequisiteCanFail(t *testing.T) {
	t.Parallel()

	subject := orderaftertest.NewMixedStub(t,
		orderaftertest.WithMixedCommit(func(context.Context) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that accepts the call without its prerequisite",
		func(tb testing.TB) {
			tb.Helper()
			orderaftertest.AssertMixedCommitRequiresItsPrerequisite(tb, subject)
		})
	testkit.Assert(t, got).Contains("must be refused",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedPrepareSmokeCanFail holds AssertMixedPrepareSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertMixedPrepareSmokeCanFail(t *testing.T) {
	t.Parallel()

	subject := orderaftertest.NewMixedStub(t,
		orderaftertest.WithMixedPrepare(func(context.Context) error {
			panic("Mixed.Prepare: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			orderaftertest.AssertMixedPrepareSmoke(tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedPrepareCancelsCanFail holds AssertMixedPrepareCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertMixedPrepareCancelsCanFail(t *testing.T) {
	t.Parallel()

	subject := orderaftertest.NewMixedStub(t,
		orderaftertest.WithMixedPrepare(func(context.Context) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			orderaftertest.AssertMixedPrepareCancels(tb, subject)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedPrepareHonoursDeadlineCanFail holds AssertMixedPrepareHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertMixedPrepareHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	subject := orderaftertest.NewMixedStub(t,
		orderaftertest.WithMixedPrepare(func(context.Context) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			orderaftertest.AssertMixedPrepareHonoursDeadline(tb, subject)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedPrepareToleratesNilContextCanFail holds AssertMixedPrepareToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertMixedPrepareToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	subject := orderaftertest.NewMixedStub(t,
		orderaftertest.WithMixedPrepare(func(context.Context) error {
			panic("Mixed.Prepare: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			orderaftertest.AssertMixedPrepareToleratesNilContext(tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance d61b837cba93875495e491c984fd673bb0f9dc8554a0bb5bc8c12ce7486b284f
