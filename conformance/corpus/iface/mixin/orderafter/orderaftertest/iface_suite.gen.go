// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/mixin/orderafter/iface.go
// Plugins:   golang 1.0.0, suite 1.9.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package orderaftertest

import (
	"context"
	"slices"
	"testing"
	"time"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/conformance/corpus/iface/mixin/orderafter"
)

// MixedCommitCheck is one assertion about Commit.
//
// Every generated check for Commit is a value of it, and so is one you
// write — so they compose, reorder, and each runs standalone. testing.TB rather
// than *testing.T is what lets a stand-in drive it and prove it can fail.
type MixedCommitCheck func(tb testing.TB, subject orderafter.Mixed)

// MixedPrepareCheck is one assertion about Prepare.
//
// Every generated check for Prepare is a value of it, and so is one you
// write — so they compose, reorder, and each runs standalone. testing.TB rather
// than *testing.T is what lets a stand-in drive it and prove it can fail.
type MixedPrepareCheck func(tb testing.TB, subject orderafter.Mixed)

// MixedFixture holds every input the generated checks run against.
//
// Derived from each method's parameter names and types. Every field has a
// companion holding a second, different value: a check comparing a result
// against a single input passes whenever the subject happened to be seeded with
// it, and a miss check whose key happens to hit asserts nothing.
//
// A field whose type admits no literal — a func, a channel, a type from a
// package this run never read — is declared and left at its zero value, and
// every generated check that needed it was dropped rather than emitted against
// a value nobody could write. Supply one through MixedWithFixture
// and the check a consumer writes has something to use.
type MixedFixture struct {
}

// DefaultMixedFixture is what this run derived. Replace any field through
// MixedWithFixture; the rest keep these values.
func DefaultMixedFixture() MixedFixture {
	return MixedFixture{}
}

// AssertMixedContract runs every generated check against every declared subject.
//
//	Checks:   9 across 2 methods, per subject
//	Subjects: declare each with MixedSubject
//	Double:   every subject runs a second time wrapped in MixedStub, so
//	          anything the wrapper fails that the subject passes is the double
//	          lying. MixedWithoutDouble declines it.
//	Extend:   MixedOnCommit, MixedOnPrepare
//	Drop:     MixedWithout, by the path each check reports under
//
// # What is checked somewhere else
//
// These need a reference implementation to compare against, which a suite run
// has no way to build. Nothing here asserts them and nothing here should:
//
//   - lifecycle, on Commit, Prepare
//
// //testkit:model on the interface derives that reference, and the
// MixedModel option it generates runs them here. Its own file
// names the path it reports under and the MixedWithout call that
// declines it — this header said "model" for every fixture, and a twinned
// reference registers under "model/twin", so it was wrong wherever the
// oracle could not be derived.
func AssertMixedContract(t *testing.T, opts ...MixedOption) {
	t.Helper()
	cfg := newMixedConfig(opts...)
	if len(cfg.subjects) == 0 {
		t.Fatalf("AssertMixedContract was given no subject; declare one with " +
			"MixedSubject(name, factory)")
	}
	for _, p := range cfg.phantoms() {
		t.Errorf("MixedWithout(%q) declines nothing: no check here reports "+
			"under that path, so this suite asserts one thing more than its "+
			"author believes", p)
	}
	for _, s := range cfg.subjects {
		runMixedChecks(t, cfg, s.name, s.factory, nil)
		if !cfg.withoutDouble {
			runMixedChecks(t, cfg, s.name+" through the double", s.factory,
				func(tb testing.TB, subject orderafter.Mixed) orderafter.Mixed {
					return NewMixedStub(tb, MixedStubDelegateTo(subject))
				})
		}
	}
}

// runMixedChecks runs the whole contract against one subject.
//
// wrap is applied after the subject is built and seeded, so a wrapper is
// transparent to the seed and every failure it causes is its own.
func runMixedChecks(
	t *testing.T,
	cfg *mixedConfig,
	label string,
	factory func() orderafter.Mixed,
	wrap func(tb testing.TB, subject orderafter.Mixed) orderafter.Mixed,
) {
	t.Helper()
	t.Run(label, func(t *testing.T) {
		t.Parallel()

		t.Run("Commit", func(t *testing.T) {
			t.Parallel()
			cfg.run(t, "Commit/smoke", "smoke", func(tb testing.TB) {
				AssertMixedCommitSmoke(tb, cfg.subject(tb, factory, wrap))
			})
			cfg.run(t, "Commit/reports a cancelled context", "reports a cancelled context", func(tb testing.TB) {
				AssertMixedCommitCancels(tb, cfg.subject(tb, factory, wrap))
			})
			cfg.run(t, "Commit/reports an expired deadline", "reports an expired deadline", func(tb testing.TB) {
				AssertMixedCommitHonoursDeadline(tb, cfg.subject(tb, factory, wrap))
			})
			cfg.run(t, "Commit/tolerates a nil context", "tolerates a nil context", func(tb testing.TB) {
				AssertMixedCommitToleratesNilContext(tb, cfg.subject(tb, factory, wrap))
			})
			cfg.run(t, "Commit/orderafter", "orderafter", func(tb testing.TB) {
				AssertMixedCommitRequiresItsPrerequisite(tb, cfg.subject(tb, factory, wrap))
			})
			for _, c := range cfg.onCommit {
				cfg.run(t, "Commit"+"/"+c.name, c.name, func(tb testing.TB) {
					c.fn(tb, cfg.subject(tb, factory, wrap))
				})
			}
		})

		t.Run("Prepare", func(t *testing.T) {
			t.Parallel()
			cfg.run(t, "Prepare/smoke", "smoke", func(tb testing.TB) {
				AssertMixedPrepareSmoke(tb, cfg.subject(tb, factory, wrap))
			})
			cfg.run(t, "Prepare/reports a cancelled context", "reports a cancelled context", func(tb testing.TB) {
				AssertMixedPrepareCancels(tb, cfg.subject(tb, factory, wrap))
			})
			cfg.run(t, "Prepare/reports an expired deadline", "reports an expired deadline", func(tb testing.TB) {
				AssertMixedPrepareHonoursDeadline(tb, cfg.subject(tb, factory, wrap))
			})
			cfg.run(t, "Prepare/tolerates a nil context", "tolerates a nil context", func(tb testing.TB) {
				AssertMixedPrepareToleratesNilContext(tb, cfg.subject(tb, factory, wrap))
			})
			for _, c := range cfg.onPrepare {
				cfg.run(t, "Prepare"+"/"+c.name, c.name, func(tb testing.TB) {
					c.fn(tb, cfg.subject(tb, factory, wrap))
				})
			}
		})

		// The extra bodies of checks this run was given, each reporting under its
		// own name and dropped by it.
		//
		// The plain subject only. The wrapped pass exists to prove the double
		// faithful and the per-method checks above already do that, so running one
		// through the wrapper prices the wrapper and says nothing new about the
		// subject.
		if wrap == nil {
			for _, e := range cfg.extensions {
				if cfg.declined(e.name) {
					t.Run(e.name, func(t *testing.T) {
						t.Skipf("dropped through MixedWithout(%q)", e.name)
					})
					continue
				}
				t.Run(e.name, func(t *testing.T) {
					t.Parallel()
					e.run(t, label, factory, cfg)
				})
			}
		}
	})
}

// AssertMixedCommitSmoke asserts Commit survives a call with derived inputs.
//
// Fails when: Commit panics. The weakest check in this file and the one
// that catches the most — a method that panics on a derived value is one no
// other check here reaches.
func AssertMixedCommitSmoke(tb testing.TB, subject orderafter.Mixed) {
	tb.Helper()
	defer func() {
		if r := recover(); r != nil {
			tb.Fatalf("Commit panicked on a derived value (%v); supply one it "+
				"accepts through MixedWithFixture", r)
		}
	}()
	ctx := tb.Context()
	_ = subject.Commit(ctx)
}

// AssertMixedCommitCancels asserts Commit reports a cancelled context as cancelled.
//
// Fails when: Commit returns nil for a context cancelled before the
// call, or returns an error that does not answer to context.Canceled. Both
// matter and they are different defects: the first is work done that the caller
// asked not to be, the second is a caller who wrote
// `errors.Is(err, context.Canceled)` and gets false for a call that was.
//
// The error rather than merely its presence is what separates this from the
// deadline check. Asserting only that something came back makes the two one
// check written twice.
func AssertMixedCommitCancels(tb testing.TB, subject orderafter.Mixed) {
	tb.Helper()
	ctx, cancel := context.WithCancel(tb.Context())
	cancel()
	err := subject.Commit(ctx)
	testkit.ErrorIs(tb, err, context.Canceled,
		"Commit must report a cancelled context as context.Canceled")
}

// AssertMixedCommitHonoursDeadline asserts Commit reports an expired deadline as exceeded.
//
// Fails when: Commit returns nil for a context whose deadline has
// passed, or returns an error that does not answer to
// context.DeadlineExceeded. Distinct from cancellation in what the caller
// learns: a cancelled call was called off, an expired one ran out of time, and
// only the second is worth retrying.
//
// The deadline is the zero time, which is unconditionally in the past. No clock
// is read: a generated check that consults the wall clock is one whose subject
// is partly the machine it runs on.
func AssertMixedCommitHonoursDeadline(tb testing.TB, subject orderafter.Mixed) {
	tb.Helper()
	ctx, cancel := context.WithDeadline(tb.Context(), time.Time{})
	defer cancel()
	err := subject.Commit(ctx)
	testkit.ErrorIs(tb, err, context.DeadlineExceeded,
		"Commit must report an expired deadline as context.DeadlineExceeded")
}

// AssertMixedCommitToleratesNilContext asserts Commit does not panic on a nil context.
//
// Fails when: Commit panics. Returning an error is correct and
// succeeding is correct — a nil context reaches production through a caller
// that forgot one, and a panic turns that into an outage rather than a failed
// request.
func AssertMixedCommitToleratesNilContext(tb testing.TB, subject orderafter.Mixed) {
	tb.Helper()
	defer func() {
		if r := recover(); r != nil {
			tb.Errorf("Commit panicked on a nil context (%v); return an error instead", r)
		}
	}()
	//nolint:staticcheck // passing nil is the check.
	var ctx context.Context
	_ = subject.Commit(ctx)
}

// AssertMixedCommitRequiresItsPrerequisite asserts the call is refused before Prepare has run.
//
// Fails when: Commit succeeds on a subject Prepare was never called
// on. An ordering constraint nothing enforces is a comment: the second caller to
// get it wrong finds out in production, and the first one wrote the code that
// looks correct.
//
// A fresh subject rather than the seeded one, so the prerequisite genuinely has
// not run — which is why this check builds its own rather than taking what the
// harness hands every other check.
func AssertMixedCommitRequiresItsPrerequisite(tb testing.TB, subject orderafter.Mixed) {
	tb.Helper()
	ctx := tb.Context()
	err := subject.Commit(ctx)
	testkit.Error(tb, err,
		"Commit must be refused until Prepare has run")
	testkit.ErrorIs(tb, err, orderafter.ErrNotReady,
		"and the refusal reports the declared unready sentinel")
}

// AssertMixedPrepareSmoke asserts Prepare survives a call with derived inputs.
//
// Fails when: Prepare panics. The weakest check in this file and the one
// that catches the most — a method that panics on a derived value is one no
// other check here reaches.
func AssertMixedPrepareSmoke(tb testing.TB, subject orderafter.Mixed) {
	tb.Helper()
	defer func() {
		if r := recover(); r != nil {
			tb.Fatalf("Prepare panicked on a derived value (%v); supply one it "+
				"accepts through MixedWithFixture", r)
		}
	}()
	ctx := tb.Context()
	_ = subject.Prepare(ctx)
}

// AssertMixedPrepareCancels asserts Prepare reports a cancelled context as cancelled.
//
// Fails when: Prepare returns nil for a context cancelled before the
// call, or returns an error that does not answer to context.Canceled. Both
// matter and they are different defects: the first is work done that the caller
// asked not to be, the second is a caller who wrote
// `errors.Is(err, context.Canceled)` and gets false for a call that was.
//
// The error rather than merely its presence is what separates this from the
// deadline check. Asserting only that something came back makes the two one
// check written twice.
func AssertMixedPrepareCancels(tb testing.TB, subject orderafter.Mixed) {
	tb.Helper()
	ctx, cancel := context.WithCancel(tb.Context())
	cancel()
	err := subject.Prepare(ctx)
	testkit.ErrorIs(tb, err, context.Canceled,
		"Prepare must report a cancelled context as context.Canceled")
}

// AssertMixedPrepareHonoursDeadline asserts Prepare reports an expired deadline as exceeded.
//
// Fails when: Prepare returns nil for a context whose deadline has
// passed, or returns an error that does not answer to
// context.DeadlineExceeded. Distinct from cancellation in what the caller
// learns: a cancelled call was called off, an expired one ran out of time, and
// only the second is worth retrying.
//
// The deadline is the zero time, which is unconditionally in the past. No clock
// is read: a generated check that consults the wall clock is one whose subject
// is partly the machine it runs on.
func AssertMixedPrepareHonoursDeadline(tb testing.TB, subject orderafter.Mixed) {
	tb.Helper()
	ctx, cancel := context.WithDeadline(tb.Context(), time.Time{})
	defer cancel()
	err := subject.Prepare(ctx)
	testkit.ErrorIs(tb, err, context.DeadlineExceeded,
		"Prepare must report an expired deadline as context.DeadlineExceeded")
}

// AssertMixedPrepareToleratesNilContext asserts Prepare does not panic on a nil context.
//
// Fails when: Prepare panics. Returning an error is correct and
// succeeding is correct — a nil context reaches production through a caller
// that forgot one, and a panic turns that into an outage rather than a failed
// request.
func AssertMixedPrepareToleratesNilContext(tb testing.TB, subject orderafter.Mixed) {
	tb.Helper()
	defer func() {
		if r := recover(); r != nil {
			tb.Errorf("Prepare panicked on a nil context (%v); return an error instead", r)
		}
	}()
	//nolint:staticcheck // passing nil is the check.
	var ctx context.Context
	_ = subject.Prepare(ctx)
}

// MixedOption configures a contract run.
type MixedOption func(*mixedConfig)

// MixedSubject declares an implementation to hold to the contract.
//
// Repeat it for every implementation that claims to satisfy the interface — an
// in-memory one, the one backed by a database, the one behind a network. Every
// check and every option is stated once and runs against all of them, which is
// what a conformance suite is for.
func MixedSubject(name string, factory func() orderafter.Mixed) MixedOption {
	return func(c *mixedConfig) {
		c.subjects = append(c.subjects, namedMixedSubject{name, factory})
	}
}

// MixedWithoutDouble declines the second run through MixedStub.
//
// The double run proves the stand-in is faithful, and costs a second pass over
// every check. Decline it where the double is not used.
func MixedWithoutDouble() MixedOption {
	return func(c *mixedConfig) { c.withoutDouble = true }
}

// MixedWithClock supplies the clock every time-reading check measures on.
//
// Defaults to the real one, so an implementation that does not take a clock
// behaves as it would have. Supply a [clock.TestClock] — the same one the
// factory builds the subject with — and a budget becomes a claim about the time
// the implementation means to spend rather than about how loaded the machine
// was.
func MixedWithClock(c clock.Clock) MixedOption {
	return func(cfg *mixedConfig) { cfg.clock = c }
}

// MixedWithFixture replaces the derived inputs.
func MixedWithFixture(f MixedFixture) MixedOption {
	return func(c *mixedConfig) { c.Fixture = f }
}

// MixedSeed replaces the derived seed.
//
// For an interface with no writer to seed itself through, which is the one
// input a conformance run cannot derive: a reader over an empty subject
// asserts nothing.
//
// Returning an error rather than swallowing one. A seed that failed quietly
// leaves every check after it asserting nothing and reporting success.
func MixedSeed(fn func(ctx context.Context, subject orderafter.Mixed) error) MixedOption {
	return func(c *mixedConfig) {
		c.seed = fn
	}
}

// MixedOnCommit adds a named check to Commit.
func MixedOnCommit(name string, fn MixedCommitCheck) MixedOption {
	return func(c *mixedConfig) {
		c.onCommit = append(c.onCommit, namedMixedCommitCheck{name, fn})
	}
}

// MixedOnPrepare adds a named check to Prepare.
func MixedOnPrepare(name string, fn MixedPrepareCheck) MixedOption {
	return func(c *mixedConfig) {
		c.onPrepare = append(c.onPrepare, namedMixedPrepareCheck{name, fn})
	}
}

// MixedWithout drops generated checks by the path each reports under.
//
// For a subject that legitimately violates one. Without this the only recourse
// is to stop running the suite, which costs every other check in the file — so
// a dropped check is reported as skipped rather than silently omitted.
func MixedWithout(paths ...string) MixedOption {
	return func(c *mixedConfig) {
		for _, p := range paths {
			c.without[p] = struct{}{}
		}
	}
}

type namedMixedCommitCheck struct {
	name string
	fn   MixedCommitCheck
}

type namedMixedPrepareCheck struct {
	name string
	fn   MixedPrepareCheck
}

type namedMixedSubject struct {
	name    string
	factory func() orderafter.Mixed
}

// mixedContractExtension is a body of checks a generated sibling adds through an option.
//
// A sibling declares the option and closes over whatever configures it, so
// everything that varies per run is typed and private to the file that owns it
// — this file understands only the name and the call. Nothing arrives except
// through an option, which is what keeps the set fixed before the first subtest
// starts.
type mixedContractExtension struct {
	// name is the path this reports under, and the one MixedWithout drops
	// it by. Anything nested beneath it is the extension's own to name.
	name string

	// run takes the factory rather than a subject. One that drives sequences
	// builds a subject per iteration, and one that compares against a reference
	// needs the unseeded subject a factory returns — which
	// mixedConfig.subject is not.
	//
	// Called once per subject, in parallel, sharing the config. Anything it
	// mutates has to be built inside the call: state captured when the option
	// was declared is state two subjects write at once.
	run func(
		t *testing.T,
		subject string,
		factory func() orderafter.Mixed,
		cfg *mixedConfig,
	)
}

type mixedConfig struct {
	Fixture       MixedFixture
	subjects      []namedMixedSubject
	withoutDouble bool
	clock         clock.Clock
	seed          func(ctx context.Context, subject orderafter.Mixed) error
	without       map[string]struct{}
	extensions    []mixedContractExtension
	onCommit      []namedMixedCommitCheck
	onPrepare     []namedMixedPrepareCheck
}

func newMixedConfig(opts ...MixedOption) *mixedConfig {
	c := &mixedConfig{
		Fixture: DefaultMixedFixture(),
		clock:   clock.RealClock(),
		without: map[string]struct{}{},
	}
	for _, o := range opts {
		o(c)
	}
	return c
}

// subject builds a fresh implementation and seeds it, so every check runs
// against state no other check touched.
func (c *mixedConfig) subject(
	tb testing.TB,
	factory func() orderafter.Mixed,
	wrap func(tb testing.TB, subject orderafter.Mixed) orderafter.Mixed,
) orderafter.Mixed {
	tb.Helper()
	s := factory()
	if c.seed != nil {
		// Reported rather than ignored. A seed that silently failed would leave
		// every check after it running against an empty subject — passing, and
		// asserting nothing.
		//
		// Which seed ran decides what the message can usefully say. The derived
		// one fails for one of two reasons and both are fixable from here, so it
		// names them. A seed the caller supplied is theirs, and telling them to
		// supply one is advice they already took.
		if err := c.seed(tb.Context(), s); err != nil {
			tb.Fatalf("the seed supplied through MixedSeed failed: %v", err)
		}
	}
	if wrap != nil {
		s = wrap(tb, s)
	}
	return s
}

// dropped reports whether a path was declined through MixedWithout.
//
// Named rather than inlined because the drop set is read from two places: here,
// and by whatever registered through the seam — which reports under paths this
// file has never heard of and must answer the same question about them.
func (c *mixedConfig) declined(path string) bool {
	_, ok := c.without[path]
	return ok
}

// mixedPaths is every path this file reports a check under.
//
// The set a MixedWithout argument has to be a member of. Declared
// rather than observed: a check that did not run is not a check that does not
// exist — an integration-only method skips without ever consulting the drop
// set, and reading that as a phantom would fail a correct suite on a machine
// with the environment unset.
//
//nolint:gochecknoglobals // the emitted check paths, read-only after init.
var mixedPaths = map[string]struct{}{
	"Commit/smoke":                        {},
	"Commit/reports a cancelled context":  {},
	"Commit/reports an expired deadline":  {},
	"Commit/tolerates a nil context":      {},
	"Commit/orderafter":                   {},
	"Prepare/smoke":                       {},
	"Prepare/reports a cancelled context": {},
	"Prepare/reports an expired deadline": {},
	"Prepare/tolerates a nil context":     {},
}

// phantoms names every MixedWithout path that declines nothing.
//
// A drop that drops nothing is this option's silent half: the consumer wrote
// it against a check since renamed, or never emitted for that method at all,
// and every run after reads green while asserting one thing more than its
// author believes. Extensions carry their own names — the seam reports under
// paths this file has never heard of — so they count as known too.
func (c *mixedConfig) phantoms() []string {
	known := func(p string) bool {
		if _, ok := mixedPaths[p]; ok {
			return true
		}
		for _, e := range c.extensions {
			if e.name == p {
				return true
			}
		}
		for _, on := range c.onCommit {
			if "Commit"+"/"+on.name == p {
				return true
			}
		}
		for _, on := range c.onPrepare {
			if "Prepare"+"/"+on.name == p {
				return true
			}
		}
		return false
	}
	var out []string
	for p := range c.without {
		if !known(p) {
			out = append(out, p)
		}
	}
	slices.Sort(out)
	return out
}

// run executes one check unless it was dropped, reporting the drop rather than
// silently omitting it.
func (c *mixedConfig) run(t *testing.T, path, name string, fn func(tb testing.TB)) {
	t.Helper()
	if c.declined(path) {
		t.Run(name, func(t *testing.T) {
			t.Skipf("dropped through MixedWithout(%q)", path)
		})
		return
	}
	t.Run(name, func(t *testing.T) {
		t.Parallel()
		fn(t)
	})
}

// testkit: end of generated content.
// testkit:provenance 81d514f641bf49b2489fed1fc6672ab1da767e3af319b62edb6d25cfb030c309
