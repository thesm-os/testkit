// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/mixin/snapshotisolation/iface.go
// Plugins:   golang 1.0.0, suite 1.15.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package snapshotisolationtest_test

import (
	"context"
	"errors"
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/iface/mixin/snapshotisolation"
	"go.thesmos.sh/testkit/conformance/corpus/iface/mixin/snapshotisolation/snapshotisolationtest"
)

// Every generated check for Mixed, driven against a stand-in built
// to violate it.
//
//	Proved:   13 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertMixedRecordSmokeCanFail holds AssertMixedRecordSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertMixedRecordSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := snapshotisolationtest.DefaultMixedFixture()
	subject := snapshotisolationtest.NewMixedStub(t,
		snapshotisolationtest.WithMixedRecord(func(context.Context, snapshotisolation.Entry) error {
			panic("Mixed.Record: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			snapshotisolationtest.AssertMixedRecordSmoke(tb, subject, fixture.E)
		})
	testkit.Assert(t, got).Contains("Record panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedRecordCancelsCanFail holds AssertMixedRecordCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertMixedRecordCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := snapshotisolationtest.DefaultMixedFixture()
	subject := snapshotisolationtest.NewMixedStub(t,
		snapshotisolationtest.WithMixedRecord(func(context.Context, snapshotisolation.Entry) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			snapshotisolationtest.AssertMixedRecordCancels(tb, subject, fixture.E)
		})
	testkit.Assert(t, got).Contains("Record must report a cancelled context as context.Canceled",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedRecordHonoursDeadlineCanFail holds AssertMixedRecordHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertMixedRecordHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := snapshotisolationtest.DefaultMixedFixture()
	subject := snapshotisolationtest.NewMixedStub(t,
		snapshotisolationtest.WithMixedRecord(func(context.Context, snapshotisolation.Entry) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			snapshotisolationtest.AssertMixedRecordHonoursDeadline(tb, subject, fixture.E)
		})
	testkit.Assert(t, got).Contains("Record must report an expired deadline as context.DeadlineExceeded",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedRecordToleratesNilContextCanFail holds AssertMixedRecordToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertMixedRecordToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := snapshotisolationtest.DefaultMixedFixture()
	subject := snapshotisolationtest.NewMixedStub(t,
		snapshotisolationtest.WithMixedRecord(func(context.Context, snapshotisolation.Entry) error {
			panic("Mixed.Record: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			snapshotisolationtest.AssertMixedRecordToleratesNilContext(tb, subject, fixture.E)
		})
	testkit.Assert(t, got).Contains("Record panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedHistorySmokeCanFail holds AssertMixedHistorySmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertMixedHistorySmokeCanFail(t *testing.T) {
	t.Parallel()

	subject := snapshotisolationtest.NewMixedStub(t,
		snapshotisolationtest.WithMixedHistory(func(context.Context) ([]snapshotisolation.Entry, error) {
			panic("Mixed.History: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			snapshotisolationtest.AssertMixedHistorySmoke(tb, subject)
		})
	testkit.Assert(t, got).Contains("History panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedHistoryCancelsCanFail holds AssertMixedHistoryCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertMixedHistoryCancelsCanFail(t *testing.T) {
	t.Parallel()

	subject := snapshotisolationtest.NewMixedStub(t,
		snapshotisolationtest.WithMixedHistory(func(context.Context) ([]snapshotisolation.Entry, error) {
			var r0 []snapshotisolation.Entry
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			snapshotisolationtest.AssertMixedHistoryCancels(tb, subject)
		})
	testkit.Assert(t, got).Contains("History must report a cancelled context as context.Canceled",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedHistoryHonoursDeadlineCanFail holds AssertMixedHistoryHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertMixedHistoryHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	subject := snapshotisolationtest.NewMixedStub(t,
		snapshotisolationtest.WithMixedHistory(func(context.Context) ([]snapshotisolation.Entry, error) {
			var r0 []snapshotisolation.Entry
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			snapshotisolationtest.AssertMixedHistoryHonoursDeadline(tb, subject)
		})
	testkit.Assert(t, got).Contains("History must report an expired deadline as context.DeadlineExceeded",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedHistoryToleratesNilContextCanFail holds AssertMixedHistoryToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertMixedHistoryToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	subject := snapshotisolationtest.NewMixedStub(t,
		snapshotisolationtest.WithMixedHistory(func(context.Context) ([]snapshotisolation.Entry, error) {
			panic("Mixed.History: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			snapshotisolationtest.AssertMixedHistoryToleratesNilContext(tb, subject)
		})
	testkit.Assert(t, got).Contains("History panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedGetSmokeCanFail holds AssertMixedGetSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertMixedGetSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := snapshotisolationtest.DefaultMixedFixture()
	subject := snapshotisolationtest.NewMixedStub(t,
		snapshotisolationtest.WithMixedGet(func(context.Context, string) (snapshotisolation.Entry, error) {
			panic("Mixed.Get: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			snapshotisolationtest.AssertMixedGetSmoke(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("Get panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedGetCancelsCanFail holds AssertMixedGetCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertMixedGetCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := snapshotisolationtest.DefaultMixedFixture()
	subject := snapshotisolationtest.NewMixedStub(t,
		snapshotisolationtest.WithMixedGet(func(context.Context, string) (snapshotisolation.Entry, error) {
			var r0 snapshotisolation.Entry
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			snapshotisolationtest.AssertMixedGetCancels(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("Get must report a cancelled context as context.Canceled",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedGetHonoursDeadlineCanFail holds AssertMixedGetHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertMixedGetHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := snapshotisolationtest.DefaultMixedFixture()
	subject := snapshotisolationtest.NewMixedStub(t,
		snapshotisolationtest.WithMixedGet(func(context.Context, string) (snapshotisolation.Entry, error) {
			var r0 snapshotisolation.Entry
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			snapshotisolationtest.AssertMixedGetHonoursDeadline(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("Get must report an expired deadline as context.DeadlineExceeded",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedGetToleratesNilContextCanFail holds AssertMixedGetToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertMixedGetToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := snapshotisolationtest.DefaultMixedFixture()
	subject := snapshotisolationtest.NewMixedStub(t,
		snapshotisolationtest.WithMixedGet(func(context.Context, string) (snapshotisolation.Entry, error) {
			panic("Mixed.Get: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			snapshotisolationtest.AssertMixedGetToleratesNilContext(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("Get panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedGetZeroOnErrorCanFail holds AssertMixedGetZeroOnError to being able to fail.
//
// The stand-in refuses the call and answers a value that looks like a result
// anyway. A check that let that pass would leave a caller who checks the error
// and a caller who checks the value disagreeing about what happened — the
// claim's own violation, not a stand-in that merely succeeds.
func TestAssertMixedGetZeroOnErrorCanFail(t *testing.T) {
	t.Parallel()

	fixture := snapshotisolationtest.DefaultMixedFixture()
	subject := snapshotisolationtest.NewMixedStub(t,
		snapshotisolationtest.WithMixedGet(func(context.Context, string) (snapshotisolation.Entry, error) {
			return snapshotisolation.Entry{Txn: 42}, errors.New("violate: refused with a believable value")
		}))

	got := testkit.Rejects(t, "a method answering a believable value beside its error",
		func(tb testing.TB) {
			tb.Helper()
			snapshotisolationtest.AssertMixedGetZeroOnError(tb, subject, fixture.KeyOther)
		})
	testkit.Assert(t, got).Contains("Get must return the zero value alongside an error",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance cc0734abdfa3509eb756250ba4510f9da75528ded3c8ba5e8d4061056b6d0ee5
