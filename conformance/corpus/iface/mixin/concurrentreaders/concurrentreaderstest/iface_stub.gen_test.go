// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/mixin/concurrentreaders/iface.go
// Plugins:   golang 1.0.0, stub 1.0.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package concurrentreaderstest_test

import (
	"context"
	"testing"
	"time"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/conformance/corpus/iface/mixin/concurrentreaders"
	"go.thesmos.sh/testkit/conformance/corpus/iface/mixin/concurrentreaders/concurrentreaderstest"
	"go.thesmos.sh/testkit/rand"
	"go.thesmos.sh/testkit/stub"
)

// Compile-time proof that MixedStub satisfies the interface it doubles.
// A drifted signature fails here rather than at the first test that assigns
// the double.
var _ concurrentreaders.Mixed = (*concurrentreaderstest.MixedStub)(nil)

// mixedStubGetSubject binds Get into the shape
// [stub.Behaviour] drives: how to call it, what it answers with, and how to
// override it.
//
// Each call builds a fresh double, because several of the checks assert on
// failure and need one bound to a failable TB rather than to the running
// test.
func mixedStubGetSubject(tb testing.TB) stub.Subject[concurrentreaderstest.MixedGetCall, concurrentreaderstest.MixedGetReturn] {
	tb.Helper()
	s := concurrentreaderstest.NewMixedStub(tb)
	return stub.Subject[concurrentreaderstest.MixedGetCall, concurrentreaderstest.MixedGetReturn]{
		Stub: s.OnGet.MethodStub,
		Call: func() {
			var a0 context.Context
			var a1 string
			_, _ = s.Get(a0, a1)
		},
		Result: func() concurrentreaderstest.MixedGetReturn {
			var a0 context.Context
			var a1 string
			got0, got1 := s.Get(a0, a1)
			return concurrentreaderstest.MixedGetReturn{Result: got0, Err: got1}
		},
		Override: func(mark func()) {
			s.OnGet.Func(func(_ context.Context, _ string) (string, error) {
				mark()
				var z0 string
				var z1 error
				return z0, z1
			})
		},
		Fails: func() error {
			var a0 context.Context
			var a1 string
			_, r1 := s.Get(a0, a1)
			return r1
		},
	}
}

// TestMixedStubGet pins how Get answers.
//
// Recording, resetting, call-count expectations, strict mode, fault injection
// and zero-value dispatch are the same contract for every method, so they are
// asserted once in [stub.Behaviour] rather than restated here. What remains
// below needs a value this method's signature can tell apart from a zero one.
func TestMixedStubGet(t *testing.T) {
	t.Parallel()

	stub.Behaviour(t, "Get", mixedStubGetSubject)

	t.Run("answers with the value pinned by Returns", func(t *testing.T) {
		t.Parallel()
		s := concurrentreaderstest.NewMixedStub(t)
		var want0 string
		var want1 error
		s.OnGet.Returns(want0, want1)
		var a0 context.Context
		var a1 string
		got0, got1 := s.Get(a0, a1)
		testkit.Equal(t, got0, want0, "Get must answer with what Returns pinned")
		testkit.Equal(t, got1, want1, "Get must answer with what Returns pinned")
	})
	t.Run("records what it was called with", func(t *testing.T) {
		t.Parallel()
		// Asserting only that a call happened would pass against a double
		// that recorded the wrong arguments, which is the failure a reader
		// most needs the log to surface.
		s := concurrentreaderstest.NewMixedStub(t)
		var a0 context.Context
		var a1 string
		_, _ = s.Get(a0, a1)
		got := s.OnGet.AssertCalledOnce(t, "Get must record the call")
		testkit.Equal(t, got.Ctx, a0, "the recorded call carries Ctx")
		testkit.Equal(t, got.Key, a1, "the recorded call carries Key")
	})

	t.Run("fires the OnRecord hook for every call", func(t *testing.T) {
		t.Parallel()
		// The hook fires synchronously as each call lands, which is what a
		// concurrency test observes progress with — polling the log instead
		// races the thing under test.
		s := concurrentreaderstest.NewMixedStub(t)
		var seen []concurrentreaderstest.MixedGetCall
		s.OnGet.OnRecord(func(c concurrentreaderstest.MixedGetCall) { seen = append(seen, c) })
		var a0 context.Context
		var a1 string
		_, _ = s.Get(a0, a1)
		_, _ = s.Get(a0, a1)
		testkit.Len(t, seen, 2, "OnRecord must fire once per Get call")
	})

	t.Run("wires WithMixedGet at construction", func(t *testing.T) {
		t.Parallel()
		called := false
		s := concurrentreaderstest.NewMixedStub(t, concurrentreaderstest.WithMixedGet(func(_ context.Context, _ string) (string, error) {
			called = true
			var z0 string
			var z1 error
			return z0, z1
		}))
		var a0 context.Context
		var a1 string
		_, _ = s.Get(a0, a1)
		testkit.True(t, called, "WithMixedGet must install the override")
	})

	t.Run("keeps the Returns configuration across a reset", func(t *testing.T) {
		t.Parallel()
		// A reset clears what happened, not what was configured. Losing the
		// configuration would make a double answer differently in the second
		// half of a test than in the first, for no reason the reader can see.
		s := concurrentreaderstest.NewMixedStub(t)
		var want0 string
		var want1 error
		s.OnGet.Returns(want0, want1)
		var a0 context.Context
		var a1 string
		_, _ = s.Get(a0, a1)
		s.ResetCalls()
		got0, got1 := s.Get(a0, a1)
		testkit.Equal(t, got0, want0, "a reset must keep what Returns pinned")
		testkit.Equal(t, got1, want1, "a reset must keep what Returns pinned")
	})
}

// mixedStubPutSubject binds Put into the shape
// [stub.Behaviour] drives: how to call it, what it answers with, and how to
// override it.
//
// Each call builds a fresh double, because several of the checks assert on
// failure and need one bound to a failable TB rather than to the running
// test.
func mixedStubPutSubject(tb testing.TB) stub.Subject[concurrentreaderstest.MixedPutCall, concurrentreaderstest.MixedPutReturn] {
	tb.Helper()
	s := concurrentreaderstest.NewMixedStub(tb)
	return stub.Subject[concurrentreaderstest.MixedPutCall, concurrentreaderstest.MixedPutReturn]{
		Stub: s.OnPut.MethodStub,
		Call: func() {
			var a0 context.Context
			var a1 string
			var a2 string
			_ = s.Put(a0, a1, a2)
		},
		Result: func() concurrentreaderstest.MixedPutReturn {
			var a0 context.Context
			var a1 string
			var a2 string
			got0 := s.Put(a0, a1, a2)
			return concurrentreaderstest.MixedPutReturn{Err: got0}
		},
		Override: func(mark func()) {
			s.OnPut.Func(func(_ context.Context, _ string, _ string) error {
				mark()
				var z0 error
				return z0
			})
		},
		Fails: func() error {
			var a0 context.Context
			var a1 string
			var a2 string
			r0 := s.Put(a0, a1, a2)
			return r0
		},
	}
}

// TestMixedStubPut pins how Put answers.
//
// Recording, resetting, call-count expectations, strict mode, fault injection
// and zero-value dispatch are the same contract for every method, so they are
// asserted once in [stub.Behaviour] rather than restated here. What remains
// below needs a value this method's signature can tell apart from a zero one.
func TestMixedStubPut(t *testing.T) {
	t.Parallel()

	stub.Behaviour(t, "Put", mixedStubPutSubject)

	t.Run("answers with the value pinned by Returns", func(t *testing.T) {
		t.Parallel()
		s := concurrentreaderstest.NewMixedStub(t)
		var want0 error
		s.OnPut.Returns(want0)
		var a0 context.Context
		var a1 string
		var a2 string
		got0 := s.Put(a0, a1, a2)
		testkit.Equal(t, got0, want0, "Put must answer with what Returns pinned")
	})
	t.Run("records what it was called with", func(t *testing.T) {
		t.Parallel()
		// Asserting only that a call happened would pass against a double
		// that recorded the wrong arguments, which is the failure a reader
		// most needs the log to surface.
		s := concurrentreaderstest.NewMixedStub(t)
		var a0 context.Context
		var a1 string
		var a2 string
		_ = s.Put(a0, a1, a2)
		got := s.OnPut.AssertCalledOnce(t, "Put must record the call")
		testkit.Equal(t, got.Ctx, a0, "the recorded call carries Ctx")
		testkit.Equal(t, got.Key, a1, "the recorded call carries Key")
		testkit.Equal(t, got.Value, a2, "the recorded call carries Value")
	})

	t.Run("fires the OnRecord hook for every call", func(t *testing.T) {
		t.Parallel()
		// The hook fires synchronously as each call lands, which is what a
		// concurrency test observes progress with — polling the log instead
		// races the thing under test.
		s := concurrentreaderstest.NewMixedStub(t)
		var seen []concurrentreaderstest.MixedPutCall
		s.OnPut.OnRecord(func(c concurrentreaderstest.MixedPutCall) { seen = append(seen, c) })
		var a0 context.Context
		var a1 string
		var a2 string
		_ = s.Put(a0, a1, a2)
		_ = s.Put(a0, a1, a2)
		testkit.Len(t, seen, 2, "OnRecord must fire once per Put call")
	})

	t.Run("wires WithMixedPut at construction", func(t *testing.T) {
		t.Parallel()
		called := false
		s := concurrentreaderstest.NewMixedStub(t, concurrentreaderstest.WithMixedPut(func(_ context.Context, _ string, _ string) error {
			called = true
			var z0 error
			return z0
		}))
		var a0 context.Context
		var a1 string
		var a2 string
		_ = s.Put(a0, a1, a2)
		testkit.True(t, called, "WithMixedPut must install the override")
	})

	t.Run("keeps the Returns configuration across a reset", func(t *testing.T) {
		t.Parallel()
		// A reset clears what happened, not what was configured. Losing the
		// configuration would make a double answer differently in the second
		// half of a test than in the first, for no reason the reader can see.
		s := concurrentreaderstest.NewMixedStub(t)
		var want0 error
		s.OnPut.Returns(want0)
		var a0 context.Context
		var a1 string
		var a2 string
		_ = s.Put(a0, a1, a2)
		s.ResetCalls()
		got0 := s.Put(a0, a1, a2)
		testkit.Equal(t, got0, want0, "a reset must keep what Returns pinned")
	})
}

// mixedStubDouble describes how to build a MixedStub under each
// option whose effect is the same whatever a method's signature.
//
// Get stands in for the double as a whole: what these checks assert
// is that an option reached it at all, and the first method witnesses that as
// well as any other would.
func mixedStubDouble() stub.Double[concurrentreaderstest.MixedGetCall] {
	instance := func(s *concurrentreaderstest.MixedStub) stub.Instance[concurrentreaderstest.MixedGetCall] {
		return stub.Instance[concurrentreaderstest.MixedGetCall]{
			Stub: s.OnGet.MethodStub,
			Call: func() {
				var a0 context.Context
				var a1 string
				_, _ = s.Get(a0, a1)
			},
			Reset: s.ResetCalls,
		}
	}
	return stub.Double[concurrentreaderstest.MixedGetCall]{
		New: func(tb testing.TB) stub.Instance[concurrentreaderstest.MixedGetCall] {
			return instance(concurrentreaderstest.NewMixedStub(tb))
		},
		WithClock: func(tb testing.TB, clk clock.Clock) stub.Instance[concurrentreaderstest.MixedGetCall] {
			return instance(concurrentreaderstest.NewMixedStub(tb, concurrentreaderstest.MixedStubWithClock(clk)))
		},
		WithRandSource: func(tb testing.TB, src rand.Source) stub.Instance[concurrentreaderstest.MixedGetCall] {
			return instance(concurrentreaderstest.NewMixedStub(tb, concurrentreaderstest.MixedStubWithRandSource(src)))
		},
		BenchMode: func(tb testing.TB) stub.Instance[concurrentreaderstest.MixedGetCall] {
			return instance(concurrentreaderstest.NewMixedStub(tb, concurrentreaderstest.MixedStubBenchMode()))
		},
		Strict: func(tb testing.TB) stub.Instance[concurrentreaderstest.MixedGetCall] {
			return instance(concurrentreaderstest.NewMixedStub(tb, concurrentreaderstest.MixedStubStrict()))
		},
	}
}

// TestMixedStubOptions pins the settings that apply to the double rather
// than to one method: bench mode, the injected clock and random source, and
// what a reset clears.
func TestMixedStubOptions(t *testing.T) {
	t.Parallel()

	stub.DoubleBehaviour(t, mixedStubDouble())
}

// TestMixedStubDelegateTo pins that a wrapped implementation is called
// through, and recorded on the way past.
//
// This is what lets one generated suite run against both the double and a
// production type, which is the point of conformance testing.
func TestMixedStubDelegateTo(t *testing.T) {
	t.Parallel()

	inner := concurrentreaderstest.NewMixedStub(t)
	s := concurrentreaderstest.NewMixedStub(t, concurrentreaderstest.MixedStubDelegateTo(inner))

	t.Run("forwards Get to the wrapped implementation", func(t *testing.T) {
		var a0 context.Context
		var a1 string
		_, _ = s.Get(a0, a1)
		inner.OnGet.AssertCalledOnce(t, "Get must reach the wrapped implementation")
	})

	t.Run("surfaces what Get answered", func(t *testing.T) {
		// Reaching the wrapped implementation is not enough: a double that
		// called through and then discarded the answer would pass the check
		// above while telling its caller nothing true.
		want := testkit.TestError("Get-delegate")
		inner.OnGet.FaultsFor(time.Hour, want)
		var a0 context.Context
		var a1 string
		_, r1 := s.Get(a0, a1)
		testkit.ErrorIs(t, r1, want, "Get must surface the wrapped answer")
	})

	t.Run("forwards Put to the wrapped implementation", func(t *testing.T) {
		var a0 context.Context
		var a1 string
		var a2 string
		_ = s.Put(a0, a1, a2)
		inner.OnPut.AssertCalledOnce(t, "Put must reach the wrapped implementation")
	})

	t.Run("surfaces what Put answered", func(t *testing.T) {
		// Reaching the wrapped implementation is not enough: a double that
		// called through and then discarded the answer would pass the check
		// above while telling its caller nothing true.
		want := testkit.TestError("Put-delegate")
		inner.OnPut.FaultsFor(time.Hour, want)
		var a0 context.Context
		var a1 string
		var a2 string
		r0 := s.Put(a0, a1, a2)
		testkit.ErrorIs(t, r0, want, "Put must surface the wrapped answer")
	})
}

// testkit: end of generated content.
// testkit:provenance f88f6dd11d58aee16111d80d32c2915a54d383a1fc784bc48707dbbb7b0ef342
