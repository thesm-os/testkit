// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/mixin/timeaware/iface.go
// Plugins:   golang 1.0.0, suite 1.15.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package timeawaretest_test

import (
	"context"
	"errors"
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/iface/mixin/timeaware/timeawaretest"
)

// Every generated check for Mixed, driven against a stand-in built
// to violate it.
//
//	Proved:   9 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertMixedTouchSmokeCanFail holds AssertMixedTouchSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertMixedTouchSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := timeawaretest.DefaultMixedFixture()
	subject := timeawaretest.NewMixedStub(t,
		timeawaretest.WithMixedTouch(func(context.Context, string) error {
			panic("Mixed.Touch: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			timeawaretest.AssertMixedTouchSmoke(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("Touch panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedTouchCancelsCanFail holds AssertMixedTouchCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertMixedTouchCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := timeawaretest.DefaultMixedFixture()
	subject := timeawaretest.NewMixedStub(t,
		timeawaretest.WithMixedTouch(func(context.Context, string) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			timeawaretest.AssertMixedTouchCancels(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("Touch must report a cancelled context as context.Canceled",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedTouchHonoursDeadlineCanFail holds AssertMixedTouchHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertMixedTouchHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := timeawaretest.DefaultMixedFixture()
	subject := timeawaretest.NewMixedStub(t,
		timeawaretest.WithMixedTouch(func(context.Context, string) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			timeawaretest.AssertMixedTouchHonoursDeadline(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("Touch must report an expired deadline as context.DeadlineExceeded",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedTouchToleratesNilContextCanFail holds AssertMixedTouchToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertMixedTouchToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := timeawaretest.DefaultMixedFixture()
	subject := timeawaretest.NewMixedStub(t,
		timeawaretest.WithMixedTouch(func(context.Context, string) error {
			panic("Mixed.Touch: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			timeawaretest.AssertMixedTouchToleratesNilContext(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("Touch panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedAgeOfSmokeCanFail holds AssertMixedAgeOfSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertMixedAgeOfSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := timeawaretest.DefaultMixedFixture()
	subject := timeawaretest.NewMixedStub(t,
		timeawaretest.WithMixedAgeOf(func(context.Context, string) (int64, error) {
			panic("Mixed.AgeOf: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			timeawaretest.AssertMixedAgeOfSmoke(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("AgeOf panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedAgeOfCancelsCanFail holds AssertMixedAgeOfCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertMixedAgeOfCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := timeawaretest.DefaultMixedFixture()
	subject := timeawaretest.NewMixedStub(t,
		timeawaretest.WithMixedAgeOf(func(context.Context, string) (int64, error) {
			var r0 int64
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			timeawaretest.AssertMixedAgeOfCancels(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("AgeOf must report a cancelled context as context.Canceled",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedAgeOfHonoursDeadlineCanFail holds AssertMixedAgeOfHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertMixedAgeOfHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := timeawaretest.DefaultMixedFixture()
	subject := timeawaretest.NewMixedStub(t,
		timeawaretest.WithMixedAgeOf(func(context.Context, string) (int64, error) {
			var r0 int64
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			timeawaretest.AssertMixedAgeOfHonoursDeadline(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("AgeOf must report an expired deadline as context.DeadlineExceeded",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedAgeOfToleratesNilContextCanFail holds AssertMixedAgeOfToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertMixedAgeOfToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := timeawaretest.DefaultMixedFixture()
	subject := timeawaretest.NewMixedStub(t,
		timeawaretest.WithMixedAgeOf(func(context.Context, string) (int64, error) {
			panic("Mixed.AgeOf: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			timeawaretest.AssertMixedAgeOfToleratesNilContext(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("AgeOf panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedAgeOfZeroOnErrorCanFail holds AssertMixedAgeOfZeroOnError to being able to fail.
//
// The stand-in refuses the call and answers a value that looks like a result
// anyway. A check that let that pass would leave a caller who checks the error
// and a caller who checks the value disagreeing about what happened — the
// claim's own violation, not a stand-in that merely succeeds.
func TestAssertMixedAgeOfZeroOnErrorCanFail(t *testing.T) {
	t.Parallel()

	fixture := timeawaretest.DefaultMixedFixture()
	subject := timeawaretest.NewMixedStub(t,
		timeawaretest.WithMixedAgeOf(func(context.Context, string) (int64, error) {
			return 42, errors.New("violate: refused with a believable value")
		}))

	got := testkit.Rejects(t, "a method answering a believable value beside its error",
		func(tb testing.TB) {
			tb.Helper()
			timeawaretest.AssertMixedAgeOfZeroOnError(tb, subject, fixture.KeyOther)
		})
	testkit.Assert(t, got).Contains("AgeOf must return the zero value alongside an error",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance 2fa37451c1970ff0346a6159774a5ae62098a38a1cc7e87137d7d3cd33799ce7
