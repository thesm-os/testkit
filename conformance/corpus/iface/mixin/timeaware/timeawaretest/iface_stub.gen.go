// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/mixin/timeaware/iface.go
// Plugins:   golang 1.0.0, stub 1.0.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/iface/mixin/timeaware/...

package timeawaretest

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/conformance/corpus/iface/mixin/timeaware"
	"go.thesmos.sh/testkit/rand"
	"go.thesmos.sh/testkit/stub"
)

// MixedTouchCall records one invocation of Mixed.Touch.
//
// Fields take their names from the source signature — parameters and named
// returns alike — so a failure message names what the author named. A slot
// the source left unnamed or blank falls back to a positional name.
type MixedTouchCall struct {
	Ctx context.Context
	Key string
	Err error
}

// MixedAgeOfCall records one invocation of Mixed.AgeOf.
//
// Fields take their names from the source signature — parameters and named
// returns alike — so a failure message names what the author named. A slot
// the source left unnamed or blank falls back to a positional name.
type MixedAgeOfCall struct {
	Ctx    context.Context
	Key    string
	Result int64
	Err    error
}

// --- Per-method configuration ---

// MixedTouchStub controls how the double answers Touch and records
// what it was asked.
//
// The embedded MethodStub supplies the machinery every method shares: call
// recording, fault injection, latency against a virtual clock, gates,
// call-count expectations, and strict mode.
type MixedTouchStub struct {
	*stub.MethodStub[MixedTouchCall]

	fn       func(context.Context, string) error
	fallback *MixedTouchReturn
}

// MixedTouchReturn holds the fixed answer configured through Returns.
type MixedTouchReturn struct {
	Err error
}

// Returns pins a fixed result for every call to Touch. A Func
// override and an injected fault both take precedence over it.
func (s *MixedTouchStub) Returns(err error) *MixedTouchStub {
	s.fallback = &MixedTouchReturn{Err: err}
	return s
}

// Func supplies a body for Touch, for when the answer depends on the
// arguments. An injected fault still takes precedence.
func (s *MixedTouchStub) Func(fn func(context.Context, string) error) *MixedTouchStub {
	s.fn = fn
	return s
}

// MixedAgeOfStub controls how the double answers AgeOf and records
// what it was asked.
//
// The embedded MethodStub supplies the machinery every method shares: call
// recording, fault injection, latency against a virtual clock, gates,
// call-count expectations, and strict mode.
type MixedAgeOfStub struct {
	*stub.MethodStub[MixedAgeOfCall]

	fn       func(context.Context, string) (int64, error)
	fallback *MixedAgeOfReturn
}

// MixedAgeOfReturn holds the fixed answer configured through Returns.
type MixedAgeOfReturn struct {
	Result int64
	Err    error
}

// Returns pins a fixed result for every call to AgeOf. A Func
// override and an injected fault both take precedence over it.
func (s *MixedAgeOfStub) Returns(result int64, err error) *MixedAgeOfStub {
	s.fallback = &MixedAgeOfReturn{Result: result, Err: err}
	return s
}

// Func supplies a body for AgeOf, for when the answer depends on the
// arguments. An injected fault still takes precedence.
func (s *MixedAgeOfStub) Func(fn func(context.Context, string) (int64, error)) *MixedAgeOfStub {
	s.fn = fn
	return s
}

// --- MixedStub ---

// MixedStubOption configures a [MixedStub] at construction time.
type MixedStubOption func(*MixedStub)

// MixedStubStrict makes an unconfigured method fail the test rather than
// return its zero value, which turns a call nobody planned for into a failure
// at the call site instead of a puzzling zero further downstream.
func MixedStubStrict() MixedStubOption {
	return func(s *MixedStub) { s.strict = true }
}

// MixedStubDelegateTo wraps a real implementation: every method forwards to
// impl and is recorded on the double on the way through.
//
// This is what lets one generated suite run against both the double and the
// production type, which is the point of conformance testing.
func MixedStubDelegateTo(impl timeaware.Mixed) MixedStubOption {
	return func(s *MixedStub) {
		s.OnTouch.Func(impl.Touch)
		s.OnAgeOf.Func(impl.AgeOf)
	}
}

// MixedStubWithClock drives latency and time-windowed faults from clk
// rather than from wall time, so a test asserting on a five-second timeout
// does not take five seconds.
func MixedStubWithClock(clk clock.Clock) MixedStubOption {
	return func(s *MixedStub) {
		for _, m := range s.all {
			m.SetClock(clk)
		}
	}
}

// MixedStubWithRandSource makes probabilistic fault injection reproducible.
// A failure nobody can replay is not a finding.
func MixedStubWithRandSource(src rand.Source) MixedStubOption {
	return func(s *MixedStub) {
		for _, m := range s.all {
			m.SetRandSource(src)
		}
	}
}

// MixedStubBenchMode disables call recording. Dispatch still works; only
// the accumulating call log is dropped, because its allocations are what a
// benchmark would otherwise be measuring.
func MixedStubBenchMode() MixedStubOption {
	return func(s *MixedStub) {
		for _, m := range s.all {
			m.BenchMode()
		}
	}
}

// WithMixedTouch sets Touch's body at construction
// time, for the common case of configuring one method and taking the
// defaults for the rest.
func WithMixedTouch(fn func(context.Context, string) error) MixedStubOption {
	return func(s *MixedStub) { s.OnTouch.Func(fn) }
}

// WithMixedAgeOf sets AgeOf's body at construction
// time, for the common case of configuring one method and taking the
// defaults for the rest.
func WithMixedAgeOf(fn func(context.Context, string) (int64, error)) MixedStubOption {
	return func(s *MixedStub) { s.OnAgeOf.Func(fn) }
}

// MixedStub is a recording test double for Mixed.
//
// Each On<Method> field is that method's configuration point. Left alone, a
// method returns its zero value and records the call.
type MixedStub struct {
	OnTouch *MixedTouchStub
	OnAgeOf *MixedAgeOfStub

	// all is every method stub above, viewed through the surface that does
	// not depend on a signature. It is what lets a setting apply to the whole
	// double in a loop rather than in one line per method.
	all    []stub.Configurable
	strict bool
}

// Compile-time proof that MixedStub satisfies Mixed.
//
// It lives here rather than in the companion so a drifted signature fails
// `go build` rather than waiting for a test run — the double is unusable the
// moment it stops satisfying the interface, and that is worth learning at the
// earliest point it can be known.
var _ timeaware.Mixed = (*MixedStub)(nil)

// NewMixedStub returns a double bound to tb.
//
// Passing tb registers a cleanup that verifies every method's call-count
// expectations when the test ends, so an unmet Times is reported without the
// caller remembering to check. A nil tb skips that, which is what benchmarks
// and non-test callers want.
func NewMixedStub(tb testing.TB, opts ...MixedStubOption) *MixedStub {
	s := &MixedStub{
		OnTouch: &MixedTouchStub{MethodStub: stub.NewMethodStub[MixedTouchCall](tb, "Mixed.Touch")},
		OnAgeOf: &MixedAgeOfStub{MethodStub: stub.NewMethodStub[MixedAgeOfCall](tb, "Mixed.AgeOf")},
	}
	s.all = []stub.Configurable{
		s.OnTouch.MethodStub,
		s.OnAgeOf.MethodStub,
	}
	for _, opt := range opts {
		opt(s)
	}
	if s.strict {
		for _, m := range s.all {
			m.Strict()
		}
	}
	if tb != nil {
		// A fuzz target reruns its body many times over and registers a
		// cleanup per run, so verifying there would report against the wrong
		// iteration.
		if _, isFuzz := tb.(*testing.F); !isFuzz {
			tb.Cleanup(func() {
				for _, m := range s.all {
					m.Verify()
				}
			})
		}
	}
	return s
}

// ResetCalls clears recorded calls, fault counters, and call-count
// expectations. Func and Returns configuration is deliberately preserved, so
// a test can reuse one double across phases without rebuilding it.
func (s *MixedStub) ResetCalls() {
	for _, m := range s.all {
		m.Reset()
	}
}

// --- Interface methods ---

// invoke adapts the Func override to the shape [stub.Answer] consumes, or
// returns nil when no override is set — which is how Answer tells "no
// override" from "an override that returns zero".
func (s *MixedTouchStub) invoke(ctx context.Context, key string) func() MixedTouchReturn {
	if s.fn == nil {
		return nil
	}
	return func() MixedTouchReturn {
		r0 := s.fn(ctx, key)
		return MixedTouchReturn{Err: r0}
	}
}

// Touch records the call and answers it.
//
// Which arm answers — injected fault, Func override, Returns fallback, or the
// zero value — is [stub.Answer]'s to decide, so every generated double
// resolves a call the same way and the ordering is tested once rather than
// restated per method.
func (s *MixedStub) Touch(ctx context.Context, key string) error {
	call := MixedTouchCall{Ctx: ctx, Key: key}
	r := stub.Answer(s.OnTouch.MethodStub, &call, stub.Arms[MixedTouchCall, MixedTouchReturn]{
		Invoke:   s.OnTouch.invoke(ctx, key),
		Fallback: s.OnTouch.fallback,
		Fault:    func(err error) MixedTouchReturn { return MixedTouchReturn{Err: err} },
		Stamp: func(c *MixedTouchCall, r MixedTouchReturn) {
			c.Err = r.Err
		},
	})
	return r.Err
}

// invoke adapts the Func override to the shape [stub.Answer] consumes, or
// returns nil when no override is set — which is how Answer tells "no
// override" from "an override that returns zero".
func (s *MixedAgeOfStub) invoke(ctx context.Context, key string) func() MixedAgeOfReturn {
	if s.fn == nil {
		return nil
	}
	return func() MixedAgeOfReturn {
		r0, r1 := s.fn(ctx, key)
		return MixedAgeOfReturn{Result: r0, Err: r1}
	}
}

// AgeOf records the call and answers it.
//
// Which arm answers — injected fault, Func override, Returns fallback, or the
// zero value — is [stub.Answer]'s to decide, so every generated double
// resolves a call the same way and the ordering is tested once rather than
// restated per method.
func (s *MixedStub) AgeOf(ctx context.Context, key string) (int64, error) {
	call := MixedAgeOfCall{Ctx: ctx, Key: key}
	r := stub.Answer(s.OnAgeOf.MethodStub, &call, stub.Arms[MixedAgeOfCall, MixedAgeOfReturn]{
		Invoke:   s.OnAgeOf.invoke(ctx, key),
		Fallback: s.OnAgeOf.fallback,
		Fault:    func(err error) MixedAgeOfReturn { return MixedAgeOfReturn{Err: err} },
		Stamp: func(c *MixedAgeOfCall, r MixedAgeOfReturn) {
			c.Result = r.Result
			c.Err = r.Err
		},
	})
	return r.Result, r.Err
}

// testkit: end of generated content.
// testkit:provenance ae9eb9b3aea26fb4622ebd665a78bb772b219e8136e4887b0c0a6e7e1bed6366
