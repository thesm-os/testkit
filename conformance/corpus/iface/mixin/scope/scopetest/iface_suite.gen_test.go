// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/mixin/scope/iface.go
// Plugins:   golang 1.0.0, suite 1.15.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package scopetest_test

import (
	"context"
	"errors"
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/iface/mixin/scope/scopetest"
)

// Every generated check for Mixed, driven against a stand-in built
// to violate it.
//
//	Proved:   9 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertMixedSetSmokeCanFail holds AssertMixedSetSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertMixedSetSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := scopetest.DefaultMixedFixture()
	subject := scopetest.NewMixedStub(t,
		scopetest.WithMixedSet(func(context.Context, string, string, string) error {
			panic("Mixed.Set: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			scopetest.AssertMixedSetSmoke(tb, subject, fixture.Scope, fixture.Key, fixture.Value)
		})
	testkit.Assert(t, got).Contains("Set panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedSetCancelsCanFail holds AssertMixedSetCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertMixedSetCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := scopetest.DefaultMixedFixture()
	subject := scopetest.NewMixedStub(t,
		scopetest.WithMixedSet(func(context.Context, string, string, string) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			scopetest.AssertMixedSetCancels(tb, subject, fixture.Scope, fixture.Key, fixture.Value)
		})
	testkit.Assert(t, got).Contains("Set must report a cancelled context as context.Canceled",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedSetHonoursDeadlineCanFail holds AssertMixedSetHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertMixedSetHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := scopetest.DefaultMixedFixture()
	subject := scopetest.NewMixedStub(t,
		scopetest.WithMixedSet(func(context.Context, string, string, string) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			scopetest.AssertMixedSetHonoursDeadline(tb, subject, fixture.Scope, fixture.Key, fixture.Value)
		})
	testkit.Assert(t, got).Contains("Set must report an expired deadline as context.DeadlineExceeded",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedSetToleratesNilContextCanFail holds AssertMixedSetToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertMixedSetToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := scopetest.DefaultMixedFixture()
	subject := scopetest.NewMixedStub(t,
		scopetest.WithMixedSet(func(context.Context, string, string, string) error {
			panic("Mixed.Set: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			scopetest.AssertMixedSetToleratesNilContext(tb, subject, fixture.Scope, fixture.Key, fixture.Value)
		})
	testkit.Assert(t, got).Contains("Set panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedGetSmokeCanFail holds AssertMixedGetSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertMixedGetSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := scopetest.DefaultMixedFixture()
	subject := scopetest.NewMixedStub(t,
		scopetest.WithMixedGet(func(context.Context, string, string) (string, error) {
			panic("Mixed.Get: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			scopetest.AssertMixedGetSmoke(tb, subject, fixture.Scope, fixture.Key)
		})
	testkit.Assert(t, got).Contains("Get panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedGetCancelsCanFail holds AssertMixedGetCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertMixedGetCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := scopetest.DefaultMixedFixture()
	subject := scopetest.NewMixedStub(t,
		scopetest.WithMixedGet(func(context.Context, string, string) (string, error) {
			var r0 string
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			scopetest.AssertMixedGetCancels(tb, subject, fixture.Scope, fixture.Key)
		})
	testkit.Assert(t, got).Contains("Get must report a cancelled context as context.Canceled",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedGetHonoursDeadlineCanFail holds AssertMixedGetHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertMixedGetHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := scopetest.DefaultMixedFixture()
	subject := scopetest.NewMixedStub(t,
		scopetest.WithMixedGet(func(context.Context, string, string) (string, error) {
			var r0 string
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			scopetest.AssertMixedGetHonoursDeadline(tb, subject, fixture.Scope, fixture.Key)
		})
	testkit.Assert(t, got).Contains("Get must report an expired deadline as context.DeadlineExceeded",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedGetToleratesNilContextCanFail holds AssertMixedGetToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertMixedGetToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := scopetest.DefaultMixedFixture()
	subject := scopetest.NewMixedStub(t,
		scopetest.WithMixedGet(func(context.Context, string, string) (string, error) {
			panic("Mixed.Get: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			scopetest.AssertMixedGetToleratesNilContext(tb, subject, fixture.Scope, fixture.Key)
		})
	testkit.Assert(t, got).Contains("Get panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedGetZeroOnErrorCanFail holds AssertMixedGetZeroOnError to being able to fail.
//
// The stand-in refuses the call and answers a value that looks like a result
// anyway. A check that let that pass would leave a caller who checks the error
// and a caller who checks the value disagreeing about what happened — the
// claim's own violation, not a stand-in that merely succeeds.
func TestAssertMixedGetZeroOnErrorCanFail(t *testing.T) {
	t.Parallel()

	fixture := scopetest.DefaultMixedFixture()
	subject := scopetest.NewMixedStub(t,
		scopetest.WithMixedGet(func(context.Context, string, string) (string, error) {
			return "test-r0", errors.New("violate: refused with a believable value")
		}))

	got := testkit.Rejects(t, "a method answering a believable value beside its error",
		func(tb testing.TB) {
			tb.Helper()
			scopetest.AssertMixedGetZeroOnError(tb, subject, fixture.ScopeOther, fixture.KeyOther)
		})
	testkit.Assert(t, got).Contains("Get must return the zero value alongside an error",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance 49136e5e82c8c5b859d2b967859689edd0b0188610c1198e3cb46f8344af0bbe
