// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/mixin/leakfree/iface.go
// Plugins:   golang 1.0.0, suite 1.0.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/iface/mixin/...

package leakfreetest_test

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/iface/mixin/leakfree/leakfreetest"
)

// Every generated check for Mixed, driven against a stand-in built
// to violate it.
//
//	Proved:   12 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertMixedAcquireSmokeCanFail holds AssertMixedAcquireSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertMixedAcquireSmokeCanFail(t *testing.T) {
	t.Parallel()

	subject := leakfreetest.NewMixedStub(t,
		leakfreetest.WithMixedAcquire(func(context.Context) error {
			panic("Mixed.Acquire: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			leakfreetest.AssertMixedAcquireSmoke(tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedAcquireCancelsCanFail holds AssertMixedAcquireCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertMixedAcquireCancelsCanFail(t *testing.T) {
	t.Parallel()

	subject := leakfreetest.NewMixedStub(t,
		leakfreetest.WithMixedAcquire(func(context.Context) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			leakfreetest.AssertMixedAcquireCancels(tb, subject)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedAcquireHonoursDeadlineCanFail holds AssertMixedAcquireHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertMixedAcquireHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	subject := leakfreetest.NewMixedStub(t,
		leakfreetest.WithMixedAcquire(func(context.Context) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			leakfreetest.AssertMixedAcquireHonoursDeadline(tb, subject)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedAcquireToleratesNilContextCanFail holds AssertMixedAcquireToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertMixedAcquireToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	subject := leakfreetest.NewMixedStub(t,
		leakfreetest.WithMixedAcquire(func(context.Context) error {
			panic("Mixed.Acquire: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			leakfreetest.AssertMixedAcquireToleratesNilContext(tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedReleaseSmokeCanFail holds AssertMixedReleaseSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertMixedReleaseSmokeCanFail(t *testing.T) {
	t.Parallel()

	subject := leakfreetest.NewMixedStub(t,
		leakfreetest.WithMixedRelease(func(context.Context) error {
			panic("Mixed.Release: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			leakfreetest.AssertMixedReleaseSmoke(tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedReleaseCancelsCanFail holds AssertMixedReleaseCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertMixedReleaseCancelsCanFail(t *testing.T) {
	t.Parallel()

	subject := leakfreetest.NewMixedStub(t,
		leakfreetest.WithMixedRelease(func(context.Context) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			leakfreetest.AssertMixedReleaseCancels(tb, subject)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedReleaseHonoursDeadlineCanFail holds AssertMixedReleaseHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertMixedReleaseHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	subject := leakfreetest.NewMixedStub(t,
		leakfreetest.WithMixedRelease(func(context.Context) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			leakfreetest.AssertMixedReleaseHonoursDeadline(tb, subject)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedReleaseToleratesNilContextCanFail holds AssertMixedReleaseToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertMixedReleaseToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	subject := leakfreetest.NewMixedStub(t,
		leakfreetest.WithMixedRelease(func(context.Context) error {
			panic("Mixed.Release: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			leakfreetest.AssertMixedReleaseToleratesNilContext(tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedOutstandingSmokeCanFail holds AssertMixedOutstandingSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertMixedOutstandingSmokeCanFail(t *testing.T) {
	t.Parallel()

	subject := leakfreetest.NewMixedStub(t,
		leakfreetest.WithMixedOutstanding(func(context.Context) (int, error) {
			panic("Mixed.Outstanding: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			leakfreetest.AssertMixedOutstandingSmoke(tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedOutstandingCancelsCanFail holds AssertMixedOutstandingCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertMixedOutstandingCancelsCanFail(t *testing.T) {
	t.Parallel()

	subject := leakfreetest.NewMixedStub(t,
		leakfreetest.WithMixedOutstanding(func(context.Context) (int, error) {
			var r0 int
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			leakfreetest.AssertMixedOutstandingCancels(tb, subject)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedOutstandingHonoursDeadlineCanFail holds AssertMixedOutstandingHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertMixedOutstandingHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	subject := leakfreetest.NewMixedStub(t,
		leakfreetest.WithMixedOutstanding(func(context.Context) (int, error) {
			var r0 int
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			leakfreetest.AssertMixedOutstandingHonoursDeadline(tb, subject)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedOutstandingToleratesNilContextCanFail holds AssertMixedOutstandingToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertMixedOutstandingToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	subject := leakfreetest.NewMixedStub(t,
		leakfreetest.WithMixedOutstanding(func(context.Context) (int, error) {
			panic("Mixed.Outstanding: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			leakfreetest.AssertMixedOutstandingToleratesNilContext(tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance 9ed47b4550d5d97e9c57d2f01987e716247400c1095b8159f8b46b4585a6dfd6
