// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/mixin/monotonic/iface.go
// Plugins:   golang 1.0.0, suite 1.0.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package monotonictest_test

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/iface/mixin/monotonic/monotonictest"
)

// Every generated check for Mixed, driven against a stand-in built
// to violate it.
//
//	Proved:   8 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertMixedVersionSmokeCanFail holds AssertMixedVersionSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertMixedVersionSmokeCanFail(t *testing.T) {
	t.Parallel()

	subject := monotonictest.NewMixedStub(t,
		monotonictest.WithMixedVersion(func(context.Context) (int64, error) {
			panic("Mixed.Version: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			monotonictest.AssertMixedVersionSmoke(tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedVersionCancelsCanFail holds AssertMixedVersionCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertMixedVersionCancelsCanFail(t *testing.T) {
	t.Parallel()

	subject := monotonictest.NewMixedStub(t,
		monotonictest.WithMixedVersion(func(context.Context) (int64, error) {
			var r0 int64
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			monotonictest.AssertMixedVersionCancels(tb, subject)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedVersionHonoursDeadlineCanFail holds AssertMixedVersionHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertMixedVersionHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	subject := monotonictest.NewMixedStub(t,
		monotonictest.WithMixedVersion(func(context.Context) (int64, error) {
			var r0 int64
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			monotonictest.AssertMixedVersionHonoursDeadline(tb, subject)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedVersionToleratesNilContextCanFail holds AssertMixedVersionToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertMixedVersionToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	subject := monotonictest.NewMixedStub(t,
		monotonictest.WithMixedVersion(func(context.Context) (int64, error) {
			panic("Mixed.Version: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			monotonictest.AssertMixedVersionToleratesNilContext(tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedAdvanceSmokeCanFail holds AssertMixedAdvanceSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertMixedAdvanceSmokeCanFail(t *testing.T) {
	t.Parallel()

	subject := monotonictest.NewMixedStub(t,
		monotonictest.WithMixedAdvance(func(context.Context) error {
			panic("Mixed.Advance: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			monotonictest.AssertMixedAdvanceSmoke(tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedAdvanceCancelsCanFail holds AssertMixedAdvanceCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertMixedAdvanceCancelsCanFail(t *testing.T) {
	t.Parallel()

	subject := monotonictest.NewMixedStub(t,
		monotonictest.WithMixedAdvance(func(context.Context) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			monotonictest.AssertMixedAdvanceCancels(tb, subject)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedAdvanceHonoursDeadlineCanFail holds AssertMixedAdvanceHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertMixedAdvanceHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	subject := monotonictest.NewMixedStub(t,
		monotonictest.WithMixedAdvance(func(context.Context) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			monotonictest.AssertMixedAdvanceHonoursDeadline(tb, subject)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedAdvanceToleratesNilContextCanFail holds AssertMixedAdvanceToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertMixedAdvanceToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	subject := monotonictest.NewMixedStub(t,
		monotonictest.WithMixedAdvance(func(context.Context) error {
			panic("Mixed.Advance: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			monotonictest.AssertMixedAdvanceToleratesNilContext(tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance c0ab7dd61c1ec057950444a1457ca50afe174da45c0c18a80e3201ecb59b7d2f
