// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/mixin/eventually/iface.go
// Plugins:   golang 1.0.0, suite 1.3.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package eventuallytest_test

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/iface/mixin/eventually"
	"go.thesmos.sh/testkit/conformance/corpus/iface/mixin/eventually/eventuallytest"
)

// Every generated check for Mixed, driven against a stand-in built
// to violate it.
//
//	Proved:   16 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertMixedPublishSmokeCanFail holds AssertMixedPublishSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertMixedPublishSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := eventuallytest.DefaultMixedFixture()
	subject := eventuallytest.NewMixedStub(t,
		eventuallytest.WithMixedPublish(func(context.Context, string) error {
			panic("Mixed.Publish: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			eventuallytest.AssertMixedPublishSmoke(tb, subject, fixture.Item)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedPublishCancelsCanFail holds AssertMixedPublishCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertMixedPublishCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := eventuallytest.DefaultMixedFixture()
	subject := eventuallytest.NewMixedStub(t,
		eventuallytest.WithMixedPublish(func(context.Context, string) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			eventuallytest.AssertMixedPublishCancels(tb, subject, fixture.Item)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedPublishHonoursDeadlineCanFail holds AssertMixedPublishHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertMixedPublishHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := eventuallytest.DefaultMixedFixture()
	subject := eventuallytest.NewMixedStub(t,
		eventuallytest.WithMixedPublish(func(context.Context, string) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			eventuallytest.AssertMixedPublishHonoursDeadline(tb, subject, fixture.Item)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedPublishToleratesNilContextCanFail holds AssertMixedPublishToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertMixedPublishToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := eventuallytest.DefaultMixedFixture()
	subject := eventuallytest.NewMixedStub(t,
		eventuallytest.WithMixedPublish(func(context.Context, string) error {
			panic("Mixed.Publish: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			eventuallytest.AssertMixedPublishToleratesNilContext(tb, subject, fixture.Item)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedSettleSmokeCanFail holds AssertMixedSettleSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertMixedSettleSmokeCanFail(t *testing.T) {
	t.Parallel()

	subject := eventuallytest.NewMixedStub(t,
		eventuallytest.WithMixedSettle(func(context.Context) error {
			panic("Mixed.Settle: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			eventuallytest.AssertMixedSettleSmoke(tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedSettleCancelsCanFail holds AssertMixedSettleCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertMixedSettleCancelsCanFail(t *testing.T) {
	t.Parallel()

	subject := eventuallytest.NewMixedStub(t,
		eventuallytest.WithMixedSettle(func(context.Context) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			eventuallytest.AssertMixedSettleCancels(tb, subject)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedSettleHonoursDeadlineCanFail holds AssertMixedSettleHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertMixedSettleHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	subject := eventuallytest.NewMixedStub(t,
		eventuallytest.WithMixedSettle(func(context.Context) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			eventuallytest.AssertMixedSettleHonoursDeadline(tb, subject)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedSettleToleratesNilContextCanFail holds AssertMixedSettleToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertMixedSettleToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	subject := eventuallytest.NewMixedStub(t,
		eventuallytest.WithMixedSettle(func(context.Context) error {
			panic("Mixed.Settle: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			eventuallytest.AssertMixedSettleToleratesNilContext(tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedSyncSmokeCanFail holds AssertMixedSyncSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertMixedSyncSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := eventuallytest.DefaultMixedFixture()
	subject := eventuallytest.NewMixedStub(t,
		eventuallytest.WithMixedSync(func(context.Context, eventually.Mixed) error {
			panic("Mixed.Sync: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			eventuallytest.AssertMixedSyncSmoke(tb, subject, fixture.Peer)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedSyncCancelsCanFail holds AssertMixedSyncCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertMixedSyncCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := eventuallytest.DefaultMixedFixture()
	subject := eventuallytest.NewMixedStub(t,
		eventuallytest.WithMixedSync(func(context.Context, eventually.Mixed) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			eventuallytest.AssertMixedSyncCancels(tb, subject, fixture.Peer)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedSyncHonoursDeadlineCanFail holds AssertMixedSyncHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertMixedSyncHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := eventuallytest.DefaultMixedFixture()
	subject := eventuallytest.NewMixedStub(t,
		eventuallytest.WithMixedSync(func(context.Context, eventually.Mixed) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			eventuallytest.AssertMixedSyncHonoursDeadline(tb, subject, fixture.Peer)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedSyncToleratesNilContextCanFail holds AssertMixedSyncToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertMixedSyncToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := eventuallytest.DefaultMixedFixture()
	subject := eventuallytest.NewMixedStub(t,
		eventuallytest.WithMixedSync(func(context.Context, eventually.Mixed) error {
			panic("Mixed.Sync: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			eventuallytest.AssertMixedSyncToleratesNilContext(tb, subject, fixture.Peer)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedItemsSmokeCanFail holds AssertMixedItemsSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertMixedItemsSmokeCanFail(t *testing.T) {
	t.Parallel()

	subject := eventuallytest.NewMixedStub(t,
		eventuallytest.WithMixedItems(func(context.Context) ([]string, error) {
			panic("Mixed.Items: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			eventuallytest.AssertMixedItemsSmoke(tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedItemsCancelsCanFail holds AssertMixedItemsCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertMixedItemsCancelsCanFail(t *testing.T) {
	t.Parallel()

	subject := eventuallytest.NewMixedStub(t,
		eventuallytest.WithMixedItems(func(context.Context) ([]string, error) {
			var r0 []string
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			eventuallytest.AssertMixedItemsCancels(tb, subject)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedItemsHonoursDeadlineCanFail holds AssertMixedItemsHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertMixedItemsHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	subject := eventuallytest.NewMixedStub(t,
		eventuallytest.WithMixedItems(func(context.Context) ([]string, error) {
			var r0 []string
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			eventuallytest.AssertMixedItemsHonoursDeadline(tb, subject)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedItemsToleratesNilContextCanFail holds AssertMixedItemsToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertMixedItemsToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	subject := eventuallytest.NewMixedStub(t,
		eventuallytest.WithMixedItems(func(context.Context) ([]string, error) {
			panic("Mixed.Items: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			eventuallytest.AssertMixedItemsToleratesNilContext(tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance 09ef7e140e5e097dea9dddf933c938d4c36874c9fe18c3ba13897f2ba607f405
