// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/mixin/wrappedvia/iface.go
// Plugins:   golang 1.0.0, stub 1.0.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package wrappedviatest

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/conformance/corpus/iface/mixin/wrappedvia"
	"go.thesmos.sh/testkit/rand"
	"go.thesmos.sh/testkit/stub"
)

// MixedOpenCall records one invocation of Mixed.Open.
//
// Fields take their names from the source signature — parameters and named
// returns alike — so a failure message names what the author named. A slot
// the source left unnamed or blank falls back to a positional name.
type MixedOpenCall struct {
	Ctx  context.Context
	Name string
	Err  error
}

// MixedCauseCall records one invocation of Mixed.Cause.
//
// Fields take their names from the source signature — parameters and named
// returns alike — so a failure message names what the author named. A slot
// the source left unnamed or blank falls back to a positional name.
type MixedCauseCall struct {
	Ctx context.Context
	Err error
}

// --- Per-method configuration ---

// MixedOpenStub controls how the double answers Open and records
// what it was asked.
//
// The embedded MethodStub supplies the machinery every method shares: call
// recording, fault injection, latency against a virtual clock, gates,
// call-count expectations, and strict mode.
type MixedOpenStub struct {
	*stub.MethodStub[MixedOpenCall]

	fn       func(context.Context, string) error
	fallback *MixedOpenReturn
}

// MixedOpenReturn holds the fixed answer configured through Returns.
type MixedOpenReturn struct {
	Err error
}

// Returns pins a fixed result for every call to Open. A Func
// override and an injected fault both take precedence over it.
func (s *MixedOpenStub) Returns(err error) *MixedOpenStub {
	s.fallback = &MixedOpenReturn{Err: err}
	return s
}

// Func supplies a body for Open, for when the answer depends on the
// arguments. An injected fault still takes precedence.
func (s *MixedOpenStub) Func(fn func(context.Context, string) error) *MixedOpenStub {
	s.fn = fn
	return s
}

// MixedCauseStub controls how the double answers Cause and records
// what it was asked.
//
// The embedded MethodStub supplies the machinery every method shares: call
// recording, fault injection, latency against a virtual clock, gates,
// call-count expectations, and strict mode.
type MixedCauseStub struct {
	*stub.MethodStub[MixedCauseCall]

	fn       func(context.Context) error
	fallback *MixedCauseReturn
}

// MixedCauseReturn holds the fixed answer configured through Returns.
type MixedCauseReturn struct {
	Err error
}

// Returns pins a fixed result for every call to Cause. A Func
// override and an injected fault both take precedence over it.
func (s *MixedCauseStub) Returns(err error) *MixedCauseStub {
	s.fallback = &MixedCauseReturn{Err: err}
	return s
}

// Func supplies a body for Cause, for when the answer depends on the
// arguments. An injected fault still takes precedence.
func (s *MixedCauseStub) Func(fn func(context.Context) error) *MixedCauseStub {
	s.fn = fn
	return s
}

// --- MixedStub ---

// MixedStubOption configures a [MixedStub] at construction time.
type MixedStubOption func(*MixedStub)

// MixedStubStrict makes an unconfigured method fail the test rather than
// return its zero value, which turns a call nobody planned for into a failure
// at the call site instead of a puzzling zero further downstream.
func MixedStubStrict() MixedStubOption {
	return func(s *MixedStub) { s.strict = true }
}

// MixedStubDelegateTo wraps a real implementation: every method forwards to
// impl and is recorded on the double on the way through.
//
// This is what lets one generated suite run against both the double and the
// production type, which is the point of conformance testing.
func MixedStubDelegateTo(impl wrappedvia.Mixed) MixedStubOption {
	return func(s *MixedStub) {
		s.OnOpen.Func(impl.Open)
		s.OnCause.Func(impl.Cause)
	}
}

// MixedStubWithClock drives latency and time-windowed faults from clk
// rather than from wall time, so a test asserting on a five-second timeout
// does not take five seconds.
func MixedStubWithClock(clk clock.Clock) MixedStubOption {
	return func(s *MixedStub) {
		for _, m := range s.all {
			m.SetClock(clk)
		}
	}
}

// MixedStubWithRandSource makes probabilistic fault injection reproducible.
// A failure nobody can replay is not a finding.
func MixedStubWithRandSource(src rand.Source) MixedStubOption {
	return func(s *MixedStub) {
		for _, m := range s.all {
			m.SetRandSource(src)
		}
	}
}

// MixedStubBenchMode disables call recording. Dispatch still works; only
// the accumulating call log is dropped, because its allocations are what a
// benchmark would otherwise be measuring.
func MixedStubBenchMode() MixedStubOption {
	return func(s *MixedStub) {
		for _, m := range s.all {
			m.BenchMode()
		}
	}
}

// WithMixedOpen sets Open's body at construction
// time, for the common case of configuring one method and taking the
// defaults for the rest.
func WithMixedOpen(fn func(context.Context, string) error) MixedStubOption {
	return func(s *MixedStub) { s.OnOpen.Func(fn) }
}

// WithMixedCause sets Cause's body at construction
// time, for the common case of configuring one method and taking the
// defaults for the rest.
func WithMixedCause(fn func(context.Context) error) MixedStubOption {
	return func(s *MixedStub) { s.OnCause.Func(fn) }
}

// MixedStub is a recording test double for Mixed.
//
// Each On<Method> field is that method's configuration point. Left alone, a
// method returns its zero value and records the call.
type MixedStub struct {
	OnOpen  *MixedOpenStub
	OnCause *MixedCauseStub

	// all is every method stub above, viewed through the surface that does
	// not depend on a signature. It is what lets a setting apply to the whole
	// double in a loop rather than in one line per method.
	all    []stub.Configurable
	strict bool
}

// Compile-time proof that MixedStub satisfies Mixed.
//
// It lives here rather than in the companion so a drifted signature fails
// `go build` rather than waiting for a test run — the double is unusable the
// moment it stops satisfying the interface, and that is worth learning at the
// earliest point it can be known.
var _ wrappedvia.Mixed = (*MixedStub)(nil)

// NewMixedStub returns a double bound to tb.
//
// Passing tb registers a cleanup that verifies every method's call-count
// expectations when the test ends, so an unmet Times is reported without the
// caller remembering to check. A nil tb skips that, which is what benchmarks
// and non-test callers want.
func NewMixedStub(tb testing.TB, opts ...MixedStubOption) *MixedStub {
	s := &MixedStub{
		OnOpen:  &MixedOpenStub{MethodStub: stub.NewMethodStub[MixedOpenCall](tb, "Mixed.Open")},
		OnCause: &MixedCauseStub{MethodStub: stub.NewMethodStub[MixedCauseCall](tb, "Mixed.Cause")},
	}
	s.all = []stub.Configurable{
		s.OnOpen.MethodStub,
		s.OnCause.MethodStub,
	}
	for _, opt := range opts {
		opt(s)
	}
	if s.strict {
		for _, m := range s.all {
			m.Strict()
		}
	}
	if tb != nil {
		// A fuzz target reruns its body many times over and registers a
		// cleanup per run, so verifying there would report against the wrong
		// iteration.
		if _, isFuzz := tb.(*testing.F); !isFuzz {
			tb.Cleanup(func() {
				for _, m := range s.all {
					m.Verify()
				}
			})
		}
	}
	return s
}

// ResetCalls clears recorded calls, fault counters, and call-count
// expectations. Func and Returns configuration is deliberately preserved, so
// a test can reuse one double across phases without rebuilding it.
func (s *MixedStub) ResetCalls() {
	for _, m := range s.all {
		m.Reset()
	}
}

// --- Interface methods ---

// invoke adapts the Func override to the shape [stub.Answer] consumes, or
// returns nil when no override is set — which is how Answer tells "no
// override" from "an override that returns zero".
func (s *MixedOpenStub) invoke(ctx context.Context, name string) func() MixedOpenReturn {
	if s.fn == nil {
		return nil
	}
	return func() MixedOpenReturn {
		r0 := s.fn(ctx, name)
		return MixedOpenReturn{Err: r0}
	}
}

// Open records the call and answers it.
//
// Which arm answers — injected fault, Func override, Returns fallback, or the
// zero value — is [stub.Answer]'s to decide, so every generated double
// resolves a call the same way and the ordering is tested once rather than
// restated per method.
func (s *MixedStub) Open(ctx context.Context, name string) error {
	call := MixedOpenCall{Ctx: ctx, Name: name}
	r := stub.Answer(s.OnOpen.MethodStub, &call, stub.Arms[MixedOpenCall, MixedOpenReturn]{
		Invoke:   s.OnOpen.invoke(ctx, name),
		Fallback: s.OnOpen.fallback,
		Fault:    func(err error) MixedOpenReturn { return MixedOpenReturn{Err: err} },
		Stamp: func(c *MixedOpenCall, r MixedOpenReturn) {
			c.Err = r.Err
		},
	})
	return r.Err
}

// invoke adapts the Func override to the shape [stub.Answer] consumes, or
// returns nil when no override is set — which is how Answer tells "no
// override" from "an override that returns zero".
func (s *MixedCauseStub) invoke(ctx context.Context) func() MixedCauseReturn {
	if s.fn == nil {
		return nil
	}
	return func() MixedCauseReturn {
		r0 := s.fn(ctx)
		return MixedCauseReturn{Err: r0}
	}
}

// Cause records the call and answers it.
//
// Which arm answers — injected fault, Func override, Returns fallback, or the
// zero value — is [stub.Answer]'s to decide, so every generated double
// resolves a call the same way and the ordering is tested once rather than
// restated per method.
func (s *MixedStub) Cause(ctx context.Context) error {
	call := MixedCauseCall{Ctx: ctx}
	r := stub.Answer(s.OnCause.MethodStub, &call, stub.Arms[MixedCauseCall, MixedCauseReturn]{
		Invoke:   s.OnCause.invoke(ctx),
		Fallback: s.OnCause.fallback,
		Fault:    func(err error) MixedCauseReturn { return MixedCauseReturn{Err: err} },
		Stamp: func(c *MixedCauseCall, r MixedCauseReturn) {
			c.Err = r.Err
		},
	})
	return r.Err
}

// testkit: end of generated content.
// testkit:provenance 46c15bbb339baad8d5a2aaba4621e88199225403e0cd63e6313c07875ede4770
