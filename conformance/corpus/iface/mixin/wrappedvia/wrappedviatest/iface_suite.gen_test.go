// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/mixin/wrappedvia/iface.go
// Plugins:   golang 1.0.0, suite 1.15.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package wrappedviatest_test

import (
	"context"
	"errors"
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/iface/mixin/wrappedvia/wrappedviatest"
)

// Every generated check for Mixed, driven against a stand-in built
// to violate it.
//
//	Proved:   9 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertMixedOpenSmokeCanFail holds AssertMixedOpenSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertMixedOpenSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := wrappedviatest.DefaultMixedFixture()
	subject := wrappedviatest.NewMixedStub(t,
		wrappedviatest.WithMixedOpen(func(context.Context, string) error {
			panic("Mixed.Open: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			wrappedviatest.AssertMixedOpenSmoke(tb, subject, fixture.Name)
		})
	testkit.Assert(t, got).Contains("Open panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedOpenCancelsCanFail holds AssertMixedOpenCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertMixedOpenCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := wrappedviatest.DefaultMixedFixture()
	subject := wrappedviatest.NewMixedStub(t,
		wrappedviatest.WithMixedOpen(func(context.Context, string) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			wrappedviatest.AssertMixedOpenCancels(tb, subject, fixture.Name)
		})
	testkit.Assert(t, got).Contains("Open must report a cancelled context as context.Canceled",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedOpenHonoursDeadlineCanFail holds AssertMixedOpenHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertMixedOpenHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := wrappedviatest.DefaultMixedFixture()
	subject := wrappedviatest.NewMixedStub(t,
		wrappedviatest.WithMixedOpen(func(context.Context, string) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			wrappedviatest.AssertMixedOpenHonoursDeadline(tb, subject, fixture.Name)
		})
	testkit.Assert(t, got).Contains("Open must report an expired deadline as context.DeadlineExceeded",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedOpenToleratesNilContextCanFail holds AssertMixedOpenToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertMixedOpenToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := wrappedviatest.DefaultMixedFixture()
	subject := wrappedviatest.NewMixedStub(t,
		wrappedviatest.WithMixedOpen(func(context.Context, string) error {
			panic("Mixed.Open: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			wrappedviatest.AssertMixedOpenToleratesNilContext(tb, subject, fixture.Name)
		})
	testkit.Assert(t, got).Contains("Open panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedOpenWrapsCauseCanFail holds AssertMixedOpenWrapsCause to being able to fail.
//
// The stand-in fails with an error unrelated to the cause it reports. A check
// that let that pass would leave a caller's `errors.Is(err, target)` returning
// false against the documented cause, and the branch they wrote for it never
// running.
func TestAssertMixedOpenWrapsCauseCanFail(t *testing.T) {
	t.Parallel()

	fixture := wrappedviatest.DefaultMixedFixture()
	// Two errors, because one would wrap by accident: what the check asks is
	// whether the failure carries *that* cause, and a stand-in returning the
	// same value both times would satisfy it without wrapping anything.
	cause := errors.New("the cause the subject reports")
	unrelated := errors.New("something else entirely")

	subject := wrappedviatest.NewMixedStub(t,
		wrappedviatest.WithMixedOpen(func(context.Context, string) error {
			return unrelated
		}),
		wrappedviatest.WithMixedCause(func(context.Context) error {
			return cause
		}))

	got := testkit.Rejects(t, "a failure carrying a cause of its own",
		func(tb testing.TB) {
			tb.Helper()
			wrappedviatest.AssertMixedOpenWrapsCause(tb, subject, fixture.Name)
		})
	testkit.Assert(t, got).Contains("Open must report Cause's cause where errors.Is can find it",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedCauseSmokeCanFail holds AssertMixedCauseSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertMixedCauseSmokeCanFail(t *testing.T) {
	t.Parallel()

	subject := wrappedviatest.NewMixedStub(t,
		wrappedviatest.WithMixedCause(func(context.Context) error {
			panic("Mixed.Cause: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			wrappedviatest.AssertMixedCauseSmoke(tb, subject)
		})
	testkit.Assert(t, got).Contains("Cause panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedCauseCancelsCanFail holds AssertMixedCauseCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertMixedCauseCancelsCanFail(t *testing.T) {
	t.Parallel()

	subject := wrappedviatest.NewMixedStub(t,
		wrappedviatest.WithMixedCause(func(context.Context) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			wrappedviatest.AssertMixedCauseCancels(tb, subject)
		})
	testkit.Assert(t, got).Contains("Cause must report a cancelled context as context.Canceled",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedCauseHonoursDeadlineCanFail holds AssertMixedCauseHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertMixedCauseHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	subject := wrappedviatest.NewMixedStub(t,
		wrappedviatest.WithMixedCause(func(context.Context) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			wrappedviatest.AssertMixedCauseHonoursDeadline(tb, subject)
		})
	testkit.Assert(t, got).Contains("Cause must report an expired deadline as context.DeadlineExceeded",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedCauseToleratesNilContextCanFail holds AssertMixedCauseToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertMixedCauseToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	subject := wrappedviatest.NewMixedStub(t,
		wrappedviatest.WithMixedCause(func(context.Context) error {
			panic("Mixed.Cause: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			wrappedviatest.AssertMixedCauseToleratesNilContext(tb, subject)
		})
	testkit.Assert(t, got).Contains("Cause panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance fdde03a081f91fdad647370450cc1729417d344678f7d0bfb37f0ade81497ed8
