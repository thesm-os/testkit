// Code generated by testkit. DO NOT EDIT.
//
// Source:    conformance/corpus/iface/mixin/wrappedvia/iface.go
// Plugins:   golang 1.0.0, suite 1.16.0, backend.golang 1.0.0
// Command:   testkit run ./conformance/corpus/...

package wrappedviatest

import (
	"context"
	"slices"
	"testing"

	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/conformance/corpus/iface/mixin/wrappedvia"
	"go.thesmos.sh/testkit/engine/suite"
)

// MixedOpenCheck is one assertion about Open.
//
// Every generated check for Open is a value of it, and so is one you
// write — so they compose, reorder, and each runs standalone. testing.TB rather
// than *testing.T is what lets a stand-in drive it and prove it can fail.
type MixedOpenCheck func(tb testing.TB, subject wrappedvia.Mixed, name string)

// MixedCauseCheck is one assertion about Cause.
//
// Every generated check for Cause is a value of it, and so is one you
// write — so they compose, reorder, and each runs standalone. testing.TB rather
// than *testing.T is what lets a stand-in drive it and prove it can fail.
type MixedCauseCheck func(tb testing.TB, subject wrappedvia.Mixed)

// MixedFixture holds every input the generated checks run against.
//
// Derived from each method's parameter names and types. Every field has a
// companion holding a second, different value: a check comparing a result
// against a single input passes whenever the subject happened to be seeded with
// it, and a miss check whose key happens to hit asserts nothing.
//
// A field whose type admits no literal — a func, a channel, a type from a
// package this run never read — is declared and left at its zero value, and
// every generated check that needed it was dropped rather than emitted against
// a value nobody could write. Supply one through MixedWithFixture
// and the check a consumer writes has something to use.
type MixedFixture struct {
	Name      string
	NameOther string
}

// DefaultMixedFixture is what this run derived. Replace any field through
// MixedWithFixture; the rest keep these values.
func DefaultMixedFixture() MixedFixture {
	return MixedFixture{
		Name:      "test-name",
		NameOther: "other-name",
	}
}

// Mixed is the witnessed instantiation.
//
// A generic interface aliases with its own parameters, which Go admits
// since 1.24 — so every declaration below names Mixed and
// the arguments follow, exactly as they do for a concrete one.
type Mixed = wrappedvia.Mixed

// MixedRunOpt is one thing you can pass to RunMixed.
type MixedRunOpt interface {
	applyTo(*mixedRunConfig)
}

type mixedRunConfig struct {
	suite.Bundle[Mixed]
}

// MixedHarness declares one Mixed implementation under test.
type MixedHarness[T Mixed] struct {
	Name string

	// Exactly one constructor.
	New   func() T
	Start func(tb testing.TB) T

	// Excuse lists checks this subject structurally cannot take.
	Excuse []suite.ID

	// Oracle marks this subject as the run's reference. At most one.
	Oracle bool

	// Serial keeps this subject out of the parallel group.
	Serial bool

	// Provide answers open capabilities beyond the named fields — the
	// doors only this interface's checks know. The runner's gate reads
	// it for any capability without a dedicated field.
	Provide map[suite.Capability]any
}

// Subject lowers the declaration into what the runner consumes.
func (h MixedHarness[T]) Subject() (suite.Subject[Mixed], error) {
	build, err := suite.OneCtor(h.Name, h.New, h.Start)
	if err != nil {
		return suite.Subject[Mixed]{}, err
	}
	return suite.Subject[Mixed]{
		Name:     h.Name,
		Provides: h.Provide,
		New:      func(tb testing.TB) Mixed { return build(tb) },
		Oracle:   h.Oracle,
		Serial:   h.Serial,
		Excused:  suite.ExcuseSet(h.Excuse),
	}, nil
}

func (h MixedHarness[T]) applyTo(rc *mixedRunConfig) {
	rc.AddSubject(h.Subject())
}

type mixedDropOpt []suite.ID

func (d mixedDropOpt) applyTo(rc *mixedRunConfig) {
	rc.AddDrops(d...)
}

// mixedWithout drops checks by identity.
func mixedWithout(ids ...suite.ID) MixedRunOpt {
	return mixedDropOpt(ids)
}

// --- The veneer -------------------------------------------------------------

// MixedSuite is the entry a consumer names checks through.
var MixedSuite = mixedVeneer{Checks: mixedCheckIndex}

type mixedVeneer struct {
	// Checks is the check index — the constants a Without call is
	// written against.
	Checks mixedCheckIndexT
}

// Without drops a check by ID.
func (mixedVeneer) Without(ids ...suite.ID) MixedRunOpt {
	return mixedWithout(ids...)
}

// --- Drop hints -------------------------------------------------------------

// mixedIndexPath spells every emitted ID as the path a
// consumer would write to drop it, so a run that declines something can
// say what to type rather than what failed.
var mixedIndexPath = map[suite.ID]string{
	mixedCheckIndex.Open.Smoke():       "MixedSuite.Checks.Open.Smoke()",
	mixedCheckIndex.Open.Cancels():     "MixedSuite.Checks.Open.Cancels()",
	mixedCheckIndex.Open.NilContext():  "MixedSuite.Checks.Open.NilContext()",
	mixedCheckIndex.Open.Deadline():    "MixedSuite.Checks.Open.Deadline()",
	mixedCheckIndex.Cause.Smoke():      "MixedSuite.Checks.Cause.Smoke()",
	mixedCheckIndex.Cause.Cancels():    "MixedSuite.Checks.Cause.Cancels()",
	mixedCheckIndex.Cause.NilContext(): "MixedSuite.Checks.Cause.NilContext()",
	mixedCheckIndex.Model.Laws():       "MixedSuite.Checks.Model.Laws()",
}

var mixedDropHint = suite.DropHinter(
	"MixedSuite", mixedIndexPath,
)

// The names this file spells more than once: one home each.
const (
	mixedOpen  = "Open"
	mixedCause = "Cause"

	// The interface's word inside a family-scoped identity.
	mixedQualifier = "mixed"
)

// mixedCheckIndex indexes every check this package emits, by
// method and then by check.
var mixedCheckIndex = mixedCheckIndexT{
	Open:  mixedOpenChecks{},
	Cause: mixedCauseChecks{},
	Model: mixedModelChecks{},
}

type mixedCheckIndexT struct {
	Open  mixedOpenChecks
	Cause mixedCauseChecks
	Model mixedModelChecks
}

// All returns every ID this package emits.
func (mixedCheckIndexT) All() []suite.ID {
	var out []suite.ID
	out = append(out, mixedOpenChecks{}.All()...)
	out = append(out, mixedCauseChecks{}.All()...)
	out = append(out, mixedModelChecks{}.All()...)
	return out
}

type mixedOpenChecks struct{}

func (mixedOpenChecks) Smoke() suite.ID {
	return suite.MethodID(mixedOpen, suite.SegSmoke)
}

func (mixedOpenChecks) Cancels() suite.ID {
	return suite.MethodID(mixedOpen, suite.SegCancel)
}

func (mixedOpenChecks) NilContext() suite.ID {
	return suite.MethodID(mixedOpen, suite.SegNilContext)
}

func (mixedOpenChecks) Deadline() suite.ID {
	return suite.MethodID(mixedOpen, suite.SegDeadline)
}

func (mixedOpenChecks) All() []suite.ID {
	return []suite.ID{
		mixedOpenChecks{}.Smoke(),
		mixedOpenChecks{}.Cancels(),
		mixedOpenChecks{}.NilContext(),
		mixedOpenChecks{}.Deadline(),
	}
}

type mixedCauseChecks struct{}

func (mixedCauseChecks) Smoke() suite.ID {
	return suite.MethodID(mixedCause, suite.SegSmoke)
}

func (mixedCauseChecks) Cancels() suite.ID {
	return suite.MethodID(mixedCause, suite.SegCancel)
}

func (mixedCauseChecks) NilContext() suite.ID {
	return suite.MethodID(mixedCause, suite.SegNilContext)
}

func (mixedCauseChecks) All() []suite.ID {
	return []suite.ID{
		mixedCauseChecks{}.Smoke(),
		mixedCauseChecks{}.Cancels(),
		mixedCauseChecks{}.NilContext(),
	}
}

type mixedModelChecks struct{}

func (mixedModelChecks) Laws() suite.ID {
	return suite.FamilyID(suite.FamilyModel, mixedQualifier, suite.SegLaws)
}

func (mixedModelChecks) All() []suite.ID {
	return []suite.ID{
		mixedModelChecks{}.Laws(),
	}
}

// mixedSignatureChecks builds every check this run derived.
//
// sig is the one shape they share; the literals below state only what
// differs. Falsifiable is Proven wherever a defect rule reached the
// claim, which the parity gate refuses to stamp without one.
func mixedSignatureChecks(fx MixedFixture) []suite.Check[Mixed] {
	sig := suite.ProvenCheck[Mixed]
	ix := mixedCheckIndex
	return []suite.Check[Mixed]{
		sig(ix.Open.Smoke(), suite.ClassSmoke,
			"Open survives a call with a derived name",
			func(tb testing.TB, m Mixed) {
				mixedAssertOpenSmoke(tb, m, fx)
			}),
		sig(ix.Open.Cancels(), suite.ClassCancel,
			"Open reports a cancelled context as cancelled",
			func(tb testing.TB, m Mixed) {
				mixedAssertOpenCancels(tb, m, fx)
			}),
		sig(ix.Open.NilContext(), suite.ClassNilContext,
			"Open returns an error rather than panicking on a nil context",
			func(tb testing.TB, m Mixed) {
				mixedAssertOpenToleratesNilContext(tb, m, fx)
			}),
		sig(ix.Open.Deadline(), suite.ClassDeadline,
			"Open reports an expired deadline as exceeded",
			func(tb testing.TB, m Mixed) {
				mixedAssertOpenHonoursDeadline(tb, m, fx)
			}),
		sig(ix.Cause.Smoke(), suite.ClassSmoke,
			"Cause survives a call",
			func(tb testing.TB, m Mixed) {
				mixedAssertCauseSmoke(tb, m)
			}),
		sig(ix.Cause.Cancels(), suite.ClassCancel,
			"Cause reports a cancelled context as cancelled",
			func(tb testing.TB, m Mixed) {
				mixedAssertCauseCancels(tb, m)
			}),
		sig(ix.Cause.NilContext(), suite.ClassNilContext,
			"Cause returns an error rather than panicking on a nil context",
			func(tb testing.TB, m Mixed) {
				mixedAssertCauseToleratesNilContext(tb, m)
			}),
	}
}

// mixedAssertOpenSmoke asserts Open survives a call with a derived name.
func mixedAssertOpenSmoke(
	tb testing.TB,
	m Mixed,
	fx MixedFixture,
) {
	tb.Helper()
	suite.Survives(tb, mixedOpen, func(ctx context.Context) {
		_ = m.Open(ctx, fx.Name)
	})
}

// mixedAssertOpenCancels asserts Open reports a cancelled context as cancelled.
func mixedAssertOpenCancels(
	tb testing.TB,
	m Mixed,
	fx MixedFixture,
) {
	tb.Helper()
	suite.ReportsCancelled(tb, mixedOpen, func(ctx context.Context) error {
		return m.Open(ctx, fx.Name)
	})
}

// mixedAssertOpenToleratesNilContext asserts Open returns an error rather than panicking on a nil context.
func mixedAssertOpenToleratesNilContext(
	tb testing.TB,
	m Mixed,
	fx MixedFixture,
) {
	tb.Helper()
	suite.ToleratesNilContext(tb, mixedOpen, func(ctx context.Context) error {
		return m.Open(ctx, fx.Name)
	})
}

// mixedAssertOpenHonoursDeadline asserts Open reports an expired deadline as exceeded.
func mixedAssertOpenHonoursDeadline(
	tb testing.TB,
	m Mixed,
	fx MixedFixture,
) {
	tb.Helper()
	suite.ReportsDeadlineExceeded(tb, mixedOpen, func(ctx context.Context) error {
		return m.Open(ctx, fx.Name)
	})
}

// mixedAssertCauseSmoke asserts Cause survives a call.
func mixedAssertCauseSmoke(
	tb testing.TB,
	m Mixed,
) {
	tb.Helper()
	suite.Survives(tb, mixedCause, func(ctx context.Context) {
		_ = m.Cause(ctx)
	})
}

// mixedAssertCauseCancels asserts Cause reports a cancelled context as cancelled.
func mixedAssertCauseCancels(
	tb testing.TB,
	m Mixed,
) {
	tb.Helper()
	suite.ReportsCancelled(tb, mixedCause, func(ctx context.Context) error {
		return m.Cause(ctx)
	})
}

// mixedAssertCauseToleratesNilContext asserts Cause returns an error rather than panicking on a nil context.
func mixedAssertCauseToleratesNilContext(
	tb testing.TB,
	m Mixed,
) {
	tb.Helper()
	suite.ToleratesNilContext(tb, mixedCause, func(ctx context.Context) error {
		return m.Cause(ctx)
	})
}

// AssertMixedContract runs every generated check against every declared subject.
//
//	Subjects: declare each with MixedSubject
//	Extend:   MixedOnOpen, MixedOnCause
//	Drop:     MixedWithout, by the path each check reports under
func AssertMixedContract(t *testing.T, opts ...MixedOption) {
	t.Helper()
	cfg := newMixedConfig(opts...)
	if len(cfg.subjects) == 0 {
		t.Fatalf("AssertMixedContract was given no subject; declare one with " +
			"MixedSubject(name, factory)")
	}
	for _, p := range cfg.phantoms() {
		t.Errorf("MixedWithout(%q) declines nothing: no check here reports "+
			"under that path, so this suite asserts one thing more than its "+
			"author believes", p)
	}
	for _, s := range cfg.subjects {
		runMixedChecks(t, cfg, s.name, s.factory, nil)
	}
}

// runMixedChecks runs the whole contract against one subject.
//
// wrap is applied after the subject is built and seeded, so a wrapper is
// transparent to the seed and every failure it causes is its own.
func runMixedChecks(
	t *testing.T,
	cfg *mixedConfig,
	label string,
	factory func() wrappedvia.Mixed,
	wrap func(tb testing.TB, subject wrappedvia.Mixed) wrappedvia.Mixed,
) {
	t.Helper()
	t.Run(label, func(t *testing.T) {
		t.Parallel()

		t.Run(mixedOpen, func(t *testing.T) {
			t.Parallel()

			for _, c := range cfg.onOpen {
				cfg.run(t, string(suite.MethodID(mixedOpen, c.name)), c.name, func(tb testing.TB) {
					c.fn(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.Name)
				})
			}
		})

		t.Run(mixedCause, func(t *testing.T) {
			t.Parallel()

			for _, c := range cfg.onCause {
				cfg.run(t, string(suite.MethodID(mixedCause, c.name)), c.name, func(tb testing.TB) {
					c.fn(tb, cfg.subject(tb, factory, wrap))
				})
			}
		})

		// The extra bodies of checks this run was given, each reporting under its
		// own name and dropped by it.
		//
		// The plain subject only. The wrapped pass exists to prove the double
		// faithful and the per-method checks above already do that, so running one
		// through the wrapper prices the wrapper and says nothing new about the
		// subject.
		if wrap == nil {
			for _, e := range cfg.extensions {
				if cfg.declined(e.name) {
					t.Run(e.name, func(t *testing.T) {
						t.Skipf("dropped through MixedWithout(%q)", e.name)
					})
					continue
				}
				t.Run(e.name, func(t *testing.T) {
					t.Parallel()
					e.run(t, label, factory, cfg)
				})
			}
		}
	})
}

// MixedOption configures a contract run.
type MixedOption func(*mixedConfig)

// MixedSubject declares an implementation to hold to the contract.
//
// Repeat it for every implementation that claims to satisfy the interface — an
// in-memory one, the one backed by a database, the one behind a network. Every
// check and every option is stated once and runs against all of them, which is
// what a conformance suite is for.
func MixedSubject(name string, factory func() wrappedvia.Mixed) MixedOption {
	return func(c *mixedConfig) {
		c.subjects = append(c.subjects, namedMixedSubject{name, factory})
	}
}

// MixedWithClock supplies the clock every time-reading check measures on.
//
// Defaults to the real one, so an implementation that does not take a clock
// behaves as it would have. Supply a [clock.TestClock] — the same one the
// factory builds the subject with — and a budget becomes a claim about the time
// the implementation means to spend rather than about how loaded the machine
// was.
func MixedWithClock(c clock.Clock) MixedOption {
	return func(cfg *mixedConfig) { cfg.clock = c }
}

// MixedWithFixture replaces the derived inputs.
func MixedWithFixture(f MixedFixture) MixedOption {
	return func(c *mixedConfig) { c.Fixture = f }
}

// MixedSeed replaces the derived seed.
//
// For an interface with no writer to seed itself through, which is the one
// input a conformance run cannot derive: a reader over an empty subject
// asserts nothing.
//
// Returning an error rather than swallowing one. A seed that failed quietly
// leaves every check after it asserting nothing and reporting success.
func MixedSeed(fn func(ctx context.Context, subject wrappedvia.Mixed) error) MixedOption {
	return func(c *mixedConfig) {
		c.seed = fn
		c.seedIsDerived = false
	}
}

// MixedOnOpen adds a named check to Open.
func MixedOnOpen(name string, fn MixedOpenCheck) MixedOption {
	return func(c *mixedConfig) {
		c.onOpen = append(c.onOpen, namedMixedOpenCheck{name, fn})
	}
}

// MixedOnCause adds a named check to Cause.
func MixedOnCause(name string, fn MixedCauseCheck) MixedOption {
	return func(c *mixedConfig) {
		c.onCause = append(c.onCause, namedMixedCauseCheck{name, fn})
	}
}

// MixedWithout drops generated checks by the path each reports under.
//
// For a subject that legitimately violates one. Without this the only recourse
// is to stop running the suite, which costs every other check in the file — so
// a dropped check is reported as skipped rather than silently omitted.
func MixedWithout(paths ...string) MixedOption {
	return func(c *mixedConfig) {
		for _, p := range paths {
			c.without[p] = struct{}{}
		}
	}
}

type namedMixedOpenCheck struct {
	name string
	fn   MixedOpenCheck
}

type namedMixedCauseCheck struct {
	name string
	fn   MixedCauseCheck
}

type namedMixedSubject struct {
	name    string
	factory func() wrappedvia.Mixed
}

// mixedContractExtension is a body of checks a generated sibling adds through an option.
//
// A sibling declares the option and closes over whatever configures it, so
// everything that varies per run is typed and private to the file that owns it
// — this file understands only the name and the call. Nothing arrives except
// through an option, which is what keeps the set fixed before the first subtest
// starts.
type mixedContractExtension struct {
	// name is the path this reports under, and the one MixedWithout drops
	// it by. Anything nested beneath it is the extension's own to name.
	name string

	// run takes the factory rather than a subject. One that drives sequences
	// builds a subject per iteration, and one that compares against a reference
	// needs the unseeded subject a factory returns — which
	// mixedConfig.subject is not.
	//
	// Called once per subject, in parallel, sharing the config. Anything it
	// mutates has to be built inside the call: state captured when the option
	// was declared is state two subjects write at once.
	run func(
		t *testing.T,
		subject string,
		factory func() wrappedvia.Mixed,
		cfg *mixedConfig,
	)
}

type mixedConfig struct {
	Fixture  MixedFixture
	subjects []namedMixedSubject
	clock    clock.Clock
	seed     func(ctx context.Context, subject wrappedvia.Mixed) error
	// seedIsDerived is what lets a failed seed name the right culprit. The
	// derived seed and a consumer's are the same field, and the advice for one
	// is useless for the other.
	seedIsDerived bool
	without       map[string]struct{}
	extensions    []mixedContractExtension
	onOpen        []namedMixedOpenCheck
	onCause       []namedMixedCauseCheck
}

func newMixedConfig(opts ...MixedOption) *mixedConfig {
	c := &mixedConfig{
		Fixture: DefaultMixedFixture(),
		clock:   clock.RealClock(),
		without: map[string]struct{}{},
	}
	// Derived: Open is classified writer, so the interface
	// populates itself and a reader has something to read.
	c.seed = func(ctx context.Context, subject wrappedvia.Mixed) error {
		return subject.Open(ctx, c.Fixture.Name)
	}
	c.seedIsDerived = true
	for _, o := range opts {
		o(c)
	}
	return c
}

// subject builds a fresh implementation and seeds it, so every check runs
// against state no other check touched.
func (c *mixedConfig) subject(
	tb testing.TB,
	factory func() wrappedvia.Mixed,
	wrap func(tb testing.TB, subject wrappedvia.Mixed) wrappedvia.Mixed,
) wrappedvia.Mixed {
	tb.Helper()
	s := factory()
	if c.seed != nil {
		// Reported rather than ignored. A seed that silently failed would leave
		// every check after it running against an empty subject — passing, and
		// asserting nothing.
		//
		// Which seed ran decides what the message can usefully say. The derived
		// one fails for one of two reasons and both are fixable from here, so it
		// names them. A seed the caller supplied is theirs, and telling them to
		// supply one is advice they already took.
		if err := c.seed(tb.Context(), s); err != nil {
			if c.seedIsDerived {
				tb.Fatalf("seeding through Open failed: %v; "+
					"supply a value it accepts through MixedWithFixture, or a "+
					"whole seed through MixedSeed", err)
			} else {
				tb.Fatalf("the seed supplied through MixedSeed failed: %v", err)
			}
		}
	}
	if wrap != nil {
		s = wrap(tb, s)
	}
	return s
}

// dropped reports whether a path was declined through MixedWithout.
//
// Named rather than inlined because the drop set is read from two places: here,
// and by whatever registered through the seam — which reports under paths this
// file has never heard of and must answer the same question about them.
func (c *mixedConfig) declined(path string) bool {
	_, ok := c.without[path]
	return ok
}

// mixedPaths is every path this file reports a check under.
//
// The set a MixedWithout argument has to be a member of. Declared
// rather than observed: a check that did not run is not a check that does not
// exist — an integration-only method skips without ever consulting the drop
// set, and reading that as a phantom would fail a correct suite on a machine
// with the environment unset.
//
//nolint:gochecknoglobals // the emitted check paths, read-only after init.
var mixedPaths = map[string]struct{}{}

// phantoms names every MixedWithout path that declines nothing.
//
// A drop that drops nothing is this option's silent half: the consumer wrote
// it against a check since renamed, or never emitted for that method at all,
// and every run after reads green while asserting one thing more than its
// author believes. Extensions carry their own names — the seam reports under
// paths this file has never heard of — so they count as known too.
func (c *mixedConfig) phantoms() []string {
	known := func(p string) bool {
		if _, ok := mixedPaths[p]; ok {
			return true
		}
		for _, e := range c.extensions {
			if e.name == p {
				return true
			}
		}
		for _, on := range c.onOpen {
			if string(suite.MethodID(mixedOpen, on.name)) == p {
				return true
			}
		}
		for _, on := range c.onCause {
			if string(suite.MethodID(mixedCause, on.name)) == p {
				return true
			}
		}
		return false
	}
	var out []string
	for p := range c.without {
		if !known(p) {
			out = append(out, p)
		}
	}
	slices.Sort(out)
	return out
}

// run executes one check unless it was dropped, reporting the drop rather than
// silently omitting it.
func (c *mixedConfig) run(t *testing.T, path, name string, fn func(tb testing.TB)) {
	t.Helper()
	if c.declined(path) {
		t.Run(name, func(t *testing.T) {
			t.Skipf("dropped through MixedWithout(%q)", path)
		})
		return
	}
	t.Run(name, func(t *testing.T) {
		t.Parallel()
		fn(t)
	})
}

// testkit: end of generated content.
// testkit:provenance f07c5ea7bd5d58e0a2fd6acff9f3a0f9689d8d5c4257e0dcb52b2436d5ef8a31
