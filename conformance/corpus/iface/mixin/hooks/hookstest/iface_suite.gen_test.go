// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/mixin/hooks/iface.go
// Plugins:   golang 1.0.0, suite 1.0.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package hookstest_test

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/iface/mixin/hooks/hookstest"
)

// Every generated check for Mixed, driven against a stand-in built
// to violate it.
//
//	Proved:   6 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertMixedFireSmokeCanFail holds AssertMixedFireSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertMixedFireSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := hookstest.DefaultMixedFixture()
	subject := hookstest.NewMixedStub(t,
		hookstest.WithMixedFire(func(context.Context, string) error {
			panic("Mixed.Fire: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			hookstest.AssertMixedFireSmoke(tb, subject, fixture.Event)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedFireCancelsCanFail holds AssertMixedFireCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertMixedFireCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := hookstest.DefaultMixedFixture()
	subject := hookstest.NewMixedStub(t,
		hookstest.WithMixedFire(func(context.Context, string) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			hookstest.AssertMixedFireCancels(tb, subject, fixture.Event)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedFireHonoursDeadlineCanFail holds AssertMixedFireHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertMixedFireHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := hookstest.DefaultMixedFixture()
	subject := hookstest.NewMixedStub(t,
		hookstest.WithMixedFire(func(context.Context, string) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			hookstest.AssertMixedFireHonoursDeadline(tb, subject, fixture.Event)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedFireToleratesNilContextCanFail holds AssertMixedFireToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertMixedFireToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := hookstest.DefaultMixedFixture()
	subject := hookstest.NewMixedStub(t,
		hookstest.WithMixedFire(func(context.Context, string) error {
			panic("Mixed.Fire: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			hookstest.AssertMixedFireToleratesNilContext(tb, subject, fixture.Event)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedFireFiresRegisteredHooksCanFail holds AssertMixedFireFiresRegisteredHooks to being able to fail.
//
// The stand-in takes the registration and forgets it. A check that let that pass
// would report a working hook for a subject where nothing ever runs.
func TestAssertMixedFireFiresRegisteredHooksCanFail(t *testing.T) {
	t.Parallel()

	fixture := hookstest.DefaultMixedFixture()
	subject := hookstest.NewMixedStub(t,
		hookstest.WithMixedFire(func(context.Context, string) error {
			return nil
		}),
		hookstest.WithMixedOnEvent(func(func(string)) {
			return
		}))

	got := testkit.Rejects(t, "a subject that takes a registration and forgets it",
		func(tb testing.TB) {
			tb.Helper()
			hookstest.AssertMixedFireFiresRegisteredHooks(tb, subject, fixture.Event)
		})
	testkit.Assert(t, got).Contains("must invoke what",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedOnEventSmokeCanFail holds AssertMixedOnEventSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertMixedOnEventSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := hookstest.DefaultMixedFixture()
	subject := hookstest.NewMixedStub(t,
		hookstest.WithMixedOnEvent(func(func(string)) {
			panic("Mixed.OnEvent: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			hookstest.AssertMixedOnEventSmoke(tb, subject, fixture.Fn)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance a594db9b0a1acc9087433c03bf68bc940c349b2d99b1026080a242c0434498f0
