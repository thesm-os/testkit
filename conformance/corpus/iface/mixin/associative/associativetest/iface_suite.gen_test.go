// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/mixin/associative/iface.go
// Plugins:   golang 1.0.0, suite 1.15.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package associativetest_test

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/iface/mixin/associative"
	"go.thesmos.sh/testkit/conformance/corpus/iface/mixin/associative/associativetest"
)

// Every generated check for Mixed, driven against a stand-in built
// to violate it.
//
//	Proved:   8 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertMixedApplySmokeCanFail holds AssertMixedApplySmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertMixedApplySmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := associativetest.DefaultMixedFixture()
	subject := associativetest.NewMixedStub(t,
		associativetest.WithMixedApply(func(context.Context, associative.Delta) error {
			panic("Mixed.Apply: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			associativetest.AssertMixedApplySmoke(tb, subject, fixture.D)
		})
	testkit.Assert(t, got).Contains("Apply panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedApplyCancelsCanFail holds AssertMixedApplyCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertMixedApplyCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := associativetest.DefaultMixedFixture()
	subject := associativetest.NewMixedStub(t,
		associativetest.WithMixedApply(func(context.Context, associative.Delta) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			associativetest.AssertMixedApplyCancels(tb, subject, fixture.D)
		})
	testkit.Assert(t, got).Contains("Apply must report a cancelled context as context.Canceled",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedApplyHonoursDeadlineCanFail holds AssertMixedApplyHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertMixedApplyHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := associativetest.DefaultMixedFixture()
	subject := associativetest.NewMixedStub(t,
		associativetest.WithMixedApply(func(context.Context, associative.Delta) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			associativetest.AssertMixedApplyHonoursDeadline(tb, subject, fixture.D)
		})
	testkit.Assert(t, got).Contains("Apply must report an expired deadline as context.DeadlineExceeded",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedApplyToleratesNilContextCanFail holds AssertMixedApplyToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertMixedApplyToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := associativetest.DefaultMixedFixture()
	subject := associativetest.NewMixedStub(t,
		associativetest.WithMixedApply(func(context.Context, associative.Delta) error {
			panic("Mixed.Apply: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			associativetest.AssertMixedApplyToleratesNilContext(tb, subject, fixture.D)
		})
	testkit.Assert(t, got).Contains("Apply panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedTotalSmokeCanFail holds AssertMixedTotalSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertMixedTotalSmokeCanFail(t *testing.T) {
	t.Parallel()

	subject := associativetest.NewMixedStub(t,
		associativetest.WithMixedTotal(func(context.Context) (int, error) {
			panic("Mixed.Total: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			associativetest.AssertMixedTotalSmoke(tb, subject)
		})
	testkit.Assert(t, got).Contains("Total panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedTotalCancelsCanFail holds AssertMixedTotalCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertMixedTotalCancelsCanFail(t *testing.T) {
	t.Parallel()

	subject := associativetest.NewMixedStub(t,
		associativetest.WithMixedTotal(func(context.Context) (int, error) {
			var r0 int
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			associativetest.AssertMixedTotalCancels(tb, subject)
		})
	testkit.Assert(t, got).Contains("Total must report a cancelled context as context.Canceled",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedTotalHonoursDeadlineCanFail holds AssertMixedTotalHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertMixedTotalHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	subject := associativetest.NewMixedStub(t,
		associativetest.WithMixedTotal(func(context.Context) (int, error) {
			var r0 int
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			associativetest.AssertMixedTotalHonoursDeadline(tb, subject)
		})
	testkit.Assert(t, got).Contains("Total must report an expired deadline as context.DeadlineExceeded",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedTotalToleratesNilContextCanFail holds AssertMixedTotalToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertMixedTotalToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	subject := associativetest.NewMixedStub(t,
		associativetest.WithMixedTotal(func(context.Context) (int, error) {
			panic("Mixed.Total: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			associativetest.AssertMixedTotalToleratesNilContext(tb, subject)
		})
	testkit.Assert(t, got).Contains("Total panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance 9d02ea068652f9209adc390fc84511ac49a61350bb66f0eb5d431423e5555657
