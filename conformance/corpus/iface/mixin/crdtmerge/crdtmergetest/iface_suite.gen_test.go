// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/mixin/crdtmerge/iface.go
// Plugins:   golang 1.0.0, suite 1.15.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package crdtmergetest_test

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/iface/mixin/crdtmerge"
	"go.thesmos.sh/testkit/conformance/corpus/iface/mixin/crdtmerge/crdtmergetest"
)

// Every generated check for Replica, driven against a stand-in built
// to violate it.
//
//	Proved:   4 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertReplicaItemsSmokeCanFail holds AssertReplicaItemsSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertReplicaItemsSmokeCanFail(t *testing.T) {
	t.Parallel()

	subject := crdtmergetest.NewReplicaStub(t,
		crdtmergetest.WithReplicaItems(func(context.Context) ([]string, error) {
			panic("Replica.Items: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			crdtmergetest.AssertReplicaItemsSmoke(tb, subject)
		})
	testkit.Assert(t, got).Contains("Items panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertReplicaItemsCancelsCanFail holds AssertReplicaItemsCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertReplicaItemsCancelsCanFail(t *testing.T) {
	t.Parallel()

	subject := crdtmergetest.NewReplicaStub(t,
		crdtmergetest.WithReplicaItems(func(context.Context) ([]string, error) {
			var r0 []string
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			crdtmergetest.AssertReplicaItemsCancels(tb, subject)
		})
	testkit.Assert(t, got).Contains("Items must report a cancelled context as context.Canceled",
		"and rejects it for the reason the check is about")
}

// TestAssertReplicaItemsHonoursDeadlineCanFail holds AssertReplicaItemsHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertReplicaItemsHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	subject := crdtmergetest.NewReplicaStub(t,
		crdtmergetest.WithReplicaItems(func(context.Context) ([]string, error) {
			var r0 []string
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			crdtmergetest.AssertReplicaItemsHonoursDeadline(tb, subject)
		})
	testkit.Assert(t, got).Contains("Items must report an expired deadline as context.DeadlineExceeded",
		"and rejects it for the reason the check is about")
}

// TestAssertReplicaItemsToleratesNilContextCanFail holds AssertReplicaItemsToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertReplicaItemsToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	subject := crdtmergetest.NewReplicaStub(t,
		crdtmergetest.WithReplicaItems(func(context.Context) ([]string, error) {
			panic("Replica.Items: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			crdtmergetest.AssertReplicaItemsToleratesNilContext(tb, subject)
		})
	testkit.Assert(t, got).Contains("Items panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// Every generated check for Mixed, driven against a stand-in built
// to violate it.
//
//	Proved:   12 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertMixedMergeSmokeCanFail holds AssertMixedMergeSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertMixedMergeSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := crdtmergetest.DefaultMixedFixture()
	subject := crdtmergetest.NewMixedStub(t,
		crdtmergetest.WithMixedMerge(func(context.Context, crdtmerge.Replica) error {
			panic("Mixed.Merge: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			crdtmergetest.AssertMixedMergeSmoke(tb, subject, fixture.Peer)
		})
	testkit.Assert(t, got).Contains("Merge panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedMergeCancelsCanFail holds AssertMixedMergeCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertMixedMergeCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := crdtmergetest.DefaultMixedFixture()
	subject := crdtmergetest.NewMixedStub(t,
		crdtmergetest.WithMixedMerge(func(context.Context, crdtmerge.Replica) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			crdtmergetest.AssertMixedMergeCancels(tb, subject, fixture.Peer)
		})
	testkit.Assert(t, got).Contains("Merge must report a cancelled context as context.Canceled",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedMergeHonoursDeadlineCanFail holds AssertMixedMergeHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertMixedMergeHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := crdtmergetest.DefaultMixedFixture()
	subject := crdtmergetest.NewMixedStub(t,
		crdtmergetest.WithMixedMerge(func(context.Context, crdtmerge.Replica) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			crdtmergetest.AssertMixedMergeHonoursDeadline(tb, subject, fixture.Peer)
		})
	testkit.Assert(t, got).Contains("Merge must report an expired deadline as context.DeadlineExceeded",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedMergeToleratesNilContextCanFail holds AssertMixedMergeToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertMixedMergeToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := crdtmergetest.DefaultMixedFixture()
	subject := crdtmergetest.NewMixedStub(t,
		crdtmergetest.WithMixedMerge(func(context.Context, crdtmerge.Replica) error {
			panic("Mixed.Merge: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			crdtmergetest.AssertMixedMergeToleratesNilContext(tb, subject, fixture.Peer)
		})
	testkit.Assert(t, got).Contains("Merge panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedAddSmokeCanFail holds AssertMixedAddSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertMixedAddSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := crdtmergetest.DefaultMixedFixture()
	subject := crdtmergetest.NewMixedStub(t,
		crdtmergetest.WithMixedAdd(func(context.Context, string) error {
			panic("Mixed.Add: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			crdtmergetest.AssertMixedAddSmoke(tb, subject, fixture.Item)
		})
	testkit.Assert(t, got).Contains("Add panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedAddCancelsCanFail holds AssertMixedAddCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertMixedAddCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := crdtmergetest.DefaultMixedFixture()
	subject := crdtmergetest.NewMixedStub(t,
		crdtmergetest.WithMixedAdd(func(context.Context, string) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			crdtmergetest.AssertMixedAddCancels(tb, subject, fixture.Item)
		})
	testkit.Assert(t, got).Contains("Add must report a cancelled context as context.Canceled",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedAddHonoursDeadlineCanFail holds AssertMixedAddHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertMixedAddHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := crdtmergetest.DefaultMixedFixture()
	subject := crdtmergetest.NewMixedStub(t,
		crdtmergetest.WithMixedAdd(func(context.Context, string) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			crdtmergetest.AssertMixedAddHonoursDeadline(tb, subject, fixture.Item)
		})
	testkit.Assert(t, got).Contains("Add must report an expired deadline as context.DeadlineExceeded",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedAddToleratesNilContextCanFail holds AssertMixedAddToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertMixedAddToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := crdtmergetest.DefaultMixedFixture()
	subject := crdtmergetest.NewMixedStub(t,
		crdtmergetest.WithMixedAdd(func(context.Context, string) error {
			panic("Mixed.Add: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			crdtmergetest.AssertMixedAddToleratesNilContext(tb, subject, fixture.Item)
		})
	testkit.Assert(t, got).Contains("Add panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedItemsSmokeCanFail holds AssertMixedItemsSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertMixedItemsSmokeCanFail(t *testing.T) {
	t.Parallel()

	subject := crdtmergetest.NewMixedStub(t,
		crdtmergetest.WithMixedItems(func(context.Context) ([]string, error) {
			panic("Mixed.Items: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			crdtmergetest.AssertMixedItemsSmoke(tb, subject)
		})
	testkit.Assert(t, got).Contains("Items panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedItemsCancelsCanFail holds AssertMixedItemsCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertMixedItemsCancelsCanFail(t *testing.T) {
	t.Parallel()

	subject := crdtmergetest.NewMixedStub(t,
		crdtmergetest.WithMixedItems(func(context.Context) ([]string, error) {
			var r0 []string
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			crdtmergetest.AssertMixedItemsCancels(tb, subject)
		})
	testkit.Assert(t, got).Contains("Items must report a cancelled context as context.Canceled",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedItemsHonoursDeadlineCanFail holds AssertMixedItemsHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertMixedItemsHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	subject := crdtmergetest.NewMixedStub(t,
		crdtmergetest.WithMixedItems(func(context.Context) ([]string, error) {
			var r0 []string
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			crdtmergetest.AssertMixedItemsHonoursDeadline(tb, subject)
		})
	testkit.Assert(t, got).Contains("Items must report an expired deadline as context.DeadlineExceeded",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedItemsToleratesNilContextCanFail holds AssertMixedItemsToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertMixedItemsToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	subject := crdtmergetest.NewMixedStub(t,
		crdtmergetest.WithMixedItems(func(context.Context) ([]string, error) {
			panic("Mixed.Items: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			crdtmergetest.AssertMixedItemsToleratesNilContext(tb, subject)
		})
	testkit.Assert(t, got).Contains("Items panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance 88bf4e90ab90aea2f021b7bbf733be494c2f8e770547a6cd770fa292ef22706b
