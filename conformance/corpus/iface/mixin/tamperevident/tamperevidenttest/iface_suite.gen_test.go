// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/mixin/tamperevident/iface.go
// Plugins:   golang 1.0.0, suite 1.15.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package tamperevidenttest_test

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/iface/mixin/tamperevident/tamperevidenttest"
)

// Every generated check for Mixed, driven against a stand-in built
// to violate it.
//
//	Proved:   12 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertMixedStoreSmokeCanFail holds AssertMixedStoreSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertMixedStoreSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := tamperevidenttest.DefaultMixedFixture()
	subject := tamperevidenttest.NewMixedStub(t,
		tamperevidenttest.WithMixedStore(func(context.Context, string) error {
			panic("Mixed.Store: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			tamperevidenttest.AssertMixedStoreSmoke(tb, subject, fixture.Body)
		})
	testkit.Assert(t, got).Contains("Store panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedStoreCancelsCanFail holds AssertMixedStoreCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertMixedStoreCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := tamperevidenttest.DefaultMixedFixture()
	subject := tamperevidenttest.NewMixedStub(t,
		tamperevidenttest.WithMixedStore(func(context.Context, string) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			tamperevidenttest.AssertMixedStoreCancels(tb, subject, fixture.Body)
		})
	testkit.Assert(t, got).Contains("Store must report a cancelled context as context.Canceled",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedStoreHonoursDeadlineCanFail holds AssertMixedStoreHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertMixedStoreHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := tamperevidenttest.DefaultMixedFixture()
	subject := tamperevidenttest.NewMixedStub(t,
		tamperevidenttest.WithMixedStore(func(context.Context, string) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			tamperevidenttest.AssertMixedStoreHonoursDeadline(tb, subject, fixture.Body)
		})
	testkit.Assert(t, got).Contains("Store must report an expired deadline as context.DeadlineExceeded",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedStoreToleratesNilContextCanFail holds AssertMixedStoreToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertMixedStoreToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := tamperevidenttest.DefaultMixedFixture()
	subject := tamperevidenttest.NewMixedStub(t,
		tamperevidenttest.WithMixedStore(func(context.Context, string) error {
			panic("Mixed.Store: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			tamperevidenttest.AssertMixedStoreToleratesNilContext(tb, subject, fixture.Body)
		})
	testkit.Assert(t, got).Contains("Store panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedCorruptSmokeCanFail holds AssertMixedCorruptSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertMixedCorruptSmokeCanFail(t *testing.T) {
	t.Parallel()

	subject := tamperevidenttest.NewMixedStub(t,
		tamperevidenttest.WithMixedCorrupt(func(context.Context) error {
			panic("Mixed.Corrupt: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			tamperevidenttest.AssertMixedCorruptSmoke(tb, subject)
		})
	testkit.Assert(t, got).Contains("Corrupt panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedCorruptCancelsCanFail holds AssertMixedCorruptCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertMixedCorruptCancelsCanFail(t *testing.T) {
	t.Parallel()

	subject := tamperevidenttest.NewMixedStub(t,
		tamperevidenttest.WithMixedCorrupt(func(context.Context) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			tamperevidenttest.AssertMixedCorruptCancels(tb, subject)
		})
	testkit.Assert(t, got).Contains("Corrupt must report a cancelled context as context.Canceled",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedCorruptHonoursDeadlineCanFail holds AssertMixedCorruptHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertMixedCorruptHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	subject := tamperevidenttest.NewMixedStub(t,
		tamperevidenttest.WithMixedCorrupt(func(context.Context) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			tamperevidenttest.AssertMixedCorruptHonoursDeadline(tb, subject)
		})
	testkit.Assert(t, got).Contains("Corrupt must report an expired deadline as context.DeadlineExceeded",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedCorruptToleratesNilContextCanFail holds AssertMixedCorruptToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertMixedCorruptToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	subject := tamperevidenttest.NewMixedStub(t,
		tamperevidenttest.WithMixedCorrupt(func(context.Context) error {
			panic("Mixed.Corrupt: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			tamperevidenttest.AssertMixedCorruptToleratesNilContext(tb, subject)
		})
	testkit.Assert(t, got).Contains("Corrupt panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedVerifySmokeCanFail holds AssertMixedVerifySmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertMixedVerifySmokeCanFail(t *testing.T) {
	t.Parallel()

	subject := tamperevidenttest.NewMixedStub(t,
		tamperevidenttest.WithMixedVerify(func(context.Context) error {
			panic("Mixed.Verify: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			tamperevidenttest.AssertMixedVerifySmoke(tb, subject)
		})
	testkit.Assert(t, got).Contains("Verify panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedVerifyCancelsCanFail holds AssertMixedVerifyCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertMixedVerifyCancelsCanFail(t *testing.T) {
	t.Parallel()

	subject := tamperevidenttest.NewMixedStub(t,
		tamperevidenttest.WithMixedVerify(func(context.Context) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			tamperevidenttest.AssertMixedVerifyCancels(tb, subject)
		})
	testkit.Assert(t, got).Contains("Verify must report a cancelled context as context.Canceled",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedVerifyHonoursDeadlineCanFail holds AssertMixedVerifyHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertMixedVerifyHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	subject := tamperevidenttest.NewMixedStub(t,
		tamperevidenttest.WithMixedVerify(func(context.Context) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			tamperevidenttest.AssertMixedVerifyHonoursDeadline(tb, subject)
		})
	testkit.Assert(t, got).Contains("Verify must report an expired deadline as context.DeadlineExceeded",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedVerifyToleratesNilContextCanFail holds AssertMixedVerifyToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertMixedVerifyToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	subject := tamperevidenttest.NewMixedStub(t,
		tamperevidenttest.WithMixedVerify(func(context.Context) error {
			panic("Mixed.Verify: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			tamperevidenttest.AssertMixedVerifyToleratesNilContext(tb, subject)
		})
	testkit.Assert(t, got).Contains("Verify panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance 56af1f01fcc18d4b1129b6e6f427edad87994810366e54676ce3fd898de6fd4f
