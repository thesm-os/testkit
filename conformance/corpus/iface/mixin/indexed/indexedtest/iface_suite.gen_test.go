// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/mixin/indexed/iface.go
// Plugins:   golang 1.0.0, suite 1.6.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package indexedtest_test

import (
	"context"
	"errors"
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/iface/mixin/indexed"
	"go.thesmos.sh/testkit/conformance/corpus/iface/mixin/indexed/indexedtest"
)

// Every generated check for Ranked, driven against a stand-in built
// to violate it.
//
//	Proved:   13 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertRankedAddSmokeCanFail holds AssertRankedAddSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertRankedAddSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := indexedtest.DefaultRankedFixture()
	subject := indexedtest.NewRankedStub(t,
		indexedtest.WithRankedAdd(func(context.Context, indexed.Value) error {
			panic("Ranked.Add: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			indexedtest.AssertRankedAddSmoke(tb, subject, fixture.V)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertRankedAddCancelsCanFail holds AssertRankedAddCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertRankedAddCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := indexedtest.DefaultRankedFixture()
	subject := indexedtest.NewRankedStub(t,
		indexedtest.WithRankedAdd(func(context.Context, indexed.Value) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			indexedtest.AssertRankedAddCancels(tb, subject, fixture.V)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertRankedAddHonoursDeadlineCanFail holds AssertRankedAddHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertRankedAddHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := indexedtest.DefaultRankedFixture()
	subject := indexedtest.NewRankedStub(t,
		indexedtest.WithRankedAdd(func(context.Context, indexed.Value) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			indexedtest.AssertRankedAddHonoursDeadline(tb, subject, fixture.V)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertRankedAddToleratesNilContextCanFail holds AssertRankedAddToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertRankedAddToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := indexedtest.DefaultRankedFixture()
	subject := indexedtest.NewRankedStub(t,
		indexedtest.WithRankedAdd(func(context.Context, indexed.Value) error {
			panic("Ranked.Add: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			indexedtest.AssertRankedAddToleratesNilContext(tb, subject, fixture.V)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertRankedLenSmokeCanFail holds AssertRankedLenSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertRankedLenSmokeCanFail(t *testing.T) {
	t.Parallel()

	subject := indexedtest.NewRankedStub(t,
		indexedtest.WithRankedLen(func(context.Context) (int, error) {
			panic("Ranked.Len: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			indexedtest.AssertRankedLenSmoke(tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertRankedLenCancelsCanFail holds AssertRankedLenCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertRankedLenCancelsCanFail(t *testing.T) {
	t.Parallel()

	subject := indexedtest.NewRankedStub(t,
		indexedtest.WithRankedLen(func(context.Context) (int, error) {
			var r0 int
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			indexedtest.AssertRankedLenCancels(tb, subject)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertRankedLenHonoursDeadlineCanFail holds AssertRankedLenHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertRankedLenHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	subject := indexedtest.NewRankedStub(t,
		indexedtest.WithRankedLen(func(context.Context) (int, error) {
			var r0 int
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			indexedtest.AssertRankedLenHonoursDeadline(tb, subject)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertRankedLenToleratesNilContextCanFail holds AssertRankedLenToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertRankedLenToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	subject := indexedtest.NewRankedStub(t,
		indexedtest.WithRankedLen(func(context.Context) (int, error) {
			panic("Ranked.Len: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			indexedtest.AssertRankedLenToleratesNilContext(tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertRankedAtSmokeCanFail holds AssertRankedAtSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertRankedAtSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := indexedtest.DefaultRankedFixture()
	subject := indexedtest.NewRankedStub(t,
		indexedtest.WithRankedAt(func(context.Context, int) (indexed.Value, error) {
			panic("Ranked.At: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			indexedtest.AssertRankedAtSmoke(tb, subject, fixture.I)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertRankedAtCancelsCanFail holds AssertRankedAtCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertRankedAtCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := indexedtest.DefaultRankedFixture()
	subject := indexedtest.NewRankedStub(t,
		indexedtest.WithRankedAt(func(context.Context, int) (indexed.Value, error) {
			var r0 indexed.Value
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			indexedtest.AssertRankedAtCancels(tb, subject, fixture.I)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertRankedAtHonoursDeadlineCanFail holds AssertRankedAtHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertRankedAtHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := indexedtest.DefaultRankedFixture()
	subject := indexedtest.NewRankedStub(t,
		indexedtest.WithRankedAt(func(context.Context, int) (indexed.Value, error) {
			var r0 indexed.Value
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			indexedtest.AssertRankedAtHonoursDeadline(tb, subject, fixture.I)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertRankedAtToleratesNilContextCanFail holds AssertRankedAtToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertRankedAtToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := indexedtest.DefaultRankedFixture()
	subject := indexedtest.NewRankedStub(t,
		indexedtest.WithRankedAt(func(context.Context, int) (indexed.Value, error) {
			panic("Ranked.At: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			indexedtest.AssertRankedAtToleratesNilContext(tb, subject, fixture.I)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertRankedAtZeroOnErrorCanFail holds AssertRankedAtZeroOnError to being able to fail.
//
// The stand-in refuses the call and answers a value that looks like a result
// anyway. A check that let that pass would leave a caller who checks the error
// and a caller who checks the value disagreeing about what happened — the
// claim's own violation, not a stand-in that merely succeeds.
func TestAssertRankedAtZeroOnErrorCanFail(t *testing.T) {
	t.Parallel()

	fixture := indexedtest.DefaultRankedFixture()
	subject := indexedtest.NewRankedStub(t,
		indexedtest.WithRankedAt(func(context.Context, int) (indexed.Value, error) {
			return indexed.Value{Key: "test-r0"}, errors.New("violate: refused with a believable value")
		}))

	got := testkit.Rejects(t, "a method answering a believable value beside its error",
		func(tb testing.TB) {
			tb.Helper()
			indexedtest.AssertRankedAtZeroOnError(tb, subject, fixture.IOther)
		})
	testkit.Assert(t, got).Contains("must return the zero value",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance 3a99aea6d4a0eb57bf7484327a4228111602f8f696d065ccb3614c7cd68c4b0e
