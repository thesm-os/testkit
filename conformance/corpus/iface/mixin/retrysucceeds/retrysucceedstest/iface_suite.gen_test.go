// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/mixin/retrysucceeds/iface.go
// Plugins:   golang 1.0.0, suite 1.0.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package retrysucceedstest_test

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/iface/mixin/retrysucceeds/retrysucceedstest"
)

// Every generated check for Mixed, driven against a stand-in built
// to violate it.
//
//	Proved:   8 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertMixedCallSmokeCanFail holds AssertMixedCallSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertMixedCallSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := retrysucceedstest.DefaultMixedFixture()
	subject := retrysucceedstest.NewMixedStub(t,
		retrysucceedstest.WithMixedCall(func(context.Context, string) error {
			panic("Mixed.Call: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			retrysucceedstest.AssertMixedCallSmoke(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedCallCancelsCanFail holds AssertMixedCallCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertMixedCallCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := retrysucceedstest.DefaultMixedFixture()
	subject := retrysucceedstest.NewMixedStub(t,
		retrysucceedstest.WithMixedCall(func(context.Context, string) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			retrysucceedstest.AssertMixedCallCancels(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedCallHonoursDeadlineCanFail holds AssertMixedCallHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertMixedCallHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := retrysucceedstest.DefaultMixedFixture()
	subject := retrysucceedstest.NewMixedStub(t,
		retrysucceedstest.WithMixedCall(func(context.Context, string) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			retrysucceedstest.AssertMixedCallHonoursDeadline(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedCallToleratesNilContextCanFail holds AssertMixedCallToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertMixedCallToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := retrysucceedstest.DefaultMixedFixture()
	subject := retrysucceedstest.NewMixedStub(t,
		retrysucceedstest.WithMixedCall(func(context.Context, string) error {
			panic("Mixed.Call: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			retrysucceedstest.AssertMixedCallToleratesNilContext(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedAttemptsSmokeCanFail holds AssertMixedAttemptsSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertMixedAttemptsSmokeCanFail(t *testing.T) {
	t.Parallel()

	subject := retrysucceedstest.NewMixedStub(t,
		retrysucceedstest.WithMixedAttempts(func(context.Context) (int, error) {
			panic("Mixed.Attempts: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			retrysucceedstest.AssertMixedAttemptsSmoke(tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedAttemptsCancelsCanFail holds AssertMixedAttemptsCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertMixedAttemptsCancelsCanFail(t *testing.T) {
	t.Parallel()

	subject := retrysucceedstest.NewMixedStub(t,
		retrysucceedstest.WithMixedAttempts(func(context.Context) (int, error) {
			var r0 int
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			retrysucceedstest.AssertMixedAttemptsCancels(tb, subject)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedAttemptsHonoursDeadlineCanFail holds AssertMixedAttemptsHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertMixedAttemptsHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	subject := retrysucceedstest.NewMixedStub(t,
		retrysucceedstest.WithMixedAttempts(func(context.Context) (int, error) {
			var r0 int
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			retrysucceedstest.AssertMixedAttemptsHonoursDeadline(tb, subject)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedAttemptsToleratesNilContextCanFail holds AssertMixedAttemptsToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertMixedAttemptsToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	subject := retrysucceedstest.NewMixedStub(t,
		retrysucceedstest.WithMixedAttempts(func(context.Context) (int, error) {
			panic("Mixed.Attempts: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			retrysucceedstest.AssertMixedAttemptsToleratesNilContext(tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance 353b9c5063c7cd3c61966da3509a1fa62ad45f0148eb8d68774a346ea5224d3d
