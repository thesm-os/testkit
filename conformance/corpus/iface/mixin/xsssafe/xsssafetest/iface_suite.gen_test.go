// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/mixin/xsssafe/iface.go
// Plugins:   golang 1.0.0, suite 1.15.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package xsssafetest_test

import (
	"context"
	"errors"
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/iface/mixin/xsssafe/xsssafetest"
)

// Every generated check for Mixed, driven against a stand-in built
// to violate it.
//
//	Proved:   5 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertMixedRenderSmokeCanFail holds AssertMixedRenderSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertMixedRenderSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := xsssafetest.DefaultMixedFixture()
	subject := xsssafetest.NewMixedStub(t,
		xsssafetest.WithMixedRender(func(context.Context, string) (string, error) {
			panic("Mixed.Render: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			xsssafetest.AssertMixedRenderSmoke(tb, subject, fixture.In)
		})
	testkit.Assert(t, got).Contains("Render panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedRenderCancelsCanFail holds AssertMixedRenderCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertMixedRenderCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := xsssafetest.DefaultMixedFixture()
	subject := xsssafetest.NewMixedStub(t,
		xsssafetest.WithMixedRender(func(context.Context, string) (string, error) {
			var r0 string
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			xsssafetest.AssertMixedRenderCancels(tb, subject, fixture.In)
		})
	testkit.Assert(t, got).Contains("Render must report a cancelled context as context.Canceled",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedRenderHonoursDeadlineCanFail holds AssertMixedRenderHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertMixedRenderHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := xsssafetest.DefaultMixedFixture()
	subject := xsssafetest.NewMixedStub(t,
		xsssafetest.WithMixedRender(func(context.Context, string) (string, error) {
			var r0 string
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			xsssafetest.AssertMixedRenderHonoursDeadline(tb, subject, fixture.In)
		})
	testkit.Assert(t, got).Contains("Render must report an expired deadline as context.DeadlineExceeded",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedRenderToleratesNilContextCanFail holds AssertMixedRenderToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertMixedRenderToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := xsssafetest.DefaultMixedFixture()
	subject := xsssafetest.NewMixedStub(t,
		xsssafetest.WithMixedRender(func(context.Context, string) (string, error) {
			panic("Mixed.Render: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			xsssafetest.AssertMixedRenderToleratesNilContext(tb, subject, fixture.In)
		})
	testkit.Assert(t, got).Contains("Render panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedRenderZeroOnErrorCanFail holds AssertMixedRenderZeroOnError to being able to fail.
//
// The stand-in refuses the call and answers a value that looks like a result
// anyway. A check that let that pass would leave a caller who checks the error
// and a caller who checks the value disagreeing about what happened — the
// claim's own violation, not a stand-in that merely succeeds.
func TestAssertMixedRenderZeroOnErrorCanFail(t *testing.T) {
	t.Parallel()

	fixture := xsssafetest.DefaultMixedFixture()
	subject := xsssafetest.NewMixedStub(t,
		xsssafetest.WithMixedRender(func(context.Context, string) (string, error) {
			return "test-r0", errors.New("violate: refused with a believable value")
		}))

	got := testkit.Rejects(t, "a method answering a believable value beside its error",
		func(tb testing.TB) {
			tb.Helper()
			xsssafetest.AssertMixedRenderZeroOnError(tb, subject, fixture.InOther)
		})
	testkit.Assert(t, got).Contains("Render must return the zero value alongside an error",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance 8094dfae80e089c3f5db7366394c14af413fc6ef2356c1b35bf98bd8ee516fd1
