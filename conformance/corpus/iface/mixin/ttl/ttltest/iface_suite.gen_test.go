// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/mixin/ttl/iface.go
// Plugins:   golang 1.0.0, suite 1.1.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package ttltest_test

import (
	"context"
	"errors"
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/iface/mixin/ttl"
	"go.thesmos.sh/testkit/conformance/corpus/iface/mixin/ttl/ttltest"
)

// Every generated check for Mixed, driven against a stand-in built
// to violate it.
//
//	Proved:   9 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertMixedPutSmokeCanFail holds AssertMixedPutSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertMixedPutSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := ttltest.DefaultMixedFixture()
	subject := ttltest.NewMixedStub(t,
		ttltest.WithMixedPut(func(context.Context, ttl.Value) error {
			panic("Mixed.Put: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			ttltest.AssertMixedPutSmoke(tb, subject, fixture.V)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedPutCancelsCanFail holds AssertMixedPutCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertMixedPutCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := ttltest.DefaultMixedFixture()
	subject := ttltest.NewMixedStub(t,
		ttltest.WithMixedPut(func(context.Context, ttl.Value) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			ttltest.AssertMixedPutCancels(tb, subject, fixture.V)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedPutHonoursDeadlineCanFail holds AssertMixedPutHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertMixedPutHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := ttltest.DefaultMixedFixture()
	subject := ttltest.NewMixedStub(t,
		ttltest.WithMixedPut(func(context.Context, ttl.Value) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			ttltest.AssertMixedPutHonoursDeadline(tb, subject, fixture.V)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedPutToleratesNilContextCanFail holds AssertMixedPutToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertMixedPutToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := ttltest.DefaultMixedFixture()
	subject := ttltest.NewMixedStub(t,
		ttltest.WithMixedPut(func(context.Context, ttl.Value) error {
			panic("Mixed.Put: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			ttltest.AssertMixedPutToleratesNilContext(tb, subject, fixture.V)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedReadSmokeCanFail holds AssertMixedReadSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertMixedReadSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := ttltest.DefaultMixedFixture()
	subject := ttltest.NewMixedStub(t,
		ttltest.WithMixedRead(func(context.Context, string) (ttl.Value, error) {
			panic("Mixed.Read: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			ttltest.AssertMixedReadSmoke(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedReadCancelsCanFail holds AssertMixedReadCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertMixedReadCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := ttltest.DefaultMixedFixture()
	subject := ttltest.NewMixedStub(t,
		ttltest.WithMixedRead(func(context.Context, string) (ttl.Value, error) {
			var r0 ttl.Value
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			ttltest.AssertMixedReadCancels(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedReadHonoursDeadlineCanFail holds AssertMixedReadHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertMixedReadHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := ttltest.DefaultMixedFixture()
	subject := ttltest.NewMixedStub(t,
		ttltest.WithMixedRead(func(context.Context, string) (ttl.Value, error) {
			var r0 ttl.Value
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			ttltest.AssertMixedReadHonoursDeadline(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedReadToleratesNilContextCanFail holds AssertMixedReadToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertMixedReadToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := ttltest.DefaultMixedFixture()
	subject := ttltest.NewMixedStub(t,
		ttltest.WithMixedRead(func(context.Context, string) (ttl.Value, error) {
			panic("Mixed.Read: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			ttltest.AssertMixedReadToleratesNilContext(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedReadZeroOnErrorCanFail holds AssertMixedReadZeroOnError to being able to fail.
//
// The stand-in refuses the call and answers a value that looks like a result
// anyway. A check that let that pass would leave a caller who checks the error
// and a caller who checks the value disagreeing about what happened — the
// claim's own violation, not a stand-in that merely succeeds.
func TestAssertMixedReadZeroOnErrorCanFail(t *testing.T) {
	t.Parallel()

	fixture := ttltest.DefaultMixedFixture()
	subject := ttltest.NewMixedStub(t,
		ttltest.WithMixedRead(func(context.Context, string) (ttl.Value, error) {
			return ttl.Value{Key: "test-r0"}, errors.New("violate: refused with a believable value")
		}))

	got := testkit.Rejects(t, "a method answering a believable value beside its error",
		func(tb testing.TB) {
			tb.Helper()
			ttltest.AssertMixedReadZeroOnError(tb, subject, fixture.KeyOther)
		})
	testkit.Assert(t, got).Contains("must return the zero value",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance 4837d8a7a849e21ab67c5ef9644ae3e076ffac45ead48cda4d79472c4ee8c9cd
