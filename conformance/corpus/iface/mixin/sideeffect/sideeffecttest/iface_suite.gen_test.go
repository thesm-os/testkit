// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/mixin/sideeffect/iface.go
// Plugins:   golang 1.0.0, suite 1.0.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/iface/mixin/orderafter/... ./corpus/iface/mixin/nilsafe/... ./corpus/iface/mixin/sideeffect/... ./corpus/iface/mixin/hooks/...

package sideeffecttest_test

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/iface/mixin/sideeffect/sideeffecttest"
)

// Every generated check for Mixed, driven against a stand-in built
// to violate it.
//
//	Proved:   10 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertMixedTouchSmokeCanFail holds AssertMixedTouchSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertMixedTouchSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := sideeffecttest.DefaultMixedFixture()
	subject := sideeffecttest.NewMixedStub(t,
		sideeffecttest.WithMixedTouch(func(context.Context, string) error {
			panic("Mixed.Touch: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			sideeffecttest.AssertMixedTouchSmoke(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedTouchCancelsCanFail holds AssertMixedTouchCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertMixedTouchCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := sideeffecttest.DefaultMixedFixture()
	subject := sideeffecttest.NewMixedStub(t,
		sideeffecttest.WithMixedTouch(func(context.Context, string) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			sideeffecttest.AssertMixedTouchCancels(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedTouchHonoursDeadlineCanFail holds AssertMixedTouchHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertMixedTouchHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := sideeffecttest.DefaultMixedFixture()
	subject := sideeffecttest.NewMixedStub(t,
		sideeffecttest.WithMixedTouch(func(context.Context, string) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			sideeffecttest.AssertMixedTouchHonoursDeadline(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedTouchToleratesNilContextCanFail holds AssertMixedTouchToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertMixedTouchToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := sideeffecttest.DefaultMixedFixture()
	subject := sideeffecttest.NewMixedStub(t,
		sideeffecttest.WithMixedTouch(func(context.Context, string) error {
			panic("Mixed.Touch: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			sideeffecttest.AssertMixedTouchToleratesNilContext(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedTouchHasAnObservableEffectCanFail holds AssertMixedTouchHasAnObservableEffect to being able to fail.
//
// The stand-in's observer answers the same thing whichever side of the call it
// is asked on. A check that let that pass would report an effect for a method
// that accepted the call and did nothing.
func TestAssertMixedTouchHasAnObservableEffectCanFail(t *testing.T) {
	t.Parallel()

	fixture := sideeffecttest.DefaultMixedFixture()
	subject := sideeffecttest.NewMixedStub(t,
		sideeffecttest.WithMixedTouch(func(context.Context, string) error {
			return nil
		}),
		sideeffecttest.WithMixedObserved(func(context.Context, string) (int, error) {
			var r0 int
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method whose effect nothing can observe",
		func(tb testing.TB) {
			tb.Helper()
			sideeffecttest.AssertMixedTouchHasAnObservableEffect(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("must change what",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedObservedSmokeCanFail holds AssertMixedObservedSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertMixedObservedSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := sideeffecttest.DefaultMixedFixture()
	subject := sideeffecttest.NewMixedStub(t,
		sideeffecttest.WithMixedObserved(func(context.Context, string) (int, error) {
			panic("Mixed.Observed: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			sideeffecttest.AssertMixedObservedSmoke(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedObservedCancelsCanFail holds AssertMixedObservedCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertMixedObservedCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := sideeffecttest.DefaultMixedFixture()
	subject := sideeffecttest.NewMixedStub(t,
		sideeffecttest.WithMixedObserved(func(context.Context, string) (int, error) {
			var r0 int
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			sideeffecttest.AssertMixedObservedCancels(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedObservedHonoursDeadlineCanFail holds AssertMixedObservedHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertMixedObservedHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := sideeffecttest.DefaultMixedFixture()
	subject := sideeffecttest.NewMixedStub(t,
		sideeffecttest.WithMixedObserved(func(context.Context, string) (int, error) {
			var r0 int
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			sideeffecttest.AssertMixedObservedHonoursDeadline(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedObservedToleratesNilContextCanFail holds AssertMixedObservedToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertMixedObservedToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := sideeffecttest.DefaultMixedFixture()
	subject := sideeffecttest.NewMixedStub(t,
		sideeffecttest.WithMixedObserved(func(context.Context, string) (int, error) {
			panic("Mixed.Observed: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			sideeffecttest.AssertMixedObservedToleratesNilContext(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedObservedZeroOnErrorCanFail holds AssertMixedObservedZeroOnError to being able to fail.
//
// The stand-in succeeds for the input the check chose so it would miss. A check
// that let that pass would never reach the comparison it exists to make, and
// would report success without having compared anything.
func TestAssertMixedObservedZeroOnErrorCanFail(t *testing.T) {
	t.Parallel()

	fixture := sideeffecttest.DefaultMixedFixture()
	subject := sideeffecttest.NewMixedStub(t,
		sideeffecttest.WithMixedObserved(func(context.Context, string) (int, error) {
			var r0 int
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that succeeds for an input it should miss",
		func(tb testing.TB) {
			tb.Helper()
			sideeffecttest.AssertMixedObservedZeroOnError(tb, subject, fixture.KeyOther)
		})
	testkit.Assert(t, got).Contains("supply inputs it misses",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance 5be6981ee1e5346f71b17ffd44c82bbbf87ac2ef9591216c27a5426b55540bfc
