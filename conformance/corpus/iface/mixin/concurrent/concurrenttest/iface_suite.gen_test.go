// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/mixin/concurrent/iface.go
// Plugins:   golang 1.0.0, suite 1.1.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package concurrenttest_test

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/iface/mixin/concurrent/concurrenttest"
)

// Every generated check for Mixed, driven against a stand-in built
// to violate it.
//
//	Proved:   9 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertMixedBumpSmokeCanFail holds AssertMixedBumpSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertMixedBumpSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := concurrenttest.DefaultMixedFixture()
	subject := concurrenttest.NewMixedStub(t,
		concurrenttest.WithMixedBump(func(context.Context, string) error {
			panic("Mixed.Bump: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			concurrenttest.AssertMixedBumpSmoke(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedBumpCancelsCanFail holds AssertMixedBumpCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertMixedBumpCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := concurrenttest.DefaultMixedFixture()
	subject := concurrenttest.NewMixedStub(t,
		concurrenttest.WithMixedBump(func(context.Context, string) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			concurrenttest.AssertMixedBumpCancels(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedBumpHonoursDeadlineCanFail holds AssertMixedBumpHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertMixedBumpHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := concurrenttest.DefaultMixedFixture()
	subject := concurrenttest.NewMixedStub(t,
		concurrenttest.WithMixedBump(func(context.Context, string) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			concurrenttest.AssertMixedBumpHonoursDeadline(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedBumpToleratesNilContextCanFail holds AssertMixedBumpToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertMixedBumpToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := concurrenttest.DefaultMixedFixture()
	subject := concurrenttest.NewMixedStub(t,
		concurrenttest.WithMixedBump(func(context.Context, string) error {
			panic("Mixed.Bump: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			concurrenttest.AssertMixedBumpToleratesNilContext(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedBumpSurvivesConcurrentCallersCanFail holds AssertMixedBumpSurvivesConcurrentCallers to being able to fail.
//
// The stand-in panics under any caller, so every one of the check's
// goroutines meets the recover arm this proves.
func TestAssertMixedBumpSurvivesConcurrentCallersCanFail(t *testing.T) {
	t.Parallel()

	fixture := concurrenttest.DefaultMixedFixture()
	subject := concurrenttest.NewMixedStub(t,
		concurrenttest.WithMixedBump(func(context.Context, string) error {
			panic("violate: unsynchronized state")
		}))

	got := testkit.Rejects(t, "a method that panics the moment callers overlap",
		func(tb testing.TB) {
			tb.Helper()
			concurrenttest.AssertMixedBumpSurvivesConcurrentCallers(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("panicked under concurrent callers",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedCountSmokeCanFail holds AssertMixedCountSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertMixedCountSmokeCanFail(t *testing.T) {
	t.Parallel()

	subject := concurrenttest.NewMixedStub(t,
		concurrenttest.WithMixedCount(func(context.Context) (int, error) {
			panic("Mixed.Count: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			concurrenttest.AssertMixedCountSmoke(tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedCountCancelsCanFail holds AssertMixedCountCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertMixedCountCancelsCanFail(t *testing.T) {
	t.Parallel()

	subject := concurrenttest.NewMixedStub(t,
		concurrenttest.WithMixedCount(func(context.Context) (int, error) {
			var r0 int
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			concurrenttest.AssertMixedCountCancels(tb, subject)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedCountHonoursDeadlineCanFail holds AssertMixedCountHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertMixedCountHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	subject := concurrenttest.NewMixedStub(t,
		concurrenttest.WithMixedCount(func(context.Context) (int, error) {
			var r0 int
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			concurrenttest.AssertMixedCountHonoursDeadline(tb, subject)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedCountToleratesNilContextCanFail holds AssertMixedCountToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertMixedCountToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	subject := concurrenttest.NewMixedStub(t,
		concurrenttest.WithMixedCount(func(context.Context) (int, error) {
			panic("Mixed.Count: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			concurrenttest.AssertMixedCountToleratesNilContext(tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance 092cdf9725a0e022160315f9ced716fe3f412671d5bdfe70afe7d50789d36f3f
