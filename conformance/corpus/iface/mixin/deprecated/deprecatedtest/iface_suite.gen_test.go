// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/mixin/deprecated/iface.go
// Plugins:   golang 1.0.0, suite 1.1.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package deprecatedtest_test

import (
	"context"
	"errors"
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/iface/mixin/deprecated/deprecatedtest"
)

// Every generated check for Mixed, driven against a stand-in built
// to violate it.
//
//	Proved:   10 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertMixedOldSmokeCanFail holds AssertMixedOldSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertMixedOldSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := deprecatedtest.DefaultMixedFixture()
	subject := deprecatedtest.NewMixedStub(t,
		deprecatedtest.WithMixedOld(func(context.Context, string) (string, error) {
			panic("Mixed.Old: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			deprecatedtest.AssertMixedOldSmoke(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedOldCancelsCanFail holds AssertMixedOldCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertMixedOldCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := deprecatedtest.DefaultMixedFixture()
	subject := deprecatedtest.NewMixedStub(t,
		deprecatedtest.WithMixedOld(func(context.Context, string) (string, error) {
			var r0 string
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			deprecatedtest.AssertMixedOldCancels(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedOldHonoursDeadlineCanFail holds AssertMixedOldHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertMixedOldHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := deprecatedtest.DefaultMixedFixture()
	subject := deprecatedtest.NewMixedStub(t,
		deprecatedtest.WithMixedOld(func(context.Context, string) (string, error) {
			var r0 string
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			deprecatedtest.AssertMixedOldHonoursDeadline(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedOldToleratesNilContextCanFail holds AssertMixedOldToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertMixedOldToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := deprecatedtest.DefaultMixedFixture()
	subject := deprecatedtest.NewMixedStub(t,
		deprecatedtest.WithMixedOld(func(context.Context, string) (string, error) {
			panic("Mixed.Old: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			deprecatedtest.AssertMixedOldToleratesNilContext(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedOldZeroOnErrorCanFail holds AssertMixedOldZeroOnError to being able to fail.
//
// The stand-in refuses the call and answers a value that looks like a result
// anyway. A check that let that pass would leave a caller who checks the error
// and a caller who checks the value disagreeing about what happened — the
// claim's own violation, not a stand-in that merely succeeds.
func TestAssertMixedOldZeroOnErrorCanFail(t *testing.T) {
	t.Parallel()

	fixture := deprecatedtest.DefaultMixedFixture()
	subject := deprecatedtest.NewMixedStub(t,
		deprecatedtest.WithMixedOld(func(context.Context, string) (string, error) {
			return "test-r0", errors.New("violate: refused with a believable value")
		}))

	got := testkit.Rejects(t, "a method answering a believable value beside its error",
		func(tb testing.TB) {
			tb.Helper()
			deprecatedtest.AssertMixedOldZeroOnError(tb, subject, fixture.KeyOther)
		})
	testkit.Assert(t, got).Contains("must return the zero value",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedNewSmokeCanFail holds AssertMixedNewSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertMixedNewSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := deprecatedtest.DefaultMixedFixture()
	subject := deprecatedtest.NewMixedStub(t,
		deprecatedtest.WithMixedNew(func(context.Context, string) (string, error) {
			panic("Mixed.New: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			deprecatedtest.AssertMixedNewSmoke(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedNewCancelsCanFail holds AssertMixedNewCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertMixedNewCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := deprecatedtest.DefaultMixedFixture()
	subject := deprecatedtest.NewMixedStub(t,
		deprecatedtest.WithMixedNew(func(context.Context, string) (string, error) {
			var r0 string
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			deprecatedtest.AssertMixedNewCancels(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedNewHonoursDeadlineCanFail holds AssertMixedNewHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertMixedNewHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := deprecatedtest.DefaultMixedFixture()
	subject := deprecatedtest.NewMixedStub(t,
		deprecatedtest.WithMixedNew(func(context.Context, string) (string, error) {
			var r0 string
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			deprecatedtest.AssertMixedNewHonoursDeadline(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedNewToleratesNilContextCanFail holds AssertMixedNewToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertMixedNewToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := deprecatedtest.DefaultMixedFixture()
	subject := deprecatedtest.NewMixedStub(t,
		deprecatedtest.WithMixedNew(func(context.Context, string) (string, error) {
			panic("Mixed.New: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			deprecatedtest.AssertMixedNewToleratesNilContext(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedNewZeroOnErrorCanFail holds AssertMixedNewZeroOnError to being able to fail.
//
// The stand-in refuses the call and answers a value that looks like a result
// anyway. A check that let that pass would leave a caller who checks the error
// and a caller who checks the value disagreeing about what happened — the
// claim's own violation, not a stand-in that merely succeeds.
func TestAssertMixedNewZeroOnErrorCanFail(t *testing.T) {
	t.Parallel()

	fixture := deprecatedtest.DefaultMixedFixture()
	subject := deprecatedtest.NewMixedStub(t,
		deprecatedtest.WithMixedNew(func(context.Context, string) (string, error) {
			return "test-r0", errors.New("violate: refused with a believable value")
		}))

	got := testkit.Rejects(t, "a method answering a believable value beside its error",
		func(tb testing.TB) {
			tb.Helper()
			deprecatedtest.AssertMixedNewZeroOnError(tb, subject, fixture.KeyOther)
		})
	testkit.Assert(t, got).Contains("must return the zero value",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance 9bae3da92b25bb8bb92945bc24d9a0887fc15192e3d7578221b5f87a52aa4b95
