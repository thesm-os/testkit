// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/mixin/deprecated/iface.go
// Plugins:   golang 1.0.0, suite 1.0.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package deprecatedtest

import (
	"context"
	"testing"
	"time"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/conformance/corpus/iface/mixin/deprecated"
)

// MixedOldCheck is one assertion about Old.
//
// Every generated check for Old is a value of it, and so is one you
// write — so they compose, reorder, and each runs standalone. testing.TB rather
// than *testing.T is what lets a stand-in drive it and prove it can fail.
type MixedOldCheck func(tb testing.TB, subject deprecated.Mixed, key string)

// MixedNewCheck is one assertion about New.
//
// Every generated check for New is a value of it, and so is one you
// write — so they compose, reorder, and each runs standalone. testing.TB rather
// than *testing.T is what lets a stand-in drive it and prove it can fail.
type MixedNewCheck func(tb testing.TB, subject deprecated.Mixed, key string)

// MixedFixture holds every input the generated checks run against.
//
// Derived from each method's parameter names and types. Every field has a
// companion holding a second, different value: a check comparing a result
// against a single input passes whenever the subject happened to be seeded with
// it, and a miss check whose key happens to hit asserts nothing.
//
// A field whose type admits no literal — a func, a channel, a type from a
// package this run never read — is declared and left at its zero value, and
// every generated check that needed it was dropped rather than emitted against
// a value nobody could write. Supply one through MixedWithFixture
// and the check a consumer writes has something to use.
type MixedFixture struct {
	Key      string
	KeyOther string
}

// DefaultMixedFixture is what this run derived. Replace any field through
// MixedWithFixture; the rest keep these values.
func DefaultMixedFixture() MixedFixture {
	return MixedFixture{
		Key:      "test-key",
		KeyOther: "other-key",
	}
}

// AssertMixedContract runs every generated check against every declared subject.
//
//	Checks:   10 across 2 methods, per subject
//	Subjects: declare each with MixedSubject
//	Double:   every subject runs a second time wrapped in MixedStub, so
//	          anything the wrapper fails that the subject passes is the double
//	          lying. MixedWithoutDouble declines it.
//	Extend:   MixedOnOld, MixedOnNew
//	Drop:     MixedWithout, by the path each check reports under
//
// # What this file does not check
//
// The interface declares these and nothing here asserts them. Each is statable
// against a subject you can build, so the check is yours to write:
//
//   - deprecated, on Old — write it with MixedOnOld
func AssertMixedContract(t *testing.T, opts ...MixedOption) {
	t.Helper()
	cfg := newMixedConfig(opts...)
	if len(cfg.subjects) == 0 {
		t.Fatalf("AssertMixedContract was given no subject; declare one with " +
			"MixedSubject(name, factory)")
	}
	for _, s := range cfg.subjects {
		runMixedChecks(t, cfg, s.name, s.factory, nil)
		if !cfg.withoutDouble {
			runMixedChecks(t, cfg, s.name+" through the double", s.factory,
				func(tb testing.TB, subject deprecated.Mixed) deprecated.Mixed {
					return NewMixedStub(tb, MixedStubDelegateTo(subject))
				})
		}
	}
}

// runMixedChecks runs the whole contract against one subject.
//
// wrap is applied after the subject is built and seeded, so a wrapper is
// transparent to the seed and every failure it causes is its own.
func runMixedChecks(
	t *testing.T,
	cfg *mixedConfig,
	label string,
	factory func() deprecated.Mixed,
	wrap func(tb testing.TB, subject deprecated.Mixed) deprecated.Mixed,
) {
	t.Helper()
	t.Run(label, func(t *testing.T) {
		t.Parallel()

		t.Run("Old", func(t *testing.T) {
			t.Parallel()
			cfg.run(t, "Old/smoke", "smoke", func(tb testing.TB) {
				AssertMixedOldSmoke(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.Key)
			})
			cfg.run(t, "Old/reports a cancelled context", "reports a cancelled context", func(tb testing.TB) {
				AssertMixedOldCancels(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.Key)
			})
			cfg.run(t, "Old/reports an expired deadline", "reports an expired deadline", func(tb testing.TB) {
				AssertMixedOldHonoursDeadline(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.Key)
			})
			cfg.run(t, "Old/tolerates a nil context", "tolerates a nil context", func(tb testing.TB) {
				AssertMixedOldToleratesNilContext(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.Key)
			})
			cfg.run(t, "Old/an error carries the zero value", "an error carries the zero value", func(tb testing.TB) {
				AssertMixedOldZeroOnError(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.KeyOther)
			})
			for _, c := range cfg.onOld {
				cfg.run(t, "Old"+"/"+c.name, c.name, func(tb testing.TB) {
					c.fn(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.Key)
				})
			}
		})

		t.Run("New", func(t *testing.T) {
			t.Parallel()
			cfg.run(t, "New/smoke", "smoke", func(tb testing.TB) {
				AssertMixedNewSmoke(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.Key)
			})
			cfg.run(t, "New/reports a cancelled context", "reports a cancelled context", func(tb testing.TB) {
				AssertMixedNewCancels(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.Key)
			})
			cfg.run(t, "New/reports an expired deadline", "reports an expired deadline", func(tb testing.TB) {
				AssertMixedNewHonoursDeadline(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.Key)
			})
			cfg.run(t, "New/tolerates a nil context", "tolerates a nil context", func(tb testing.TB) {
				AssertMixedNewToleratesNilContext(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.Key)
			})
			cfg.run(t, "New/an error carries the zero value", "an error carries the zero value", func(tb testing.TB) {
				AssertMixedNewZeroOnError(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.KeyOther)
			})
			for _, c := range cfg.onNew {
				cfg.run(t, "New"+"/"+c.name, c.name, func(tb testing.TB) {
					c.fn(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.Key)
				})
			}
		})
	})
}

// AssertMixedOldSmoke asserts Old survives a call with derived inputs.
//
// Fails when: Old panics. The weakest check in this file and the one
// that catches the most — a method that panics on a derived value is one no
// other check here reaches.
func AssertMixedOldSmoke(tb testing.TB, subject deprecated.Mixed, key string) {
	tb.Helper()
	defer func() {
		if r := recover(); r != nil {
			tb.Fatalf("Old panicked on a derived value (%v); supply one it "+
				"accepts through MixedWithFixture", r)
		}
	}()
	ctx := tb.Context()
	_, _ = subject.Old(ctx, key)
}

// AssertMixedOldCancels asserts Old reports a cancelled context as cancelled.
//
// Fails when: Old returns nil for a context cancelled before the
// call, or returns an error that does not answer to context.Canceled. Both
// matter and they are different defects: the first is work done that the caller
// asked not to be, the second is a caller who wrote
// `errors.Is(err, context.Canceled)` and gets false for a call that was.
//
// The error rather than merely its presence is what separates this from the
// deadline check. Asserting only that something came back makes the two one
// check written twice.
func AssertMixedOldCancels(tb testing.TB, subject deprecated.Mixed, key string) {
	tb.Helper()
	ctx, cancel := context.WithCancel(tb.Context())
	cancel()
	_, err := subject.Old(ctx, key)
	testkit.ErrorIs(tb, err, context.Canceled,
		"Old must report a cancelled context as context.Canceled")
}

// AssertMixedOldHonoursDeadline asserts Old reports an expired deadline as exceeded.
//
// Fails when: Old returns nil for a context whose deadline has
// passed, or returns an error that does not answer to
// context.DeadlineExceeded. Distinct from cancellation in what the caller
// learns: a cancelled call was called off, an expired one ran out of time, and
// only the second is worth retrying.
//
// The deadline is the zero time, which is unconditionally in the past. No clock
// is read: a generated check that consults the wall clock is one whose subject
// is partly the machine it runs on.
func AssertMixedOldHonoursDeadline(tb testing.TB, subject deprecated.Mixed, key string) {
	tb.Helper()
	ctx, cancel := context.WithDeadline(tb.Context(), time.Time{})
	defer cancel()
	_, err := subject.Old(ctx, key)
	testkit.ErrorIs(tb, err, context.DeadlineExceeded,
		"Old must report an expired deadline as context.DeadlineExceeded")
}

// AssertMixedOldToleratesNilContext asserts Old does not panic on a nil context.
//
// Fails when: Old panics. Returning an error is correct and
// succeeding is correct — a nil context reaches production through a caller
// that forgot one, and a panic turns that into an outage rather than a failed
// request.
func AssertMixedOldToleratesNilContext(tb testing.TB, subject deprecated.Mixed, key string) {
	tb.Helper()
	defer func() {
		if r := recover(); r != nil {
			tb.Errorf("Old panicked on a nil context (%v); return an error instead", r)
		}
	}()
	//nolint:staticcheck // passing nil is the check.
	var ctx context.Context
	_, _ = subject.Old(ctx, key)
}

// AssertMixedOldZeroOnError asserts an error is accompanied by the zero value.
//
// Fails when: Old returns a non-zero result alongside a non-nil
// error. A caller who checks the error and a caller who checks the value must
// not disagree about whether the call succeeded.
func AssertMixedOldZeroOnError(tb testing.TB, subject deprecated.Mixed, key string) {
	tb.Helper()
	ctx := tb.Context()
	r0, r1 := subject.Old(ctx, key)
	if r1 == nil {
		tb.Fatalf("Old succeeded; supply inputs it misses through " +
			"MixedWithFixture")
	}
	{
		var zero string
		testkit.Equal(tb, r0, zero,
			"Old must return the zero value alongside an error")
	}
}

// AssertMixedNewSmoke asserts New survives a call with derived inputs.
//
// Fails when: New panics. The weakest check in this file and the one
// that catches the most — a method that panics on a derived value is one no
// other check here reaches.
func AssertMixedNewSmoke(tb testing.TB, subject deprecated.Mixed, key string) {
	tb.Helper()
	defer func() {
		if r := recover(); r != nil {
			tb.Fatalf("New panicked on a derived value (%v); supply one it "+
				"accepts through MixedWithFixture", r)
		}
	}()
	ctx := tb.Context()
	_, _ = subject.New(ctx, key)
}

// AssertMixedNewCancels asserts New reports a cancelled context as cancelled.
//
// Fails when: New returns nil for a context cancelled before the
// call, or returns an error that does not answer to context.Canceled. Both
// matter and they are different defects: the first is work done that the caller
// asked not to be, the second is a caller who wrote
// `errors.Is(err, context.Canceled)` and gets false for a call that was.
//
// The error rather than merely its presence is what separates this from the
// deadline check. Asserting only that something came back makes the two one
// check written twice.
func AssertMixedNewCancels(tb testing.TB, subject deprecated.Mixed, key string) {
	tb.Helper()
	ctx, cancel := context.WithCancel(tb.Context())
	cancel()
	_, err := subject.New(ctx, key)
	testkit.ErrorIs(tb, err, context.Canceled,
		"New must report a cancelled context as context.Canceled")
}

// AssertMixedNewHonoursDeadline asserts New reports an expired deadline as exceeded.
//
// Fails when: New returns nil for a context whose deadline has
// passed, or returns an error that does not answer to
// context.DeadlineExceeded. Distinct from cancellation in what the caller
// learns: a cancelled call was called off, an expired one ran out of time, and
// only the second is worth retrying.
//
// The deadline is the zero time, which is unconditionally in the past. No clock
// is read: a generated check that consults the wall clock is one whose subject
// is partly the machine it runs on.
func AssertMixedNewHonoursDeadline(tb testing.TB, subject deprecated.Mixed, key string) {
	tb.Helper()
	ctx, cancel := context.WithDeadline(tb.Context(), time.Time{})
	defer cancel()
	_, err := subject.New(ctx, key)
	testkit.ErrorIs(tb, err, context.DeadlineExceeded,
		"New must report an expired deadline as context.DeadlineExceeded")
}

// AssertMixedNewToleratesNilContext asserts New does not panic on a nil context.
//
// Fails when: New panics. Returning an error is correct and
// succeeding is correct — a nil context reaches production through a caller
// that forgot one, and a panic turns that into an outage rather than a failed
// request.
func AssertMixedNewToleratesNilContext(tb testing.TB, subject deprecated.Mixed, key string) {
	tb.Helper()
	defer func() {
		if r := recover(); r != nil {
			tb.Errorf("New panicked on a nil context (%v); return an error instead", r)
		}
	}()
	//nolint:staticcheck // passing nil is the check.
	var ctx context.Context
	_, _ = subject.New(ctx, key)
}

// AssertMixedNewZeroOnError asserts an error is accompanied by the zero value.
//
// Fails when: New returns a non-zero result alongside a non-nil
// error. A caller who checks the error and a caller who checks the value must
// not disagree about whether the call succeeded.
func AssertMixedNewZeroOnError(tb testing.TB, subject deprecated.Mixed, key string) {
	tb.Helper()
	ctx := tb.Context()
	r0, r1 := subject.New(ctx, key)
	if r1 == nil {
		tb.Fatalf("New succeeded; supply inputs it misses through " +
			"MixedWithFixture")
	}
	{
		var zero string
		testkit.Equal(tb, r0, zero,
			"New must return the zero value alongside an error")
	}
}

// MixedOption configures a contract run.
type MixedOption func(*mixedConfig)

// MixedSubject declares an implementation to hold to the contract.
//
// Repeat it for every implementation that claims to satisfy the interface — an
// in-memory one, the one backed by a database, the one behind a network. Every
// check and every option is stated once and runs against all of them, which is
// what a conformance suite is for.
func MixedSubject(name string, factory func() deprecated.Mixed) MixedOption {
	return func(c *mixedConfig) {
		c.subjects = append(c.subjects, namedMixedSubject{name, factory})
	}
}

// MixedWithoutDouble declines the second run through MixedStub.
//
// The double run proves the stand-in is faithful, and costs a second pass over
// every check. Decline it where the double is not used.
func MixedWithoutDouble() MixedOption {
	return func(c *mixedConfig) { c.withoutDouble = true }
}

// MixedWithClock supplies the clock every time-reading check measures on.
//
// Defaults to the real one, so an implementation that does not take a clock
// behaves as it would have. Supply a [clock.TestClock] — the same one the
// factory builds the subject with — and a budget becomes a claim about the time
// the implementation means to spend rather than about how loaded the machine
// was.
func MixedWithClock(c clock.Clock) MixedOption {
	return func(cfg *mixedConfig) { cfg.clock = c }
}

// MixedWithFixture replaces the derived inputs.
func MixedWithFixture(f MixedFixture) MixedOption {
	return func(c *mixedConfig) { c.Fixture = f }
}

// MixedSeed replaces the derived seed.
//
// For an interface with no writer to seed itself through, which is the one
// input a conformance run cannot derive: a reader over an empty subject
// asserts nothing.
//
// Returning an error rather than swallowing one. A seed that failed quietly
// leaves every check after it asserting nothing and reporting success.
func MixedSeed(fn func(ctx context.Context, subject deprecated.Mixed) error) MixedOption {
	return func(c *mixedConfig) {
		c.seed = fn
	}
}

// MixedOnOld adds a named check to Old.
func MixedOnOld(name string, fn MixedOldCheck) MixedOption {
	return func(c *mixedConfig) {
		c.onOld = append(c.onOld, namedMixedOldCheck{name, fn})
	}
}

// MixedOnNew adds a named check to New.
func MixedOnNew(name string, fn MixedNewCheck) MixedOption {
	return func(c *mixedConfig) {
		c.onNew = append(c.onNew, namedMixedNewCheck{name, fn})
	}
}

// MixedWithout drops generated checks by the path each reports under.
//
// For a subject that legitimately violates one. Without this the only recourse
// is to stop running the suite, which costs every other check in the file — so
// a dropped check is reported as skipped rather than silently omitted.
func MixedWithout(paths ...string) MixedOption {
	return func(c *mixedConfig) {
		for _, p := range paths {
			c.without[p] = struct{}{}
		}
	}
}

type namedMixedOldCheck struct {
	name string
	fn   MixedOldCheck
}

type namedMixedNewCheck struct {
	name string
	fn   MixedNewCheck
}

type namedMixedSubject struct {
	name    string
	factory func() deprecated.Mixed
}

type mixedConfig struct {
	Fixture       MixedFixture
	subjects      []namedMixedSubject
	withoutDouble bool
	clock         clock.Clock
	seed          func(ctx context.Context, subject deprecated.Mixed) error
	without       map[string]struct{}
	onOld         []namedMixedOldCheck
	onNew         []namedMixedNewCheck
}

func newMixedConfig(opts ...MixedOption) *mixedConfig {
	c := &mixedConfig{
		Fixture: DefaultMixedFixture(),
		clock:   clock.RealClock(),
		without: map[string]struct{}{},
	}
	for _, o := range opts {
		o(c)
	}
	return c
}

// subject builds a fresh implementation and seeds it, so every check runs
// against state no other check touched.
func (c *mixedConfig) subject(
	tb testing.TB,
	factory func() deprecated.Mixed,
	wrap func(tb testing.TB, subject deprecated.Mixed) deprecated.Mixed,
) deprecated.Mixed {
	tb.Helper()
	s := factory()
	if c.seed != nil {
		// Reported rather than ignored. A seed that silently failed would leave
		// every check after it running against an empty subject — passing, and
		// asserting nothing.
		//
		// Which seed ran decides what the message can usefully say. The derived
		// one fails for one of two reasons and both are fixable from here, so it
		// names them. A seed the caller supplied is theirs, and telling them to
		// supply one is advice they already took.
		if err := c.seed(tb.Context(), s); err != nil {
			tb.Fatalf("the seed supplied through MixedSeed failed: %v", err)
		}
	}
	if wrap != nil {
		s = wrap(tb, s)
	}
	return s
}

// run executes one check unless it was dropped, reporting the drop rather than
// silently omitting it.
func (c *mixedConfig) run(t *testing.T, path, name string, fn func(tb testing.TB)) {
	t.Helper()
	if _, dropped := c.without[path]; dropped {
		t.Run(name, func(t *testing.T) {
			t.Skipf("dropped through MixedWithout(%q)", path)
		})
		return
	}
	t.Run(name, func(t *testing.T) {
		t.Parallel()
		fn(t)
	})
}

// testkit: end of generated content.
// testkit:provenance 0be5752deb72751c837e33d44e277d7efa6ca4182faecc373ef92f1cda49c76f
