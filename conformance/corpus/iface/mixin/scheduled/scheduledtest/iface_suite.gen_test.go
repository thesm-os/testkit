// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/mixin/scheduled/iface.go
// Plugins:   golang 1.0.0, suite 1.15.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package scheduledtest_test

import (
	"context"
	"testing"
	"time"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/iface/mixin/scheduled/scheduledtest"
)

// Every generated check for Mixed, driven against a stand-in built
// to violate it.
//
//	Proved:   8 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertMixedAtSmokeCanFail holds AssertMixedAtSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertMixedAtSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := scheduledtest.DefaultMixedFixture()
	subject := scheduledtest.NewMixedStub(t,
		scheduledtest.WithMixedAt(func(context.Context, time.Duration) error {
			panic("Mixed.At: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			scheduledtest.AssertMixedAtSmoke(tb, subject, fixture.After)
		})
	testkit.Assert(t, got).Contains("At panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedAtCancelsCanFail holds AssertMixedAtCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertMixedAtCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := scheduledtest.DefaultMixedFixture()
	subject := scheduledtest.NewMixedStub(t,
		scheduledtest.WithMixedAt(func(context.Context, time.Duration) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			scheduledtest.AssertMixedAtCancels(tb, subject, fixture.After)
		})
	testkit.Assert(t, got).Contains("At must report a cancelled context as context.Canceled",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedAtHonoursDeadlineCanFail holds AssertMixedAtHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertMixedAtHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := scheduledtest.DefaultMixedFixture()
	subject := scheduledtest.NewMixedStub(t,
		scheduledtest.WithMixedAt(func(context.Context, time.Duration) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			scheduledtest.AssertMixedAtHonoursDeadline(tb, subject, fixture.After)
		})
	testkit.Assert(t, got).Contains("At must report an expired deadline as context.DeadlineExceeded",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedAtToleratesNilContextCanFail holds AssertMixedAtToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertMixedAtToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := scheduledtest.DefaultMixedFixture()
	subject := scheduledtest.NewMixedStub(t,
		scheduledtest.WithMixedAt(func(context.Context, time.Duration) error {
			panic("Mixed.At: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			scheduledtest.AssertMixedAtToleratesNilContext(tb, subject, fixture.After)
		})
	testkit.Assert(t, got).Contains("At panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedFiredSmokeCanFail holds AssertMixedFiredSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertMixedFiredSmokeCanFail(t *testing.T) {
	t.Parallel()

	subject := scheduledtest.NewMixedStub(t,
		scheduledtest.WithMixedFired(func(context.Context) (int, error) {
			panic("Mixed.Fired: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			scheduledtest.AssertMixedFiredSmoke(tb, subject)
		})
	testkit.Assert(t, got).Contains("Fired panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedFiredCancelsCanFail holds AssertMixedFiredCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertMixedFiredCancelsCanFail(t *testing.T) {
	t.Parallel()

	subject := scheduledtest.NewMixedStub(t,
		scheduledtest.WithMixedFired(func(context.Context) (int, error) {
			var r0 int
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			scheduledtest.AssertMixedFiredCancels(tb, subject)
		})
	testkit.Assert(t, got).Contains("Fired must report a cancelled context as context.Canceled",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedFiredHonoursDeadlineCanFail holds AssertMixedFiredHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertMixedFiredHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	subject := scheduledtest.NewMixedStub(t,
		scheduledtest.WithMixedFired(func(context.Context) (int, error) {
			var r0 int
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			scheduledtest.AssertMixedFiredHonoursDeadline(tb, subject)
		})
	testkit.Assert(t, got).Contains("Fired must report an expired deadline as context.DeadlineExceeded",
		"and rejects it for the reason the check is about")
}

// TestAssertMixedFiredToleratesNilContextCanFail holds AssertMixedFiredToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertMixedFiredToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	subject := scheduledtest.NewMixedStub(t,
		scheduledtest.WithMixedFired(func(context.Context) (int, error) {
			panic("Mixed.Fired: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			scheduledtest.AssertMixedFiredToleratesNilContext(tb, subject)
		})
	testkit.Assert(t, got).Contains("Fired panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance 98c1e8b65099348f0bf38ae17d7c24f6b6bbf93a8d350295c4a02be8d0793aac
