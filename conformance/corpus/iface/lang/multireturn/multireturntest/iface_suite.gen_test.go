// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/lang/multireturn/iface.go
// Plugins:   golang 1.0.0, suite 1.0.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package multireturntest_test

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/iface/lang/multireturn/multireturntest"
)

// Every generated check for Wide, driven against a stand-in built
// to violate it.
//
//	Proved:   10 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertWideQuadSmokeCanFail holds AssertWideQuadSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertWideQuadSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := multireturntest.DefaultWideFixture()
	subject := multireturntest.NewWideStub(t,
		multireturntest.WithWideQuad(func(context.Context, string) (string, int, bool, error) {
			panic("Wide.Quad: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			multireturntest.AssertWideQuadSmoke(tb, subject, fixture.ID)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertWideQuadCancelsCanFail holds AssertWideQuadCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertWideQuadCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := multireturntest.DefaultWideFixture()
	subject := multireturntest.NewWideStub(t,
		multireturntest.WithWideQuad(func(context.Context, string) (string, int, bool, error) {
			var r0 string
			var r1 int
			var r2 bool
			return r0, r1, r2, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			multireturntest.AssertWideQuadCancels(tb, subject, fixture.ID)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertWideQuadHonoursDeadlineCanFail holds AssertWideQuadHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertWideQuadHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := multireturntest.DefaultWideFixture()
	subject := multireturntest.NewWideStub(t,
		multireturntest.WithWideQuad(func(context.Context, string) (string, int, bool, error) {
			var r0 string
			var r1 int
			var r2 bool
			return r0, r1, r2, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			multireturntest.AssertWideQuadHonoursDeadline(tb, subject, fixture.ID)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertWideQuadToleratesNilContextCanFail holds AssertWideQuadToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertWideQuadToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := multireturntest.DefaultWideFixture()
	subject := multireturntest.NewWideStub(t,
		multireturntest.WithWideQuad(func(context.Context, string) (string, int, bool, error) {
			panic("Wide.Quad: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			multireturntest.AssertWideQuadToleratesNilContext(tb, subject, fixture.ID)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertWideQuadZeroOnErrorCanFail holds AssertWideQuadZeroOnError to being able to fail.
//
// The stand-in succeeds for the input the check chose so it would miss. A check
// that let that pass would never reach the comparison it exists to make, and
// would report success without having compared anything.
func TestAssertWideQuadZeroOnErrorCanFail(t *testing.T) {
	t.Parallel()

	fixture := multireturntest.DefaultWideFixture()
	subject := multireturntest.NewWideStub(t,
		multireturntest.WithWideQuad(func(context.Context, string) (string, int, bool, error) {
			var r0 string
			var r1 int
			var r2 bool
			return r0, r1, r2, nil
		}))

	got := testkit.Rejects(t, "a method that succeeds for an input it should miss",
		func(tb testing.TB) {
			tb.Helper()
			multireturntest.AssertWideQuadZeroOnError(tb, subject, fixture.IDOther)
		})
	testkit.Assert(t, got).Contains("supply inputs it misses",
		"and rejects it for the reason the check is about")
}

// TestAssertWideTripleSmokeCanFail holds AssertWideTripleSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertWideTripleSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := multireturntest.DefaultWideFixture()
	subject := multireturntest.NewWideStub(t,
		multireturntest.WithWideTriple(func(context.Context, string) (string, int, bool) {
			panic("Wide.Triple: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			multireturntest.AssertWideTripleSmoke(tb, subject, fixture.ID)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertWideTripleToleratesNilContextCanFail holds AssertWideTripleToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertWideTripleToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := multireturntest.DefaultWideFixture()
	subject := multireturntest.NewWideStub(t,
		multireturntest.WithWideTriple(func(context.Context, string) (string, int, bool) {
			panic("Wide.Triple: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			multireturntest.AssertWideTripleToleratesNilContext(tb, subject, fixture.ID)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertWideTripleReportsAMissCanFail holds AssertWideTripleReportsAMiss to being able to fail.
//
// The stand-in answers beside a flag reporting a miss with a value that looks like a
// result. A check that let that pass would leave a caller unable to tell an
// answer from an absence, which is the whole of what this shape signals with.
func TestAssertWideTripleReportsAMissCanFail(t *testing.T) {
	t.Parallel()

	fixture := multireturntest.DefaultWideFixture()
	subject := multireturntest.NewWideStub(t,
		multireturntest.WithWideTriple(func(context.Context, string) (string, int, bool) {
			var r2 bool
			return "test-r0", 42, r2
		}))

	got := testkit.Rejects(t, "a reader answering with a plausible value beside a false flag",
		func(tb testing.TB) {
			tb.Helper()
			multireturntest.AssertWideTripleReportsAMiss(tb, subject, fixture.IDOther)
		})
	testkit.Assert(t, got).Contains("must return the zero value",
		"and rejects it for the reason the check is about")
}

// TestAssertWideNoErrorSmokeCanFail holds AssertWideNoErrorSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertWideNoErrorSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := multireturntest.DefaultWideFixture()
	subject := multireturntest.NewWideStub(t,
		multireturntest.WithWideNoError(func(context.Context, string) (string, int) {
			panic("Wide.NoError: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			multireturntest.AssertWideNoErrorSmoke(tb, subject, fixture.ID)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertWideNoErrorToleratesNilContextCanFail holds AssertWideNoErrorToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertWideNoErrorToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := multireturntest.DefaultWideFixture()
	subject := multireturntest.NewWideStub(t,
		multireturntest.WithWideNoError(func(context.Context, string) (string, int) {
			panic("Wide.NoError: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			multireturntest.AssertWideNoErrorToleratesNilContext(tb, subject, fixture.ID)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance c695fd4b1a088b6f33fc58d25e7704433809cec2151280def74a91c04f29f3be
