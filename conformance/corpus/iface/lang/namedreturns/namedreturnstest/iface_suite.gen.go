// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/lang/namedreturns/iface.go
// Plugins:   golang 1.0.0, suite 1.0.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package namedreturnstest

import (
	"context"
	"testing"
	"time"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/iface/lang/namedreturns"
)

// ServiceNamedCheck is one assertion about Named.
//
// Every generated check for Named is a value of it, and so is one you
// write — so they compose, reorder, and each runs standalone. testing.TB rather
// than *testing.T is what lets a stand-in drive it and prove it can fail.
type ServiceNamedCheck func(tb testing.TB, subject namedreturns.Service, id string)

// ServiceUnnamedCheck is one assertion about Unnamed.
//
// Every generated check for Unnamed is a value of it, and so is one you
// write — so they compose, reorder, and each runs standalone. testing.TB rather
// than *testing.T is what lets a stand-in drive it and prove it can fail.
type ServiceUnnamedCheck func(tb testing.TB, subject namedreturns.Service, id string)

// ServicePartiallyNamedCheck is one assertion about PartiallyNamed.
//
// Every generated check for PartiallyNamed is a value of it, and so is one you
// write — so they compose, reorder, and each runs standalone. testing.TB rather
// than *testing.T is what lets a stand-in drive it and prove it can fail.
type ServicePartiallyNamedCheck func(tb testing.TB, subject namedreturns.Service, id string)

// ServiceFixture holds every input the generated checks run against.
//
// Derived from each method's parameter names and types. Every field has a
// companion holding a second, different value: a check comparing a result
// against a single input passes whenever the subject happened to be seeded with
// it, and a miss check whose key happens to hit asserts nothing.
//
// A field whose type admits no literal — a func, a channel, a type from a
// package this run never read — is declared and left at its zero value, and
// every generated check that needed it was dropped rather than emitted against
// a value nobody could write. Supply one through ServiceWithFixture
// and the check a consumer writes has something to use.
type ServiceFixture struct {
	ID      string
	IDOther string
}

// DefaultServiceFixture is what this run derived. Replace any field through
// ServiceWithFixture; the rest keep these values.
func DefaultServiceFixture() ServiceFixture {
	return ServiceFixture{
		ID:      "test-id",
		IDOther: "other-id",
	}
}

// AssertServiceContract runs every generated check against every declared subject.
//
//	Checks:   15 across 3 methods, per subject
//	Subjects: declare each with ServiceSubject
//	Double:   every subject runs a second time wrapped in ServiceStub, so
//	          anything the wrapper fails that the subject passes is the double
//	          lying. ServiceWithoutDouble declines it.
//	Extend:   ServiceOnNamed, ServiceOnUnnamed, ServiceOnPartiallyNamed
//	Drop:     ServiceWithout, by the path each check reports under
func AssertServiceContract(t *testing.T, opts ...ServiceOption) {
	t.Helper()
	cfg := newServiceConfig(opts...)
	if len(cfg.subjects) == 0 {
		t.Fatalf("AssertServiceContract was given no subject; declare one with " +
			"ServiceSubject(name, factory)")
	}
	for _, s := range cfg.subjects {
		runServiceChecks(t, cfg, s.name, s.factory, nil)
		if !cfg.withoutDouble {
			runServiceChecks(t, cfg, s.name+" through the double", s.factory,
				func(tb testing.TB, subject namedreturns.Service) namedreturns.Service {
					return NewServiceStub(tb, ServiceStubDelegateTo(subject))
				})
		}
	}
}

// runServiceChecks runs the whole contract against one subject.
//
// wrap is applied after the subject is built and seeded, so a wrapper is
// transparent to the seed and every failure it causes is its own.
func runServiceChecks(
	t *testing.T,
	cfg *serviceConfig,
	label string,
	factory func() namedreturns.Service,
	wrap func(tb testing.TB, subject namedreturns.Service) namedreturns.Service,
) {
	t.Helper()
	t.Run(label, func(t *testing.T) {
		t.Parallel()

		t.Run("Named", func(t *testing.T) {
			t.Parallel()
			cfg.run(t, "Named/smoke", "smoke", func(tb testing.TB) {
				AssertServiceNamedSmoke(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.ID)
			})
			cfg.run(t, "Named/reports a cancelled context", "reports a cancelled context", func(tb testing.TB) {
				AssertServiceNamedCancels(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.ID)
			})
			cfg.run(t, "Named/reports an expired deadline", "reports an expired deadline", func(tb testing.TB) {
				AssertServiceNamedHonoursDeadline(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.ID)
			})
			cfg.run(t, "Named/tolerates a nil context", "tolerates a nil context", func(tb testing.TB) {
				AssertServiceNamedToleratesNilContext(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.ID)
			})
			cfg.run(t, "Named/an error carries the zero value", "an error carries the zero value", func(tb testing.TB) {
				AssertServiceNamedZeroOnError(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.IDOther)
			})
			for _, c := range cfg.onNamed {
				cfg.run(t, "Named"+"/"+c.name, c.name, func(tb testing.TB) {
					c.fn(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.ID)
				})
			}
		})

		t.Run("Unnamed", func(t *testing.T) {
			t.Parallel()
			cfg.run(t, "Unnamed/smoke", "smoke", func(tb testing.TB) {
				AssertServiceUnnamedSmoke(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.ID)
			})
			cfg.run(t, "Unnamed/reports a cancelled context", "reports a cancelled context", func(tb testing.TB) {
				AssertServiceUnnamedCancels(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.ID)
			})
			cfg.run(t, "Unnamed/reports an expired deadline", "reports an expired deadline", func(tb testing.TB) {
				AssertServiceUnnamedHonoursDeadline(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.ID)
			})
			cfg.run(t, "Unnamed/tolerates a nil context", "tolerates a nil context", func(tb testing.TB) {
				AssertServiceUnnamedToleratesNilContext(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.ID)
			})
			cfg.run(t, "Unnamed/an error carries the zero value", "an error carries the zero value", func(tb testing.TB) {
				AssertServiceUnnamedZeroOnError(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.IDOther)
			})
			for _, c := range cfg.onUnnamed {
				cfg.run(t, "Unnamed"+"/"+c.name, c.name, func(tb testing.TB) {
					c.fn(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.ID)
				})
			}
		})

		t.Run("PartiallyNamed", func(t *testing.T) {
			t.Parallel()
			cfg.run(t, "PartiallyNamed/smoke", "smoke", func(tb testing.TB) {
				AssertServicePartiallyNamedSmoke(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.ID)
			})
			cfg.run(t, "PartiallyNamed/reports a cancelled context", "reports a cancelled context", func(tb testing.TB) {
				AssertServicePartiallyNamedCancels(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.ID)
			})
			cfg.run(t, "PartiallyNamed/reports an expired deadline", "reports an expired deadline", func(tb testing.TB) {
				AssertServicePartiallyNamedHonoursDeadline(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.ID)
			})
			cfg.run(t, "PartiallyNamed/tolerates a nil context", "tolerates a nil context", func(tb testing.TB) {
				AssertServicePartiallyNamedToleratesNilContext(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.ID)
			})
			cfg.run(t, "PartiallyNamed/an error carries the zero value", "an error carries the zero value", func(tb testing.TB) {
				AssertServicePartiallyNamedZeroOnError(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.IDOther)
			})
			for _, c := range cfg.onPartiallyNamed {
				cfg.run(t, "PartiallyNamed"+"/"+c.name, c.name, func(tb testing.TB) {
					c.fn(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.ID)
				})
			}
		})
	})
}

// AssertServiceNamedSmoke asserts Named survives a call with derived inputs.
//
// Fails when: Named panics. The weakest check in this file and the one
// that catches the most — a method that panics on a derived value is one no
// other check here reaches.
func AssertServiceNamedSmoke(tb testing.TB, subject namedreturns.Service, id string) {
	tb.Helper()
	defer func() {
		if r := recover(); r != nil {
			tb.Fatalf("Named panicked on a derived value (%v); supply one it "+
				"accepts through ServiceWithFixture", r)
		}
	}()
	ctx := tb.Context()
	_, _ = subject.Named(ctx, id)
}

// AssertServiceNamedCancels asserts Named reports a cancelled context as cancelled.
//
// Fails when: Named returns nil for a context cancelled before the
// call, or returns an error that does not answer to context.Canceled. Both
// matter and they are different defects: the first is work done that the caller
// asked not to be, the second is a caller who wrote
// `errors.Is(err, context.Canceled)` and gets false for a call that was.
//
// The error rather than merely its presence is what separates this from the
// deadline check. Asserting only that something came back makes the two one
// check written twice.
func AssertServiceNamedCancels(tb testing.TB, subject namedreturns.Service, id string) {
	tb.Helper()
	ctx, cancel := context.WithCancel(tb.Context())
	cancel()
	_, err := subject.Named(ctx, id)
	testkit.ErrorIs(tb, err, context.Canceled,
		"Named must report a cancelled context as context.Canceled")
}

// AssertServiceNamedHonoursDeadline asserts Named reports an expired deadline as exceeded.
//
// Fails when: Named returns nil for a context whose deadline has
// passed, or returns an error that does not answer to
// context.DeadlineExceeded. Distinct from cancellation in what the caller
// learns: a cancelled call was called off, an expired one ran out of time, and
// only the second is worth retrying.
//
// The deadline is the zero time, which is unconditionally in the past. No clock
// is read: a generated check that consults the wall clock is one whose subject
// is partly the machine it runs on.
func AssertServiceNamedHonoursDeadline(tb testing.TB, subject namedreturns.Service, id string) {
	tb.Helper()
	ctx, cancel := context.WithDeadline(tb.Context(), time.Time{})
	defer cancel()
	_, err := subject.Named(ctx, id)
	testkit.ErrorIs(tb, err, context.DeadlineExceeded,
		"Named must report an expired deadline as context.DeadlineExceeded")
}

// AssertServiceNamedToleratesNilContext asserts Named does not panic on a nil context.
//
// Fails when: Named panics. Returning an error is correct and
// succeeding is correct — a nil context reaches production through a caller
// that forgot one, and a panic turns that into an outage rather than a failed
// request.
func AssertServiceNamedToleratesNilContext(tb testing.TB, subject namedreturns.Service, id string) {
	tb.Helper()
	defer func() {
		if r := recover(); r != nil {
			tb.Errorf("Named panicked on a nil context (%v); return an error instead", r)
		}
	}()
	//nolint:staticcheck // passing nil is the check.
	var ctx context.Context
	_, _ = subject.Named(ctx, id)
}

// AssertServiceNamedZeroOnError asserts an error is accompanied by the zero value.
//
// Fails when: Named returns a non-zero result alongside a non-nil
// error. A caller who checks the error and a caller who checks the value must
// not disagree about whether the call succeeded.
func AssertServiceNamedZeroOnError(tb testing.TB, subject namedreturns.Service, id string) {
	tb.Helper()
	ctx := tb.Context()
	item, err := subject.Named(ctx, id)
	if err == nil {
		tb.Fatalf("Named succeeded; supply inputs it misses through " +
			"ServiceWithFixture")
	}
	{
		var zero string
		testkit.Equal(tb, item, zero,
			"Named must return the zero value alongside an error")
	}
}

// AssertServiceUnnamedSmoke asserts Unnamed survives a call with derived inputs.
//
// Fails when: Unnamed panics. The weakest check in this file and the one
// that catches the most — a method that panics on a derived value is one no
// other check here reaches.
func AssertServiceUnnamedSmoke(tb testing.TB, subject namedreturns.Service, id string) {
	tb.Helper()
	defer func() {
		if r := recover(); r != nil {
			tb.Fatalf("Unnamed panicked on a derived value (%v); supply one it "+
				"accepts through ServiceWithFixture", r)
		}
	}()
	ctx := tb.Context()
	_, _ = subject.Unnamed(ctx, id)
}

// AssertServiceUnnamedCancels asserts Unnamed reports a cancelled context as cancelled.
//
// Fails when: Unnamed returns nil for a context cancelled before the
// call, or returns an error that does not answer to context.Canceled. Both
// matter and they are different defects: the first is work done that the caller
// asked not to be, the second is a caller who wrote
// `errors.Is(err, context.Canceled)` and gets false for a call that was.
//
// The error rather than merely its presence is what separates this from the
// deadline check. Asserting only that something came back makes the two one
// check written twice.
func AssertServiceUnnamedCancels(tb testing.TB, subject namedreturns.Service, id string) {
	tb.Helper()
	ctx, cancel := context.WithCancel(tb.Context())
	cancel()
	_, err := subject.Unnamed(ctx, id)
	testkit.ErrorIs(tb, err, context.Canceled,
		"Unnamed must report a cancelled context as context.Canceled")
}

// AssertServiceUnnamedHonoursDeadline asserts Unnamed reports an expired deadline as exceeded.
//
// Fails when: Unnamed returns nil for a context whose deadline has
// passed, or returns an error that does not answer to
// context.DeadlineExceeded. Distinct from cancellation in what the caller
// learns: a cancelled call was called off, an expired one ran out of time, and
// only the second is worth retrying.
//
// The deadline is the zero time, which is unconditionally in the past. No clock
// is read: a generated check that consults the wall clock is one whose subject
// is partly the machine it runs on.
func AssertServiceUnnamedHonoursDeadline(tb testing.TB, subject namedreturns.Service, id string) {
	tb.Helper()
	ctx, cancel := context.WithDeadline(tb.Context(), time.Time{})
	defer cancel()
	_, err := subject.Unnamed(ctx, id)
	testkit.ErrorIs(tb, err, context.DeadlineExceeded,
		"Unnamed must report an expired deadline as context.DeadlineExceeded")
}

// AssertServiceUnnamedToleratesNilContext asserts Unnamed does not panic on a nil context.
//
// Fails when: Unnamed panics. Returning an error is correct and
// succeeding is correct — a nil context reaches production through a caller
// that forgot one, and a panic turns that into an outage rather than a failed
// request.
func AssertServiceUnnamedToleratesNilContext(tb testing.TB, subject namedreturns.Service, id string) {
	tb.Helper()
	defer func() {
		if r := recover(); r != nil {
			tb.Errorf("Unnamed panicked on a nil context (%v); return an error instead", r)
		}
	}()
	//nolint:staticcheck // passing nil is the check.
	var ctx context.Context
	_, _ = subject.Unnamed(ctx, id)
}

// AssertServiceUnnamedZeroOnError asserts an error is accompanied by the zero value.
//
// Fails when: Unnamed returns a non-zero result alongside a non-nil
// error. A caller who checks the error and a caller who checks the value must
// not disagree about whether the call succeeded.
func AssertServiceUnnamedZeroOnError(tb testing.TB, subject namedreturns.Service, id string) {
	tb.Helper()
	ctx := tb.Context()
	r0, r1 := subject.Unnamed(ctx, id)
	if r1 == nil {
		tb.Fatalf("Unnamed succeeded; supply inputs it misses through " +
			"ServiceWithFixture")
	}
	{
		var zero string
		testkit.Equal(tb, r0, zero,
			"Unnamed must return the zero value alongside an error")
	}
}

// AssertServicePartiallyNamedSmoke asserts PartiallyNamed survives a call with derived inputs.
//
// Fails when: PartiallyNamed panics. The weakest check in this file and the one
// that catches the most — a method that panics on a derived value is one no
// other check here reaches.
func AssertServicePartiallyNamedSmoke(tb testing.TB, subject namedreturns.Service, id string) {
	tb.Helper()
	defer func() {
		if r := recover(); r != nil {
			tb.Fatalf("PartiallyNamed panicked on a derived value (%v); supply one it "+
				"accepts through ServiceWithFixture", r)
		}
	}()
	ctx := tb.Context()
	_, _ = subject.PartiallyNamed(ctx, id)
}

// AssertServicePartiallyNamedCancels asserts PartiallyNamed reports a cancelled context as cancelled.
//
// Fails when: PartiallyNamed returns nil for a context cancelled before the
// call, or returns an error that does not answer to context.Canceled. Both
// matter and they are different defects: the first is work done that the caller
// asked not to be, the second is a caller who wrote
// `errors.Is(err, context.Canceled)` and gets false for a call that was.
//
// The error rather than merely its presence is what separates this from the
// deadline check. Asserting only that something came back makes the two one
// check written twice.
func AssertServicePartiallyNamedCancels(tb testing.TB, subject namedreturns.Service, id string) {
	tb.Helper()
	ctx, cancel := context.WithCancel(tb.Context())
	cancel()
	_, err := subject.PartiallyNamed(ctx, id)
	testkit.ErrorIs(tb, err, context.Canceled,
		"PartiallyNamed must report a cancelled context as context.Canceled")
}

// AssertServicePartiallyNamedHonoursDeadline asserts PartiallyNamed reports an expired deadline as exceeded.
//
// Fails when: PartiallyNamed returns nil for a context whose deadline has
// passed, or returns an error that does not answer to
// context.DeadlineExceeded. Distinct from cancellation in what the caller
// learns: a cancelled call was called off, an expired one ran out of time, and
// only the second is worth retrying.
//
// The deadline is the zero time, which is unconditionally in the past. No clock
// is read: a generated check that consults the wall clock is one whose subject
// is partly the machine it runs on.
func AssertServicePartiallyNamedHonoursDeadline(tb testing.TB, subject namedreturns.Service, id string) {
	tb.Helper()
	ctx, cancel := context.WithDeadline(tb.Context(), time.Time{})
	defer cancel()
	_, err := subject.PartiallyNamed(ctx, id)
	testkit.ErrorIs(tb, err, context.DeadlineExceeded,
		"PartiallyNamed must report an expired deadline as context.DeadlineExceeded")
}

// AssertServicePartiallyNamedToleratesNilContext asserts PartiallyNamed does not panic on a nil context.
//
// Fails when: PartiallyNamed panics. Returning an error is correct and
// succeeding is correct — a nil context reaches production through a caller
// that forgot one, and a panic turns that into an outage rather than a failed
// request.
func AssertServicePartiallyNamedToleratesNilContext(tb testing.TB, subject namedreturns.Service, id string) {
	tb.Helper()
	defer func() {
		if r := recover(); r != nil {
			tb.Errorf("PartiallyNamed panicked on a nil context (%v); return an error instead", r)
		}
	}()
	//nolint:staticcheck // passing nil is the check.
	var ctx context.Context
	_, _ = subject.PartiallyNamed(ctx, id)
}

// AssertServicePartiallyNamedZeroOnError asserts an error is accompanied by the zero value.
//
// Fails when: PartiallyNamed returns a non-zero result alongside a non-nil
// error. A caller who checks the error and a caller who checks the value must
// not disagree about whether the call succeeded.
func AssertServicePartiallyNamedZeroOnError(tb testing.TB, subject namedreturns.Service, id string) {
	tb.Helper()
	ctx := tb.Context()
	r0, r1 := subject.PartiallyNamed(ctx, id)
	if r1 == nil {
		tb.Fatalf("PartiallyNamed succeeded; supply inputs it misses through " +
			"ServiceWithFixture")
	}
	{
		var zero string
		testkit.Equal(tb, r0, zero,
			"PartiallyNamed must return the zero value alongside an error")
	}
}

// ServiceOption configures a contract run.
type ServiceOption func(*serviceConfig)

// ServiceSubject declares an implementation to hold to the contract.
//
// Repeat it for every implementation that claims to satisfy the interface — an
// in-memory one, the one backed by a database, the one behind a network. Every
// check and every option is stated once and runs against all of them, which is
// what a conformance suite is for.
func ServiceSubject(name string, factory func() namedreturns.Service) ServiceOption {
	return func(c *serviceConfig) {
		c.subjects = append(c.subjects, namedServiceSubject{name, factory})
	}
}

// ServiceWithoutDouble declines the second run through ServiceStub.
//
// The double run proves the stand-in is faithful, and costs a second pass over
// every check. Decline it where the double is not used.
func ServiceWithoutDouble() ServiceOption {
	return func(c *serviceConfig) { c.withoutDouble = true }
}

// ServiceWithFixture replaces the derived inputs.
func ServiceWithFixture(f ServiceFixture) ServiceOption {
	return func(c *serviceConfig) { c.Fixture = f }
}

// ServiceSeed replaces the derived seed.
//
// For an interface with no writer to seed itself through, which is the one
// input a conformance run cannot derive: a reader over an empty subject
// asserts nothing.
//
// Returning an error rather than swallowing one. A seed that failed quietly
// leaves every check after it asserting nothing and reporting success.
func ServiceSeed(fn func(ctx context.Context, subject namedreturns.Service) error) ServiceOption {
	return func(c *serviceConfig) {
		c.seed = fn
	}
}

// ServiceOnNamed adds a named check to Named.
func ServiceOnNamed(name string, fn ServiceNamedCheck) ServiceOption {
	return func(c *serviceConfig) {
		c.onNamed = append(c.onNamed, namedServiceNamedCheck{name, fn})
	}
}

// ServiceOnUnnamed adds a named check to Unnamed.
func ServiceOnUnnamed(name string, fn ServiceUnnamedCheck) ServiceOption {
	return func(c *serviceConfig) {
		c.onUnnamed = append(c.onUnnamed, namedServiceUnnamedCheck{name, fn})
	}
}

// ServiceOnPartiallyNamed adds a named check to PartiallyNamed.
func ServiceOnPartiallyNamed(name string, fn ServicePartiallyNamedCheck) ServiceOption {
	return func(c *serviceConfig) {
		c.onPartiallyNamed = append(c.onPartiallyNamed, namedServicePartiallyNamedCheck{name, fn})
	}
}

// ServiceWithout drops generated checks by the path each reports under.
//
// For a subject that legitimately violates one. Without this the only recourse
// is to stop running the suite, which costs every other check in the file — so
// a dropped check is reported as skipped rather than silently omitted.
func ServiceWithout(paths ...string) ServiceOption {
	return func(c *serviceConfig) {
		for _, p := range paths {
			c.without[p] = struct{}{}
		}
	}
}

type namedServiceNamedCheck struct {
	name string
	fn   ServiceNamedCheck
}

type namedServiceUnnamedCheck struct {
	name string
	fn   ServiceUnnamedCheck
}

type namedServicePartiallyNamedCheck struct {
	name string
	fn   ServicePartiallyNamedCheck
}

type namedServiceSubject struct {
	name    string
	factory func() namedreturns.Service
}

type serviceConfig struct {
	Fixture          ServiceFixture
	subjects         []namedServiceSubject
	withoutDouble    bool
	seed             func(ctx context.Context, subject namedreturns.Service) error
	without          map[string]struct{}
	onNamed          []namedServiceNamedCheck
	onUnnamed        []namedServiceUnnamedCheck
	onPartiallyNamed []namedServicePartiallyNamedCheck
}

func newServiceConfig(opts ...ServiceOption) *serviceConfig {
	c := &serviceConfig{
		Fixture: DefaultServiceFixture(),
		without: map[string]struct{}{},
	}
	for _, o := range opts {
		o(c)
	}
	return c
}

// subject builds a fresh implementation and seeds it, so every check runs
// against state no other check touched.
func (c *serviceConfig) subject(
	tb testing.TB,
	factory func() namedreturns.Service,
	wrap func(tb testing.TB, subject namedreturns.Service) namedreturns.Service,
) namedreturns.Service {
	tb.Helper()
	s := factory()
	if c.seed != nil {
		// Reported rather than ignored. A seed that silently failed would leave
		// every check after it running against an empty subject — passing, and
		// asserting nothing.
		//
		// Which seed ran decides what the message can usefully say. The derived
		// one fails for one of two reasons and both are fixable from here, so it
		// names them. A seed the caller supplied is theirs, and telling them to
		// supply one is advice they already took.
		if err := c.seed(tb.Context(), s); err != nil {
			tb.Fatalf("the seed supplied through ServiceSeed failed: %v", err)
		}
	}
	if wrap != nil {
		s = wrap(tb, s)
	}
	return s
}

// run executes one check unless it was dropped, reporting the drop rather than
// silently omitting it.
func (c *serviceConfig) run(t *testing.T, path, name string, fn func(tb testing.TB)) {
	t.Helper()
	if _, dropped := c.without[path]; dropped {
		t.Run(name, func(t *testing.T) {
			t.Skipf("dropped through ServiceWithout(%q)", path)
		})
		return
	}
	t.Run(name, func(t *testing.T) {
		t.Parallel()
		fn(t)
	})
}

// testkit: end of generated content.
// testkit:provenance bcf60932484ea89743586cc0755901710bdc596d8f9600b7bb1d53f6382dd603
