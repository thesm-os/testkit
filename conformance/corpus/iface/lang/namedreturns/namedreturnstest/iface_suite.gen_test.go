// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/lang/namedreturns/iface.go
// Plugins:   golang 1.0.0, suite 1.1.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package namedreturnstest_test

import (
	"context"
	"errors"
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/iface/lang/namedreturns/namedreturnstest"
)

// Every generated check for Service, driven against a stand-in built
// to violate it.
//
//	Proved:   15 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertServiceNamedSmokeCanFail holds AssertServiceNamedSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertServiceNamedSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := namedreturnstest.DefaultServiceFixture()
	subject := namedreturnstest.NewServiceStub(t,
		namedreturnstest.WithServiceNamed(func(context.Context, string) (string, error) {
			panic("Service.Named: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			namedreturnstest.AssertServiceNamedSmoke(tb, subject, fixture.ID)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertServiceNamedCancelsCanFail holds AssertServiceNamedCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertServiceNamedCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := namedreturnstest.DefaultServiceFixture()
	subject := namedreturnstest.NewServiceStub(t,
		namedreturnstest.WithServiceNamed(func(context.Context, string) (string, error) {
			var item string
			return item, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			namedreturnstest.AssertServiceNamedCancels(tb, subject, fixture.ID)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertServiceNamedHonoursDeadlineCanFail holds AssertServiceNamedHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertServiceNamedHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := namedreturnstest.DefaultServiceFixture()
	subject := namedreturnstest.NewServiceStub(t,
		namedreturnstest.WithServiceNamed(func(context.Context, string) (string, error) {
			var item string
			return item, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			namedreturnstest.AssertServiceNamedHonoursDeadline(tb, subject, fixture.ID)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertServiceNamedToleratesNilContextCanFail holds AssertServiceNamedToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertServiceNamedToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := namedreturnstest.DefaultServiceFixture()
	subject := namedreturnstest.NewServiceStub(t,
		namedreturnstest.WithServiceNamed(func(context.Context, string) (string, error) {
			panic("Service.Named: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			namedreturnstest.AssertServiceNamedToleratesNilContext(tb, subject, fixture.ID)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertServiceNamedZeroOnErrorCanFail holds AssertServiceNamedZeroOnError to being able to fail.
//
// The stand-in refuses the call and answers a value that looks like a result
// anyway. A check that let that pass would leave a caller who checks the error
// and a caller who checks the value disagreeing about what happened — the
// claim's own violation, not a stand-in that merely succeeds.
func TestAssertServiceNamedZeroOnErrorCanFail(t *testing.T) {
	t.Parallel()

	fixture := namedreturnstest.DefaultServiceFixture()
	subject := namedreturnstest.NewServiceStub(t,
		namedreturnstest.WithServiceNamed(func(context.Context, string) (string, error) {
			return "test-item", errors.New("violate: refused with a believable value")
		}))

	got := testkit.Rejects(t, "a method answering a believable value beside its error",
		func(tb testing.TB) {
			tb.Helper()
			namedreturnstest.AssertServiceNamedZeroOnError(tb, subject, fixture.IDOther)
		})
	testkit.Assert(t, got).Contains("must return the zero value",
		"and rejects it for the reason the check is about")
}

// TestAssertServiceUnnamedSmokeCanFail holds AssertServiceUnnamedSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertServiceUnnamedSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := namedreturnstest.DefaultServiceFixture()
	subject := namedreturnstest.NewServiceStub(t,
		namedreturnstest.WithServiceUnnamed(func(context.Context, string) (string, error) {
			panic("Service.Unnamed: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			namedreturnstest.AssertServiceUnnamedSmoke(tb, subject, fixture.ID)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertServiceUnnamedCancelsCanFail holds AssertServiceUnnamedCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertServiceUnnamedCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := namedreturnstest.DefaultServiceFixture()
	subject := namedreturnstest.NewServiceStub(t,
		namedreturnstest.WithServiceUnnamed(func(context.Context, string) (string, error) {
			var r0 string
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			namedreturnstest.AssertServiceUnnamedCancels(tb, subject, fixture.ID)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertServiceUnnamedHonoursDeadlineCanFail holds AssertServiceUnnamedHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertServiceUnnamedHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := namedreturnstest.DefaultServiceFixture()
	subject := namedreturnstest.NewServiceStub(t,
		namedreturnstest.WithServiceUnnamed(func(context.Context, string) (string, error) {
			var r0 string
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			namedreturnstest.AssertServiceUnnamedHonoursDeadline(tb, subject, fixture.ID)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertServiceUnnamedToleratesNilContextCanFail holds AssertServiceUnnamedToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertServiceUnnamedToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := namedreturnstest.DefaultServiceFixture()
	subject := namedreturnstest.NewServiceStub(t,
		namedreturnstest.WithServiceUnnamed(func(context.Context, string) (string, error) {
			panic("Service.Unnamed: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			namedreturnstest.AssertServiceUnnamedToleratesNilContext(tb, subject, fixture.ID)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertServiceUnnamedZeroOnErrorCanFail holds AssertServiceUnnamedZeroOnError to being able to fail.
//
// The stand-in refuses the call and answers a value that looks like a result
// anyway. A check that let that pass would leave a caller who checks the error
// and a caller who checks the value disagreeing about what happened — the
// claim's own violation, not a stand-in that merely succeeds.
func TestAssertServiceUnnamedZeroOnErrorCanFail(t *testing.T) {
	t.Parallel()

	fixture := namedreturnstest.DefaultServiceFixture()
	subject := namedreturnstest.NewServiceStub(t,
		namedreturnstest.WithServiceUnnamed(func(context.Context, string) (string, error) {
			return "test-r0", errors.New("violate: refused with a believable value")
		}))

	got := testkit.Rejects(t, "a method answering a believable value beside its error",
		func(tb testing.TB) {
			tb.Helper()
			namedreturnstest.AssertServiceUnnamedZeroOnError(tb, subject, fixture.IDOther)
		})
	testkit.Assert(t, got).Contains("must return the zero value",
		"and rejects it for the reason the check is about")
}

// TestAssertServicePartiallyNamedSmokeCanFail holds AssertServicePartiallyNamedSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertServicePartiallyNamedSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := namedreturnstest.DefaultServiceFixture()
	subject := namedreturnstest.NewServiceStub(t,
		namedreturnstest.WithServicePartiallyNamed(func(context.Context, string) (string, error) {
			panic("Service.PartiallyNamed: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			namedreturnstest.AssertServicePartiallyNamedSmoke(tb, subject, fixture.ID)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertServicePartiallyNamedCancelsCanFail holds AssertServicePartiallyNamedCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertServicePartiallyNamedCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := namedreturnstest.DefaultServiceFixture()
	subject := namedreturnstest.NewServiceStub(t,
		namedreturnstest.WithServicePartiallyNamed(func(context.Context, string) (string, error) {
			var r0 string
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			namedreturnstest.AssertServicePartiallyNamedCancels(tb, subject, fixture.ID)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertServicePartiallyNamedHonoursDeadlineCanFail holds AssertServicePartiallyNamedHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertServicePartiallyNamedHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := namedreturnstest.DefaultServiceFixture()
	subject := namedreturnstest.NewServiceStub(t,
		namedreturnstest.WithServicePartiallyNamed(func(context.Context, string) (string, error) {
			var r0 string
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			namedreturnstest.AssertServicePartiallyNamedHonoursDeadline(tb, subject, fixture.ID)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertServicePartiallyNamedToleratesNilContextCanFail holds AssertServicePartiallyNamedToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertServicePartiallyNamedToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := namedreturnstest.DefaultServiceFixture()
	subject := namedreturnstest.NewServiceStub(t,
		namedreturnstest.WithServicePartiallyNamed(func(context.Context, string) (string, error) {
			panic("Service.PartiallyNamed: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			namedreturnstest.AssertServicePartiallyNamedToleratesNilContext(tb, subject, fixture.ID)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertServicePartiallyNamedZeroOnErrorCanFail holds AssertServicePartiallyNamedZeroOnError to being able to fail.
//
// The stand-in refuses the call and answers a value that looks like a result
// anyway. A check that let that pass would leave a caller who checks the error
// and a caller who checks the value disagreeing about what happened — the
// claim's own violation, not a stand-in that merely succeeds.
func TestAssertServicePartiallyNamedZeroOnErrorCanFail(t *testing.T) {
	t.Parallel()

	fixture := namedreturnstest.DefaultServiceFixture()
	subject := namedreturnstest.NewServiceStub(t,
		namedreturnstest.WithServicePartiallyNamed(func(context.Context, string) (string, error) {
			return "test-r0", errors.New("violate: refused with a believable value")
		}))

	got := testkit.Rejects(t, "a method answering a believable value beside its error",
		func(tb testing.TB) {
			tb.Helper()
			namedreturnstest.AssertServicePartiallyNamedZeroOnError(tb, subject, fixture.IDOther)
		})
	testkit.Assert(t, got).Contains("must return the zero value",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance 506320f352ea4c9954136cb444275b1dd2ef145d8cd4aa31bf62adb87cfd5fa7
