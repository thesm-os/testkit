// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/lang/partnernaming/iface.go
// Plugins:   golang 1.0.0, suite 1.16.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/iface/lang/partnernaming/...

package partnernamingtest

import (
	"context"
	"slices"
	"testing"
	"time"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/conformance/corpus/iface/lang/partnernaming"
)

// StoreTouchCheck is one assertion about Touch.
//
// Every generated check for Touch is a value of it, and so is one you
// write — so they compose, reorder, and each runs standalone. testing.TB rather
// than *testing.T is what lets a stand-in drive it and prove it can fail.
type StoreTouchCheck func(tb testing.TB, subject partnernaming.Store, key string, weight int)

// StoreSeenCheck is one assertion about Seen.
//
// Every generated check for Seen is a value of it, and so is one you
// write — so they compose, reorder, and each runs standalone. testing.TB rather
// than *testing.T is what lets a stand-in drive it and prove it can fail.
type StoreSeenCheck func(tb testing.TB, subject partnernaming.Store, k string)

// StoreMoveCheck is one assertion about Move.
//
// Every generated check for Move is a value of it, and so is one you
// write — so they compose, reorder, and each runs standalone. testing.TB rather
// than *testing.T is what lets a stand-in drive it and prove it can fail.
type StoreMoveCheck func(tb testing.TB, subject partnernaming.Store, from string, to string)

// StoreAtCheck is one assertion about At.
//
// Every generated check for At is a value of it, and so is one you
// write — so they compose, reorder, and each runs standalone. testing.TB rather
// than *testing.T is what lets a stand-in drive it and prove it can fail.
type StoreAtCheck func(tb testing.TB, subject partnernaming.Store, where string)

// StoreEmitCheck is one assertion about Emit.
//
// Every generated check for Emit is a value of it, and so is one you
// write — so they compose, reorder, and each runs standalone. testing.TB rather
// than *testing.T is what lets a stand-in drive it and prove it can fail.
type StoreEmitCheck func(tb testing.TB, subject partnernaming.Store, id string)

// StoreCountCheck is one assertion about Count.
//
// Every generated check for Count is a value of it, and so is one you
// write — so they compose, reorder, and each runs standalone. testing.TB rather
// than *testing.T is what lets a stand-in drive it and prove it can fail.
type StoreCountCheck func(tb testing.TB, subject partnernaming.Store, bucket int)

// StoreFixture holds every input the generated checks run against.
//
// Derived from each method's parameter names and types. Every field has a
// companion holding a second, different value: a check comparing a result
// against a single input passes whenever the subject happened to be seeded with
// it, and a miss check whose key happens to hit asserts nothing.
//
// A field whose type admits no literal — a func, a channel, a type from a
// package this run never read — is declared and left at its zero value, and
// every generated check that needed it was dropped rather than emitted against
// a value nobody could write. Supply one through StoreWithFixture
// and the check a consumer writes has something to use.
type StoreFixture struct {
	Key         string
	KeyOther    string
	Weight      int
	WeightOther int
	K           string
	KOther      string
	From        string
	FromOther   string
	To          string
	ToOther     string
	Where       string
	WhereOther  string
	ID          string
	IDOther     string
	Bucket      int
	BucketOther int
}

// DefaultStoreFixture is what this run derived. Replace any field through
// StoreWithFixture; the rest keep these values.
func DefaultStoreFixture() StoreFixture {
	return StoreFixture{
		Key:         "test-key",
		KeyOther:    "other-key",
		Weight:      1,
		WeightOther: 2,
		K:           "test-k",
		KOther:      "other-k",
		From:        "test-from",
		FromOther:   "other-from",
		To:          "test-to",
		ToOther:     "other-to",
		Where:       "test-where",
		WhereOther:  "other-where",
		ID:          "test-id",
		IDOther:     "other-id",
		Bucket:      1,
		BucketOther: 2,
	}
}

// AssertStoreContract runs every generated check against every declared subject.
//
//	Checks:   28 across 6 methods, per subject
//	Subjects: declare each with StoreSubject
//	Double:   every subject runs a second time wrapped in StoreStub, so
//	          anything the wrapper fails that the subject passes is the double
//	          lying. StoreWithoutDouble declines it.
//	Extend:   StoreOnTouch, StoreOnSeen, StoreOnMove, StoreOnAt, StoreOnEmit, StoreOnCount
//	Drop:     StoreWithout, by the path each check reports under
//
// # What this file does not check
//
// The interface declares these and nothing here asserts them. Each is statable
// against a subject you can build, so the check is yours to write:
//
//   - compositewriter, on Touch, Move — write it with StoreOnTouch
//   - sideeffect, on Move, Emit — write it with StoreOnMove
//   - writer, on Emit — write it with StoreOnEmit
func AssertStoreContract(t *testing.T, opts ...StoreOption) {
	t.Helper()
	cfg := newStoreConfig(opts...)
	if len(cfg.subjects) == 0 {
		t.Fatalf("AssertStoreContract was given no subject; declare one with " +
			"StoreSubject(name, factory)")
	}
	for _, p := range cfg.phantoms() {
		t.Errorf("StoreWithout(%q) declines nothing: no check here reports "+
			"under that path, so this suite asserts one thing more than its "+
			"author believes", p)
	}
	for _, s := range cfg.subjects {
		runStoreChecks(t, cfg, s.name, s.factory, nil)
		if !cfg.withoutDouble {
			runStoreChecks(t, cfg, s.name+" through the double", s.factory,
				func(tb testing.TB, subject partnernaming.Store) partnernaming.Store {
					return NewStoreStub(tb, StoreStubDelegateTo(subject))
				})
		}
	}
}

// runStoreChecks runs the whole contract against one subject.
//
// wrap is applied after the subject is built and seeded, so a wrapper is
// transparent to the seed and every failure it causes is its own.
func runStoreChecks(
	t *testing.T,
	cfg *storeConfig,
	label string,
	factory func() partnernaming.Store,
	wrap func(tb testing.TB, subject partnernaming.Store) partnernaming.Store,
) {
	t.Helper()
	t.Run(label, func(t *testing.T) {
		t.Parallel()

		t.Run("Touch", func(t *testing.T) {
			t.Parallel()
			cfg.run(t, "Touch/smoke", "smoke", func(tb testing.TB) {
				AssertStoreTouchSmoke(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.Key, cfg.Fixture.Weight)
			})
			cfg.run(t, "Touch/reports a cancelled context", "reports a cancelled context", func(tb testing.TB) {
				AssertStoreTouchCancels(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.Key, cfg.Fixture.Weight)
			})
			cfg.run(t, "Touch/reports an expired deadline", "reports an expired deadline", func(tb testing.TB) {
				AssertStoreTouchHonoursDeadline(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.Key, cfg.Fixture.Weight)
			})
			cfg.run(t, "Touch/tolerates a nil context", "tolerates a nil context", func(tb testing.TB) {
				AssertStoreTouchToleratesNilContext(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.Key, cfg.Fixture.Weight)
			})
			cfg.run(t, "Touch/sideeffect", "sideeffect", func(tb testing.TB) {
				AssertStoreTouchHasAnObservableEffect(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.Key, cfg.Fixture.Weight)
			})
			for _, c := range cfg.onTouch {
				cfg.run(t, "Touch"+"/"+c.name, c.name, func(tb testing.TB) {
					c.fn(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.Key, cfg.Fixture.Weight)
				})
			}
		})

		t.Run("Seen", func(t *testing.T) {
			t.Parallel()
			cfg.run(t, "Seen/smoke", "smoke", func(tb testing.TB) {
				AssertStoreSeenSmoke(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.K)
			})
			cfg.run(t, "Seen/reports a cancelled context", "reports a cancelled context", func(tb testing.TB) {
				AssertStoreSeenCancels(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.K)
			})
			cfg.run(t, "Seen/reports an expired deadline", "reports an expired deadline", func(tb testing.TB) {
				AssertStoreSeenHonoursDeadline(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.K)
			})
			cfg.run(t, "Seen/tolerates a nil context", "tolerates a nil context", func(tb testing.TB) {
				AssertStoreSeenToleratesNilContext(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.K)
			})
			cfg.run(t, "Seen/an error carries the zero value", "an error carries the zero value", func(tb testing.TB) {
				AssertStoreSeenZeroOnError(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.KOther)
			})
			for _, c := range cfg.onSeen {
				cfg.run(t, "Seen"+"/"+c.name, c.name, func(tb testing.TB) {
					c.fn(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.K)
				})
			}
		})

		t.Run("Move", func(t *testing.T) {
			t.Parallel()
			cfg.run(t, "Move/smoke", "smoke", func(tb testing.TB) {
				AssertStoreMoveSmoke(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.From, cfg.Fixture.To)
			})
			cfg.run(t, "Move/reports a cancelled context", "reports a cancelled context", func(tb testing.TB) {
				AssertStoreMoveCancels(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.From, cfg.Fixture.To)
			})
			cfg.run(t, "Move/reports an expired deadline", "reports an expired deadline", func(tb testing.TB) {
				AssertStoreMoveHonoursDeadline(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.From, cfg.Fixture.To)
			})
			cfg.run(t, "Move/tolerates a nil context", "tolerates a nil context", func(tb testing.TB) {
				AssertStoreMoveToleratesNilContext(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.From, cfg.Fixture.To)
			})
			for _, c := range cfg.onMove {
				cfg.run(t, "Move"+"/"+c.name, c.name, func(tb testing.TB) {
					c.fn(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.From, cfg.Fixture.To)
				})
			}
		})

		t.Run("At", func(t *testing.T) {
			t.Parallel()
			cfg.run(t, "At/smoke", "smoke", func(tb testing.TB) {
				AssertStoreAtSmoke(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.Where)
			})
			cfg.run(t, "At/reports a cancelled context", "reports a cancelled context", func(tb testing.TB) {
				AssertStoreAtCancels(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.Where)
			})
			cfg.run(t, "At/reports an expired deadline", "reports an expired deadline", func(tb testing.TB) {
				AssertStoreAtHonoursDeadline(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.Where)
			})
			cfg.run(t, "At/tolerates a nil context", "tolerates a nil context", func(tb testing.TB) {
				AssertStoreAtToleratesNilContext(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.Where)
			})
			cfg.run(t, "At/an error carries the zero value", "an error carries the zero value", func(tb testing.TB) {
				AssertStoreAtZeroOnError(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.WhereOther)
			})
			for _, c := range cfg.onAt {
				cfg.run(t, "At"+"/"+c.name, c.name, func(tb testing.TB) {
					c.fn(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.Where)
				})
			}
		})

		t.Run("Emit", func(t *testing.T) {
			t.Parallel()
			cfg.run(t, "Emit/smoke", "smoke", func(tb testing.TB) {
				AssertStoreEmitSmoke(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.ID)
			})
			cfg.run(t, "Emit/reports a cancelled context", "reports a cancelled context", func(tb testing.TB) {
				AssertStoreEmitCancels(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.ID)
			})
			cfg.run(t, "Emit/reports an expired deadline", "reports an expired deadline", func(tb testing.TB) {
				AssertStoreEmitHonoursDeadline(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.ID)
			})
			cfg.run(t, "Emit/tolerates a nil context", "tolerates a nil context", func(tb testing.TB) {
				AssertStoreEmitToleratesNilContext(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.ID)
			})
			for _, c := range cfg.onEmit {
				cfg.run(t, "Emit"+"/"+c.name, c.name, func(tb testing.TB) {
					c.fn(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.ID)
				})
			}
		})

		t.Run("Count", func(t *testing.T) {
			t.Parallel()
			cfg.run(t, "Count/smoke", "smoke", func(tb testing.TB) {
				AssertStoreCountSmoke(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.Bucket)
			})
			cfg.run(t, "Count/reports a cancelled context", "reports a cancelled context", func(tb testing.TB) {
				AssertStoreCountCancels(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.Bucket)
			})
			cfg.run(t, "Count/reports an expired deadline", "reports an expired deadline", func(tb testing.TB) {
				AssertStoreCountHonoursDeadline(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.Bucket)
			})
			cfg.run(t, "Count/tolerates a nil context", "tolerates a nil context", func(tb testing.TB) {
				AssertStoreCountToleratesNilContext(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.Bucket)
			})
			cfg.run(t, "Count/an error carries the zero value", "an error carries the zero value", func(tb testing.TB) {
				AssertStoreCountZeroOnError(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.BucketOther)
			})
			for _, c := range cfg.onCount {
				cfg.run(t, "Count"+"/"+c.name, c.name, func(tb testing.TB) {
					c.fn(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.Bucket)
				})
			}
		})

		// The extra bodies of checks this run was given, each reporting under its
		// own name and dropped by it.
		//
		// The plain subject only. The wrapped pass exists to prove the double
		// faithful and the per-method checks above already do that, so running one
		// through the wrapper prices the wrapper and says nothing new about the
		// subject.
		if wrap == nil {
			for _, e := range cfg.extensions {
				if cfg.declined(e.name) {
					t.Run(e.name, func(t *testing.T) {
						t.Skipf("dropped through StoreWithout(%q)", e.name)
					})
					continue
				}
				t.Run(e.name, func(t *testing.T) {
					t.Parallel()
					e.run(t, label, factory, cfg)
				})
			}
		}
	})
}

// AssertStoreTouchSmoke asserts Touch survives a call with derived inputs.
//
// Fails when: Touch panics. The weakest check in this file and the one
// that catches the most — a method that panics on a derived value is one no
// other check here reaches.
func AssertStoreTouchSmoke(tb testing.TB, subject partnernaming.Store, key string, weight int) {
	tb.Helper()
	defer func() {
		if r := recover(); r != nil {
			tb.Fatalf("Touch panicked on a derived value (%v); supply one it "+
				"accepts through StoreWithFixture", r)
		}
	}()
	ctx := tb.Context()
	_ = subject.Touch(ctx, key, weight)
}

// AssertStoreTouchCancels asserts Touch reports a cancelled context as cancelled.
//
// Fails when: Touch returns nil for a context cancelled before the
// call, or returns an error that does not answer to context.Canceled. Both
// matter and they are different defects: the first is work done that the caller
// asked not to be, the second is a caller who wrote
// `errors.Is(err, context.Canceled)` and gets false for a call that was.
//
// The error rather than merely its presence is what separates this from the
// deadline check. Asserting only that something came back makes the two one
// check written twice.
func AssertStoreTouchCancels(tb testing.TB, subject partnernaming.Store, key string, weight int) {
	tb.Helper()
	ctx, cancel := context.WithCancel(tb.Context())
	cancel()
	err := subject.Touch(ctx, key, weight)
	testkit.ErrorIs(tb, err, context.Canceled,
		"Touch must report a cancelled context as context.Canceled")
}

// AssertStoreTouchHonoursDeadline asserts Touch reports an expired deadline as exceeded.
//
// Fails when: Touch returns nil for a context whose deadline has
// passed, or returns an error that does not answer to
// context.DeadlineExceeded. Distinct from cancellation in what the caller
// learns: a cancelled call was called off, an expired one ran out of time, and
// only the second is worth retrying.
//
// The deadline is the zero time, which is unconditionally in the past. No clock
// is read: a generated check that consults the wall clock is one whose subject
// is partly the machine it runs on.
func AssertStoreTouchHonoursDeadline(tb testing.TB, subject partnernaming.Store, key string, weight int) {
	tb.Helper()
	ctx, cancel := context.WithDeadline(tb.Context(), time.Time{})
	defer cancel()
	err := subject.Touch(ctx, key, weight)
	testkit.ErrorIs(tb, err, context.DeadlineExceeded,
		"Touch must report an expired deadline as context.DeadlineExceeded")
}

// AssertStoreTouchToleratesNilContext asserts Touch does not panic on a nil context.
//
// Fails when: Touch panics. Returning an error is correct and
// succeeding is correct — a nil context reaches production through a caller
// that forgot one, and a panic turns that into an outage rather than a failed
// request.
func AssertStoreTouchToleratesNilContext(tb testing.TB, subject partnernaming.Store, key string, weight int) {
	tb.Helper()
	defer func() {
		if r := recover(); r != nil {
			tb.Errorf("Touch panicked on a nil context (%v); return an error instead", r)
		}
	}()
	//nolint:staticcheck // passing nil is the check.
	var ctx context.Context
	_ = subject.Touch(ctx, key, weight)
}

// AssertStoreTouchHasAnObservableEffect asserts the call changes what Seen observes.
//
// Fails when: Seen reports the same thing before and after
// Touch. The whole content of the mixin is that the effect is out of
// band — Touch says only whether it failed — so a subject that accepted
// the call and did nothing satisfies every other check in this file.
//
// Both observations run against one subject, which is why this cannot be two
// checks: what is compared is the difference, and a fresh subject per check
// would have nothing to differ from.
func AssertStoreTouchHasAnObservableEffect(tb testing.TB, subject partnernaming.Store, key string, weight int) {
	tb.Helper()
	ctx := tb.Context()
	before, beforeErr := subject.Seen(ctx, key)
	testkit.NoError(tb, beforeErr,
		"Seen must be observable before Touch runs")

	err := subject.Touch(ctx, key, weight)
	testkit.NoError(tb, err,
		"Touch must succeed for its effect to be observable")

	after, afterErr := subject.Seen(ctx, key)
	testkit.NoError(tb, afterErr,
		"Seen must still be observable after Touch runs")

	testkit.NotEqual(tb, after, before,
		"Touch must change what Seen observes")
}

// AssertStoreSeenSmoke asserts Seen survives a call with derived inputs.
//
// Fails when: Seen panics. The weakest check in this file and the one
// that catches the most — a method that panics on a derived value is one no
// other check here reaches.
func AssertStoreSeenSmoke(tb testing.TB, subject partnernaming.Store, k string) {
	tb.Helper()
	defer func() {
		if r := recover(); r != nil {
			tb.Fatalf("Seen panicked on a derived value (%v); supply one it "+
				"accepts through StoreWithFixture", r)
		}
	}()
	ctx := tb.Context()
	_, _ = subject.Seen(ctx, k)
}

// AssertStoreSeenCancels asserts Seen reports a cancelled context as cancelled.
//
// Fails when: Seen returns nil for a context cancelled before the
// call, or returns an error that does not answer to context.Canceled. Both
// matter and they are different defects: the first is work done that the caller
// asked not to be, the second is a caller who wrote
// `errors.Is(err, context.Canceled)` and gets false for a call that was.
//
// The error rather than merely its presence is what separates this from the
// deadline check. Asserting only that something came back makes the two one
// check written twice.
func AssertStoreSeenCancels(tb testing.TB, subject partnernaming.Store, k string) {
	tb.Helper()
	ctx, cancel := context.WithCancel(tb.Context())
	cancel()
	_, err := subject.Seen(ctx, k)
	testkit.ErrorIs(tb, err, context.Canceled,
		"Seen must report a cancelled context as context.Canceled")
}

// AssertStoreSeenHonoursDeadline asserts Seen reports an expired deadline as exceeded.
//
// Fails when: Seen returns nil for a context whose deadline has
// passed, or returns an error that does not answer to
// context.DeadlineExceeded. Distinct from cancellation in what the caller
// learns: a cancelled call was called off, an expired one ran out of time, and
// only the second is worth retrying.
//
// The deadline is the zero time, which is unconditionally in the past. No clock
// is read: a generated check that consults the wall clock is one whose subject
// is partly the machine it runs on.
func AssertStoreSeenHonoursDeadline(tb testing.TB, subject partnernaming.Store, k string) {
	tb.Helper()
	ctx, cancel := context.WithDeadline(tb.Context(), time.Time{})
	defer cancel()
	_, err := subject.Seen(ctx, k)
	testkit.ErrorIs(tb, err, context.DeadlineExceeded,
		"Seen must report an expired deadline as context.DeadlineExceeded")
}

// AssertStoreSeenToleratesNilContext asserts Seen does not panic on a nil context.
//
// Fails when: Seen panics. Returning an error is correct and
// succeeding is correct — a nil context reaches production through a caller
// that forgot one, and a panic turns that into an outage rather than a failed
// request.
func AssertStoreSeenToleratesNilContext(tb testing.TB, subject partnernaming.Store, k string) {
	tb.Helper()
	defer func() {
		if r := recover(); r != nil {
			tb.Errorf("Seen panicked on a nil context (%v); return an error instead", r)
		}
	}()
	//nolint:staticcheck // passing nil is the check.
	var ctx context.Context
	_, _ = subject.Seen(ctx, k)
}

// AssertStoreSeenZeroOnError asserts an error is accompanied by the zero value.
//
// Fails when: Seen returns a non-zero result alongside a non-nil
// error. A caller who checks the error and a caller who checks the value must
// not disagree about whether the call succeeded.
//
// Skips when the alternate input succeeds: a total method has no failure this
// run can reach, and a skip says so where a green line would claim a
// comparison that never happened.
func AssertStoreSeenZeroOnError(tb testing.TB, subject partnernaming.Store, k string) {
	tb.Helper()
	ctx := tb.Context()
	r0, r1 := subject.Seen(ctx, k)
	if r1 == nil {
		tb.Skipf("Seen accepted the alternate input; nothing this run " +
			"supplies makes it fail, so the zero-on-error claim goes unexercised")
		return
	}
	{
		var zero int
		testkit.Equal(tb, r0, zero,
			"Seen must return the zero value alongside an error")
	}
}

// AssertStoreMoveSmoke asserts Move survives a call with derived inputs.
//
// Fails when: Move panics. The weakest check in this file and the one
// that catches the most — a method that panics on a derived value is one no
// other check here reaches.
func AssertStoreMoveSmoke(tb testing.TB, subject partnernaming.Store, from string, to string) {
	tb.Helper()
	defer func() {
		if r := recover(); r != nil {
			tb.Fatalf("Move panicked on a derived value (%v); supply one it "+
				"accepts through StoreWithFixture", r)
		}
	}()
	ctx := tb.Context()
	_ = subject.Move(ctx, from, to)
}

// AssertStoreMoveCancels asserts Move reports a cancelled context as cancelled.
//
// Fails when: Move returns nil for a context cancelled before the
// call, or returns an error that does not answer to context.Canceled. Both
// matter and they are different defects: the first is work done that the caller
// asked not to be, the second is a caller who wrote
// `errors.Is(err, context.Canceled)` and gets false for a call that was.
//
// The error rather than merely its presence is what separates this from the
// deadline check. Asserting only that something came back makes the two one
// check written twice.
func AssertStoreMoveCancels(tb testing.TB, subject partnernaming.Store, from string, to string) {
	tb.Helper()
	ctx, cancel := context.WithCancel(tb.Context())
	cancel()
	err := subject.Move(ctx, from, to)
	testkit.ErrorIs(tb, err, context.Canceled,
		"Move must report a cancelled context as context.Canceled")
}

// AssertStoreMoveHonoursDeadline asserts Move reports an expired deadline as exceeded.
//
// Fails when: Move returns nil for a context whose deadline has
// passed, or returns an error that does not answer to
// context.DeadlineExceeded. Distinct from cancellation in what the caller
// learns: a cancelled call was called off, an expired one ran out of time, and
// only the second is worth retrying.
//
// The deadline is the zero time, which is unconditionally in the past. No clock
// is read: a generated check that consults the wall clock is one whose subject
// is partly the machine it runs on.
func AssertStoreMoveHonoursDeadline(tb testing.TB, subject partnernaming.Store, from string, to string) {
	tb.Helper()
	ctx, cancel := context.WithDeadline(tb.Context(), time.Time{})
	defer cancel()
	err := subject.Move(ctx, from, to)
	testkit.ErrorIs(tb, err, context.DeadlineExceeded,
		"Move must report an expired deadline as context.DeadlineExceeded")
}

// AssertStoreMoveToleratesNilContext asserts Move does not panic on a nil context.
//
// Fails when: Move panics. Returning an error is correct and
// succeeding is correct — a nil context reaches production through a caller
// that forgot one, and a panic turns that into an outage rather than a failed
// request.
func AssertStoreMoveToleratesNilContext(tb testing.TB, subject partnernaming.Store, from string, to string) {
	tb.Helper()
	defer func() {
		if r := recover(); r != nil {
			tb.Errorf("Move panicked on a nil context (%v); return an error instead", r)
		}
	}()
	//nolint:staticcheck // passing nil is the check.
	var ctx context.Context
	_ = subject.Move(ctx, from, to)
}

// AssertStoreAtSmoke asserts At survives a call with derived inputs.
//
// Fails when: At panics. The weakest check in this file and the one
// that catches the most — a method that panics on a derived value is one no
// other check here reaches.
func AssertStoreAtSmoke(tb testing.TB, subject partnernaming.Store, where string) {
	tb.Helper()
	defer func() {
		if r := recover(); r != nil {
			tb.Fatalf("At panicked on a derived value (%v); supply one it "+
				"accepts through StoreWithFixture", r)
		}
	}()
	ctx := tb.Context()
	_, _ = subject.At(ctx, where)
}

// AssertStoreAtCancels asserts At reports a cancelled context as cancelled.
//
// Fails when: At returns nil for a context cancelled before the
// call, or returns an error that does not answer to context.Canceled. Both
// matter and they are different defects: the first is work done that the caller
// asked not to be, the second is a caller who wrote
// `errors.Is(err, context.Canceled)` and gets false for a call that was.
//
// The error rather than merely its presence is what separates this from the
// deadline check. Asserting only that something came back makes the two one
// check written twice.
func AssertStoreAtCancels(tb testing.TB, subject partnernaming.Store, where string) {
	tb.Helper()
	ctx, cancel := context.WithCancel(tb.Context())
	cancel()
	_, err := subject.At(ctx, where)
	testkit.ErrorIs(tb, err, context.Canceled,
		"At must report a cancelled context as context.Canceled")
}

// AssertStoreAtHonoursDeadline asserts At reports an expired deadline as exceeded.
//
// Fails when: At returns nil for a context whose deadline has
// passed, or returns an error that does not answer to
// context.DeadlineExceeded. Distinct from cancellation in what the caller
// learns: a cancelled call was called off, an expired one ran out of time, and
// only the second is worth retrying.
//
// The deadline is the zero time, which is unconditionally in the past. No clock
// is read: a generated check that consults the wall clock is one whose subject
// is partly the machine it runs on.
func AssertStoreAtHonoursDeadline(tb testing.TB, subject partnernaming.Store, where string) {
	tb.Helper()
	ctx, cancel := context.WithDeadline(tb.Context(), time.Time{})
	defer cancel()
	_, err := subject.At(ctx, where)
	testkit.ErrorIs(tb, err, context.DeadlineExceeded,
		"At must report an expired deadline as context.DeadlineExceeded")
}

// AssertStoreAtToleratesNilContext asserts At does not panic on a nil context.
//
// Fails when: At panics. Returning an error is correct and
// succeeding is correct — a nil context reaches production through a caller
// that forgot one, and a panic turns that into an outage rather than a failed
// request.
func AssertStoreAtToleratesNilContext(tb testing.TB, subject partnernaming.Store, where string) {
	tb.Helper()
	defer func() {
		if r := recover(); r != nil {
			tb.Errorf("At panicked on a nil context (%v); return an error instead", r)
		}
	}()
	//nolint:staticcheck // passing nil is the check.
	var ctx context.Context
	_, _ = subject.At(ctx, where)
}

// AssertStoreAtZeroOnError asserts an error is accompanied by the zero value.
//
// Fails when: At returns a non-zero result alongside a non-nil
// error. A caller who checks the error and a caller who checks the value must
// not disagree about whether the call succeeded.
//
// Skips when the alternate input succeeds: a total method has no failure this
// run can reach, and a skip says so where a green line would claim a
// comparison that never happened.
func AssertStoreAtZeroOnError(tb testing.TB, subject partnernaming.Store, where string) {
	tb.Helper()
	ctx := tb.Context()
	r0, r1 := subject.At(ctx, where)
	if r1 == nil {
		tb.Skipf("At accepted the alternate input; nothing this run " +
			"supplies makes it fail, so the zero-on-error claim goes unexercised")
		return
	}
	{
		var zero int
		testkit.Equal(tb, r0, zero,
			"At must return the zero value alongside an error")
	}
}

// AssertStoreEmitSmoke asserts Emit survives a call with derived inputs.
//
// Fails when: Emit panics. The weakest check in this file and the one
// that catches the most — a method that panics on a derived value is one no
// other check here reaches.
func AssertStoreEmitSmoke(tb testing.TB, subject partnernaming.Store, id string) {
	tb.Helper()
	defer func() {
		if r := recover(); r != nil {
			tb.Fatalf("Emit panicked on a derived value (%v); supply one it "+
				"accepts through StoreWithFixture", r)
		}
	}()
	ctx := tb.Context()
	_ = subject.Emit(ctx, id)
}

// AssertStoreEmitCancels asserts Emit reports a cancelled context as cancelled.
//
// Fails when: Emit returns nil for a context cancelled before the
// call, or returns an error that does not answer to context.Canceled. Both
// matter and they are different defects: the first is work done that the caller
// asked not to be, the second is a caller who wrote
// `errors.Is(err, context.Canceled)` and gets false for a call that was.
//
// The error rather than merely its presence is what separates this from the
// deadline check. Asserting only that something came back makes the two one
// check written twice.
func AssertStoreEmitCancels(tb testing.TB, subject partnernaming.Store, id string) {
	tb.Helper()
	ctx, cancel := context.WithCancel(tb.Context())
	cancel()
	err := subject.Emit(ctx, id)
	testkit.ErrorIs(tb, err, context.Canceled,
		"Emit must report a cancelled context as context.Canceled")
}

// AssertStoreEmitHonoursDeadline asserts Emit reports an expired deadline as exceeded.
//
// Fails when: Emit returns nil for a context whose deadline has
// passed, or returns an error that does not answer to
// context.DeadlineExceeded. Distinct from cancellation in what the caller
// learns: a cancelled call was called off, an expired one ran out of time, and
// only the second is worth retrying.
//
// The deadline is the zero time, which is unconditionally in the past. No clock
// is read: a generated check that consults the wall clock is one whose subject
// is partly the machine it runs on.
func AssertStoreEmitHonoursDeadline(tb testing.TB, subject partnernaming.Store, id string) {
	tb.Helper()
	ctx, cancel := context.WithDeadline(tb.Context(), time.Time{})
	defer cancel()
	err := subject.Emit(ctx, id)
	testkit.ErrorIs(tb, err, context.DeadlineExceeded,
		"Emit must report an expired deadline as context.DeadlineExceeded")
}

// AssertStoreEmitToleratesNilContext asserts Emit does not panic on a nil context.
//
// Fails when: Emit panics. Returning an error is correct and
// succeeding is correct — a nil context reaches production through a caller
// that forgot one, and a panic turns that into an outage rather than a failed
// request.
func AssertStoreEmitToleratesNilContext(tb testing.TB, subject partnernaming.Store, id string) {
	tb.Helper()
	defer func() {
		if r := recover(); r != nil {
			tb.Errorf("Emit panicked on a nil context (%v); return an error instead", r)
		}
	}()
	//nolint:staticcheck // passing nil is the check.
	var ctx context.Context
	_ = subject.Emit(ctx, id)
}

// AssertStoreCountSmoke asserts Count survives a call with derived inputs.
//
// Fails when: Count panics. The weakest check in this file and the one
// that catches the most — a method that panics on a derived value is one no
// other check here reaches.
func AssertStoreCountSmoke(tb testing.TB, subject partnernaming.Store, bucket int) {
	tb.Helper()
	defer func() {
		if r := recover(); r != nil {
			tb.Fatalf("Count panicked on a derived value (%v); supply one it "+
				"accepts through StoreWithFixture", r)
		}
	}()
	ctx := tb.Context()
	_, _ = subject.Count(ctx, bucket)
}

// AssertStoreCountCancels asserts Count reports a cancelled context as cancelled.
//
// Fails when: Count returns nil for a context cancelled before the
// call, or returns an error that does not answer to context.Canceled. Both
// matter and they are different defects: the first is work done that the caller
// asked not to be, the second is a caller who wrote
// `errors.Is(err, context.Canceled)` and gets false for a call that was.
//
// The error rather than merely its presence is what separates this from the
// deadline check. Asserting only that something came back makes the two one
// check written twice.
func AssertStoreCountCancels(tb testing.TB, subject partnernaming.Store, bucket int) {
	tb.Helper()
	ctx, cancel := context.WithCancel(tb.Context())
	cancel()
	_, err := subject.Count(ctx, bucket)
	testkit.ErrorIs(tb, err, context.Canceled,
		"Count must report a cancelled context as context.Canceled")
}

// AssertStoreCountHonoursDeadline asserts Count reports an expired deadline as exceeded.
//
// Fails when: Count returns nil for a context whose deadline has
// passed, or returns an error that does not answer to
// context.DeadlineExceeded. Distinct from cancellation in what the caller
// learns: a cancelled call was called off, an expired one ran out of time, and
// only the second is worth retrying.
//
// The deadline is the zero time, which is unconditionally in the past. No clock
// is read: a generated check that consults the wall clock is one whose subject
// is partly the machine it runs on.
func AssertStoreCountHonoursDeadline(tb testing.TB, subject partnernaming.Store, bucket int) {
	tb.Helper()
	ctx, cancel := context.WithDeadline(tb.Context(), time.Time{})
	defer cancel()
	_, err := subject.Count(ctx, bucket)
	testkit.ErrorIs(tb, err, context.DeadlineExceeded,
		"Count must report an expired deadline as context.DeadlineExceeded")
}

// AssertStoreCountToleratesNilContext asserts Count does not panic on a nil context.
//
// Fails when: Count panics. Returning an error is correct and
// succeeding is correct — a nil context reaches production through a caller
// that forgot one, and a panic turns that into an outage rather than a failed
// request.
func AssertStoreCountToleratesNilContext(tb testing.TB, subject partnernaming.Store, bucket int) {
	tb.Helper()
	defer func() {
		if r := recover(); r != nil {
			tb.Errorf("Count panicked on a nil context (%v); return an error instead", r)
		}
	}()
	//nolint:staticcheck // passing nil is the check.
	var ctx context.Context
	_, _ = subject.Count(ctx, bucket)
}

// AssertStoreCountZeroOnError asserts an error is accompanied by the zero value.
//
// Fails when: Count returns a non-zero result alongside a non-nil
// error. A caller who checks the error and a caller who checks the value must
// not disagree about whether the call succeeded.
//
// Skips when the alternate input succeeds: a total method has no failure this
// run can reach, and a skip says so where a green line would claim a
// comparison that never happened.
func AssertStoreCountZeroOnError(tb testing.TB, subject partnernaming.Store, bucket int) {
	tb.Helper()
	ctx := tb.Context()
	r0, r1 := subject.Count(ctx, bucket)
	if r1 == nil {
		tb.Skipf("Count accepted the alternate input; nothing this run " +
			"supplies makes it fail, so the zero-on-error claim goes unexercised")
		return
	}
	{
		var zero int
		testkit.Equal(tb, r0, zero,
			"Count must return the zero value alongside an error")
	}
}

// StoreOption configures a contract run.
type StoreOption func(*storeConfig)

// StoreSubject declares an implementation to hold to the contract.
//
// Repeat it for every implementation that claims to satisfy the interface — an
// in-memory one, the one backed by a database, the one behind a network. Every
// check and every option is stated once and runs against all of them, which is
// what a conformance suite is for.
func StoreSubject(name string, factory func() partnernaming.Store) StoreOption {
	return func(c *storeConfig) {
		c.subjects = append(c.subjects, namedStoreSubject{name, factory})
	}
}

// StoreWithoutDouble declines the second run through StoreStub.
//
// The double run proves the stand-in is faithful, and costs a second pass over
// every check. Decline it where the double is not used.
func StoreWithoutDouble() StoreOption {
	return func(c *storeConfig) { c.withoutDouble = true }
}

// StoreWithClock supplies the clock every time-reading check measures on.
//
// Defaults to the real one, so an implementation that does not take a clock
// behaves as it would have. Supply a [clock.TestClock] — the same one the
// factory builds the subject with — and a budget becomes a claim about the time
// the implementation means to spend rather than about how loaded the machine
// was.
func StoreWithClock(c clock.Clock) StoreOption {
	return func(cfg *storeConfig) { cfg.clock = c }
}

// StoreWithFixture replaces the derived inputs.
func StoreWithFixture(f StoreFixture) StoreOption {
	return func(c *storeConfig) { c.Fixture = f }
}

// StoreSeed replaces the derived seed.
//
// For an interface with no writer to seed itself through, which is the one
// input a conformance run cannot derive: a reader over an empty subject
// asserts nothing.
//
// Returning an error rather than swallowing one. A seed that failed quietly
// leaves every check after it asserting nothing and reporting success.
func StoreSeed(fn func(ctx context.Context, subject partnernaming.Store) error) StoreOption {
	return func(c *storeConfig) {
		c.seed = fn
		c.seedIsDerived = false
	}
}

// StoreOnTouch adds a named check to Touch.
func StoreOnTouch(name string, fn StoreTouchCheck) StoreOption {
	return func(c *storeConfig) {
		c.onTouch = append(c.onTouch, namedStoreTouchCheck{name, fn})
	}
}

// StoreOnSeen adds a named check to Seen.
func StoreOnSeen(name string, fn StoreSeenCheck) StoreOption {
	return func(c *storeConfig) {
		c.onSeen = append(c.onSeen, namedStoreSeenCheck{name, fn})
	}
}

// StoreOnMove adds a named check to Move.
func StoreOnMove(name string, fn StoreMoveCheck) StoreOption {
	return func(c *storeConfig) {
		c.onMove = append(c.onMove, namedStoreMoveCheck{name, fn})
	}
}

// StoreOnAt adds a named check to At.
func StoreOnAt(name string, fn StoreAtCheck) StoreOption {
	return func(c *storeConfig) {
		c.onAt = append(c.onAt, namedStoreAtCheck{name, fn})
	}
}

// StoreOnEmit adds a named check to Emit.
func StoreOnEmit(name string, fn StoreEmitCheck) StoreOption {
	return func(c *storeConfig) {
		c.onEmit = append(c.onEmit, namedStoreEmitCheck{name, fn})
	}
}

// StoreOnCount adds a named check to Count.
func StoreOnCount(name string, fn StoreCountCheck) StoreOption {
	return func(c *storeConfig) {
		c.onCount = append(c.onCount, namedStoreCountCheck{name, fn})
	}
}

// StoreWithout drops generated checks by the path each reports under.
//
// For a subject that legitimately violates one. Without this the only recourse
// is to stop running the suite, which costs every other check in the file — so
// a dropped check is reported as skipped rather than silently omitted.
func StoreWithout(paths ...string) StoreOption {
	return func(c *storeConfig) {
		for _, p := range paths {
			c.without[p] = struct{}{}
		}
	}
}

type namedStoreTouchCheck struct {
	name string
	fn   StoreTouchCheck
}

type namedStoreSeenCheck struct {
	name string
	fn   StoreSeenCheck
}

type namedStoreMoveCheck struct {
	name string
	fn   StoreMoveCheck
}

type namedStoreAtCheck struct {
	name string
	fn   StoreAtCheck
}

type namedStoreEmitCheck struct {
	name string
	fn   StoreEmitCheck
}

type namedStoreCountCheck struct {
	name string
	fn   StoreCountCheck
}

type namedStoreSubject struct {
	name    string
	factory func() partnernaming.Store
}

// storeContractExtension is a body of checks a generated sibling adds through an option.
//
// A sibling declares the option and closes over whatever configures it, so
// everything that varies per run is typed and private to the file that owns it
// — this file understands only the name and the call. Nothing arrives except
// through an option, which is what keeps the set fixed before the first subtest
// starts.
type storeContractExtension struct {
	// name is the path this reports under, and the one StoreWithout drops
	// it by. Anything nested beneath it is the extension's own to name.
	name string

	// run takes the factory rather than a subject. One that drives sequences
	// builds a subject per iteration, and one that compares against a reference
	// needs the unseeded subject a factory returns — which
	// storeConfig.subject is not.
	//
	// Called once per subject, in parallel, sharing the config. Anything it
	// mutates has to be built inside the call: state captured when the option
	// was declared is state two subjects write at once.
	run func(
		t *testing.T,
		subject string,
		factory func() partnernaming.Store,
		cfg *storeConfig,
	)
}

type storeConfig struct {
	Fixture       StoreFixture
	subjects      []namedStoreSubject
	withoutDouble bool
	clock         clock.Clock
	seed          func(ctx context.Context, subject partnernaming.Store) error
	// seedIsDerived is what lets a failed seed name the right culprit. The
	// derived seed and a consumer's are the same field, and the advice for one
	// is useless for the other.
	seedIsDerived bool
	without       map[string]struct{}
	extensions    []storeContractExtension
	onTouch       []namedStoreTouchCheck
	onSeen        []namedStoreSeenCheck
	onMove        []namedStoreMoveCheck
	onAt          []namedStoreAtCheck
	onEmit        []namedStoreEmitCheck
	onCount       []namedStoreCountCheck
}

func newStoreConfig(opts ...StoreOption) *storeConfig {
	c := &storeConfig{
		Fixture: DefaultStoreFixture(),
		clock:   clock.RealClock(),
		without: map[string]struct{}{},
	}
	// Derived: Touch is classified writer, so the interface
	// populates itself and a reader has something to read.
	c.seed = func(ctx context.Context, subject partnernaming.Store) error {
		return subject.Touch(ctx, c.Fixture.Key, c.Fixture.Weight)
	}
	c.seedIsDerived = true
	for _, o := range opts {
		o(c)
	}
	return c
}

// subject builds a fresh implementation and seeds it, so every check runs
// against state no other check touched.
func (c *storeConfig) subject(
	tb testing.TB,
	factory func() partnernaming.Store,
	wrap func(tb testing.TB, subject partnernaming.Store) partnernaming.Store,
) partnernaming.Store {
	tb.Helper()
	s := factory()
	if c.seed != nil {
		// Reported rather than ignored. A seed that silently failed would leave
		// every check after it running against an empty subject — passing, and
		// asserting nothing.
		//
		// Which seed ran decides what the message can usefully say. The derived
		// one fails for one of two reasons and both are fixable from here, so it
		// names them. A seed the caller supplied is theirs, and telling them to
		// supply one is advice they already took.
		if err := c.seed(tb.Context(), s); err != nil {
			if c.seedIsDerived {
				tb.Fatalf("seeding through Touch failed: %v; "+
					"supply a value it accepts through StoreWithFixture, or a "+
					"whole seed through StoreSeed", err)
			} else {
				tb.Fatalf("the seed supplied through StoreSeed failed: %v", err)
			}
		}
	}
	if wrap != nil {
		s = wrap(tb, s)
	}
	return s
}

// dropped reports whether a path was declined through StoreWithout.
//
// Named rather than inlined because the drop set is read from two places: here,
// and by whatever registered through the seam — which reports under paths this
// file has never heard of and must answer the same question about them.
func (c *storeConfig) declined(path string) bool {
	_, ok := c.without[path]
	return ok
}

// storePaths is every path this file reports a check under.
//
// The set a StoreWithout argument has to be a member of. Declared
// rather than observed: a check that did not run is not a check that does not
// exist — an integration-only method skips without ever consulting the drop
// set, and reading that as a phantom would fail a correct suite on a machine
// with the environment unset.
//
//nolint:gochecknoglobals // the emitted check paths, read-only after init.
var storePaths = map[string]struct{}{
	"Touch/smoke":                           {},
	"Touch/reports a cancelled context":     {},
	"Touch/reports an expired deadline":     {},
	"Touch/tolerates a nil context":         {},
	"Touch/sideeffect":                      {},
	"Seen/smoke":                            {},
	"Seen/reports a cancelled context":      {},
	"Seen/reports an expired deadline":      {},
	"Seen/tolerates a nil context":          {},
	"Seen/an error carries the zero value":  {},
	"Move/smoke":                            {},
	"Move/reports a cancelled context":      {},
	"Move/reports an expired deadline":      {},
	"Move/tolerates a nil context":          {},
	"At/smoke":                              {},
	"At/reports a cancelled context":        {},
	"At/reports an expired deadline":        {},
	"At/tolerates a nil context":            {},
	"At/an error carries the zero value":    {},
	"Emit/smoke":                            {},
	"Emit/reports a cancelled context":      {},
	"Emit/reports an expired deadline":      {},
	"Emit/tolerates a nil context":          {},
	"Count/smoke":                           {},
	"Count/reports a cancelled context":     {},
	"Count/reports an expired deadline":     {},
	"Count/tolerates a nil context":         {},
	"Count/an error carries the zero value": {},
}

// phantoms names every StoreWithout path that declines nothing.
//
// A drop that drops nothing is this option's silent half: the consumer wrote
// it against a check since renamed, or never emitted for that method at all,
// and every run after reads green while asserting one thing more than its
// author believes. Extensions carry their own names — the seam reports under
// paths this file has never heard of — so they count as known too.
func (c *storeConfig) phantoms() []string {
	known := func(p string) bool {
		if _, ok := storePaths[p]; ok {
			return true
		}
		for _, e := range c.extensions {
			if e.name == p {
				return true
			}
		}
		for _, on := range c.onTouch {
			if "Touch"+"/"+on.name == p {
				return true
			}
		}
		for _, on := range c.onSeen {
			if "Seen"+"/"+on.name == p {
				return true
			}
		}
		for _, on := range c.onMove {
			if "Move"+"/"+on.name == p {
				return true
			}
		}
		for _, on := range c.onAt {
			if "At"+"/"+on.name == p {
				return true
			}
		}
		for _, on := range c.onEmit {
			if "Emit"+"/"+on.name == p {
				return true
			}
		}
		for _, on := range c.onCount {
			if "Count"+"/"+on.name == p {
				return true
			}
		}
		return false
	}
	var out []string
	for p := range c.without {
		if !known(p) {
			out = append(out, p)
		}
	}
	slices.Sort(out)
	return out
}

// run executes one check unless it was dropped, reporting the drop rather than
// silently omitting it.
func (c *storeConfig) run(t *testing.T, path, name string, fn func(tb testing.TB)) {
	t.Helper()
	if c.declined(path) {
		t.Run(name, func(t *testing.T) {
			t.Skipf("dropped through StoreWithout(%q)", path)
		})
		return
	}
	t.Run(name, func(t *testing.T) {
		t.Parallel()
		fn(t)
	})
}

// testkit: end of generated content.
// testkit:provenance f1939b0d9a9118d985e8741b21989f7e44fe3475adaecb850fdc037bb0325c1d
