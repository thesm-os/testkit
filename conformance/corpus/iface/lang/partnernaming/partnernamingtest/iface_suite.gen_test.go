// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/lang/partnernaming/iface.go
// Plugins:   golang 1.0.0, suite 1.16.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/iface/lang/partnernaming/...

package partnernamingtest_test

import (
	"context"
	"errors"
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/iface/lang/partnernaming/partnernamingtest"
)

// Every generated check for Store, driven against a stand-in built
// to violate it.
//
//	Proved:   28 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertStoreTouchSmokeCanFail holds AssertStoreTouchSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertStoreTouchSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := partnernamingtest.DefaultStoreFixture()
	subject := partnernamingtest.NewStoreStub(t,
		partnernamingtest.WithStoreTouch(func(context.Context, string, int) error {
			panic("Store.Touch: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			partnernamingtest.AssertStoreTouchSmoke(tb, subject, fixture.Key, fixture.Weight)
		})
	testkit.Assert(t, got).Contains("Touch panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertStoreTouchCancelsCanFail holds AssertStoreTouchCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertStoreTouchCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := partnernamingtest.DefaultStoreFixture()
	subject := partnernamingtest.NewStoreStub(t,
		partnernamingtest.WithStoreTouch(func(context.Context, string, int) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			partnernamingtest.AssertStoreTouchCancels(tb, subject, fixture.Key, fixture.Weight)
		})
	testkit.Assert(t, got).Contains("Touch must report a cancelled context as context.Canceled",
		"and rejects it for the reason the check is about")
}

// TestAssertStoreTouchHonoursDeadlineCanFail holds AssertStoreTouchHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertStoreTouchHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := partnernamingtest.DefaultStoreFixture()
	subject := partnernamingtest.NewStoreStub(t,
		partnernamingtest.WithStoreTouch(func(context.Context, string, int) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			partnernamingtest.AssertStoreTouchHonoursDeadline(tb, subject, fixture.Key, fixture.Weight)
		})
	testkit.Assert(t, got).Contains("Touch must report an expired deadline as context.DeadlineExceeded",
		"and rejects it for the reason the check is about")
}

// TestAssertStoreTouchToleratesNilContextCanFail holds AssertStoreTouchToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertStoreTouchToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := partnernamingtest.DefaultStoreFixture()
	subject := partnernamingtest.NewStoreStub(t,
		partnernamingtest.WithStoreTouch(func(context.Context, string, int) error {
			panic("Store.Touch: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			partnernamingtest.AssertStoreTouchToleratesNilContext(tb, subject, fixture.Key, fixture.Weight)
		})
	testkit.Assert(t, got).Contains("Touch panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertStoreTouchHasAnObservableEffectCanFail holds AssertStoreTouchHasAnObservableEffect to being able to fail.
//
// The stand-in's observer answers the same thing whichever side of the call it
// is asked on. A check that let that pass would report an effect for a method
// that accepted the call and did nothing.
func TestAssertStoreTouchHasAnObservableEffectCanFail(t *testing.T) {
	t.Parallel()

	fixture := partnernamingtest.DefaultStoreFixture()
	subject := partnernamingtest.NewStoreStub(t,
		partnernamingtest.WithStoreTouch(func(context.Context, string, int) error {
			return nil
		}),
		partnernamingtest.WithStoreSeen(func(context.Context, string) (int, error) {
			var r0 int
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method whose effect nothing can observe",
		func(tb testing.TB) {
			tb.Helper()
			partnernamingtest.AssertStoreTouchHasAnObservableEffect(tb, subject, fixture.Key, fixture.Weight)
		})
	testkit.Assert(t, got).Contains("Touch must change what Seen observes",
		"and rejects it for the reason the check is about")
}

// TestAssertStoreSeenSmokeCanFail holds AssertStoreSeenSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertStoreSeenSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := partnernamingtest.DefaultStoreFixture()
	subject := partnernamingtest.NewStoreStub(t,
		partnernamingtest.WithStoreSeen(func(context.Context, string) (int, error) {
			panic("Store.Seen: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			partnernamingtest.AssertStoreSeenSmoke(tb, subject, fixture.K)
		})
	testkit.Assert(t, got).Contains("Seen panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertStoreSeenCancelsCanFail holds AssertStoreSeenCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertStoreSeenCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := partnernamingtest.DefaultStoreFixture()
	subject := partnernamingtest.NewStoreStub(t,
		partnernamingtest.WithStoreSeen(func(context.Context, string) (int, error) {
			var r0 int
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			partnernamingtest.AssertStoreSeenCancels(tb, subject, fixture.K)
		})
	testkit.Assert(t, got).Contains("Seen must report a cancelled context as context.Canceled",
		"and rejects it for the reason the check is about")
}

// TestAssertStoreSeenHonoursDeadlineCanFail holds AssertStoreSeenHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertStoreSeenHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := partnernamingtest.DefaultStoreFixture()
	subject := partnernamingtest.NewStoreStub(t,
		partnernamingtest.WithStoreSeen(func(context.Context, string) (int, error) {
			var r0 int
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			partnernamingtest.AssertStoreSeenHonoursDeadline(tb, subject, fixture.K)
		})
	testkit.Assert(t, got).Contains("Seen must report an expired deadline as context.DeadlineExceeded",
		"and rejects it for the reason the check is about")
}

// TestAssertStoreSeenToleratesNilContextCanFail holds AssertStoreSeenToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertStoreSeenToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := partnernamingtest.DefaultStoreFixture()
	subject := partnernamingtest.NewStoreStub(t,
		partnernamingtest.WithStoreSeen(func(context.Context, string) (int, error) {
			panic("Store.Seen: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			partnernamingtest.AssertStoreSeenToleratesNilContext(tb, subject, fixture.K)
		})
	testkit.Assert(t, got).Contains("Seen panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertStoreSeenZeroOnErrorCanFail holds AssertStoreSeenZeroOnError to being able to fail.
//
// The stand-in refuses the call and answers a value that looks like a result
// anyway. A check that let that pass would leave a caller who checks the error
// and a caller who checks the value disagreeing about what happened — the
// claim's own violation, not a stand-in that merely succeeds.
func TestAssertStoreSeenZeroOnErrorCanFail(t *testing.T) {
	t.Parallel()

	fixture := partnernamingtest.DefaultStoreFixture()
	subject := partnernamingtest.NewStoreStub(t,
		partnernamingtest.WithStoreSeen(func(context.Context, string) (int, error) {
			return 42, errors.New("violate: refused with a believable value")
		}))

	got := testkit.Rejects(t, "a method answering a believable value beside its error",
		func(tb testing.TB) {
			tb.Helper()
			partnernamingtest.AssertStoreSeenZeroOnError(tb, subject, fixture.KOther)
		})
	testkit.Assert(t, got).Contains("Seen must return the zero value alongside an error",
		"and rejects it for the reason the check is about")
}

// TestAssertStoreMoveSmokeCanFail holds AssertStoreMoveSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertStoreMoveSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := partnernamingtest.DefaultStoreFixture()
	subject := partnernamingtest.NewStoreStub(t,
		partnernamingtest.WithStoreMove(func(context.Context, string, string) error {
			panic("Store.Move: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			partnernamingtest.AssertStoreMoveSmoke(tb, subject, fixture.From, fixture.To)
		})
	testkit.Assert(t, got).Contains("Move panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertStoreMoveCancelsCanFail holds AssertStoreMoveCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertStoreMoveCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := partnernamingtest.DefaultStoreFixture()
	subject := partnernamingtest.NewStoreStub(t,
		partnernamingtest.WithStoreMove(func(context.Context, string, string) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			partnernamingtest.AssertStoreMoveCancels(tb, subject, fixture.From, fixture.To)
		})
	testkit.Assert(t, got).Contains("Move must report a cancelled context as context.Canceled",
		"and rejects it for the reason the check is about")
}

// TestAssertStoreMoveHonoursDeadlineCanFail holds AssertStoreMoveHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertStoreMoveHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := partnernamingtest.DefaultStoreFixture()
	subject := partnernamingtest.NewStoreStub(t,
		partnernamingtest.WithStoreMove(func(context.Context, string, string) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			partnernamingtest.AssertStoreMoveHonoursDeadline(tb, subject, fixture.From, fixture.To)
		})
	testkit.Assert(t, got).Contains("Move must report an expired deadline as context.DeadlineExceeded",
		"and rejects it for the reason the check is about")
}

// TestAssertStoreMoveToleratesNilContextCanFail holds AssertStoreMoveToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertStoreMoveToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := partnernamingtest.DefaultStoreFixture()
	subject := partnernamingtest.NewStoreStub(t,
		partnernamingtest.WithStoreMove(func(context.Context, string, string) error {
			panic("Store.Move: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			partnernamingtest.AssertStoreMoveToleratesNilContext(tb, subject, fixture.From, fixture.To)
		})
	testkit.Assert(t, got).Contains("Move panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertStoreAtSmokeCanFail holds AssertStoreAtSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertStoreAtSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := partnernamingtest.DefaultStoreFixture()
	subject := partnernamingtest.NewStoreStub(t,
		partnernamingtest.WithStoreAt(func(context.Context, string) (int, error) {
			panic("Store.At: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			partnernamingtest.AssertStoreAtSmoke(tb, subject, fixture.Where)
		})
	testkit.Assert(t, got).Contains("At panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertStoreAtCancelsCanFail holds AssertStoreAtCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertStoreAtCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := partnernamingtest.DefaultStoreFixture()
	subject := partnernamingtest.NewStoreStub(t,
		partnernamingtest.WithStoreAt(func(context.Context, string) (int, error) {
			var r0 int
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			partnernamingtest.AssertStoreAtCancels(tb, subject, fixture.Where)
		})
	testkit.Assert(t, got).Contains("At must report a cancelled context as context.Canceled",
		"and rejects it for the reason the check is about")
}

// TestAssertStoreAtHonoursDeadlineCanFail holds AssertStoreAtHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertStoreAtHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := partnernamingtest.DefaultStoreFixture()
	subject := partnernamingtest.NewStoreStub(t,
		partnernamingtest.WithStoreAt(func(context.Context, string) (int, error) {
			var r0 int
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			partnernamingtest.AssertStoreAtHonoursDeadline(tb, subject, fixture.Where)
		})
	testkit.Assert(t, got).Contains("At must report an expired deadline as context.DeadlineExceeded",
		"and rejects it for the reason the check is about")
}

// TestAssertStoreAtToleratesNilContextCanFail holds AssertStoreAtToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertStoreAtToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := partnernamingtest.DefaultStoreFixture()
	subject := partnernamingtest.NewStoreStub(t,
		partnernamingtest.WithStoreAt(func(context.Context, string) (int, error) {
			panic("Store.At: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			partnernamingtest.AssertStoreAtToleratesNilContext(tb, subject, fixture.Where)
		})
	testkit.Assert(t, got).Contains("At panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertStoreAtZeroOnErrorCanFail holds AssertStoreAtZeroOnError to being able to fail.
//
// The stand-in refuses the call and answers a value that looks like a result
// anyway. A check that let that pass would leave a caller who checks the error
// and a caller who checks the value disagreeing about what happened — the
// claim's own violation, not a stand-in that merely succeeds.
func TestAssertStoreAtZeroOnErrorCanFail(t *testing.T) {
	t.Parallel()

	fixture := partnernamingtest.DefaultStoreFixture()
	subject := partnernamingtest.NewStoreStub(t,
		partnernamingtest.WithStoreAt(func(context.Context, string) (int, error) {
			return 42, errors.New("violate: refused with a believable value")
		}))

	got := testkit.Rejects(t, "a method answering a believable value beside its error",
		func(tb testing.TB) {
			tb.Helper()
			partnernamingtest.AssertStoreAtZeroOnError(tb, subject, fixture.WhereOther)
		})
	testkit.Assert(t, got).Contains("At must return the zero value alongside an error",
		"and rejects it for the reason the check is about")
}

// TestAssertStoreEmitSmokeCanFail holds AssertStoreEmitSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertStoreEmitSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := partnernamingtest.DefaultStoreFixture()
	subject := partnernamingtest.NewStoreStub(t,
		partnernamingtest.WithStoreEmit(func(context.Context, string) error {
			panic("Store.Emit: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			partnernamingtest.AssertStoreEmitSmoke(tb, subject, fixture.ID)
		})
	testkit.Assert(t, got).Contains("Emit panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertStoreEmitCancelsCanFail holds AssertStoreEmitCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertStoreEmitCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := partnernamingtest.DefaultStoreFixture()
	subject := partnernamingtest.NewStoreStub(t,
		partnernamingtest.WithStoreEmit(func(context.Context, string) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			partnernamingtest.AssertStoreEmitCancels(tb, subject, fixture.ID)
		})
	testkit.Assert(t, got).Contains("Emit must report a cancelled context as context.Canceled",
		"and rejects it for the reason the check is about")
}

// TestAssertStoreEmitHonoursDeadlineCanFail holds AssertStoreEmitHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertStoreEmitHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := partnernamingtest.DefaultStoreFixture()
	subject := partnernamingtest.NewStoreStub(t,
		partnernamingtest.WithStoreEmit(func(context.Context, string) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			partnernamingtest.AssertStoreEmitHonoursDeadline(tb, subject, fixture.ID)
		})
	testkit.Assert(t, got).Contains("Emit must report an expired deadline as context.DeadlineExceeded",
		"and rejects it for the reason the check is about")
}

// TestAssertStoreEmitToleratesNilContextCanFail holds AssertStoreEmitToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertStoreEmitToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := partnernamingtest.DefaultStoreFixture()
	subject := partnernamingtest.NewStoreStub(t,
		partnernamingtest.WithStoreEmit(func(context.Context, string) error {
			panic("Store.Emit: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			partnernamingtest.AssertStoreEmitToleratesNilContext(tb, subject, fixture.ID)
		})
	testkit.Assert(t, got).Contains("Emit panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertStoreCountSmokeCanFail holds AssertStoreCountSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertStoreCountSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := partnernamingtest.DefaultStoreFixture()
	subject := partnernamingtest.NewStoreStub(t,
		partnernamingtest.WithStoreCount(func(context.Context, int) (int, error) {
			panic("Store.Count: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			partnernamingtest.AssertStoreCountSmoke(tb, subject, fixture.Bucket)
		})
	testkit.Assert(t, got).Contains("Count panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertStoreCountCancelsCanFail holds AssertStoreCountCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertStoreCountCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := partnernamingtest.DefaultStoreFixture()
	subject := partnernamingtest.NewStoreStub(t,
		partnernamingtest.WithStoreCount(func(context.Context, int) (int, error) {
			var r0 int
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			partnernamingtest.AssertStoreCountCancels(tb, subject, fixture.Bucket)
		})
	testkit.Assert(t, got).Contains("Count must report a cancelled context as context.Canceled",
		"and rejects it for the reason the check is about")
}

// TestAssertStoreCountHonoursDeadlineCanFail holds AssertStoreCountHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertStoreCountHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := partnernamingtest.DefaultStoreFixture()
	subject := partnernamingtest.NewStoreStub(t,
		partnernamingtest.WithStoreCount(func(context.Context, int) (int, error) {
			var r0 int
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			partnernamingtest.AssertStoreCountHonoursDeadline(tb, subject, fixture.Bucket)
		})
	testkit.Assert(t, got).Contains("Count must report an expired deadline as context.DeadlineExceeded",
		"and rejects it for the reason the check is about")
}

// TestAssertStoreCountToleratesNilContextCanFail holds AssertStoreCountToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertStoreCountToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := partnernamingtest.DefaultStoreFixture()
	subject := partnernamingtest.NewStoreStub(t,
		partnernamingtest.WithStoreCount(func(context.Context, int) (int, error) {
			panic("Store.Count: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			partnernamingtest.AssertStoreCountToleratesNilContext(tb, subject, fixture.Bucket)
		})
	testkit.Assert(t, got).Contains("Count panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertStoreCountZeroOnErrorCanFail holds AssertStoreCountZeroOnError to being able to fail.
//
// The stand-in refuses the call and answers a value that looks like a result
// anyway. A check that let that pass would leave a caller who checks the error
// and a caller who checks the value disagreeing about what happened — the
// claim's own violation, not a stand-in that merely succeeds.
func TestAssertStoreCountZeroOnErrorCanFail(t *testing.T) {
	t.Parallel()

	fixture := partnernamingtest.DefaultStoreFixture()
	subject := partnernamingtest.NewStoreStub(t,
		partnernamingtest.WithStoreCount(func(context.Context, int) (int, error) {
			return 42, errors.New("violate: refused with a believable value")
		}))

	got := testkit.Rejects(t, "a method answering a believable value beside its error",
		func(tb testing.TB) {
			tb.Helper()
			partnernamingtest.AssertStoreCountZeroOnError(tb, subject, fixture.BucketOther)
		})
	testkit.Assert(t, got).Contains("Count must return the zero value alongside an error",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance f22203428db610820a8150cdcd3b1743e135cce1bb393d4e4a6d9340d1b00371
