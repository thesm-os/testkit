// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/lang/genericbound/iface.go
// Plugins:   golang 1.0.0, suite 1.2.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package genericboundtest_test

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/iface/lang/genericbound"
	"go.thesmos.sh/testkit/conformance/corpus/iface/lang/genericbound/genericboundtest"
)

// Every generated check for Ranked, driven against a stand-in built
// to violate it.
//
//	Proved:   8 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertRankedRankSmokeCanFail holds AssertRankedRankSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertRankedRankSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := genericboundtest.DefaultRankedFixture[int, genericbound.Score]()
	subject := genericboundtest.NewRankedStub[int, genericbound.Score](t,
		genericboundtest.WithRankedRank[int, genericbound.Score](func(context.Context, int) (genericbound.Score, error) {
			panic("Ranked.Rank: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			genericboundtest.AssertRankedRankSmoke[int, genericbound.Score](tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertRankedRankCancelsCanFail holds AssertRankedRankCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertRankedRankCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := genericboundtest.DefaultRankedFixture[int, genericbound.Score]()
	subject := genericboundtest.NewRankedStub[int, genericbound.Score](t,
		genericboundtest.WithRankedRank[int, genericbound.Score](func(context.Context, int) (genericbound.Score, error) {
			var r0 genericbound.Score
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			genericboundtest.AssertRankedRankCancels[int, genericbound.Score](tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertRankedRankHonoursDeadlineCanFail holds AssertRankedRankHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertRankedRankHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := genericboundtest.DefaultRankedFixture[int, genericbound.Score]()
	subject := genericboundtest.NewRankedStub[int, genericbound.Score](t,
		genericboundtest.WithRankedRank[int, genericbound.Score](func(context.Context, int) (genericbound.Score, error) {
			var r0 genericbound.Score
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			genericboundtest.AssertRankedRankHonoursDeadline[int, genericbound.Score](tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertRankedRankToleratesNilContextCanFail holds AssertRankedRankToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertRankedRankToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := genericboundtest.DefaultRankedFixture[int, genericbound.Score]()
	subject := genericboundtest.NewRankedStub[int, genericbound.Score](t,
		genericboundtest.WithRankedRank[int, genericbound.Score](func(context.Context, int) (genericbound.Score, error) {
			panic("Ranked.Rank: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			genericboundtest.AssertRankedRankToleratesNilContext[int, genericbound.Score](tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertRankedResetSmokeCanFail holds AssertRankedResetSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertRankedResetSmokeCanFail(t *testing.T) {
	t.Parallel()

	subject := genericboundtest.NewRankedStub[int, genericbound.Score](t,
		genericboundtest.WithRankedReset[int, genericbound.Score](func(context.Context) error {
			panic("Ranked.Reset: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			genericboundtest.AssertRankedResetSmoke[int, genericbound.Score](tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertRankedResetCancelsCanFail holds AssertRankedResetCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertRankedResetCancelsCanFail(t *testing.T) {
	t.Parallel()

	subject := genericboundtest.NewRankedStub[int, genericbound.Score](t,
		genericboundtest.WithRankedReset[int, genericbound.Score](func(context.Context) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			genericboundtest.AssertRankedResetCancels[int, genericbound.Score](tb, subject)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertRankedResetHonoursDeadlineCanFail holds AssertRankedResetHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertRankedResetHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	subject := genericboundtest.NewRankedStub[int, genericbound.Score](t,
		genericboundtest.WithRankedReset[int, genericbound.Score](func(context.Context) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			genericboundtest.AssertRankedResetHonoursDeadline[int, genericbound.Score](tb, subject)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertRankedResetToleratesNilContextCanFail holds AssertRankedResetToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertRankedResetToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	subject := genericboundtest.NewRankedStub[int, genericbound.Score](t,
		genericboundtest.WithRankedReset[int, genericbound.Score](func(context.Context) error {
			panic("Ranked.Reset: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			genericboundtest.AssertRankedResetToleratesNilContext[int, genericbound.Score](tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance dfcd0a488a52a7373e9fa81216745f41ef63e882ebc3b368501eed698e49a152
