// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/lang/nocontext/iface.go
// Plugins:   golang 1.0.0, suite 1.0.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package nocontexttest_test

import (
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/iface/lang/nocontext/nocontexttest"
)

// Every generated check for Calculator, driven against a stand-in built
// to violate it.
//
//	Proved:   4 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertCalculatorAddSmokeCanFail holds AssertCalculatorAddSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertCalculatorAddSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := nocontexttest.DefaultCalculatorFixture()
	subject := nocontexttest.NewCalculatorStub(t,
		nocontexttest.WithCalculatorAdd(func(int, int) int {
			panic("Calculator.Add: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			nocontexttest.AssertCalculatorAddSmoke(tb, subject, fixture.A, fixture.B)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertCalculatorDivideSmokeCanFail holds AssertCalculatorDivideSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertCalculatorDivideSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := nocontexttest.DefaultCalculatorFixture()
	subject := nocontexttest.NewCalculatorStub(t,
		nocontexttest.WithCalculatorDivide(func(int, int) (int, error) {
			panic("Calculator.Divide: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			nocontexttest.AssertCalculatorDivideSmoke(tb, subject, fixture.A, fixture.B)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertCalculatorDivideZeroOnErrorCanFail holds AssertCalculatorDivideZeroOnError to being able to fail.
//
// The stand-in succeeds for the input the check chose so it would miss. A check
// that let that pass would never reach the comparison it exists to make, and
// would report success without having compared anything.
func TestAssertCalculatorDivideZeroOnErrorCanFail(t *testing.T) {
	t.Parallel()

	fixture := nocontexttest.DefaultCalculatorFixture()
	subject := nocontexttest.NewCalculatorStub(t,
		nocontexttest.WithCalculatorDivide(func(int, int) (int, error) {
			var r0 int
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that succeeds for an input it should miss",
		func(tb testing.TB) {
			tb.Helper()
			nocontexttest.AssertCalculatorDivideZeroOnError(tb, subject, fixture.AOther, fixture.BOther)
		})
	testkit.Assert(t, got).Contains("supply inputs it misses",
		"and rejects it for the reason the check is about")
}

// TestAssertCalculatorResetSmokeCanFail holds AssertCalculatorResetSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertCalculatorResetSmokeCanFail(t *testing.T) {
	t.Parallel()

	subject := nocontexttest.NewCalculatorStub(t,
		nocontexttest.WithCalculatorReset(func() {
			panic("Calculator.Reset: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			nocontexttest.AssertCalculatorResetSmoke(tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance 67323f6f93888807bed325c46617a489baee06cddf199e669161b2f243b0f32d
