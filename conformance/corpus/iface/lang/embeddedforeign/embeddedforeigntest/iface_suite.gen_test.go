// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/lang/embeddedforeign/iface.go
// Plugins:   golang 1.0.0, suite 1.0.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package embeddedforeigntest_test

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/iface/lang/embeddedforeign/embeddedforeigntest"
)

// Every generated check for Stream, driven against a stand-in built
// to violate it.
//
//	Proved:   6 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertStreamReadSmokeCanFail holds AssertStreamReadSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertStreamReadSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := embeddedforeigntest.DefaultStreamFixture()
	subject := embeddedforeigntest.NewStreamStub(t,
		embeddedforeigntest.WithStreamRead(func(context.Context, string) (string, error) {
			panic("Stream.Read: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			embeddedforeigntest.AssertStreamReadSmoke(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertStreamReadCancelsCanFail holds AssertStreamReadCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertStreamReadCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := embeddedforeigntest.DefaultStreamFixture()
	subject := embeddedforeigntest.NewStreamStub(t,
		embeddedforeigntest.WithStreamRead(func(context.Context, string) (string, error) {
			var r0 string
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			embeddedforeigntest.AssertStreamReadCancels(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertStreamReadHonoursDeadlineCanFail holds AssertStreamReadHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertStreamReadHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := embeddedforeigntest.DefaultStreamFixture()
	subject := embeddedforeigntest.NewStreamStub(t,
		embeddedforeigntest.WithStreamRead(func(context.Context, string) (string, error) {
			var r0 string
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			embeddedforeigntest.AssertStreamReadHonoursDeadline(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertStreamReadToleratesNilContextCanFail holds AssertStreamReadToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertStreamReadToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := embeddedforeigntest.DefaultStreamFixture()
	subject := embeddedforeigntest.NewStreamStub(t,
		embeddedforeigntest.WithStreamRead(func(context.Context, string) (string, error) {
			panic("Stream.Read: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			embeddedforeigntest.AssertStreamReadToleratesNilContext(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertStreamReadZeroOnErrorCanFail holds AssertStreamReadZeroOnError to being able to fail.
//
// The stand-in succeeds for the input the check chose so it would miss. A check
// that let that pass would never reach the comparison it exists to make, and
// would report success without having compared anything.
func TestAssertStreamReadZeroOnErrorCanFail(t *testing.T) {
	t.Parallel()

	fixture := embeddedforeigntest.DefaultStreamFixture()
	subject := embeddedforeigntest.NewStreamStub(t,
		embeddedforeigntest.WithStreamRead(func(context.Context, string) (string, error) {
			var r0 string
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that succeeds for an input it should miss",
		func(tb testing.TB) {
			tb.Helper()
			embeddedforeigntest.AssertStreamReadZeroOnError(tb, subject, fixture.KeyOther)
		})
	testkit.Assert(t, got).Contains("supply inputs it misses",
		"and rejects it for the reason the check is about")
}

// TestAssertStreamCloseSmokeCanFail holds AssertStreamCloseSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertStreamCloseSmokeCanFail(t *testing.T) {
	t.Parallel()

	subject := embeddedforeigntest.NewStreamStub(t,
		embeddedforeigntest.WithStreamClose(func() error {
			panic("Stream.Close: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			embeddedforeigntest.AssertStreamCloseSmoke(tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance 836b4bf5c8a6c5d151481bfa8470c1ac45e0f4c6a88c9747125651e1d3b7faa5
