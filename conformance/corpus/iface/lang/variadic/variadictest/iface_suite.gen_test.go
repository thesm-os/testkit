// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/lang/variadic/iface.go
// Plugins:   golang 1.0.0, suite 1.0.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package variadictest_test

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/iface/lang/variadic/variadictest"
)

// Every generated check for Finder, driven against a stand-in built
// to violate it.
//
//	Proved:   11 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertFinderFindSmokeCanFail holds AssertFinderFindSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertFinderFindSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := variadictest.DefaultFinderFixture()
	subject := variadictest.NewFinderStub(t,
		variadictest.WithFinderFind(func(context.Context, ...string) ([]string, error) {
			panic("Finder.Find: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			variadictest.AssertFinderFindSmoke(tb, subject, fixture.Keys)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertFinderFindCancelsCanFail holds AssertFinderFindCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertFinderFindCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := variadictest.DefaultFinderFixture()
	subject := variadictest.NewFinderStub(t,
		variadictest.WithFinderFind(func(context.Context, ...string) ([]string, error) {
			var r0 []string
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			variadictest.AssertFinderFindCancels(tb, subject, fixture.Keys)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertFinderFindHonoursDeadlineCanFail holds AssertFinderFindHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertFinderFindHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := variadictest.DefaultFinderFixture()
	subject := variadictest.NewFinderStub(t,
		variadictest.WithFinderFind(func(context.Context, ...string) ([]string, error) {
			var r0 []string
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			variadictest.AssertFinderFindHonoursDeadline(tb, subject, fixture.Keys)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertFinderFindToleratesNilContextCanFail holds AssertFinderFindToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertFinderFindToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := variadictest.DefaultFinderFixture()
	subject := variadictest.NewFinderStub(t,
		variadictest.WithFinderFind(func(context.Context, ...string) ([]string, error) {
			panic("Finder.Find: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			variadictest.AssertFinderFindToleratesNilContext(tb, subject, fixture.Keys)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertFinderFindZeroOnErrorCanFail holds AssertFinderFindZeroOnError to being able to fail.
//
// The stand-in succeeds for the input the check chose so it would miss. A check
// that let that pass would never reach the comparison it exists to make, and
// would report success without having compared anything.
func TestAssertFinderFindZeroOnErrorCanFail(t *testing.T) {
	t.Parallel()

	fixture := variadictest.DefaultFinderFixture()
	subject := variadictest.NewFinderStub(t,
		variadictest.WithFinderFind(func(context.Context, ...string) ([]string, error) {
			var r0 []string
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that succeeds for an input it should miss",
		func(tb testing.TB) {
			tb.Helper()
			variadictest.AssertFinderFindZeroOnError(tb, subject, fixture.KeysOther)
		})
	testkit.Assert(t, got).Contains("supply inputs it misses",
		"and rejects it for the reason the check is about")
}

// TestAssertFinderFindAnswersPerKeyCanFail holds AssertFinderFindAnswersPerKey to being able to fail.
//
// The stand-in answers once for a batch of two. A check that let that pass
// would report a working batch read for a subject whose results no longer line
// up with the keys that asked for them — and nothing about the value returned
// says so.
func TestAssertFinderFindAnswersPerKeyCanFail(t *testing.T) {
	t.Parallel()

	fixture := variadictest.DefaultFinderFixture()
	subject := variadictest.NewFinderStub(t,
		variadictest.WithFinderFind(func(context.Context, ...string) ([]string, error) {
			return make([]string, 1), nil
		}))

	got := testkit.Rejects(t, "a reader answering once for every batch",
		func(tb testing.TB) {
			tb.Helper()
			variadictest.AssertFinderFindAnswersPerKey(tb, subject, fixture.Keys, fixture.KeysOther)
		})
	testkit.Assert(t, got).Contains("once per key",
		"and rejects it for the reason the check is about")
}

// TestAssertFinderFindWithLimitSmokeCanFail holds AssertFinderFindWithLimitSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertFinderFindWithLimitSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := variadictest.DefaultFinderFixture()
	subject := variadictest.NewFinderStub(t,
		variadictest.WithFinderFindWithLimit(func(context.Context, int, ...string) ([]string, error) {
			panic("Finder.FindWithLimit: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			variadictest.AssertFinderFindWithLimitSmoke(tb, subject, fixture.Limit, fixture.Keys)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertFinderFindWithLimitCancelsCanFail holds AssertFinderFindWithLimitCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertFinderFindWithLimitCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := variadictest.DefaultFinderFixture()
	subject := variadictest.NewFinderStub(t,
		variadictest.WithFinderFindWithLimit(func(context.Context, int, ...string) ([]string, error) {
			var r0 []string
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			variadictest.AssertFinderFindWithLimitCancels(tb, subject, fixture.Limit, fixture.Keys)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertFinderFindWithLimitHonoursDeadlineCanFail holds AssertFinderFindWithLimitHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertFinderFindWithLimitHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := variadictest.DefaultFinderFixture()
	subject := variadictest.NewFinderStub(t,
		variadictest.WithFinderFindWithLimit(func(context.Context, int, ...string) ([]string, error) {
			var r0 []string
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			variadictest.AssertFinderFindWithLimitHonoursDeadline(tb, subject, fixture.Limit, fixture.Keys)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertFinderFindWithLimitToleratesNilContextCanFail holds AssertFinderFindWithLimitToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertFinderFindWithLimitToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := variadictest.DefaultFinderFixture()
	subject := variadictest.NewFinderStub(t,
		variadictest.WithFinderFindWithLimit(func(context.Context, int, ...string) ([]string, error) {
			panic("Finder.FindWithLimit: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			variadictest.AssertFinderFindWithLimitToleratesNilContext(tb, subject, fixture.Limit, fixture.Keys)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertFinderFindWithLimitZeroOnErrorCanFail holds AssertFinderFindWithLimitZeroOnError to being able to fail.
//
// The stand-in succeeds for the input the check chose so it would miss. A check
// that let that pass would never reach the comparison it exists to make, and
// would report success without having compared anything.
func TestAssertFinderFindWithLimitZeroOnErrorCanFail(t *testing.T) {
	t.Parallel()

	fixture := variadictest.DefaultFinderFixture()
	subject := variadictest.NewFinderStub(t,
		variadictest.WithFinderFindWithLimit(func(context.Context, int, ...string) ([]string, error) {
			var r0 []string
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that succeeds for an input it should miss",
		func(tb testing.TB) {
			tb.Helper()
			variadictest.AssertFinderFindWithLimitZeroOnError(tb, subject, fixture.LimitOther, fixture.KeysOther)
		})
	testkit.Assert(t, got).Contains("supply inputs it misses",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance 9950f81f36262919837327828b9d74a246600e2bb1a2fa9e8b8ee5997736cdf4
