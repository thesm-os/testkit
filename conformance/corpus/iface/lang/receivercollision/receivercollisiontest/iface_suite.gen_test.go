// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/lang/receivercollision/iface.go
// Plugins:   golang 1.0.0, suite 1.15.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package receivercollisiontest_test

import (
	"context"
	"errors"
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/iface/lang/receivercollision"
	"go.thesmos.sh/testkit/conformance/corpus/iface/lang/receivercollision/receivercollisiontest"
)

// Every generated check for Store, driven against a stand-in built
// to violate it.
//
//	Proved:   11 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertStorePutSmokeCanFail holds AssertStorePutSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertStorePutSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := receivercollisiontest.DefaultStoreFixture()
	subject := receivercollisiontest.NewStoreStub(t,
		receivercollisiontest.WithStorePut(func(context.Context, receivercollision.Session) error {
			panic("Store.Put: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			receivercollisiontest.AssertStorePutSmoke(tb, subject, fixture.PutS)
		})
	testkit.Assert(t, got).Contains("Put panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertStorePutCancelsCanFail holds AssertStorePutCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertStorePutCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := receivercollisiontest.DefaultStoreFixture()
	subject := receivercollisiontest.NewStoreStub(t,
		receivercollisiontest.WithStorePut(func(context.Context, receivercollision.Session) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			receivercollisiontest.AssertStorePutCancels(tb, subject, fixture.PutS)
		})
	testkit.Assert(t, got).Contains("Put must report a cancelled context as context.Canceled",
		"and rejects it for the reason the check is about")
}

// TestAssertStorePutHonoursDeadlineCanFail holds AssertStorePutHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertStorePutHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := receivercollisiontest.DefaultStoreFixture()
	subject := receivercollisiontest.NewStoreStub(t,
		receivercollisiontest.WithStorePut(func(context.Context, receivercollision.Session) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			receivercollisiontest.AssertStorePutHonoursDeadline(tb, subject, fixture.PutS)
		})
	testkit.Assert(t, got).Contains("Put must report an expired deadline as context.DeadlineExceeded",
		"and rejects it for the reason the check is about")
}

// TestAssertStorePutToleratesNilContextCanFail holds AssertStorePutToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertStorePutToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := receivercollisiontest.DefaultStoreFixture()
	subject := receivercollisiontest.NewStoreStub(t,
		receivercollisiontest.WithStorePut(func(context.Context, receivercollision.Session) error {
			panic("Store.Put: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			receivercollisiontest.AssertStorePutToleratesNilContext(tb, subject, fixture.PutS)
		})
	testkit.Assert(t, got).Contains("Put panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertStoreGetSmokeCanFail holds AssertStoreGetSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertStoreGetSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := receivercollisiontest.DefaultStoreFixture()
	subject := receivercollisiontest.NewStoreStub(t,
		receivercollisiontest.WithStoreGet(func(context.Context, string) (receivercollision.Session, error) {
			panic("Store.Get: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			receivercollisiontest.AssertStoreGetSmoke(tb, subject, fixture.GetS)
		})
	testkit.Assert(t, got).Contains("Get panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertStoreGetCancelsCanFail holds AssertStoreGetCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertStoreGetCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := receivercollisiontest.DefaultStoreFixture()
	subject := receivercollisiontest.NewStoreStub(t,
		receivercollisiontest.WithStoreGet(func(context.Context, string) (receivercollision.Session, error) {
			var r0 receivercollision.Session
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			receivercollisiontest.AssertStoreGetCancels(tb, subject, fixture.GetS)
		})
	testkit.Assert(t, got).Contains("Get must report a cancelled context as context.Canceled",
		"and rejects it for the reason the check is about")
}

// TestAssertStoreGetHonoursDeadlineCanFail holds AssertStoreGetHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertStoreGetHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := receivercollisiontest.DefaultStoreFixture()
	subject := receivercollisiontest.NewStoreStub(t,
		receivercollisiontest.WithStoreGet(func(context.Context, string) (receivercollision.Session, error) {
			var r0 receivercollision.Session
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			receivercollisiontest.AssertStoreGetHonoursDeadline(tb, subject, fixture.GetS)
		})
	testkit.Assert(t, got).Contains("Get must report an expired deadline as context.DeadlineExceeded",
		"and rejects it for the reason the check is about")
}

// TestAssertStoreGetToleratesNilContextCanFail holds AssertStoreGetToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertStoreGetToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := receivercollisiontest.DefaultStoreFixture()
	subject := receivercollisiontest.NewStoreStub(t,
		receivercollisiontest.WithStoreGet(func(context.Context, string) (receivercollision.Session, error) {
			panic("Store.Get: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			receivercollisiontest.AssertStoreGetToleratesNilContext(tb, subject, fixture.GetS)
		})
	testkit.Assert(t, got).Contains("Get panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertStoreGetZeroOnErrorCanFail holds AssertStoreGetZeroOnError to being able to fail.
//
// The stand-in refuses the call and answers a value that looks like a result
// anyway. A check that let that pass would leave a caller who checks the error
// and a caller who checks the value disagreeing about what happened — the
// claim's own violation, not a stand-in that merely succeeds.
func TestAssertStoreGetZeroOnErrorCanFail(t *testing.T) {
	t.Parallel()

	fixture := receivercollisiontest.DefaultStoreFixture()
	subject := receivercollisiontest.NewStoreStub(t,
		receivercollisiontest.WithStoreGet(func(context.Context, string) (receivercollision.Session, error) {
			return receivercollision.Session{ID: "test-r0"}, errors.New("violate: refused with a believable value")
		}))

	got := testkit.Rejects(t, "a method answering a believable value beside its error",
		func(tb testing.TB) {
			tb.Helper()
			receivercollisiontest.AssertStoreGetZeroOnError(tb, subject, fixture.GetSOther)
		})
	testkit.Assert(t, got).Contains("Get must return the zero value alongside an error",
		"and rejects it for the reason the check is about")
}

// TestAssertStoreTouchSmokeCanFail holds AssertStoreTouchSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertStoreTouchSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := receivercollisiontest.DefaultStoreFixture()
	subject := receivercollisiontest.NewStoreStub(t,
		receivercollisiontest.WithStoreTouch(func(context.Context, receivercollision.Session) {
			panic("Store.Touch: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			receivercollisiontest.AssertStoreTouchSmoke(tb, subject, fixture.PutS)
		})
	testkit.Assert(t, got).Contains("Touch panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertStoreTouchToleratesNilContextCanFail holds AssertStoreTouchToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertStoreTouchToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := receivercollisiontest.DefaultStoreFixture()
	subject := receivercollisiontest.NewStoreStub(t,
		receivercollisiontest.WithStoreTouch(func(context.Context, receivercollision.Session) {
			panic("Store.Touch: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			receivercollisiontest.AssertStoreTouchToleratesNilContext(tb, subject, fixture.PutS)
		})
	testkit.Assert(t, got).Contains("Touch panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance aaf1dc111e6c8aeb6fd738d4a45b1fec91f0886efe2d28557b33149fa2e1b775
