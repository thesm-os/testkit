// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/lang/embedded/iface.go
// Plugins:   golang 1.0.0, suite 1.1.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package embeddedtest_test

import (
	"context"
	"errors"
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/iface/lang/embedded/embeddedtest"
)

// Every generated check for Base, driven against a stand-in built
// to violate it.
//
//	Proved:   4 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertBasePingSmokeCanFail holds AssertBasePingSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertBasePingSmokeCanFail(t *testing.T) {
	t.Parallel()

	subject := embeddedtest.NewBaseStub(t,
		embeddedtest.WithBasePing(func(context.Context) error {
			panic("Base.Ping: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			embeddedtest.AssertBasePingSmoke(tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertBasePingCancelsCanFail holds AssertBasePingCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertBasePingCancelsCanFail(t *testing.T) {
	t.Parallel()

	subject := embeddedtest.NewBaseStub(t,
		embeddedtest.WithBasePing(func(context.Context) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			embeddedtest.AssertBasePingCancels(tb, subject)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertBasePingHonoursDeadlineCanFail holds AssertBasePingHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertBasePingHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	subject := embeddedtest.NewBaseStub(t,
		embeddedtest.WithBasePing(func(context.Context) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			embeddedtest.AssertBasePingHonoursDeadline(tb, subject)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertBasePingToleratesNilContextCanFail holds AssertBasePingToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertBasePingToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	subject := embeddedtest.NewBaseStub(t,
		embeddedtest.WithBasePing(func(context.Context) error {
			panic("Base.Ping: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			embeddedtest.AssertBasePingToleratesNilContext(tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// Every generated check for Closer, driven against a stand-in built
// to violate it.
//
//	Proved:   4 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertCloserCloseSmokeCanFail holds AssertCloserCloseSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertCloserCloseSmokeCanFail(t *testing.T) {
	t.Parallel()

	subject := embeddedtest.NewCloserStub(t,
		embeddedtest.WithCloserClose(func(context.Context) error {
			panic("Closer.Close: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			embeddedtest.AssertCloserCloseSmoke(tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertCloserCloseCancelsCanFail holds AssertCloserCloseCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertCloserCloseCancelsCanFail(t *testing.T) {
	t.Parallel()

	subject := embeddedtest.NewCloserStub(t,
		embeddedtest.WithCloserClose(func(context.Context) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			embeddedtest.AssertCloserCloseCancels(tb, subject)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertCloserCloseHonoursDeadlineCanFail holds AssertCloserCloseHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertCloserCloseHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	subject := embeddedtest.NewCloserStub(t,
		embeddedtest.WithCloserClose(func(context.Context) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			embeddedtest.AssertCloserCloseHonoursDeadline(tb, subject)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertCloserCloseToleratesNilContextCanFail holds AssertCloserCloseToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertCloserCloseToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	subject := embeddedtest.NewCloserStub(t,
		embeddedtest.WithCloserClose(func(context.Context) error {
			panic("Closer.Close: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			embeddedtest.AssertCloserCloseToleratesNilContext(tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// Every generated check for Composed, driven against a stand-in built
// to violate it.
//
//	Proved:   13 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertComposedGetSmokeCanFail holds AssertComposedGetSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertComposedGetSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := embeddedtest.DefaultComposedFixture()
	subject := embeddedtest.NewComposedStub(t,
		embeddedtest.WithComposedGet(func(context.Context, string) (string, error) {
			panic("Composed.Get: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			embeddedtest.AssertComposedGetSmoke(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertComposedGetCancelsCanFail holds AssertComposedGetCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertComposedGetCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := embeddedtest.DefaultComposedFixture()
	subject := embeddedtest.NewComposedStub(t,
		embeddedtest.WithComposedGet(func(context.Context, string) (string, error) {
			var r0 string
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			embeddedtest.AssertComposedGetCancels(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertComposedGetHonoursDeadlineCanFail holds AssertComposedGetHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertComposedGetHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := embeddedtest.DefaultComposedFixture()
	subject := embeddedtest.NewComposedStub(t,
		embeddedtest.WithComposedGet(func(context.Context, string) (string, error) {
			var r0 string
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			embeddedtest.AssertComposedGetHonoursDeadline(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertComposedGetToleratesNilContextCanFail holds AssertComposedGetToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertComposedGetToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := embeddedtest.DefaultComposedFixture()
	subject := embeddedtest.NewComposedStub(t,
		embeddedtest.WithComposedGet(func(context.Context, string) (string, error) {
			panic("Composed.Get: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			embeddedtest.AssertComposedGetToleratesNilContext(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertComposedGetZeroOnErrorCanFail holds AssertComposedGetZeroOnError to being able to fail.
//
// The stand-in refuses the call and answers a value that looks like a result
// anyway. A check that let that pass would leave a caller who checks the error
// and a caller who checks the value disagreeing about what happened — the
// claim's own violation, not a stand-in that merely succeeds.
func TestAssertComposedGetZeroOnErrorCanFail(t *testing.T) {
	t.Parallel()

	fixture := embeddedtest.DefaultComposedFixture()
	subject := embeddedtest.NewComposedStub(t,
		embeddedtest.WithComposedGet(func(context.Context, string) (string, error) {
			return "test-r0", errors.New("violate: refused with a believable value")
		}))

	got := testkit.Rejects(t, "a method answering a believable value beside its error",
		func(tb testing.TB) {
			tb.Helper()
			embeddedtest.AssertComposedGetZeroOnError(tb, subject, fixture.KeyOther)
		})
	testkit.Assert(t, got).Contains("must return the zero value",
		"and rejects it for the reason the check is about")
}

// TestAssertComposedPingSmokeCanFail holds AssertComposedPingSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertComposedPingSmokeCanFail(t *testing.T) {
	t.Parallel()

	subject := embeddedtest.NewComposedStub(t,
		embeddedtest.WithComposedPing(func(context.Context) error {
			panic("Composed.Ping: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			embeddedtest.AssertComposedPingSmoke(tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertComposedPingCancelsCanFail holds AssertComposedPingCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertComposedPingCancelsCanFail(t *testing.T) {
	t.Parallel()

	subject := embeddedtest.NewComposedStub(t,
		embeddedtest.WithComposedPing(func(context.Context) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			embeddedtest.AssertComposedPingCancels(tb, subject)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertComposedPingHonoursDeadlineCanFail holds AssertComposedPingHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertComposedPingHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	subject := embeddedtest.NewComposedStub(t,
		embeddedtest.WithComposedPing(func(context.Context) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			embeddedtest.AssertComposedPingHonoursDeadline(tb, subject)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertComposedPingToleratesNilContextCanFail holds AssertComposedPingToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertComposedPingToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	subject := embeddedtest.NewComposedStub(t,
		embeddedtest.WithComposedPing(func(context.Context) error {
			panic("Composed.Ping: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			embeddedtest.AssertComposedPingToleratesNilContext(tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertComposedCloseSmokeCanFail holds AssertComposedCloseSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertComposedCloseSmokeCanFail(t *testing.T) {
	t.Parallel()

	subject := embeddedtest.NewComposedStub(t,
		embeddedtest.WithComposedClose(func(context.Context) error {
			panic("Composed.Close: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			embeddedtest.AssertComposedCloseSmoke(tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertComposedCloseCancelsCanFail holds AssertComposedCloseCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertComposedCloseCancelsCanFail(t *testing.T) {
	t.Parallel()

	subject := embeddedtest.NewComposedStub(t,
		embeddedtest.WithComposedClose(func(context.Context) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			embeddedtest.AssertComposedCloseCancels(tb, subject)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertComposedCloseHonoursDeadlineCanFail holds AssertComposedCloseHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertComposedCloseHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	subject := embeddedtest.NewComposedStub(t,
		embeddedtest.WithComposedClose(func(context.Context) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			embeddedtest.AssertComposedCloseHonoursDeadline(tb, subject)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertComposedCloseToleratesNilContextCanFail holds AssertComposedCloseToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertComposedCloseToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	subject := embeddedtest.NewComposedStub(t,
		embeddedtest.WithComposedClose(func(context.Context) error {
			panic("Composed.Close: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			embeddedtest.AssertComposedCloseToleratesNilContext(tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance 539e3d805d9c8fbcc2fd3f554e9b67e3e24fa974fa99d73c389d395a53fd453f
