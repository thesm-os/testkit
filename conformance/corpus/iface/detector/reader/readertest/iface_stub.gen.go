// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/detector/reader/iface.go
// Plugins:   golang 1.0.0, stub 1.0.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package readertest

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/reader"
	"go.thesmos.sh/testkit/rand"
	"go.thesmos.sh/testkit/stub"
)

// ReaderGetCall records one invocation of Reader.Get.
//
// Fields take their names from the source signature — parameters and named
// returns alike — so a failure message names what the author named. A slot
// the source left unnamed or blank falls back to a positional name.
type ReaderGetCall struct {
	Ctx    context.Context
	Key    string
	Result reader.Value
	Err    error
}

// --- Per-method configuration ---

// ReaderGetStub controls how the double answers Get and records
// what it was asked.
//
// The embedded MethodStub supplies the machinery every method shares: call
// recording, fault injection, latency against a virtual clock, gates,
// call-count expectations, and strict mode.
type ReaderGetStub struct {
	*stub.MethodStub[ReaderGetCall]

	fn       func(context.Context, string) (reader.Value, error)
	fallback *ReaderGetReturn
}

// ReaderGetReturn holds the fixed answer configured through Returns.
type ReaderGetReturn struct {
	Result reader.Value
	Err    error
}

// Returns pins a fixed result for every call to Get. A Func
// override and an injected fault both take precedence over it.
func (s *ReaderGetStub) Returns(result reader.Value, err error) *ReaderGetStub {
	s.fallback = &ReaderGetReturn{Result: result, Err: err}
	return s
}

// Func supplies a body for Get, for when the answer depends on the
// arguments. An injected fault still takes precedence.
func (s *ReaderGetStub) Func(fn func(context.Context, string) (reader.Value, error)) *ReaderGetStub {
	s.fn = fn
	return s
}

// --- ReaderStub ---

// ReaderStubOption configures a [ReaderStub] at construction time.
type ReaderStubOption func(*ReaderStub)

// ReaderStubStrict makes an unconfigured method fail the test rather than
// return its zero value, which turns a call nobody planned for into a failure
// at the call site instead of a puzzling zero further downstream.
func ReaderStubStrict() ReaderStubOption {
	return func(s *ReaderStub) { s.strict = true }
}

// ReaderStubDelegateTo wraps a real implementation: every method forwards to
// impl and is recorded on the double on the way through.
//
// This is what lets one generated suite run against both the double and the
// production type, which is the point of conformance testing.
func ReaderStubDelegateTo(impl reader.Reader) ReaderStubOption {
	return func(s *ReaderStub) {
		s.OnGet.Func(impl.Get)
	}
}

// ReaderStubWithClock drives latency and time-windowed faults from clk
// rather than from wall time, so a test asserting on a five-second timeout
// does not take five seconds.
func ReaderStubWithClock(clk clock.Clock) ReaderStubOption {
	return func(s *ReaderStub) {
		for _, m := range s.all {
			m.SetClock(clk)
		}
	}
}

// ReaderStubWithRandSource makes probabilistic fault injection reproducible.
// A failure nobody can replay is not a finding.
func ReaderStubWithRandSource(src rand.Source) ReaderStubOption {
	return func(s *ReaderStub) {
		for _, m := range s.all {
			m.SetRandSource(src)
		}
	}
}

// ReaderStubBenchMode disables call recording. Dispatch still works; only
// the accumulating call log is dropped, because its allocations are what a
// benchmark would otherwise be measuring.
func ReaderStubBenchMode() ReaderStubOption {
	return func(s *ReaderStub) {
		for _, m := range s.all {
			m.BenchMode()
		}
	}
}

// WithReaderGet sets Get's body at construction
// time, for the common case of configuring one method and taking the
// defaults for the rest.
func WithReaderGet(fn func(context.Context, string) (reader.Value, error)) ReaderStubOption {
	return func(s *ReaderStub) { s.OnGet.Func(fn) }
}

// ReaderStub is a recording test double for Reader.
//
// Each On<Method> field is that method's configuration point. Left alone, a
// method returns its zero value and records the call.
type ReaderStub struct {
	OnGet *ReaderGetStub

	// all is every method stub above, viewed through the surface that does
	// not depend on a signature. It is what lets a setting apply to the whole
	// double in a loop rather than in one line per method.
	all    []stub.Configurable
	strict bool
}

// NewReaderStub returns a double bound to tb.
//
// Passing tb registers a cleanup that verifies every method's call-count
// expectations when the test ends, so an unmet Times is reported without the
// caller remembering to check. A nil tb skips that, which is what benchmarks
// and non-test callers want.
func NewReaderStub(tb testing.TB, opts ...ReaderStubOption) *ReaderStub {
	s := &ReaderStub{
		OnGet: &ReaderGetStub{MethodStub: stub.NewMethodStub[ReaderGetCall](tb, "Reader.Get")},
	}
	s.all = []stub.Configurable{
		s.OnGet.MethodStub,
	}
	for _, opt := range opts {
		opt(s)
	}
	if s.strict {
		for _, m := range s.all {
			m.Strict()
		}
	}
	if tb != nil {
		// A fuzz target reruns its body many times over and registers a
		// cleanup per run, so verifying there would report against the wrong
		// iteration.
		if _, isFuzz := tb.(*testing.F); !isFuzz {
			tb.Cleanup(func() {
				for _, m := range s.all {
					m.Verify()
				}
			})
		}
	}
	return s
}

// ResetCalls clears recorded calls, fault counters, and call-count
// expectations. Func and Returns configuration is deliberately preserved, so
// a test can reuse one double across phases without rebuilding it.
func (s *ReaderStub) ResetCalls() {
	for _, m := range s.all {
		m.Reset()
	}
}

// --- Interface methods ---

// invoke adapts the Func override to the shape [stub.Answer] consumes, or
// returns nil when no override is set — which is how Answer tells "no
// override" from "an override that returns zero".
func (s *ReaderGetStub) invoke(ctx context.Context, key string) func() ReaderGetReturn {
	if s.fn == nil {
		return nil
	}
	return func() ReaderGetReturn {
		r0, r1 := s.fn(ctx, key)
		return ReaderGetReturn{Result: r0, Err: r1}
	}
}

// Get records the call and answers it.
//
// Which arm answers — injected fault, Func override, Returns fallback, or the
// zero value — is [stub.Answer]'s to decide, so every generated double
// resolves a call the same way and the ordering is tested once rather than
// restated per method.
func (s *ReaderStub) Get(ctx context.Context, key string) (reader.Value, error) {
	call := ReaderGetCall{Ctx: ctx, Key: key}
	r := stub.Answer(s.OnGet.MethodStub, &call, stub.Arms[ReaderGetCall, ReaderGetReturn]{
		Invoke:   s.OnGet.invoke(ctx, key),
		Fallback: s.OnGet.fallback,
		Fault:    func(err error) ReaderGetReturn { return ReaderGetReturn{Err: err} },
		Stamp: func(c *ReaderGetCall, r ReaderGetReturn) {
			c.Result = r.Result
			c.Err = r.Err
		},
	})
	return r.Result, r.Err
}

// testkit: end of generated content.
// testkit:provenance 0eb2c9234297c5b9eee798897a31c8fc5ca1f8f595ded8a34950f3f9c939adae
