// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/detector/readernoerror/iface.go
// Plugins:   golang 1.0.0, suite 1.0.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package readernoerrortest

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/readernoerror"
)

// ReaderNoErrorLookupCheck is one assertion about Lookup.
//
// Every generated check for Lookup is a value of it, and so is one you
// write — so they compose, reorder, and each runs standalone. testing.TB rather
// than *testing.T is what lets a stand-in drive it and prove it can fail.
type ReaderNoErrorLookupCheck func(tb testing.TB, subject readernoerror.ReaderNoError, key string)

// ReaderNoErrorFixture holds every input the generated checks run against.
//
// Derived from each method's parameter names and types. Every field has a
// companion holding a second, different value: a check comparing a result
// against a single input passes whenever the subject happened to be seeded with
// it, and a miss check whose key happens to hit asserts nothing.
//
// A field whose type admits no literal — a func, a channel, a type from a
// package this run never read — is declared and left at its zero value, and
// every generated check that needed it was dropped rather than emitted against
// a value nobody could write. Supply one through ReaderNoErrorWithFixture
// and the check a consumer writes has something to use.
type ReaderNoErrorFixture struct {
	Key      string
	KeyOther string
}

// DefaultReaderNoErrorFixture is what this run derived. Replace any field through
// ReaderNoErrorWithFixture; the rest keep these values.
func DefaultReaderNoErrorFixture() ReaderNoErrorFixture {
	return ReaderNoErrorFixture{
		Key:      "test-key",
		KeyOther: "other-key",
	}
}

// AssertReaderNoErrorContract runs every generated check against every declared subject.
//
//	Checks:   3 across 1 method, per subject
//	Subjects: declare each with ReaderNoErrorSubject
//	Double:   every subject runs a second time wrapped in ReaderNoErrorStub, so
//	          anything the wrapper fails that the subject passes is the double
//	          lying. ReaderNoErrorWithoutDouble declines it.
//	Extend:   ReaderNoErrorOnLookup
//	Drop:     ReaderNoErrorWithout, by the path each check reports under
func AssertReaderNoErrorContract(t *testing.T, opts ...ReaderNoErrorOption) {
	t.Helper()
	cfg := newReaderNoErrorConfig(opts...)
	if len(cfg.subjects) == 0 {
		t.Fatalf("AssertReaderNoErrorContract was given no subject; declare one with " +
			"ReaderNoErrorSubject(name, factory)")
	}
	for _, s := range cfg.subjects {
		runReaderNoErrorChecks(t, cfg, s.name, s.factory, nil)
		if !cfg.withoutDouble {
			runReaderNoErrorChecks(t, cfg, s.name+" through the double", s.factory,
				func(tb testing.TB, subject readernoerror.ReaderNoError) readernoerror.ReaderNoError {
					return NewReaderNoErrorStub(tb, ReaderNoErrorStubDelegateTo(subject))
				})
		}
	}
}

// runReaderNoErrorChecks runs the whole contract against one subject.
//
// wrap is applied after the subject is built and seeded, so a wrapper is
// transparent to the seed and every failure it causes is its own.
func runReaderNoErrorChecks(
	t *testing.T,
	cfg *readernoerrorConfig,
	label string,
	factory func() readernoerror.ReaderNoError,
	wrap func(tb testing.TB, subject readernoerror.ReaderNoError) readernoerror.ReaderNoError,
) {
	t.Helper()
	t.Run(label, func(t *testing.T) {
		t.Parallel()

		t.Run("Lookup", func(t *testing.T) {
			t.Parallel()
			cfg.run(t, "Lookup/smoke", "smoke", func(tb testing.TB) {
				AssertReaderNoErrorLookupSmoke(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.Key)
			})
			cfg.run(t, "Lookup/tolerates a nil context", "tolerates a nil context", func(tb testing.TB) {
				AssertReaderNoErrorLookupToleratesNilContext(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.Key)
			})
			cfg.run(t, "Lookup/reports a miss", "reports a miss", func(tb testing.TB) {
				AssertReaderNoErrorLookupReportsAMiss(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.KeyOther)
			})
			for _, c := range cfg.onLookup {
				cfg.run(t, "Lookup"+"/"+c.name, c.name, func(tb testing.TB) {
					c.fn(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.Key)
				})
			}
		})
	})
}

// AssertReaderNoErrorLookupSmoke asserts Lookup survives a call with derived inputs.
//
// Fails when: Lookup panics. The weakest check in this file and the one
// that catches the most — a method that panics on a derived value is one no
// other check here reaches.
func AssertReaderNoErrorLookupSmoke(tb testing.TB, subject readernoerror.ReaderNoError, key string) {
	tb.Helper()
	defer func() {
		if r := recover(); r != nil {
			tb.Fatalf("Lookup panicked on a derived value (%v); supply one it "+
				"accepts through ReaderNoErrorWithFixture", r)
		}
	}()
	ctx := tb.Context()
	_ = subject.Lookup(ctx, key)
}

// AssertReaderNoErrorLookupToleratesNilContext asserts Lookup does not panic on a nil context.
//
// Fails when: Lookup panics. Returning an error is correct and
// succeeding is correct — a nil context reaches production through a caller
// that forgot one, and a panic turns that into an outage rather than a failed
// request.
func AssertReaderNoErrorLookupToleratesNilContext(tb testing.TB, subject readernoerror.ReaderNoError, key string) {
	tb.Helper()
	defer func() {
		if r := recover(); r != nil {
			tb.Errorf("Lookup panicked on a nil context (%v); return an error instead", r)
		}
	}()
	//nolint:staticcheck // passing nil is the check.
	var ctx context.Context
	_ = subject.Lookup(ctx, key)
}

// AssertReaderNoErrorLookupReportsAMiss asserts an absent input yields the zero value.
//
// Fails when: Lookup returns anything else for an input derived to be
// absent. With no error and no flag, the zero is the only signal this shape
// has — so a subject returning a plausible value for a key it does not hold is
// indistinguishable from one that found it.
func AssertReaderNoErrorLookupReportsAMiss(tb testing.TB, subject readernoerror.ReaderNoError, key string) {
	tb.Helper()
	ctx := tb.Context()
	r0 := subject.Lookup(ctx, key)
	{
		var zero readernoerror.Value
		testkit.Equal(tb, r0, zero,
			"Lookup must return the zero value for an input it does not hold")
	}
}

// ReaderNoErrorOption configures a contract run.
type ReaderNoErrorOption func(*readernoerrorConfig)

// ReaderNoErrorSubject declares an implementation to hold to the contract.
//
// Repeat it for every implementation that claims to satisfy the interface — an
// in-memory one, the one backed by a database, the one behind a network. Every
// check and every option is stated once and runs against all of them, which is
// what a conformance suite is for.
func ReaderNoErrorSubject(name string, factory func() readernoerror.ReaderNoError) ReaderNoErrorOption {
	return func(c *readernoerrorConfig) {
		c.subjects = append(c.subjects, namedReaderNoErrorSubject{name, factory})
	}
}

// ReaderNoErrorWithoutDouble declines the second run through ReaderNoErrorStub.
//
// The double run proves the stand-in is faithful, and costs a second pass over
// every check. Decline it where the double is not used.
func ReaderNoErrorWithoutDouble() ReaderNoErrorOption {
	return func(c *readernoerrorConfig) { c.withoutDouble = true }
}

// ReaderNoErrorWithClock supplies the clock every time-reading check measures on.
//
// Defaults to the real one, so an implementation that does not take a clock
// behaves as it would have. Supply a [clock.TestClock] — the same one the
// factory builds the subject with — and a budget becomes a claim about the time
// the implementation means to spend rather than about how loaded the machine
// was.
func ReaderNoErrorWithClock(c clock.Clock) ReaderNoErrorOption {
	return func(cfg *readernoerrorConfig) { cfg.clock = c }
}

// ReaderNoErrorWithFixture replaces the derived inputs.
func ReaderNoErrorWithFixture(f ReaderNoErrorFixture) ReaderNoErrorOption {
	return func(c *readernoerrorConfig) { c.Fixture = f }
}

// ReaderNoErrorSeed replaces the derived seed.
//
// For an interface with no writer to seed itself through, which is the one
// input a conformance run cannot derive: a reader over an empty subject
// asserts nothing.
//
// Returning an error rather than swallowing one. A seed that failed quietly
// leaves every check after it asserting nothing and reporting success.
func ReaderNoErrorSeed(fn func(ctx context.Context, subject readernoerror.ReaderNoError) error) ReaderNoErrorOption {
	return func(c *readernoerrorConfig) {
		c.seed = fn
	}
}

// ReaderNoErrorOnLookup adds a named check to Lookup.
func ReaderNoErrorOnLookup(name string, fn ReaderNoErrorLookupCheck) ReaderNoErrorOption {
	return func(c *readernoerrorConfig) {
		c.onLookup = append(c.onLookup, namedReaderNoErrorLookupCheck{name, fn})
	}
}

// ReaderNoErrorWithout drops generated checks by the path each reports under.
//
// For a subject that legitimately violates one. Without this the only recourse
// is to stop running the suite, which costs every other check in the file — so
// a dropped check is reported as skipped rather than silently omitted.
func ReaderNoErrorWithout(paths ...string) ReaderNoErrorOption {
	return func(c *readernoerrorConfig) {
		for _, p := range paths {
			c.without[p] = struct{}{}
		}
	}
}

type namedReaderNoErrorLookupCheck struct {
	name string
	fn   ReaderNoErrorLookupCheck
}

type namedReaderNoErrorSubject struct {
	name    string
	factory func() readernoerror.ReaderNoError
}

type readernoerrorConfig struct {
	Fixture       ReaderNoErrorFixture
	subjects      []namedReaderNoErrorSubject
	withoutDouble bool
	clock         clock.Clock
	seed          func(ctx context.Context, subject readernoerror.ReaderNoError) error
	without       map[string]struct{}
	onLookup      []namedReaderNoErrorLookupCheck
}

func newReaderNoErrorConfig(opts ...ReaderNoErrorOption) *readernoerrorConfig {
	c := &readernoerrorConfig{
		Fixture: DefaultReaderNoErrorFixture(),
		clock:   clock.RealClock(),
		without: map[string]struct{}{},
	}
	for _, o := range opts {
		o(c)
	}
	return c
}

// subject builds a fresh implementation and seeds it, so every check runs
// against state no other check touched.
func (c *readernoerrorConfig) subject(
	tb testing.TB,
	factory func() readernoerror.ReaderNoError,
	wrap func(tb testing.TB, subject readernoerror.ReaderNoError) readernoerror.ReaderNoError,
) readernoerror.ReaderNoError {
	tb.Helper()
	s := factory()
	if c.seed != nil {
		// Reported rather than ignored. A seed that silently failed would leave
		// every check after it running against an empty subject — passing, and
		// asserting nothing.
		//
		// Which seed ran decides what the message can usefully say. The derived
		// one fails for one of two reasons and both are fixable from here, so it
		// names them. A seed the caller supplied is theirs, and telling them to
		// supply one is advice they already took.
		if err := c.seed(tb.Context(), s); err != nil {
			tb.Fatalf("the seed supplied through ReaderNoErrorSeed failed: %v", err)
		}
	}
	if wrap != nil {
		s = wrap(tb, s)
	}
	return s
}

// run executes one check unless it was dropped, reporting the drop rather than
// silently omitting it.
func (c *readernoerrorConfig) run(t *testing.T, path, name string, fn func(tb testing.TB)) {
	t.Helper()
	if _, dropped := c.without[path]; dropped {
		t.Run(name, func(t *testing.T) {
			t.Skipf("dropped through ReaderNoErrorWithout(%q)", path)
		})
		return
	}
	t.Run(name, func(t *testing.T) {
		t.Parallel()
		fn(t)
	})
}

// testkit: end of generated content.
// testkit:provenance 0d07c081862bf47cbdd95018a95dd826d43913378ee2cd227e41da992c82e3a2
