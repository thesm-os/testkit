// Code generated by testkit. DO NOT EDIT.
//
// Source:    conformance/corpus/iface/detector/readernoerror/iface.go
// Plugins:   golang 1.0.0, suite 1.17.0, backend.golang 1.0.0
// Command:   testkit run ./conformance/corpus/...

package readernoerrortest

import (
	"context"
	"fmt"
	"testing"

	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/readernoerror"
	"go.thesmos.sh/testkit/engine/suite"
	"go.thesmos.sh/testkit/engine/suite/prove"
)

// The generated conformance surface for ReaderNoError.
//
// A file-level comment rather than a package doc: three generators write
// into this package and a package doc has one owner, so the listing
// lives with the witness it describes.
//
// Entry points:
//
//	RunReaderNoError(t, opts...)      ProveReaderNoError(t, checks)
//
// A harness is a config literal (ReaderNoErrorHarness); your own checks are a
// table (ReaderNoErrorChecks); drops are written against the typed index:
//
//	ReaderNoErrorSuite.Checks.<Method>.<Check>()
//
// Every ID this package emits:
//
//	Lookup/smoke
//
// Reached by a rule and not derivable here. Each is a claim this file
// does NOT make, so a reader counting coverage from the list above
// knows what is missing and what would bring it back:
//
//	Lookup's miss check — nothing on this interface writes and no corpus seeds it, so no input is one nothing supplied. To close it: declare what a miss reports with //testkit:mixin notfound sentinel=Err…, or write the claim as a row.
//
// The compatibility handshake. A breaking change to the check surface
// renames the witness, and every file generated against this one stops
// compiling with the skew named rather than dispatching wrong.
var _ = suite.CompatV2

// ReaderNoErrorFixture holds every input the generated checks run against.
//
// Derived from each method's parameter names and types. Every field has a
// companion holding a second, different value: a check comparing a result
// against a single input passes whenever the subject happened to be seeded with
// it, and a miss check whose key happens to hit asserts nothing.
//
// A field whose type admits no literal — a func, a channel, a type from a
// package this run never read — is declared and left at its zero value, and
// every generated check that needed it was dropped rather than emitted against
// a value nobody could write. Those drops are listed above with what closes
// each; a row you write is handed this fixture either way.
type ReaderNoErrorFixture struct {
	key      string
	keyOther string
}

// DefaultReaderNoErrorFixture is what a run with no config draws.
func DefaultReaderNoErrorFixture() ReaderNoErrorFixture {
	return readerNoErrorNewFixture()
}

// readerNoErrorNewFixture is what a run draws from, given its config.
//
// Read through the accessors below rather than as fields: a roled value
// comes from the config's pool and an unroled one from its derived
// literal, and no check that draws should be able to tell which.
func readerNoErrorNewFixture() ReaderNoErrorFixture {
	return ReaderNoErrorFixture{
		key:      "test-key",
		keyOther: "other-key",
	}
}

// Key is the canonical key every check draws.
func (f ReaderNoErrorFixture) Key() string { return f.key }

// KeyOther is a second key, different from the first — which is
// what lets a miss check miss and a comparison mean something.
func (f ReaderNoErrorFixture) KeyOther() string { return f.keyOther }

// ReaderNoError is the witnessed instantiation.
//
// A generic interface aliases with its own parameters, which Go admits
// since 1.24 — so every declaration below names ReaderNoError and
// the arguments follow, exactly as they do for a concrete one.
type ReaderNoError = readernoerror.ReaderNoError

// ReaderNoErrorSubject is the runtime subject a RunWith body receives.
//
// Aliased rather than named through the engine, so a consumer writing a
// row against the subject stays inside this package's vocabulary.
type ReaderNoErrorSubject = suite.Subject[ReaderNoError]

// ReaderNoErrorRunOpt is one thing you can pass to RunReaderNoError.
type ReaderNoErrorRunOpt interface {
	applyTo(*readerNoErrorRunConfig)
}

type readerNoErrorRunConfig struct {
	suite.Bundle[ReaderNoError]

	// rows are the consumer's own checks, bound against the run's
	// fixture once the options are folded — which is why they are held
	// here rather than lowered on arrival: a row bound before the
	// fixture is known would draw from a fixture no run uses.
	rows ReaderNoErrorChecks
}

// ReaderNoErrorHarness declares one ReaderNoError implementation under test.
type ReaderNoErrorHarness[T ReaderNoError] struct {
	Name string

	// Exactly one constructor.
	New   func() T
	Start func(tb testing.TB) T

	// Excuse lists checks this subject structurally cannot take.
	Excuse []suite.ID

	// Oracle marks this subject as the run's reference. At most one.
	Oracle bool

	// Serial keeps this subject out of the parallel group.
	Serial bool

	// Provide answers open capabilities beyond the named fields — the
	// doors only this interface's checks know. The runner's gate reads
	// it for any capability without a dedicated field.
	Provide map[suite.Capability]any
}

// Subject lowers the declaration into what the runner consumes.
func (h ReaderNoErrorHarness[T]) Subject() (suite.Subject[ReaderNoError], error) {
	build, err := suite.OneCtor(h.Name, h.New, h.Start)
	if err != nil {
		return suite.Subject[ReaderNoError]{}, err
	}
	return suite.Subject[ReaderNoError]{
		Name:     h.Name,
		Provides: h.Provide,
		New:      func(tb testing.TB) ReaderNoError { return build(tb) },
		Oracle:   h.Oracle,
		Serial:   h.Serial,
		Excused:  suite.ExcuseSet(h.Excuse),
	}, nil
}

func (h ReaderNoErrorHarness[T]) applyTo(rc *readerNoErrorRunConfig) {
	rc.AddSubject(h.Subject())
}

type readerNoErrorDropOpt []suite.ID

func (d readerNoErrorDropOpt) applyTo(rc *readerNoErrorRunConfig) {
	rc.AddDrops(d...)
}

// readerNoErrorWithout drops checks by identity.
func readerNoErrorWithout(ids ...suite.ID) ReaderNoErrorRunOpt {
	return readerNoErrorDropOpt(ids)
}

// --- The veneer -------------------------------------------------------------

// ReaderNoErrorSuite is the entry a consumer names checks through.
var ReaderNoErrorSuite = readerNoErrorVeneer{Checks: readerNoErrorCheckIndex}

type readerNoErrorVeneer struct {
	// Checks is the check index — the constants a Without call is
	// written against.
	Checks readerNoErrorCheckIndexT
}

// Without drops a check by ID.
func (readerNoErrorVeneer) Without(ids ...suite.ID) ReaderNoErrorRunOpt {
	return readerNoErrorWithout(ids...)
}

// Suite returns the checks as data, for tooling or a runner of your own.
//
// The way in from outside this package: the assembler and the table it
// builds are unexported, because a caller reaching them is doing tooling
// and tooling wants one name rather than one per interface.
func (readerNoErrorVeneer) Suite(fx ReaderNoErrorFixture) suite.Suite[ReaderNoError] {
	return readerNoErrorSuite(fx)
}

// --- Drop hints -------------------------------------------------------------

// readerNoErrorIndexPath spells every emitted ID as the path a
// consumer would write to drop it, so a run that declines something can
// say what to type rather than what failed.
var readerNoErrorIndexPath = map[suite.ID]string{
	readerNoErrorCheckIndex.Lookup.Smoke(): "ReaderNoErrorSuite.Checks.Lookup.Smoke()",
}

var readerNoErrorDropHint = suite.DropHinter(
	"ReaderNoErrorSuite", readerNoErrorIndexPath,
)

// The names this file spells more than once: one home each.
const (
	readerNoErrorLookup = "Lookup"
)

// readerNoErrorCheckIndex indexes every check this package emits, by
// method and then by check.
var readerNoErrorCheckIndex = readerNoErrorCheckIndexT{
	Lookup: readerNoErrorLookupChecks{},
}

type readerNoErrorCheckIndexT struct {
	Lookup readerNoErrorLookupChecks
}

// All returns every ID this package emits.
func (readerNoErrorCheckIndexT) All() []suite.ID {
	var out []suite.ID
	out = append(out, readerNoErrorLookupChecks{}.All()...)
	return out
}

type readerNoErrorLookupChecks struct{}

func (readerNoErrorLookupChecks) Smoke() suite.ID {
	return suite.MethodID(readerNoErrorLookup, suite.SegSmoke)
}

func (readerNoErrorLookupChecks) All() []suite.ID {
	return []suite.ID{
		readerNoErrorLookupChecks{}.Smoke(),
	}
}

// readerNoErrorSuite returns the checks as data, drawn from f.
//
// The seam the proofs companion binds to, and the one a runner of your
// own would. It takes the fixture rather than a config because the
// fixture is what a check draws from; a config that derives one is the
// pool work, and until that lands a caller passes what a default run
// passes.
func readerNoErrorSuite(fx ReaderNoErrorFixture) suite.Suite[ReaderNoError] {
	return suite.Suite[ReaderNoError]{
		Name:     "ReaderNoError",
		DropHint: readerNoErrorDropHint,
		Checks:   readerNoErrorSignatureChecks(fx),
	}
}

// readerNoErrorSignatureChecks builds every check this run derived.
//
// sig is the one shape they share; the literals below state only what
// differs. Falsifiable is Proven wherever a defect rule reached the
// claim AND the companion beside this file spells that defect — the
// parity gate refuses the stamp without the evidence, so a claim this
// run cannot yet plant is argued rather than asserted.
func readerNoErrorSignatureChecks(fx ReaderNoErrorFixture) []suite.Check[ReaderNoError] {
	sig := suite.ProvenCheck[ReaderNoError]
	ix := readerNoErrorCheckIndex
	return []suite.Check[ReaderNoError]{
		sig(ix.Lookup.Smoke(), suite.ClassSmoke,
			"Lookup survives a call with a seeded key",
			func(tb testing.TB, r ReaderNoError) {
				readerNoErrorAssertLookupSmoke(tb, r, fx)
			}),
	}
}

// readerNoErrorAssertLookupSmoke asserts Lookup survives a call with a seeded key.
func readerNoErrorAssertLookupSmoke(
	tb testing.TB,
	r ReaderNoError,
	fx ReaderNoErrorFixture,
) {
	tb.Helper()
	suite.Survives(tb, readerNoErrorLookup, func(ctx context.Context) {
		_ = r.Lookup(ctx, fx.Key())
	})
}

// ReaderNoErrorDefect is anything that can stand as a planted defect for a
// row — in practice a ReaderNoErrorHarness over the generated double.
type ReaderNoErrorDefect interface {
	Subject() (ReaderNoErrorSubject, error)
}

// BrokenReaderNoError names a planted defect.
//
// Sugar over the harness, because a defect IS a subject — one built to
// fail exactly one claim. Its own constructor is what keeps a proofs
// table readable beside a run's.
func BrokenReaderNoError[T ReaderNoError](
	name string, build func() T,
) ReaderNoErrorHarness[T] {
	return ReaderNoErrorHarness[T]{
		Name: name, New: build,
	}
}

// ReaderNoErrorChecks is the table of this package's own checks.
type ReaderNoErrorChecks []ReaderNoErrorCheck

func (c ReaderNoErrorChecks) applyTo(rc *readerNoErrorRunConfig) {
	rc.rows = append(rc.rows, c...)
}

// ReaderNoErrorCheck is one row. The body is a plain named function; which
// field it goes in decides what it receives.
type ReaderNoErrorCheck struct {
	// Method scopes Run's ID: "Lookup" makes it Lookup/<name>.
	// A body that fixes its own scope leaves this empty.
	Method string

	// Name is the ID's slug — the stable handle a drop and a rerun use.
	Name string

	// Claim is the sentence the manifest records and the report reads
	// out. A claim wider than its assertion is the one failure this
	// surface cannot catch for you.
	Claim string

	// Exactly one body.
	//
	// Run gets one fresh instance, which is nearly every check. RunWith
	// gets the subject itself, for a claim one instance cannot state:
	// build two, move the clock, induce a failure.
	//
	// Both take the fixture whether or not any DERIVED check draws one,
	// so the row seam has one shape across every package and a fixture
	// that later grows a field does not change a signature consumers
	// have already written against.
	Run     func(tb testing.TB, s ReaderNoError, fx ReaderNoErrorFixture)
	RunWith func(tb testing.TB, sub ReaderNoErrorSubject, fx ReaderNoErrorFixture)

	// Class buckets the report. Empty means hand-written.
	Class suite.Class

	// Needs declares capabilities. A harness that cannot meet one fails
	// this check by name with the fix in the message, never skips — a
	// check that skipped for want of wiring reads as coverage.
	Needs suite.Caps

	// ProvenBy is the planted defect that fails this check and nothing
	// else. Setting it is the claim AND the evidence: the row reports
	// Proven, and ProveReaderNoError fails unless the defect turns the check
	// red. ProvenReason, when set, is a substring that red must contain,
	// so a defect dying on an incidental guard stops counting as one.
	//
	// Argued records why no defect can be constructed — the honest third
	// answer. At most one of ProvenBy and Argued; neither means unproven,
	// which is what the report says about a check nobody has driven
	// against anything.
	ProvenBy     ReaderNoErrorDefect
	ProvenReason string
	Argued       string
}

// readerNoErrorMethods is the interface's method set, for naming a Method typo
// at run start.
//
// A misspelled method starts with a capital too, so the ID grammar alone
// would file a check under a method that does not exist and report it
// under a path no drop can name.
var readerNoErrorMethods = suite.NewNameSet("ReaderNoError", readerNoErrorLookup)

// bind lowers one row onto the runtime check type, against the run's own
// fixture — which is what lets a row's body draw what the run draws.
func (c ReaderNoErrorCheck) bind(
	fx ReaderNoErrorFixture,
) (suite.Check[ReaderNoError], error) {
	out := suite.Check[ReaderNoError]{
		Claim: c.Claim, Class: c.Class, Needs: c.Needs,
	}
	if out.Class == "" {
		out.Class = suite.ClassHandWritten
	}

	var err error
	bodies := 0
	if c.Run != nil {
		bodies++
		if out.ID, err = suite.RowID("Run", c.Method, c.Name, readerNoErrorMethods); err != nil {
			return out, err
		}
		run := c.Run
		out.Run = func(tb testing.TB, s ReaderNoError) {
			run(tb, s, fx)
		}
	}
	if c.RunWith != nil {
		bodies++
		out.ID = suite.HandRowID(c.Name)
		rw := c.RunWith
		out.RunWith = func(tb testing.TB, sub ReaderNoErrorSubject) {
			rw(tb, sub, fx)
		}
	}
	if err := suite.OneBody(c.Name, bodies, "Run, RunWith"); err != nil {
		return out, err
	}
	if c.Method != "" && c.Run == nil {
		return out, fmt.Errorf(
			"check %q sets Method, but its body fixes its own scope; drop Method", c.Name)
	}
	if out.Falsifiable, err = suite.Falsify(c.Name, c.ProvenBy != nil, c.Argued); err != nil {
		return out, err
	}
	return out, nil
}

// RunReaderNoError runs every check — generated and yours — against every
// harness.
//
//	RunReaderNoError(t,
//		ReaderNoErrorHarness[*InMemory]{Name: "in-memory", New: NewInMemory},
//		ReaderNoErrorSuite.Without(ReaderNoErrorSuite.Checks.Lookup.Smoke()),
//	)
func RunReaderNoError(
	t *testing.T, opts ...ReaderNoErrorRunOpt,
) {
	t.Helper()
	var rc readerNoErrorRunConfig
	for _, o := range opts {
		o.applyTo(&rc)
	}
	fx := readerNoErrorNewFixture()
	for _, row := range rc.rows {
		rc.AddCheck(row.bind(fx))
	}
	// Between folding and running: a misused run reports every fix it
	// needs and executes nothing, rather than reporting one and leaving
	// the rest to be found a cycle at a time.
	rc.Fail(t, "RunReaderNoError")
	suite.Run(t,
		readerNoErrorSuite(fx).With(rc.Extra...).Without(rc.Drops...),
		rc.Subjects...)
}

// ProveReaderNoError drives each of your rows against the defect it names.
//
// A row with no ProvenBy is not skipped — it is held to saying why
// through Argued. The parity gate refuses a Proven claim with no
// evidence and evidence for a claim nobody made, in both directions, so
// this is where "proven able to fail" stops being prose.
func ProveReaderNoError(
	t *testing.T, checks ReaderNoErrorChecks,
) {
	t.Helper()
	// The RUN's config, not the derived one: a check proven at default
	// pools carries no evidence about the pools a run actually uses.
	fx := readerNoErrorNewFixture()
	bound := make([]suite.Check[ReaderNoError], 0, len(checks))
	defects := prove.Defects[ReaderNoError]{}
	for _, row := range checks {
		bd, err := row.bind(fx)
		if err != nil {
			t.Fatalf("ProveReaderNoError: %v", err)
		}
		bound = append(bound, bd)
		if row.ProvenBy == nil {
			continue
		}
		sub, err := row.ProvenBy.Subject()
		if err != nil {
			t.Fatalf("ProveReaderNoError: check %q: defect: %v", bd.ID, err)
		}
		defects[bd.ID] = prove.Defect[ReaderNoError]{
			Subject: sub, Reason: row.ProvenReason,
		}
	}
	prove.All(t, bound, defects)
}

// testkit: end of generated content.
// testkit:provenance 48e5241c1ca54fc20967b191b827a7b44104b0d55f3d44b374b6700272c1e5cd
