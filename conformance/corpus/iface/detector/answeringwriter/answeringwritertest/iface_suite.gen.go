// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/detector/answeringwriter/iface.go
// Plugins:   golang 1.0.0, suite 1.9.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package answeringwritertest

import (
	"context"
	"slices"
	"testing"
	"time"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/answeringwriter"
)

// AnsweringWriterPutCheck is one assertion about Put.
//
// Every generated check for Put is a value of it, and so is one you
// write — so they compose, reorder, and each runs standalone. testing.TB rather
// than *testing.T is what lets a stand-in drive it and prove it can fail.
type AnsweringWriterPutCheck func(tb testing.TB, subject answeringwriter.AnsweringWriter, v answeringwriter.Value)

// AnsweringWriterFixture holds every input the generated checks run against.
//
// Derived from each method's parameter names and types. Every field has a
// companion holding a second, different value: a check comparing a result
// against a single input passes whenever the subject happened to be seeded with
// it, and a miss check whose key happens to hit asserts nothing.
//
// A field whose type admits no literal — a func, a channel, a type from a
// package this run never read — is declared and left at its zero value, and
// every generated check that needed it was dropped rather than emitted against
// a value nobody could write. Supply one through AnsweringWriterWithFixture
// and the check a consumer writes has something to use.
type AnsweringWriterFixture struct {
	V      answeringwriter.Value
	VOther answeringwriter.Value
}

// DefaultAnsweringWriterFixture is what this run derived. Replace any field through
// AnsweringWriterWithFixture; the rest keep these values.
func DefaultAnsweringWriterFixture() AnsweringWriterFixture {
	return AnsweringWriterFixture{
		V:      answeringwriter.Value{Key: "test-key", Body: "test-body"},
		VOther: answeringwriter.Value{Key: "other-key", Body: "other-body"},
	}
}

// AssertAnsweringWriterContract runs every generated check against every declared subject.
//
//	Checks:   5 across 1 method, per subject
//	Subjects: declare each with AnsweringWriterSubject
//	Double:   every subject runs a second time wrapped in AnsweringWriterStub, so
//	          anything the wrapper fails that the subject passes is the double
//	          lying. AnsweringWriterWithoutDouble declines it.
//	Extend:   AnsweringWriterOnPut
//	Drop:     AnsweringWriterWithout, by the path each check reports under
//
// # What this file does not check
//
// The interface declares these and nothing here asserts them. Each is statable
// against a subject you can build, so the check is yours to write:
//
//   - answeringwriter, on Put — write it with AnsweringWriterOnPut
func AssertAnsweringWriterContract(t *testing.T, opts ...AnsweringWriterOption) {
	t.Helper()
	cfg := newAnsweringWriterConfig(opts...)
	if len(cfg.subjects) == 0 {
		t.Fatalf("AssertAnsweringWriterContract was given no subject; declare one with " +
			"AnsweringWriterSubject(name, factory)")
	}
	for _, p := range cfg.phantoms() {
		t.Errorf("AnsweringWriterWithout(%q) declines nothing: no check here reports "+
			"under that path, so this suite asserts one thing more than its "+
			"author believes", p)
	}
	for _, s := range cfg.subjects {
		runAnsweringWriterChecks(t, cfg, s.name, s.factory, nil)
		if !cfg.withoutDouble {
			runAnsweringWriterChecks(t, cfg, s.name+" through the double", s.factory,
				func(tb testing.TB, subject answeringwriter.AnsweringWriter) answeringwriter.AnsweringWriter {
					return NewAnsweringWriterStub(tb, AnsweringWriterStubDelegateTo(subject))
				})
		}
	}
}

// runAnsweringWriterChecks runs the whole contract against one subject.
//
// wrap is applied after the subject is built and seeded, so a wrapper is
// transparent to the seed and every failure it causes is its own.
func runAnsweringWriterChecks(
	t *testing.T,
	cfg *answeringwriterConfig,
	label string,
	factory func() answeringwriter.AnsweringWriter,
	wrap func(tb testing.TB, subject answeringwriter.AnsweringWriter) answeringwriter.AnsweringWriter,
) {
	t.Helper()
	t.Run(label, func(t *testing.T) {
		t.Parallel()

		t.Run("Put", func(t *testing.T) {
			t.Parallel()
			cfg.run(t, "Put/smoke", "smoke", func(tb testing.TB) {
				AssertAnsweringWriterPutSmoke(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.V)
			})
			cfg.run(t, "Put/reports a cancelled context", "reports a cancelled context", func(tb testing.TB) {
				AssertAnsweringWriterPutCancels(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.V)
			})
			cfg.run(t, "Put/reports an expired deadline", "reports an expired deadline", func(tb testing.TB) {
				AssertAnsweringWriterPutHonoursDeadline(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.V)
			})
			cfg.run(t, "Put/tolerates a nil context", "tolerates a nil context", func(tb testing.TB) {
				AssertAnsweringWriterPutToleratesNilContext(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.V)
			})
			cfg.run(t, "Put/an error carries the zero value", "an error carries the zero value", func(tb testing.TB) {
				AssertAnsweringWriterPutZeroOnError(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.VOther)
			})
			for _, c := range cfg.onPut {
				cfg.run(t, "Put"+"/"+c.name, c.name, func(tb testing.TB) {
					c.fn(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.V)
				})
			}
		})

		// The extra bodies of checks this run was given, each reporting under its
		// own name and dropped by it.
		//
		// The plain subject only. The wrapped pass exists to prove the double
		// faithful and the per-method checks above already do that, so running one
		// through the wrapper prices the wrapper and says nothing new about the
		// subject.
		if wrap == nil {
			for _, e := range cfg.extensions {
				if cfg.declined(e.name) {
					t.Run(e.name, func(t *testing.T) {
						t.Skipf("dropped through AnsweringWriterWithout(%q)", e.name)
					})
					continue
				}
				t.Run(e.name, func(t *testing.T) {
					t.Parallel()
					e.run(t, label, factory, cfg)
				})
			}
		}
	})
}

// AssertAnsweringWriterPutSmoke asserts Put survives a call with derived inputs.
//
// Fails when: Put panics. The weakest check in this file and the one
// that catches the most — a method that panics on a derived value is one no
// other check here reaches.
func AssertAnsweringWriterPutSmoke(tb testing.TB, subject answeringwriter.AnsweringWriter, v answeringwriter.Value) {
	tb.Helper()
	defer func() {
		if r := recover(); r != nil {
			tb.Fatalf("Put panicked on a derived value (%v); supply one it "+
				"accepts through AnsweringWriterWithFixture", r)
		}
	}()
	ctx := tb.Context()
	_, _ = subject.Put(ctx, v)
}

// AssertAnsweringWriterPutCancels asserts Put reports a cancelled context as cancelled.
//
// Fails when: Put returns nil for a context cancelled before the
// call, or returns an error that does not answer to context.Canceled. Both
// matter and they are different defects: the first is work done that the caller
// asked not to be, the second is a caller who wrote
// `errors.Is(err, context.Canceled)` and gets false for a call that was.
//
// The error rather than merely its presence is what separates this from the
// deadline check. Asserting only that something came back makes the two one
// check written twice.
func AssertAnsweringWriterPutCancels(tb testing.TB, subject answeringwriter.AnsweringWriter, v answeringwriter.Value) {
	tb.Helper()
	ctx, cancel := context.WithCancel(tb.Context())
	cancel()
	_, err := subject.Put(ctx, v)
	testkit.ErrorIs(tb, err, context.Canceled,
		"Put must report a cancelled context as context.Canceled")
}

// AssertAnsweringWriterPutHonoursDeadline asserts Put reports an expired deadline as exceeded.
//
// Fails when: Put returns nil for a context whose deadline has
// passed, or returns an error that does not answer to
// context.DeadlineExceeded. Distinct from cancellation in what the caller
// learns: a cancelled call was called off, an expired one ran out of time, and
// only the second is worth retrying.
//
// The deadline is the zero time, which is unconditionally in the past. No clock
// is read: a generated check that consults the wall clock is one whose subject
// is partly the machine it runs on.
func AssertAnsweringWriterPutHonoursDeadline(tb testing.TB, subject answeringwriter.AnsweringWriter, v answeringwriter.Value) {
	tb.Helper()
	ctx, cancel := context.WithDeadline(tb.Context(), time.Time{})
	defer cancel()
	_, err := subject.Put(ctx, v)
	testkit.ErrorIs(tb, err, context.DeadlineExceeded,
		"Put must report an expired deadline as context.DeadlineExceeded")
}

// AssertAnsweringWriterPutToleratesNilContext asserts Put does not panic on a nil context.
//
// Fails when: Put panics. Returning an error is correct and
// succeeding is correct — a nil context reaches production through a caller
// that forgot one, and a panic turns that into an outage rather than a failed
// request.
func AssertAnsweringWriterPutToleratesNilContext(tb testing.TB, subject answeringwriter.AnsweringWriter, v answeringwriter.Value) {
	tb.Helper()
	defer func() {
		if r := recover(); r != nil {
			tb.Errorf("Put panicked on a nil context (%v); return an error instead", r)
		}
	}()
	//nolint:staticcheck // passing nil is the check.
	var ctx context.Context
	_, _ = subject.Put(ctx, v)
}

// AssertAnsweringWriterPutZeroOnError asserts an error is accompanied by the zero value.
//
// Fails when: Put returns a non-zero result alongside a non-nil
// error. A caller who checks the error and a caller who checks the value must
// not disagree about whether the call succeeded.
//
// Skips when the alternate input succeeds: a total method has no failure this
// run can reach, and a skip says so where a green line would claim a
// comparison that never happened.
func AssertAnsweringWriterPutZeroOnError(tb testing.TB, subject answeringwriter.AnsweringWriter, v answeringwriter.Value) {
	tb.Helper()
	ctx := tb.Context()
	r0, r1 := subject.Put(ctx, v)
	if r1 == nil {
		tb.Skipf("Put accepted the alternate input; nothing this run " +
			"supplies makes it fail, so the zero-on-error claim goes unexercised")
		return
	}
	{
		var zero answeringwriter.Value
		testkit.Equal(tb, r0, zero,
			"Put must return the zero value alongside an error")
	}
}

// AnsweringWriterOption configures a contract run.
type AnsweringWriterOption func(*answeringwriterConfig)

// AnsweringWriterSubject declares an implementation to hold to the contract.
//
// Repeat it for every implementation that claims to satisfy the interface — an
// in-memory one, the one backed by a database, the one behind a network. Every
// check and every option is stated once and runs against all of them, which is
// what a conformance suite is for.
func AnsweringWriterSubject(name string, factory func() answeringwriter.AnsweringWriter) AnsweringWriterOption {
	return func(c *answeringwriterConfig) {
		c.subjects = append(c.subjects, namedAnsweringWriterSubject{name, factory})
	}
}

// AnsweringWriterWithoutDouble declines the second run through AnsweringWriterStub.
//
// The double run proves the stand-in is faithful, and costs a second pass over
// every check. Decline it where the double is not used.
func AnsweringWriterWithoutDouble() AnsweringWriterOption {
	return func(c *answeringwriterConfig) { c.withoutDouble = true }
}

// AnsweringWriterWithClock supplies the clock every time-reading check measures on.
//
// Defaults to the real one, so an implementation that does not take a clock
// behaves as it would have. Supply a [clock.TestClock] — the same one the
// factory builds the subject with — and a budget becomes a claim about the time
// the implementation means to spend rather than about how loaded the machine
// was.
func AnsweringWriterWithClock(c clock.Clock) AnsweringWriterOption {
	return func(cfg *answeringwriterConfig) { cfg.clock = c }
}

// AnsweringWriterWithFixture replaces the derived inputs.
func AnsweringWriterWithFixture(f AnsweringWriterFixture) AnsweringWriterOption {
	return func(c *answeringwriterConfig) { c.Fixture = f }
}

// AnsweringWriterSeed replaces the derived seed.
//
// For an interface with no writer to seed itself through, which is the one
// input a conformance run cannot derive: a reader over an empty subject
// asserts nothing.
//
// Returning an error rather than swallowing one. A seed that failed quietly
// leaves every check after it asserting nothing and reporting success.
func AnsweringWriterSeed(fn func(ctx context.Context, subject answeringwriter.AnsweringWriter) error) AnsweringWriterOption {
	return func(c *answeringwriterConfig) {
		c.seed = fn
		c.seedIsDerived = false
	}
}

// AnsweringWriterOnPut adds a named check to Put.
func AnsweringWriterOnPut(name string, fn AnsweringWriterPutCheck) AnsweringWriterOption {
	return func(c *answeringwriterConfig) {
		c.onPut = append(c.onPut, namedAnsweringWriterPutCheck{name, fn})
	}
}

// AnsweringWriterWithout drops generated checks by the path each reports under.
//
// For a subject that legitimately violates one. Without this the only recourse
// is to stop running the suite, which costs every other check in the file — so
// a dropped check is reported as skipped rather than silently omitted.
func AnsweringWriterWithout(paths ...string) AnsweringWriterOption {
	return func(c *answeringwriterConfig) {
		for _, p := range paths {
			c.without[p] = struct{}{}
		}
	}
}

type namedAnsweringWriterPutCheck struct {
	name string
	fn   AnsweringWriterPutCheck
}

type namedAnsweringWriterSubject struct {
	name    string
	factory func() answeringwriter.AnsweringWriter
}

// answeringwriterContractExtension is a body of checks a generated sibling adds through an option.
//
// A sibling declares the option and closes over whatever configures it, so
// everything that varies per run is typed and private to the file that owns it
// — this file understands only the name and the call. Nothing arrives except
// through an option, which is what keeps the set fixed before the first subtest
// starts.
type answeringwriterContractExtension struct {
	// name is the path this reports under, and the one AnsweringWriterWithout drops
	// it by. Anything nested beneath it is the extension's own to name.
	name string

	// run takes the factory rather than a subject. One that drives sequences
	// builds a subject per iteration, and one that compares against a reference
	// needs the unseeded subject a factory returns — which
	// answeringwriterConfig.subject is not.
	//
	// Called once per subject, in parallel, sharing the config. Anything it
	// mutates has to be built inside the call: state captured when the option
	// was declared is state two subjects write at once.
	run func(
		t *testing.T,
		subject string,
		factory func() answeringwriter.AnsweringWriter,
		cfg *answeringwriterConfig,
	)
}

type answeringwriterConfig struct {
	Fixture       AnsweringWriterFixture
	subjects      []namedAnsweringWriterSubject
	withoutDouble bool
	clock         clock.Clock
	seed          func(ctx context.Context, subject answeringwriter.AnsweringWriter) error
	// seedIsDerived is what lets a failed seed name the right culprit. The
	// derived seed and a consumer's are the same field, and the advice for one
	// is useless for the other.
	seedIsDerived bool
	without       map[string]struct{}
	extensions    []answeringwriterContractExtension
	onPut         []namedAnsweringWriterPutCheck
}

func newAnsweringWriterConfig(opts ...AnsweringWriterOption) *answeringwriterConfig {
	c := &answeringwriterConfig{
		Fixture: DefaultAnsweringWriterFixture(),
		clock:   clock.RealClock(),
		without: map[string]struct{}{},
	}
	// Derived: Put is classified writer, so the interface
	// populates itself and a reader has something to read.
	c.seed = func(ctx context.Context, subject answeringwriter.AnsweringWriter) error {
		_, err := subject.Put(ctx, c.Fixture.V)
		return err
	}
	c.seedIsDerived = true
	for _, o := range opts {
		o(c)
	}
	return c
}

// subject builds a fresh implementation and seeds it, so every check runs
// against state no other check touched.
func (c *answeringwriterConfig) subject(
	tb testing.TB,
	factory func() answeringwriter.AnsweringWriter,
	wrap func(tb testing.TB, subject answeringwriter.AnsweringWriter) answeringwriter.AnsweringWriter,
) answeringwriter.AnsweringWriter {
	tb.Helper()
	s := factory()
	if c.seed != nil {
		// Reported rather than ignored. A seed that silently failed would leave
		// every check after it running against an empty subject — passing, and
		// asserting nothing.
		//
		// Which seed ran decides what the message can usefully say. The derived
		// one fails for one of two reasons and both are fixable from here, so it
		// names them. A seed the caller supplied is theirs, and telling them to
		// supply one is advice they already took.
		if err := c.seed(tb.Context(), s); err != nil {
			if c.seedIsDerived {
				tb.Fatalf("seeding through Put failed: %v; "+
					"supply a value it accepts through AnsweringWriterWithFixture, or a "+
					"whole seed through AnsweringWriterSeed", err)
			} else {
				tb.Fatalf("the seed supplied through AnsweringWriterSeed failed: %v", err)
			}
		}
	}
	if wrap != nil {
		s = wrap(tb, s)
	}
	return s
}

// dropped reports whether a path was declined through AnsweringWriterWithout.
//
// Named rather than inlined because the drop set is read from two places: here,
// and by whatever registered through the seam — which reports under paths this
// file has never heard of and must answer the same question about them.
func (c *answeringwriterConfig) declined(path string) bool {
	_, ok := c.without[path]
	return ok
}

// answeringwriterPaths is every path this file reports a check under.
//
// The set a AnsweringWriterWithout argument has to be a member of. Declared
// rather than observed: a check that did not run is not a check that does not
// exist — an integration-only method skips without ever consulting the drop
// set, and reading that as a phantom would fail a correct suite on a machine
// with the environment unset.
//
//nolint:gochecknoglobals // the emitted check paths, read-only after init.
var answeringwriterPaths = map[string]struct{}{
	"Put/smoke":                           {},
	"Put/reports a cancelled context":     {},
	"Put/reports an expired deadline":     {},
	"Put/tolerates a nil context":         {},
	"Put/an error carries the zero value": {},
}

// phantoms names every AnsweringWriterWithout path that declines nothing.
//
// A drop that drops nothing is this option's silent half: the consumer wrote
// it against a check since renamed, or never emitted for that method at all,
// and every run after reads green while asserting one thing more than its
// author believes. Extensions carry their own names — the seam reports under
// paths this file has never heard of — so they count as known too.
func (c *answeringwriterConfig) phantoms() []string {
	known := func(p string) bool {
		if _, ok := answeringwriterPaths[p]; ok {
			return true
		}
		for _, e := range c.extensions {
			if e.name == p {
				return true
			}
		}
		for _, on := range c.onPut {
			if "Put"+"/"+on.name == p {
				return true
			}
		}
		return false
	}
	var out []string
	for p := range c.without {
		if !known(p) {
			out = append(out, p)
		}
	}
	slices.Sort(out)
	return out
}

// run executes one check unless it was dropped, reporting the drop rather than
// silently omitting it.
func (c *answeringwriterConfig) run(t *testing.T, path, name string, fn func(tb testing.TB)) {
	t.Helper()
	if c.declined(path) {
		t.Run(name, func(t *testing.T) {
			t.Skipf("dropped through AnsweringWriterWithout(%q)", path)
		})
		return
	}
	t.Run(name, func(t *testing.T) {
		t.Parallel()
		fn(t)
	})
}

// testkit: end of generated content.
// testkit:provenance 29b507033dfd78838f67dae410a694e769dbf4f23dd9786c1c8cf2a2c49ff208
