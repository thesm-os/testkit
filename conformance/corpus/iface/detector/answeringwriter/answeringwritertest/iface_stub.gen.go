// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/detector/answeringwriter/iface.go
// Plugins:   golang 1.0.0, stub 1.0.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package answeringwritertest

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/answeringwriter"
	"go.thesmos.sh/testkit/rand"
	"go.thesmos.sh/testkit/stub"
)

// AnsweringWriterPutCall records one invocation of AnsweringWriter.Put.
//
// Fields take their names from the source signature — parameters and named
// returns alike — so a failure message names what the author named. A slot
// the source left unnamed or blank falls back to a positional name.
type AnsweringWriterPutCall struct {
	Ctx    context.Context
	V      answeringwriter.Value
	Result answeringwriter.Value
	Err    error
}

// --- Per-method configuration ---

// AnsweringWriterPutStub controls how the double answers Put and records
// what it was asked.
//
// The embedded MethodStub supplies the machinery every method shares: call
// recording, fault injection, latency against a virtual clock, gates,
// call-count expectations, and strict mode.
type AnsweringWriterPutStub struct {
	*stub.MethodStub[AnsweringWriterPutCall]

	fn       func(context.Context, answeringwriter.Value) (answeringwriter.Value, error)
	fallback *AnsweringWriterPutReturn
}

// AnsweringWriterPutReturn holds the fixed answer configured through Returns.
type AnsweringWriterPutReturn struct {
	Result answeringwriter.Value
	Err    error
}

// Returns pins a fixed result for every call to Put. A Func
// override and an injected fault both take precedence over it.
func (s *AnsweringWriterPutStub) Returns(result answeringwriter.Value, err error) *AnsweringWriterPutStub {
	s.fallback = &AnsweringWriterPutReturn{Result: result, Err: err}
	return s
}

// Func supplies a body for Put, for when the answer depends on the
// arguments. An injected fault still takes precedence.
func (s *AnsweringWriterPutStub) Func(fn func(context.Context, answeringwriter.Value) (answeringwriter.Value, error)) *AnsweringWriterPutStub {
	s.fn = fn
	return s
}

// --- AnsweringWriterStub ---

// AnsweringWriterStubOption configures a [AnsweringWriterStub] at construction time.
type AnsweringWriterStubOption func(*AnsweringWriterStub)

// AnsweringWriterStubStrict makes an unconfigured method fail the test rather than
// return its zero value, which turns a call nobody planned for into a failure
// at the call site instead of a puzzling zero further downstream.
func AnsweringWriterStubStrict() AnsweringWriterStubOption {
	return func(s *AnsweringWriterStub) { s.strict = true }
}

// AnsweringWriterStubDelegateTo wraps a real implementation: every method forwards to
// impl and is recorded on the double on the way through.
//
// This is what lets one generated suite run against both the double and the
// production type, which is the point of conformance testing.
func AnsweringWriterStubDelegateTo(impl answeringwriter.AnsweringWriter) AnsweringWriterStubOption {
	return func(s *AnsweringWriterStub) {
		s.OnPut.Func(impl.Put)
	}
}

// AnsweringWriterStubWithClock drives latency and time-windowed faults from clk
// rather than from wall time, so a test asserting on a five-second timeout
// does not take five seconds.
func AnsweringWriterStubWithClock(clk clock.Clock) AnsweringWriterStubOption {
	return func(s *AnsweringWriterStub) {
		for _, m := range s.all {
			m.SetClock(clk)
		}
	}
}

// AnsweringWriterStubWithRandSource makes probabilistic fault injection reproducible.
// A failure nobody can replay is not a finding.
func AnsweringWriterStubWithRandSource(src rand.Source) AnsweringWriterStubOption {
	return func(s *AnsweringWriterStub) {
		for _, m := range s.all {
			m.SetRandSource(src)
		}
	}
}

// AnsweringWriterStubBenchMode disables call recording. Dispatch still works; only
// the accumulating call log is dropped, because its allocations are what a
// benchmark would otherwise be measuring.
func AnsweringWriterStubBenchMode() AnsweringWriterStubOption {
	return func(s *AnsweringWriterStub) {
		for _, m := range s.all {
			m.BenchMode()
		}
	}
}

// WithAnsweringWriterPut sets Put's body at construction
// time, for the common case of configuring one method and taking the
// defaults for the rest.
func WithAnsweringWriterPut(fn func(context.Context, answeringwriter.Value) (answeringwriter.Value, error)) AnsweringWriterStubOption {
	return func(s *AnsweringWriterStub) { s.OnPut.Func(fn) }
}

// AnsweringWriterStub is a recording test double for AnsweringWriter.
//
// Each On<Method> field is that method's configuration point. Left alone, a
// method returns its zero value and records the call.
type AnsweringWriterStub struct {
	OnPut *AnsweringWriterPutStub

	// all is every method stub above, viewed through the surface that does
	// not depend on a signature. It is what lets a setting apply to the whole
	// double in a loop rather than in one line per method.
	all    []stub.Configurable
	strict bool
}

// Compile-time proof that AnsweringWriterStub satisfies AnsweringWriter.
//
// It lives here rather than in the companion so a drifted signature fails
// `go build` rather than waiting for a test run — the double is unusable the
// moment it stops satisfying the interface, and that is worth learning at the
// earliest point it can be known.
var _ answeringwriter.AnsweringWriter = (*AnsweringWriterStub)(nil)

// NewAnsweringWriterStub returns a double bound to tb.
//
// Passing tb registers a cleanup that verifies every method's call-count
// expectations when the test ends, so an unmet Times is reported without the
// caller remembering to check. A nil tb skips that, which is what benchmarks
// and non-test callers want.
func NewAnsweringWriterStub(tb testing.TB, opts ...AnsweringWriterStubOption) *AnsweringWriterStub {
	s := &AnsweringWriterStub{
		OnPut: &AnsweringWriterPutStub{MethodStub: stub.NewMethodStub[AnsweringWriterPutCall](tb, "AnsweringWriter.Put")},
	}
	s.all = []stub.Configurable{
		s.OnPut.MethodStub,
	}
	for _, opt := range opts {
		opt(s)
	}
	if s.strict {
		for _, m := range s.all {
			m.Strict()
		}
	}
	if tb != nil {
		// A fuzz target reruns its body many times over and registers a
		// cleanup per run, so verifying there would report against the wrong
		// iteration.
		if _, isFuzz := tb.(*testing.F); !isFuzz {
			tb.Cleanup(func() {
				for _, m := range s.all {
					m.Verify()
				}
			})
		}
	}
	return s
}

// ResetCalls clears recorded calls, fault counters, and call-count
// expectations. Func and Returns configuration is deliberately preserved, so
// a test can reuse one double across phases without rebuilding it.
func (s *AnsweringWriterStub) ResetCalls() {
	for _, m := range s.all {
		m.Reset()
	}
}

// --- Interface methods ---

// invoke adapts the Func override to the shape [stub.Answer] consumes, or
// returns nil when no override is set — which is how Answer tells "no
// override" from "an override that returns zero".
func (s *AnsweringWriterPutStub) invoke(ctx context.Context, v answeringwriter.Value) func() AnsweringWriterPutReturn {
	if s.fn == nil {
		return nil
	}
	return func() AnsweringWriterPutReturn {
		r0, r1 := s.fn(ctx, v)
		return AnsweringWriterPutReturn{Result: r0, Err: r1}
	}
}

// Put records the call and answers it.
//
// Which arm answers — injected fault, Func override, Returns fallback, or the
// zero value — is [stub.Answer]'s to decide, so every generated double
// resolves a call the same way and the ordering is tested once rather than
// restated per method.
func (s *AnsweringWriterStub) Put(ctx context.Context, v answeringwriter.Value) (answeringwriter.Value, error) {
	call := AnsweringWriterPutCall{Ctx: ctx, V: v}
	r := stub.Answer(s.OnPut.MethodStub, &call, stub.Arms[AnsweringWriterPutCall, AnsweringWriterPutReturn]{
		Invoke:   s.OnPut.invoke(ctx, v),
		Fallback: s.OnPut.fallback,
		Fault:    func(err error) AnsweringWriterPutReturn { return AnsweringWriterPutReturn{Err: err} },
		Stamp: func(c *AnsweringWriterPutCall, r AnsweringWriterPutReturn) {
			c.Result = r.Result
			c.Err = r.Err
		},
	})
	return r.Result, r.Err
}

// testkit: end of generated content.
// testkit:provenance 5b6aa2f3c56f80cf849a24beeadf6b32715c9b622abf5c186c52fce48bc1ba77
