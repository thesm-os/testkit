// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/detector/multiargwriter/iface.go
// Plugins:   golang 1.0.0, suite 1.0.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package multiargwritertest_test

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/multiargwriter/multiargwritertest"
)

// Every generated check for MultiArgWriter, driven against a stand-in built
// to violate it.
//
//	Proved:   4 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertMultiArgWriterSetSmokeCanFail holds AssertMultiArgWriterSetSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertMultiArgWriterSetSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := multiargwritertest.DefaultMultiArgWriterFixture()
	subject := multiargwritertest.NewMultiArgWriterStub(t,
		multiargwritertest.WithMultiArgWriterSet(func(context.Context, string, string) error {
			panic("MultiArgWriter.Set: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			multiargwritertest.AssertMultiArgWriterSetSmoke(tb, subject, fixture.Key, fixture.Body)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertMultiArgWriterSetCancelsCanFail holds AssertMultiArgWriterSetCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertMultiArgWriterSetCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := multiargwritertest.DefaultMultiArgWriterFixture()
	subject := multiargwritertest.NewMultiArgWriterStub(t,
		multiargwritertest.WithMultiArgWriterSet(func(context.Context, string, string) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			multiargwritertest.AssertMultiArgWriterSetCancels(tb, subject, fixture.Key, fixture.Body)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertMultiArgWriterSetHonoursDeadlineCanFail holds AssertMultiArgWriterSetHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertMultiArgWriterSetHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := multiargwritertest.DefaultMultiArgWriterFixture()
	subject := multiargwritertest.NewMultiArgWriterStub(t,
		multiargwritertest.WithMultiArgWriterSet(func(context.Context, string, string) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			multiargwritertest.AssertMultiArgWriterSetHonoursDeadline(tb, subject, fixture.Key, fixture.Body)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertMultiArgWriterSetToleratesNilContextCanFail holds AssertMultiArgWriterSetToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertMultiArgWriterSetToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := multiargwritertest.DefaultMultiArgWriterFixture()
	subject := multiargwritertest.NewMultiArgWriterStub(t,
		multiargwritertest.WithMultiArgWriterSet(func(context.Context, string, string) error {
			panic("MultiArgWriter.Set: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			multiargwritertest.AssertMultiArgWriterSetToleratesNilContext(tb, subject, fixture.Key, fixture.Body)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance 935eea97cce57807dc2c2ee5d0a8f7f7f196ae0126fba58ad2cfcdd228661c46
