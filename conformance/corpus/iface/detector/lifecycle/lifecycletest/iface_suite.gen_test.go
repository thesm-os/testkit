// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/detector/lifecycle/iface.go
// Plugins:   golang 1.0.0, suite 1.15.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package lifecycletest_test

import (
	"context"
	"errors"
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/lifecycle/lifecycletest"
)

// Every generated check for Lifecycle, driven against a stand-in built
// to violate it.
//
//	Proved:   6 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertLifecycleCloseSmokeCanFail holds AssertLifecycleCloseSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertLifecycleCloseSmokeCanFail(t *testing.T) {
	t.Parallel()

	subject := lifecycletest.NewLifecycleStub(t,
		lifecycletest.WithLifecycleClose(func(context.Context) error {
			panic("Lifecycle.Close: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			lifecycletest.AssertLifecycleCloseSmoke(tb, subject)
		})
	testkit.Assert(t, got).Contains("Close panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertLifecycleCloseCancelsCanFail holds AssertLifecycleCloseCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertLifecycleCloseCancelsCanFail(t *testing.T) {
	t.Parallel()

	subject := lifecycletest.NewLifecycleStub(t,
		lifecycletest.WithLifecycleClose(func(context.Context) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			lifecycletest.AssertLifecycleCloseCancels(tb, subject)
		})
	testkit.Assert(t, got).Contains("Close must report a cancelled context as context.Canceled",
		"and rejects it for the reason the check is about")
}

// TestAssertLifecycleCloseHonoursDeadlineCanFail holds AssertLifecycleCloseHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertLifecycleCloseHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	subject := lifecycletest.NewLifecycleStub(t,
		lifecycletest.WithLifecycleClose(func(context.Context) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			lifecycletest.AssertLifecycleCloseHonoursDeadline(tb, subject)
		})
	testkit.Assert(t, got).Contains("Close must report an expired deadline as context.DeadlineExceeded",
		"and rejects it for the reason the check is about")
}

// TestAssertLifecycleCloseToleratesNilContextCanFail holds AssertLifecycleCloseToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertLifecycleCloseToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	subject := lifecycletest.NewLifecycleStub(t,
		lifecycletest.WithLifecycleClose(func(context.Context) error {
			panic("Lifecycle.Close: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			lifecycletest.AssertLifecycleCloseToleratesNilContext(tb, subject)
		})
	testkit.Assert(t, got).Contains("Close panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertLifecycleCloseCloseCleanlyCanFail holds AssertLifecycleCloseCloseCleanly to being able to fail.
//
// The stand-in refuses every call including the first. Until this check
// existed that subject passed the whole tier: the second-call claim skipped
// on it and deferred to a finding nothing produced.
func TestAssertLifecycleCloseCloseCleanlyCanFail(t *testing.T) {
	t.Parallel()

	subject := lifecycletest.NewLifecycleStub(t,
		lifecycletest.WithLifecycleClose(func(context.Context) error {
			return errors.New("violate: teardown refused")
		}))

	got := testkit.Rejects(t, "a teardown that refuses from the first call",
		func(tb testing.TB) {
			tb.Helper()
			lifecycletest.AssertLifecycleCloseCloseCleanly(tb, subject)
		})
	testkit.Assert(t, got).Contains("Close on a fresh subject must succeed before anything about a second call can be stated",
		"and rejects it for the reason the check is about")
}

// TestAssertLifecycleCloseCloseTwiceCanFail holds AssertLifecycleCloseCloseTwice to being able to fail.
//
// The stand-in closes cleanly once and refuses every call after, which is the
// subject the second-call claim exists to reject.
func TestAssertLifecycleCloseCloseTwiceCanFail(t *testing.T) {
	t.Parallel()

	closes := 0
	subject := lifecycletest.NewLifecycleStub(t,
		lifecycletest.WithLifecycleClose(func(context.Context) error {
			closes++
			if closes > 1 {
				return errors.New("violate: already closed")
			}
			return nil
		}))

	got := testkit.Rejects(t, "a teardown that refuses its second call",
		func(tb testing.TB) {
			tb.Helper()
			lifecycletest.AssertLifecycleCloseCloseTwice(tb, subject)
		})
	testkit.Assert(t, got).Contains("a second Close after a clean one must be a no-op",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance d76879f7536b9ef47787a7f1880d14163517d38a3b30e28431a0b13860dc30b4
