// Code generated by testkit. DO NOT EDIT.
//
// Source:    conformance/corpus/iface/detector/multiaggregator/iface.go
// Plugins:   golang 1.0.0, suite 1.16.0, backend.golang 1.0.0
// Command:   testkit run ./conformance/corpus/...

package multiaggregatortest

import (
	"context"
	"slices"
	"testing"

	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/multiaggregator"
	"go.thesmos.sh/testkit/engine/suite"
)

// MultiAggregatorStatsCheck is one assertion about Stats.
//
// Every generated check for Stats is a value of it, and so is one you
// write — so they compose, reorder, and each runs standalone. testing.TB rather
// than *testing.T is what lets a stand-in drive it and prove it can fail.
//
// Stats returns a value beside its error and takes nothing to vary, so no
// "an error carries the zero value" check is generated for it. That check reaches
// the failure it is about by choosing an input that misses, and this signature
// offers none — a generated one could only demand failure from a correct
// implementation. Write it here against a subject this package can break.
type MultiAggregatorStatsCheck func(tb testing.TB, subject multiaggregator.MultiAggregator)

// MultiAggregatorFixture holds every input the generated checks run against.
//
// Derived from each method's parameter names and types. Every field has a
// companion holding a second, different value: a check comparing a result
// against a single input passes whenever the subject happened to be seeded with
// it, and a miss check whose key happens to hit asserts nothing.
//
// A field whose type admits no literal — a func, a channel, a type from a
// package this run never read — is declared and left at its zero value, and
// every generated check that needed it was dropped rather than emitted against
// a value nobody could write. Supply one through MultiAggregatorWithFixture
// and the check a consumer writes has something to use.
type MultiAggregatorFixture struct {
}

// DefaultMultiAggregatorFixture is what this run derived. Replace any field through
// MultiAggregatorWithFixture; the rest keep these values.
func DefaultMultiAggregatorFixture() MultiAggregatorFixture {
	return MultiAggregatorFixture{}
}

// MultiAggregator is the witnessed instantiation.
//
// A generic interface aliases with its own parameters, which Go admits
// since 1.24 — so every declaration below names MultiAggregator and
// the arguments follow, exactly as they do for a concrete one.
type MultiAggregator = multiaggregator.MultiAggregator

// MultiAggregatorRunOpt is one thing you can pass to RunMultiAggregator.
type MultiAggregatorRunOpt interface {
	applyTo(*multiAggregatorRunConfig)
}

type multiAggregatorRunConfig struct {
	suite.Bundle[MultiAggregator]
}

// MultiAggregatorHarness declares one MultiAggregator implementation under test.
type MultiAggregatorHarness[T MultiAggregator] struct {
	Name string

	// Exactly one constructor.
	New   func() T
	Start func(tb testing.TB) T

	// Excuse lists checks this subject structurally cannot take.
	Excuse []suite.ID

	// Oracle marks this subject as the run's reference. At most one.
	Oracle bool

	// Serial keeps this subject out of the parallel group.
	Serial bool

	// Provide answers open capabilities beyond the named fields — the
	// doors only this interface's checks know. The runner's gate reads
	// it for any capability without a dedicated field.
	Provide map[suite.Capability]any
}

// Subject lowers the declaration into what the runner consumes.
func (h MultiAggregatorHarness[T]) Subject() (suite.Subject[MultiAggregator], error) {
	build, err := suite.OneCtor(h.Name, h.New, h.Start)
	if err != nil {
		return suite.Subject[MultiAggregator]{}, err
	}
	return suite.Subject[MultiAggregator]{
		Name:     h.Name,
		Provides: h.Provide,
		New:      func(tb testing.TB) MultiAggregator { return build(tb) },
		Oracle:   h.Oracle,
		Serial:   h.Serial,
		Excused:  suite.ExcuseSet(h.Excuse),
	}, nil
}

func (h MultiAggregatorHarness[T]) applyTo(rc *multiAggregatorRunConfig) {
	rc.AddSubject(h.Subject())
}

type multiAggregatorDropOpt []suite.ID

func (d multiAggregatorDropOpt) applyTo(rc *multiAggregatorRunConfig) {
	rc.AddDrops(d...)
}

// multiAggregatorWithout drops checks by identity.
func multiAggregatorWithout(ids ...suite.ID) MultiAggregatorRunOpt {
	return multiAggregatorDropOpt(ids)
}

// --- The veneer -------------------------------------------------------------

// MultiAggregatorSuite is the entry a consumer names checks through.
var MultiAggregatorSuite = multiAggregatorVeneer{Checks: multiAggregatorCheckIndex}

type multiAggregatorVeneer struct {
	// Checks is the check index — the constants a Without call is
	// written against.
	Checks multiAggregatorCheckIndexT
}

// Without drops a check by ID.
func (multiAggregatorVeneer) Without(ids ...suite.ID) MultiAggregatorRunOpt {
	return multiAggregatorWithout(ids...)
}

// --- Drop hints -------------------------------------------------------------

// multiAggregatorIndexPath spells every emitted ID as the path a
// consumer would write to drop it, so a run that declines something can
// say what to type rather than what failed.
var multiAggregatorIndexPath = map[suite.ID]string{
	multiAggregatorCheckIndex.Stats.Smoke():       "MultiAggregatorSuite.Checks.Stats.Smoke()",
	multiAggregatorCheckIndex.Stats.Cancels():     "MultiAggregatorSuite.Checks.Stats.Cancels()",
	multiAggregatorCheckIndex.Stats.NilContext():  "MultiAggregatorSuite.Checks.Stats.NilContext()",
	multiAggregatorCheckIndex.Stats.Deadline():    "MultiAggregatorSuite.Checks.Stats.Deadline()",
	multiAggregatorCheckIndex.Stats.ZeroOnError(): "MultiAggregatorSuite.Checks.Stats.ZeroOnError()",
	multiAggregatorCheckIndex.Model.Laws():        "MultiAggregatorSuite.Checks.Model.Laws()",
}

var multiAggregatorDropHint = suite.DropHinter(
	"MultiAggregatorSuite", multiAggregatorIndexPath,
)

// The names this file spells more than once: one home each.
const (
	multiAggregatorStats = "Stats"

	// The interface's word inside a family-scoped identity.
	multiAggregatorQualifier = "multi-aggregator"
)

// multiAggregatorCheckIndex indexes every check this package emits, by
// method and then by check.
var multiAggregatorCheckIndex = multiAggregatorCheckIndexT{
	Stats: multiAggregatorStatsChecks{},
	Model: multiAggregatorModelChecks{},
}

type multiAggregatorCheckIndexT struct {
	Stats multiAggregatorStatsChecks
	Model multiAggregatorModelChecks
}

// All returns every ID this package emits.
func (multiAggregatorCheckIndexT) All() []suite.ID {
	var out []suite.ID
	out = append(out, multiAggregatorStatsChecks{}.All()...)
	out = append(out, multiAggregatorModelChecks{}.All()...)
	return out
}

type multiAggregatorStatsChecks struct{}

func (multiAggregatorStatsChecks) Smoke() suite.ID {
	return suite.MethodID(multiAggregatorStats, suite.SegSmoke)
}

func (multiAggregatorStatsChecks) Cancels() suite.ID {
	return suite.MethodID(multiAggregatorStats, suite.SegCancel)
}

func (multiAggregatorStatsChecks) NilContext() suite.ID {
	return suite.MethodID(multiAggregatorStats, suite.SegNilContext)
}

func (multiAggregatorStatsChecks) Deadline() suite.ID {
	return suite.MethodID(multiAggregatorStats, suite.SegDeadline)
}

func (multiAggregatorStatsChecks) ZeroOnError() suite.ID {
	return suite.MethodID(multiAggregatorStats, suite.SegZeroValue)
}

func (multiAggregatorStatsChecks) All() []suite.ID {
	return []suite.ID{
		multiAggregatorStatsChecks{}.Smoke(),
		multiAggregatorStatsChecks{}.Cancels(),
		multiAggregatorStatsChecks{}.NilContext(),
		multiAggregatorStatsChecks{}.Deadline(),
		multiAggregatorStatsChecks{}.ZeroOnError(),
	}
}

type multiAggregatorModelChecks struct{}

func (multiAggregatorModelChecks) Laws() suite.ID {
	return suite.FamilyID(suite.FamilyModel, multiAggregatorQualifier, suite.SegLaws)
}

func (multiAggregatorModelChecks) All() []suite.ID {
	return []suite.ID{
		multiAggregatorModelChecks{}.Laws(),
	}
}

// multiAggregatorSignatureChecks builds every check this run derived.
//
// sig is the one shape they share; the literals below state only what
// differs. Falsifiable is Proven wherever a defect rule reached the
// claim, which the parity gate refuses to stamp without one.
func multiAggregatorSignatureChecks() []suite.Check[MultiAggregator] {
	sig := suite.ProvenCheck[MultiAggregator]
	ix := multiAggregatorCheckIndex
	return []suite.Check[MultiAggregator]{
		sig(ix.Stats.Smoke(), suite.ClassSmoke,
			"Stats survives a call",
			func(tb testing.TB, m MultiAggregator) {
				multiAggregatorAssertStatsSmoke(tb, m)
			}),
		sig(ix.Stats.Cancels(), suite.ClassCancel,
			"Stats reports a cancelled context as cancelled",
			func(tb testing.TB, m MultiAggregator) {
				multiAggregatorAssertStatsCancels(tb, m)
			}),
		sig(ix.Stats.NilContext(), suite.ClassNilContext,
			"Stats returns an error rather than panicking on a nil context",
			func(tb testing.TB, m MultiAggregator) {
				multiAggregatorAssertStatsToleratesNilContext(tb, m)
			}),
		sig(ix.Stats.Deadline(), suite.ClassDeadline,
			"Stats reports an expired deadline as exceeded",
			func(tb testing.TB, m MultiAggregator) {
				multiAggregatorAssertStatsHonoursDeadline(tb, m)
			}),
		sig(ix.Stats.ZeroOnError(), suite.ClassZeroValue,
			"Stats returns zero alongside any error",
			func(tb testing.TB, m MultiAggregator) {
				multiAggregatorAssertStatsZeroOnError(tb, m)
			}),
	}
}

// multiAggregatorAssertStatsSmoke asserts Stats survives a call.
func multiAggregatorAssertStatsSmoke(
	tb testing.TB,
	m MultiAggregator,
) {
	tb.Helper()
	suite.Survives(tb, multiAggregatorStats, func(ctx context.Context) {
		_, _, _ = m.Stats(ctx)
	})
}

// multiAggregatorAssertStatsCancels asserts Stats reports a cancelled context as cancelled.
func multiAggregatorAssertStatsCancels(
	tb testing.TB,
	m MultiAggregator,
) {
	tb.Helper()
	suite.ReportsCancelled(tb, multiAggregatorStats, func(ctx context.Context) error {
		_, _, err := m.Stats(ctx)
		return err
	})
}

// multiAggregatorAssertStatsToleratesNilContext asserts Stats returns an error rather than panicking on a nil context.
func multiAggregatorAssertStatsToleratesNilContext(
	tb testing.TB,
	m MultiAggregator,
) {
	tb.Helper()
	suite.ToleratesNilContext(tb, multiAggregatorStats, func(ctx context.Context) error {
		_, _, err := m.Stats(ctx)
		return err
	})
}

// multiAggregatorAssertStatsHonoursDeadline asserts Stats reports an expired deadline as exceeded.
func multiAggregatorAssertStatsHonoursDeadline(
	tb testing.TB,
	m MultiAggregator,
) {
	tb.Helper()
	suite.ReportsDeadlineExceeded(tb, multiAggregatorStats, func(ctx context.Context) error {
		_, _, err := m.Stats(ctx)
		return err
	})
}

// multiAggregatorAssertStatsZeroOnError asserts Stats returns zero alongside any error.
func multiAggregatorAssertStatsZeroOnError(
	tb testing.TB,
	m MultiAggregator,
) {
	tb.Helper()
	ctx, cancel := context.WithCancel(tb.Context())
	cancel()
	got, _, err := m.Stats(ctx)
	if err == nil {
		tb.Skip("Stats answered for a cancelled context, so this check has no error to inspect")
	}

	var zero int
	if got != zero {
		tb.Errorf("Stats must return the zero value alongside an error: got %+v, want %+v (err %v)",
			got, zero, err)
	}
}

// AssertMultiAggregatorContract runs every generated check against every declared subject.
//
//	Subjects: declare each with MultiAggregatorSubject
//	Extend:   MultiAggregatorOnStats
//	Drop:     MultiAggregatorWithout, by the path each check reports under
//	Unseeded: every check here runs against a fresh subject, because
//	          no method here is classified as a write.
//	          A read over an empty subject cannot tell a miss from a bug;
//	          MultiAggregatorSeed is what closes it.
func AssertMultiAggregatorContract(t *testing.T, opts ...MultiAggregatorOption) {
	t.Helper()
	cfg := newMultiAggregatorConfig(opts...)
	if len(cfg.subjects) == 0 {
		t.Fatalf("AssertMultiAggregatorContract was given no subject; declare one with " +
			"MultiAggregatorSubject(name, factory)")
	}
	for _, p := range cfg.phantoms() {
		t.Errorf("MultiAggregatorWithout(%q) declines nothing: no check here reports "+
			"under that path, so this suite asserts one thing more than its "+
			"author believes", p)
	}
	for _, s := range cfg.subjects {
		runMultiAggregatorChecks(t, cfg, s.name, s.factory, nil)
	}
}

// runMultiAggregatorChecks runs the whole contract against one subject.
//
// wrap is applied after the subject is built and seeded, so a wrapper is
// transparent to the seed and every failure it causes is its own.
func runMultiAggregatorChecks(
	t *testing.T,
	cfg *multiaggregatorConfig,
	label string,
	factory func() multiaggregator.MultiAggregator,
	wrap func(tb testing.TB, subject multiaggregator.MultiAggregator) multiaggregator.MultiAggregator,
) {
	t.Helper()
	t.Run(label, func(t *testing.T) {
		t.Parallel()

		t.Run(multiAggregatorStats, func(t *testing.T) {
			t.Parallel()

			for _, c := range cfg.onStats {
				cfg.run(t, string(suite.MethodID(multiAggregatorStats, c.name)), c.name, func(tb testing.TB) {
					c.fn(tb, cfg.subject(tb, factory, wrap))
				})
			}
		})

		// The extra bodies of checks this run was given, each reporting under its
		// own name and dropped by it.
		//
		// The plain subject only. The wrapped pass exists to prove the double
		// faithful and the per-method checks above already do that, so running one
		// through the wrapper prices the wrapper and says nothing new about the
		// subject.
		if wrap == nil {
			for _, e := range cfg.extensions {
				if cfg.declined(e.name) {
					t.Run(e.name, func(t *testing.T) {
						t.Skipf("dropped through MultiAggregatorWithout(%q)", e.name)
					})
					continue
				}
				t.Run(e.name, func(t *testing.T) {
					t.Parallel()
					e.run(t, label, factory, cfg)
				})
			}
		}
	})
}

// MultiAggregatorOption configures a contract run.
type MultiAggregatorOption func(*multiaggregatorConfig)

// MultiAggregatorSubject declares an implementation to hold to the contract.
//
// Repeat it for every implementation that claims to satisfy the interface — an
// in-memory one, the one backed by a database, the one behind a network. Every
// check and every option is stated once and runs against all of them, which is
// what a conformance suite is for.
func MultiAggregatorSubject(name string, factory func() multiaggregator.MultiAggregator) MultiAggregatorOption {
	return func(c *multiaggregatorConfig) {
		c.subjects = append(c.subjects, namedMultiAggregatorSubject{name, factory})
	}
}

// MultiAggregatorWithClock supplies the clock every time-reading check measures on.
//
// Defaults to the real one, so an implementation that does not take a clock
// behaves as it would have. Supply a [clock.TestClock] — the same one the
// factory builds the subject with — and a budget becomes a claim about the time
// the implementation means to spend rather than about how loaded the machine
// was.
func MultiAggregatorWithClock(c clock.Clock) MultiAggregatorOption {
	return func(cfg *multiaggregatorConfig) { cfg.clock = c }
}

// MultiAggregatorWithFixture replaces the derived inputs.
func MultiAggregatorWithFixture(f MultiAggregatorFixture) MultiAggregatorOption {
	return func(c *multiaggregatorConfig) { c.Fixture = f }
}

// MultiAggregatorSeed replaces the derived seed.
//
// For an interface with no writer to seed itself through, which is the one
// input a conformance run cannot derive: a reader over an empty subject
// asserts nothing.
//
// Returning an error rather than swallowing one. A seed that failed quietly
// leaves every check after it asserting nothing and reporting success.
func MultiAggregatorSeed(fn func(ctx context.Context, subject multiaggregator.MultiAggregator) error) MultiAggregatorOption {
	return func(c *multiaggregatorConfig) {
		c.seed = fn
	}
}

// MultiAggregatorOnStats adds a named check to Stats.
func MultiAggregatorOnStats(name string, fn MultiAggregatorStatsCheck) MultiAggregatorOption {
	return func(c *multiaggregatorConfig) {
		c.onStats = append(c.onStats, namedMultiAggregatorStatsCheck{name, fn})
	}
}

// MultiAggregatorWithout drops generated checks by the path each reports under.
//
// For a subject that legitimately violates one. Without this the only recourse
// is to stop running the suite, which costs every other check in the file — so
// a dropped check is reported as skipped rather than silently omitted.
func MultiAggregatorWithout(paths ...string) MultiAggregatorOption {
	return func(c *multiaggregatorConfig) {
		for _, p := range paths {
			c.without[p] = struct{}{}
		}
	}
}

type namedMultiAggregatorStatsCheck struct {
	name string
	fn   MultiAggregatorStatsCheck
}

type namedMultiAggregatorSubject struct {
	name    string
	factory func() multiaggregator.MultiAggregator
}

// multiaggregatorContractExtension is a body of checks a generated sibling adds through an option.
//
// A sibling declares the option and closes over whatever configures it, so
// everything that varies per run is typed and private to the file that owns it
// — this file understands only the name and the call. Nothing arrives except
// through an option, which is what keeps the set fixed before the first subtest
// starts.
type multiaggregatorContractExtension struct {
	// name is the path this reports under, and the one MultiAggregatorWithout drops
	// it by. Anything nested beneath it is the extension's own to name.
	name string

	// run takes the factory rather than a subject. One that drives sequences
	// builds a subject per iteration, and one that compares against a reference
	// needs the unseeded subject a factory returns — which
	// multiaggregatorConfig.subject is not.
	//
	// Called once per subject, in parallel, sharing the config. Anything it
	// mutates has to be built inside the call: state captured when the option
	// was declared is state two subjects write at once.
	run func(
		t *testing.T,
		subject string,
		factory func() multiaggregator.MultiAggregator,
		cfg *multiaggregatorConfig,
	)
}

type multiaggregatorConfig struct {
	Fixture    MultiAggregatorFixture
	subjects   []namedMultiAggregatorSubject
	clock      clock.Clock
	seed       func(ctx context.Context, subject multiaggregator.MultiAggregator) error
	without    map[string]struct{}
	extensions []multiaggregatorContractExtension
	onStats    []namedMultiAggregatorStatsCheck
}

func newMultiAggregatorConfig(opts ...MultiAggregatorOption) *multiaggregatorConfig {
	c := &multiaggregatorConfig{
		Fixture: DefaultMultiAggregatorFixture(),
		clock:   clock.RealClock(),
		without: map[string]struct{}{},
	}
	for _, o := range opts {
		o(c)
	}
	return c
}

// subject builds a fresh implementation and seeds it, so every check runs
// against state no other check touched.
func (c *multiaggregatorConfig) subject(
	tb testing.TB,
	factory func() multiaggregator.MultiAggregator,
	wrap func(tb testing.TB, subject multiaggregator.MultiAggregator) multiaggregator.MultiAggregator,
) multiaggregator.MultiAggregator {
	tb.Helper()
	s := factory()
	if c.seed != nil {
		// Reported rather than ignored. A seed that silently failed would leave
		// every check after it running against an empty subject — passing, and
		// asserting nothing.
		//
		// Which seed ran decides what the message can usefully say. The derived
		// one fails for one of two reasons and both are fixable from here, so it
		// names them. A seed the caller supplied is theirs, and telling them to
		// supply one is advice they already took.
		if err := c.seed(tb.Context(), s); err != nil {
			tb.Fatalf("the seed supplied through MultiAggregatorSeed failed: %v", err)
		}
	}
	if wrap != nil {
		s = wrap(tb, s)
	}
	return s
}

// dropped reports whether a path was declined through MultiAggregatorWithout.
//
// Named rather than inlined because the drop set is read from two places: here,
// and by whatever registered through the seam — which reports under paths this
// file has never heard of and must answer the same question about them.
func (c *multiaggregatorConfig) declined(path string) bool {
	_, ok := c.without[path]
	return ok
}

// multiaggregatorPaths is every path this file reports a check under.
//
// The set a MultiAggregatorWithout argument has to be a member of. Declared
// rather than observed: a check that did not run is not a check that does not
// exist — an integration-only method skips without ever consulting the drop
// set, and reading that as a phantom would fail a correct suite on a machine
// with the environment unset.
//
//nolint:gochecknoglobals // the emitted check paths, read-only after init.
var multiaggregatorPaths = map[string]struct{}{}

// phantoms names every MultiAggregatorWithout path that declines nothing.
//
// A drop that drops nothing is this option's silent half: the consumer wrote
// it against a check since renamed, or never emitted for that method at all,
// and every run after reads green while asserting one thing more than its
// author believes. Extensions carry their own names — the seam reports under
// paths this file has never heard of — so they count as known too.
func (c *multiaggregatorConfig) phantoms() []string {
	known := func(p string) bool {
		if _, ok := multiaggregatorPaths[p]; ok {
			return true
		}
		for _, e := range c.extensions {
			if e.name == p {
				return true
			}
		}
		for _, on := range c.onStats {
			if string(suite.MethodID(multiAggregatorStats, on.name)) == p {
				return true
			}
		}
		return false
	}
	var out []string
	for p := range c.without {
		if !known(p) {
			out = append(out, p)
		}
	}
	slices.Sort(out)
	return out
}

// run executes one check unless it was dropped, reporting the drop rather than
// silently omitting it.
func (c *multiaggregatorConfig) run(t *testing.T, path, name string, fn func(tb testing.TB)) {
	t.Helper()
	if c.declined(path) {
		t.Run(name, func(t *testing.T) {
			t.Skipf("dropped through MultiAggregatorWithout(%q)", path)
		})
		return
	}
	t.Run(name, func(t *testing.T) {
		t.Parallel()
		fn(t)
	})
}

// testkit: end of generated content.
// testkit:provenance f388f6b90f85ef818829811309d6e9663e71b3488479e4f7dbbc6ca67e36b667
