// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/detector/multiaggregator/iface.go
// Plugins:   golang 1.0.0, suite 1.15.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package multiaggregatortest_test

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/multiaggregator/multiaggregatortest"
)

// Every generated check for MultiAggregator, driven against a stand-in built
// to violate it.
//
//	Proved:   4 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertMultiAggregatorStatsSmokeCanFail holds AssertMultiAggregatorStatsSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertMultiAggregatorStatsSmokeCanFail(t *testing.T) {
	t.Parallel()

	subject := multiaggregatortest.NewMultiAggregatorStub(t,
		multiaggregatortest.WithMultiAggregatorStats(func(context.Context) (int, int, error) {
			panic("MultiAggregator.Stats: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			multiaggregatortest.AssertMultiAggregatorStatsSmoke(tb, subject)
		})
	testkit.Assert(t, got).Contains("Stats panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertMultiAggregatorStatsCancelsCanFail holds AssertMultiAggregatorStatsCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertMultiAggregatorStatsCancelsCanFail(t *testing.T) {
	t.Parallel()

	subject := multiaggregatortest.NewMultiAggregatorStub(t,
		multiaggregatortest.WithMultiAggregatorStats(func(context.Context) (int, int, error) {
			var r0 int
			var r1 int
			return r0, r1, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			multiaggregatortest.AssertMultiAggregatorStatsCancels(tb, subject)
		})
	testkit.Assert(t, got).Contains("Stats must report a cancelled context as context.Canceled",
		"and rejects it for the reason the check is about")
}

// TestAssertMultiAggregatorStatsHonoursDeadlineCanFail holds AssertMultiAggregatorStatsHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertMultiAggregatorStatsHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	subject := multiaggregatortest.NewMultiAggregatorStub(t,
		multiaggregatortest.WithMultiAggregatorStats(func(context.Context) (int, int, error) {
			var r0 int
			var r1 int
			return r0, r1, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			multiaggregatortest.AssertMultiAggregatorStatsHonoursDeadline(tb, subject)
		})
	testkit.Assert(t, got).Contains("Stats must report an expired deadline as context.DeadlineExceeded",
		"and rejects it for the reason the check is about")
}

// TestAssertMultiAggregatorStatsToleratesNilContextCanFail holds AssertMultiAggregatorStatsToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertMultiAggregatorStatsToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	subject := multiaggregatortest.NewMultiAggregatorStub(t,
		multiaggregatortest.WithMultiAggregatorStats(func(context.Context) (int, int, error) {
			panic("MultiAggregator.Stats: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			multiaggregatortest.AssertMultiAggregatorStatsToleratesNilContext(tb, subject)
		})
	testkit.Assert(t, got).Contains("Stats panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance 604ce88b497b7f48e76166ba9e563a42eadfef459308241952a72372766d94d8
