// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/detector/lookup/iface.go
// Plugins:   golang 1.0.0, suite 1.0.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/iface/detector/lookup/... ./corpus/iface/detector/readerwithbool/... ./corpus/iface/detector/readernoerror/... ./corpus/iface/detector/pointerreader/...

package lookuptest_test

import (
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/lookup"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/lookup/lookuptest"
)

// Every generated check for Lookup, driven against a stand-in built
// to violate it.
//
//	Proved:   2 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertLookupInspectSmokeCanFail holds AssertLookupInspectSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertLookupInspectSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := lookuptest.DefaultLookupFixture()
	subject := lookuptest.NewLookupStub(t,
		lookuptest.WithLookupInspect(func(string) (lookup.Value, lookup.Meta, bool) {
			panic("Lookup.Inspect: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			lookuptest.AssertLookupInspectSmoke(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertLookupInspectReportsAMissCanFail holds AssertLookupInspectReportsAMiss to being able to fail.
//
// The stand-in answers beside a flag reporting a miss with a value that looks like a
// result. A check that let that pass would leave a caller unable to tell an
// answer from an absence, which is the whole of what this shape signals with.
func TestAssertLookupInspectReportsAMissCanFail(t *testing.T) {
	t.Parallel()

	fixture := lookuptest.DefaultLookupFixture()
	subject := lookuptest.NewLookupStub(t,
		lookuptest.WithLookupInspect(func(string) (lookup.Value, lookup.Meta, bool) {
			var r2 bool
			return lookup.Value{Key: "test-r0"}, lookup.Meta{Revision: 42}, r2
		}))

	got := testkit.Rejects(t, "a reader answering with a plausible value beside a false flag",
		func(tb testing.TB) {
			tb.Helper()
			lookuptest.AssertLookupInspectReportsAMiss(tb, subject, fixture.KeyOther)
		})
	testkit.Assert(t, got).Contains("must return the zero value",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance f93ffa2976bd64092fea92f639481301dec023bac4038989ffe69610f40d7d31
