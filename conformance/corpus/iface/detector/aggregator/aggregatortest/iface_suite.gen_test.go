// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/detector/aggregator/iface.go
// Plugins:   golang 1.0.0, suite 1.15.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package aggregatortest_test

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/aggregator/aggregatortest"
)

// Every generated check for Aggregator, driven against a stand-in built
// to violate it.
//
//	Proved:   4 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertAggregatorCountSmokeCanFail holds AssertAggregatorCountSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertAggregatorCountSmokeCanFail(t *testing.T) {
	t.Parallel()

	subject := aggregatortest.NewAggregatorStub(t,
		aggregatortest.WithAggregatorCount(func(context.Context) (int, error) {
			panic("Aggregator.Count: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			aggregatortest.AssertAggregatorCountSmoke(tb, subject)
		})
	testkit.Assert(t, got).Contains("Count panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertAggregatorCountCancelsCanFail holds AssertAggregatorCountCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertAggregatorCountCancelsCanFail(t *testing.T) {
	t.Parallel()

	subject := aggregatortest.NewAggregatorStub(t,
		aggregatortest.WithAggregatorCount(func(context.Context) (int, error) {
			var r0 int
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			aggregatortest.AssertAggregatorCountCancels(tb, subject)
		})
	testkit.Assert(t, got).Contains("Count must report a cancelled context as context.Canceled",
		"and rejects it for the reason the check is about")
}

// TestAssertAggregatorCountHonoursDeadlineCanFail holds AssertAggregatorCountHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertAggregatorCountHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	subject := aggregatortest.NewAggregatorStub(t,
		aggregatortest.WithAggregatorCount(func(context.Context) (int, error) {
			var r0 int
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			aggregatortest.AssertAggregatorCountHonoursDeadline(tb, subject)
		})
	testkit.Assert(t, got).Contains("Count must report an expired deadline as context.DeadlineExceeded",
		"and rejects it for the reason the check is about")
}

// TestAssertAggregatorCountToleratesNilContextCanFail holds AssertAggregatorCountToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertAggregatorCountToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	subject := aggregatortest.NewAggregatorStub(t,
		aggregatortest.WithAggregatorCount(func(context.Context) (int, error) {
			panic("Aggregator.Count: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			aggregatortest.AssertAggregatorCountToleratesNilContext(tb, subject)
		})
	testkit.Assert(t, got).Contains("Count panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance edbe5b9193facc69ba6cd57316dc45a9c4dd7232cabb5f93b92d5f4289a421cc
