// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/detector/streamreader/iface.go
// Plugins:   golang 1.0.0, suite 1.6.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package streamreadertest_test

import (
	"context"
	"iter"
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/streamreader"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/streamreader/streamreadertest"
)

// Every generated check for StreamReader, driven against a stand-in built
// to violate it.
//
//	Proved:   6 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertStreamReaderAddSmokeCanFail holds AssertStreamReaderAddSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertStreamReaderAddSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := streamreadertest.DefaultStreamReaderFixture()
	subject := streamreadertest.NewStreamReaderStub(t,
		streamreadertest.WithStreamReaderAdd(func(context.Context, streamreader.Value) error {
			panic("StreamReader.Add: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			streamreadertest.AssertStreamReaderAddSmoke(tb, subject, fixture.V)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertStreamReaderAddCancelsCanFail holds AssertStreamReaderAddCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertStreamReaderAddCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := streamreadertest.DefaultStreamReaderFixture()
	subject := streamreadertest.NewStreamReaderStub(t,
		streamreadertest.WithStreamReaderAdd(func(context.Context, streamreader.Value) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			streamreadertest.AssertStreamReaderAddCancels(tb, subject, fixture.V)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertStreamReaderAddHonoursDeadlineCanFail holds AssertStreamReaderAddHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertStreamReaderAddHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := streamreadertest.DefaultStreamReaderFixture()
	subject := streamreadertest.NewStreamReaderStub(t,
		streamreadertest.WithStreamReaderAdd(func(context.Context, streamreader.Value) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			streamreadertest.AssertStreamReaderAddHonoursDeadline(tb, subject, fixture.V)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertStreamReaderAddToleratesNilContextCanFail holds AssertStreamReaderAddToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertStreamReaderAddToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := streamreadertest.DefaultStreamReaderFixture()
	subject := streamreadertest.NewStreamReaderStub(t,
		streamreadertest.WithStreamReaderAdd(func(context.Context, streamreader.Value) error {
			panic("StreamReader.Add: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			streamreadertest.AssertStreamReaderAddToleratesNilContext(tb, subject, fixture.V)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertStreamReaderListSmokeCanFail holds AssertStreamReaderListSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertStreamReaderListSmokeCanFail(t *testing.T) {
	t.Parallel()

	subject := streamreadertest.NewStreamReaderStub(t,
		streamreadertest.WithStreamReaderList(func(context.Context) iter.Seq2[streamreader.Value, error] {
			panic("StreamReader.List: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			streamreadertest.AssertStreamReaderListSmoke(tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertStreamReaderListToleratesNilContextCanFail holds AssertStreamReaderListToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertStreamReaderListToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	subject := streamreadertest.NewStreamReaderStub(t,
		streamreadertest.WithStreamReaderList(func(context.Context) iter.Seq2[streamreader.Value, error] {
			panic("StreamReader.List: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			streamreadertest.AssertStreamReaderListToleratesNilContext(tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance 33e8b692096ecc20e82c33a5a433fbfbafd39c56b1ff9b490c5f083b5c7650d4
