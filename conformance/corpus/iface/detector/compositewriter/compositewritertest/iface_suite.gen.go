// Code generated by testkit. DO NOT EDIT.
//
// Source:    conformance/corpus/iface/detector/compositewriter/iface.go
// Plugins:   golang 1.0.0, suite 1.16.0, backend.golang 1.0.0
// Command:   testkit run ./conformance/corpus/...

package compositewritertest

import (
	"context"
	"slices"
	"testing"

	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/compositewriter"
	"go.thesmos.sh/testkit/engine/suite"
)

// CompositeWriterSetCheck is one assertion about Set.
//
// Every generated check for Set is a value of it, and so is one you
// write — so they compose, reorder, and each runs standalone. testing.TB rather
// than *testing.T is what lets a stand-in drive it and prove it can fail.
type CompositeWriterSetCheck func(tb testing.TB, subject compositewriter.CompositeWriter, key string, v compositewriter.Value)

// CompositeWriterFixture holds every input the generated checks run against.
//
// Derived from each method's parameter names and types. Every field has a
// companion holding a second, different value: a check comparing a result
// against a single input passes whenever the subject happened to be seeded with
// it, and a miss check whose key happens to hit asserts nothing.
//
// A field whose type admits no literal — a func, a channel, a type from a
// package this run never read — is declared and left at its zero value, and
// every generated check that needed it was dropped rather than emitted against
// a value nobody could write. Supply one through CompositeWriterWithFixture
// and the check a consumer writes has something to use.
type CompositeWriterFixture struct {
	Key        string
	KeyOther   string
	Value      compositewriter.Value
	ValueOther compositewriter.Value
}

// DefaultCompositeWriterFixture is what this run derived. Replace any field through
// CompositeWriterWithFixture; the rest keep these values.
func DefaultCompositeWriterFixture() CompositeWriterFixture {
	return CompositeWriterFixture{
		Key:        "test-key",
		KeyOther:   "other-key",
		Value:      compositewriter.Value{Key: "test-key", Body: "test-body", Rev: "test-rev"},
		ValueOther: compositewriter.Value{Key: "other-key", Body: "other-body", Rev: "other-rev"},
	}
}

// CompositeWriter is the witnessed instantiation.
//
// A generic interface aliases with its own parameters, which Go admits
// since 1.24 — so every declaration below names CompositeWriter and
// the arguments follow, exactly as they do for a concrete one.
type CompositeWriter = compositewriter.CompositeWriter

// CompositeWriterRunOpt is one thing you can pass to RunCompositeWriter.
type CompositeWriterRunOpt interface {
	applyTo(*compositeWriterRunConfig)
}

type compositeWriterRunConfig struct {
	suite.Bundle[CompositeWriter]
}

// CompositeWriterHarness declares one CompositeWriter implementation under test.
type CompositeWriterHarness[T CompositeWriter] struct {
	Name string

	// Exactly one constructor.
	New   func() T
	Start func(tb testing.TB) T

	// Excuse lists checks this subject structurally cannot take.
	Excuse []suite.ID

	// Oracle marks this subject as the run's reference. At most one.
	Oracle bool

	// Serial keeps this subject out of the parallel group.
	Serial bool

	// Provide answers open capabilities beyond the named fields — the
	// doors only this interface's checks know. The runner's gate reads
	// it for any capability without a dedicated field.
	Provide map[suite.Capability]any
}

// Subject lowers the declaration into what the runner consumes.
func (h CompositeWriterHarness[T]) Subject() (suite.Subject[CompositeWriter], error) {
	build, err := suite.OneCtor(h.Name, h.New, h.Start)
	if err != nil {
		return suite.Subject[CompositeWriter]{}, err
	}
	return suite.Subject[CompositeWriter]{
		Name:     h.Name,
		Provides: h.Provide,
		New:      func(tb testing.TB) CompositeWriter { return build(tb) },
		Oracle:   h.Oracle,
		Serial:   h.Serial,
		Excused:  suite.ExcuseSet(h.Excuse),
	}, nil
}

func (h CompositeWriterHarness[T]) applyTo(rc *compositeWriterRunConfig) {
	rc.AddSubject(h.Subject())
}

type compositeWriterDropOpt []suite.ID

func (d compositeWriterDropOpt) applyTo(rc *compositeWriterRunConfig) {
	rc.AddDrops(d...)
}

// compositeWriterWithout drops checks by identity.
func compositeWriterWithout(ids ...suite.ID) CompositeWriterRunOpt {
	return compositeWriterDropOpt(ids)
}

// --- The veneer -------------------------------------------------------------

// CompositeWriterSuite is the entry a consumer names checks through.
var CompositeWriterSuite = compositeWriterVeneer{Checks: compositeWriterCheckIndex}

type compositeWriterVeneer struct {
	// Checks is the check index — the constants a Without call is
	// written against.
	Checks compositeWriterCheckIndexT
}

// Without drops a check by ID.
func (compositeWriterVeneer) Without(ids ...suite.ID) CompositeWriterRunOpt {
	return compositeWriterWithout(ids...)
}

// --- Drop hints -------------------------------------------------------------

// compositeWriterIndexPath spells every emitted ID as the path a
// consumer would write to drop it, so a run that declines something can
// say what to type rather than what failed.
var compositeWriterIndexPath = map[suite.ID]string{
	compositeWriterCheckIndex.Set.Smoke():      "CompositeWriterSuite.Checks.Set.Smoke()",
	compositeWriterCheckIndex.Set.Cancels():    "CompositeWriterSuite.Checks.Set.Cancels()",
	compositeWriterCheckIndex.Set.NilContext(): "CompositeWriterSuite.Checks.Set.NilContext()",
	compositeWriterCheckIndex.Set.Deadline():   "CompositeWriterSuite.Checks.Set.Deadline()",
	compositeWriterCheckIndex.Model.Laws():     "CompositeWriterSuite.Checks.Model.Laws()",
}

var compositeWriterDropHint = suite.DropHinter(
	"CompositeWriterSuite", compositeWriterIndexPath,
)

// The names this file spells more than once: one home each.
const (
	compositeWriterSet = "Set"

	// The interface's word inside a family-scoped identity.
	compositeWriterQualifier = "composite-writer"
)

// compositeWriterCheckIndex indexes every check this package emits, by
// method and then by check.
var compositeWriterCheckIndex = compositeWriterCheckIndexT{
	Set:   compositeWriterSetChecks{},
	Model: compositeWriterModelChecks{},
}

type compositeWriterCheckIndexT struct {
	Set   compositeWriterSetChecks
	Model compositeWriterModelChecks
}

// All returns every ID this package emits.
func (compositeWriterCheckIndexT) All() []suite.ID {
	var out []suite.ID
	out = append(out, compositeWriterSetChecks{}.All()...)
	out = append(out, compositeWriterModelChecks{}.All()...)
	return out
}

type compositeWriterSetChecks struct{}

func (compositeWriterSetChecks) Smoke() suite.ID {
	return suite.MethodID(compositeWriterSet, suite.SegSmoke)
}

func (compositeWriterSetChecks) Cancels() suite.ID {
	return suite.MethodID(compositeWriterSet, suite.SegCancel)
}

func (compositeWriterSetChecks) NilContext() suite.ID {
	return suite.MethodID(compositeWriterSet, suite.SegNilContext)
}

func (compositeWriterSetChecks) Deadline() suite.ID {
	return suite.MethodID(compositeWriterSet, suite.SegDeadline)
}

func (compositeWriterSetChecks) All() []suite.ID {
	return []suite.ID{
		compositeWriterSetChecks{}.Smoke(),
		compositeWriterSetChecks{}.Cancels(),
		compositeWriterSetChecks{}.NilContext(),
		compositeWriterSetChecks{}.Deadline(),
	}
}

type compositeWriterModelChecks struct{}

func (compositeWriterModelChecks) Laws() suite.ID {
	return suite.FamilyID(suite.FamilyModel, compositeWriterQualifier, suite.SegLaws)
}

func (compositeWriterModelChecks) All() []suite.ID {
	return []suite.ID{
		compositeWriterModelChecks{}.Laws(),
	}
}

// compositeWriterSignatureChecks builds every check this run derived.
//
// sig is the one shape they share; the literals below state only what
// differs. Falsifiable is Proven wherever a defect rule reached the
// claim, which the parity gate refuses to stamp without one.
func compositeWriterSignatureChecks(fx CompositeWriterFixture) []suite.Check[CompositeWriter] {
	sig := suite.ProvenCheck[CompositeWriter]
	ix := compositeWriterCheckIndex
	return []suite.Check[CompositeWriter]{
		sig(ix.Set.Smoke(), suite.ClassSmoke,
			"Set survives a call with derived inputs",
			func(tb testing.TB, c CompositeWriter) {
				compositeWriterAssertSetSmoke(tb, c, fx)
			}),
		sig(ix.Set.Cancels(), suite.ClassCancel,
			"Set reports a cancelled context as cancelled",
			func(tb testing.TB, c CompositeWriter) {
				compositeWriterAssertSetCancels(tb, c, fx)
			}),
		sig(ix.Set.NilContext(), suite.ClassNilContext,
			"Set returns an error rather than panicking on a nil context",
			func(tb testing.TB, c CompositeWriter) {
				compositeWriterAssertSetToleratesNilContext(tb, c, fx)
			}),
		sig(ix.Set.Deadline(), suite.ClassDeadline,
			"Set reports an expired deadline as exceeded",
			func(tb testing.TB, c CompositeWriter) {
				compositeWriterAssertSetHonoursDeadline(tb, c, fx)
			}),
	}
}

// compositeWriterAssertSetSmoke asserts Set survives a call with derived inputs.
func compositeWriterAssertSetSmoke(
	tb testing.TB,
	c CompositeWriter,
	fx CompositeWriterFixture,
) {
	tb.Helper()
	suite.Survives(tb, compositeWriterSet, func(ctx context.Context) {
		_ = c.Set(ctx, fx.Key, fx.Value)
	})
}

// compositeWriterAssertSetCancels asserts Set reports a cancelled context as cancelled.
func compositeWriterAssertSetCancels(
	tb testing.TB,
	c CompositeWriter,
	fx CompositeWriterFixture,
) {
	tb.Helper()
	suite.ReportsCancelled(tb, compositeWriterSet, func(ctx context.Context) error {
		return c.Set(ctx, fx.Key, fx.Value)
	})
}

// compositeWriterAssertSetToleratesNilContext asserts Set returns an error rather than panicking on a nil context.
func compositeWriterAssertSetToleratesNilContext(
	tb testing.TB,
	c CompositeWriter,
	fx CompositeWriterFixture,
) {
	tb.Helper()
	suite.ToleratesNilContext(tb, compositeWriterSet, func(ctx context.Context) error {
		return c.Set(ctx, fx.Key, fx.Value)
	})
}

// compositeWriterAssertSetHonoursDeadline asserts Set reports an expired deadline as exceeded.
func compositeWriterAssertSetHonoursDeadline(
	tb testing.TB,
	c CompositeWriter,
	fx CompositeWriterFixture,
) {
	tb.Helper()
	suite.ReportsDeadlineExceeded(tb, compositeWriterSet, func(ctx context.Context) error {
		return c.Set(ctx, fx.Key, fx.Value)
	})
}

// AssertCompositeWriterContract runs every generated check against every declared subject.
//
//	Subjects: declare each with CompositeWriterSubject
//	Extend:   CompositeWriterOnSet
//	Drop:     CompositeWriterWithout, by the path each check reports under
func AssertCompositeWriterContract(t *testing.T, opts ...CompositeWriterOption) {
	t.Helper()
	cfg := newCompositeWriterConfig(opts...)
	if len(cfg.subjects) == 0 {
		t.Fatalf("AssertCompositeWriterContract was given no subject; declare one with " +
			"CompositeWriterSubject(name, factory)")
	}
	for _, p := range cfg.phantoms() {
		t.Errorf("CompositeWriterWithout(%q) declines nothing: no check here reports "+
			"under that path, so this suite asserts one thing more than its "+
			"author believes", p)
	}
	for _, s := range cfg.subjects {
		runCompositeWriterChecks(t, cfg, s.name, s.factory, nil)
	}
}

// runCompositeWriterChecks runs the whole contract against one subject.
//
// wrap is applied after the subject is built and seeded, so a wrapper is
// transparent to the seed and every failure it causes is its own.
func runCompositeWriterChecks(
	t *testing.T,
	cfg *compositewriterConfig,
	label string,
	factory func() compositewriter.CompositeWriter,
	wrap func(tb testing.TB, subject compositewriter.CompositeWriter) compositewriter.CompositeWriter,
) {
	t.Helper()
	t.Run(label, func(t *testing.T) {
		t.Parallel()

		t.Run(compositeWriterSet, func(t *testing.T) {
			t.Parallel()

			for _, c := range cfg.onSet {
				cfg.run(t, string(suite.MethodID(compositeWriterSet, c.name)), c.name, func(tb testing.TB) {
					c.fn(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.Key, cfg.Fixture.Value)
				})
			}
		})

		// The extra bodies of checks this run was given, each reporting under its
		// own name and dropped by it.
		//
		// The plain subject only. The wrapped pass exists to prove the double
		// faithful and the per-method checks above already do that, so running one
		// through the wrapper prices the wrapper and says nothing new about the
		// subject.
		if wrap == nil {
			for _, e := range cfg.extensions {
				if cfg.declined(e.name) {
					t.Run(e.name, func(t *testing.T) {
						t.Skipf("dropped through CompositeWriterWithout(%q)", e.name)
					})
					continue
				}
				t.Run(e.name, func(t *testing.T) {
					t.Parallel()
					e.run(t, label, factory, cfg)
				})
			}
		}
	})
}

// CompositeWriterOption configures a contract run.
type CompositeWriterOption func(*compositewriterConfig)

// CompositeWriterSubject declares an implementation to hold to the contract.
//
// Repeat it for every implementation that claims to satisfy the interface — an
// in-memory one, the one backed by a database, the one behind a network. Every
// check and every option is stated once and runs against all of them, which is
// what a conformance suite is for.
func CompositeWriterSubject(name string, factory func() compositewriter.CompositeWriter) CompositeWriterOption {
	return func(c *compositewriterConfig) {
		c.subjects = append(c.subjects, namedCompositeWriterSubject{name, factory})
	}
}

// CompositeWriterWithClock supplies the clock every time-reading check measures on.
//
// Defaults to the real one, so an implementation that does not take a clock
// behaves as it would have. Supply a [clock.TestClock] — the same one the
// factory builds the subject with — and a budget becomes a claim about the time
// the implementation means to spend rather than about how loaded the machine
// was.
func CompositeWriterWithClock(c clock.Clock) CompositeWriterOption {
	return func(cfg *compositewriterConfig) { cfg.clock = c }
}

// CompositeWriterWithFixture replaces the derived inputs.
func CompositeWriterWithFixture(f CompositeWriterFixture) CompositeWriterOption {
	return func(c *compositewriterConfig) { c.Fixture = f }
}

// CompositeWriterSeed replaces the derived seed.
//
// For an interface with no writer to seed itself through, which is the one
// input a conformance run cannot derive: a reader over an empty subject
// asserts nothing.
//
// Returning an error rather than swallowing one. A seed that failed quietly
// leaves every check after it asserting nothing and reporting success.
func CompositeWriterSeed(fn func(ctx context.Context, subject compositewriter.CompositeWriter) error) CompositeWriterOption {
	return func(c *compositewriterConfig) {
		c.seed = fn
		c.seedIsDerived = false
	}
}

// CompositeWriterOnSet adds a named check to Set.
func CompositeWriterOnSet(name string, fn CompositeWriterSetCheck) CompositeWriterOption {
	return func(c *compositewriterConfig) {
		c.onSet = append(c.onSet, namedCompositeWriterSetCheck{name, fn})
	}
}

// CompositeWriterWithout drops generated checks by the path each reports under.
//
// For a subject that legitimately violates one. Without this the only recourse
// is to stop running the suite, which costs every other check in the file — so
// a dropped check is reported as skipped rather than silently omitted.
func CompositeWriterWithout(paths ...string) CompositeWriterOption {
	return func(c *compositewriterConfig) {
		for _, p := range paths {
			c.without[p] = struct{}{}
		}
	}
}

type namedCompositeWriterSetCheck struct {
	name string
	fn   CompositeWriterSetCheck
}

type namedCompositeWriterSubject struct {
	name    string
	factory func() compositewriter.CompositeWriter
}

// compositewriterContractExtension is a body of checks a generated sibling adds through an option.
//
// A sibling declares the option and closes over whatever configures it, so
// everything that varies per run is typed and private to the file that owns it
// — this file understands only the name and the call. Nothing arrives except
// through an option, which is what keeps the set fixed before the first subtest
// starts.
type compositewriterContractExtension struct {
	// name is the path this reports under, and the one CompositeWriterWithout drops
	// it by. Anything nested beneath it is the extension's own to name.
	name string

	// run takes the factory rather than a subject. One that drives sequences
	// builds a subject per iteration, and one that compares against a reference
	// needs the unseeded subject a factory returns — which
	// compositewriterConfig.subject is not.
	//
	// Called once per subject, in parallel, sharing the config. Anything it
	// mutates has to be built inside the call: state captured when the option
	// was declared is state two subjects write at once.
	run func(
		t *testing.T,
		subject string,
		factory func() compositewriter.CompositeWriter,
		cfg *compositewriterConfig,
	)
}

type compositewriterConfig struct {
	Fixture  CompositeWriterFixture
	subjects []namedCompositeWriterSubject
	clock    clock.Clock
	seed     func(ctx context.Context, subject compositewriter.CompositeWriter) error
	// seedIsDerived is what lets a failed seed name the right culprit. The
	// derived seed and a consumer's are the same field, and the advice for one
	// is useless for the other.
	seedIsDerived bool
	without       map[string]struct{}
	extensions    []compositewriterContractExtension
	onSet         []namedCompositeWriterSetCheck
}

func newCompositeWriterConfig(opts ...CompositeWriterOption) *compositewriterConfig {
	c := &compositewriterConfig{
		Fixture: DefaultCompositeWriterFixture(),
		clock:   clock.RealClock(),
		without: map[string]struct{}{},
	}
	// Derived: Set is classified writer, so the interface
	// populates itself and a reader has something to read.
	c.seed = func(ctx context.Context, subject compositewriter.CompositeWriter) error {
		return subject.Set(ctx, c.Fixture.Key, c.Fixture.Value)
	}
	c.seedIsDerived = true
	for _, o := range opts {
		o(c)
	}
	return c
}

// subject builds a fresh implementation and seeds it, so every check runs
// against state no other check touched.
func (c *compositewriterConfig) subject(
	tb testing.TB,
	factory func() compositewriter.CompositeWriter,
	wrap func(tb testing.TB, subject compositewriter.CompositeWriter) compositewriter.CompositeWriter,
) compositewriter.CompositeWriter {
	tb.Helper()
	s := factory()
	if c.seed != nil {
		// Reported rather than ignored. A seed that silently failed would leave
		// every check after it running against an empty subject — passing, and
		// asserting nothing.
		//
		// Which seed ran decides what the message can usefully say. The derived
		// one fails for one of two reasons and both are fixable from here, so it
		// names them. A seed the caller supplied is theirs, and telling them to
		// supply one is advice they already took.
		if err := c.seed(tb.Context(), s); err != nil {
			if c.seedIsDerived {
				tb.Fatalf("seeding through Set failed: %v; "+
					"supply a value it accepts through CompositeWriterWithFixture, or a "+
					"whole seed through CompositeWriterSeed", err)
			} else {
				tb.Fatalf("the seed supplied through CompositeWriterSeed failed: %v", err)
			}
		}
	}
	if wrap != nil {
		s = wrap(tb, s)
	}
	return s
}

// dropped reports whether a path was declined through CompositeWriterWithout.
//
// Named rather than inlined because the drop set is read from two places: here,
// and by whatever registered through the seam — which reports under paths this
// file has never heard of and must answer the same question about them.
func (c *compositewriterConfig) declined(path string) bool {
	_, ok := c.without[path]
	return ok
}

// compositewriterPaths is every path this file reports a check under.
//
// The set a CompositeWriterWithout argument has to be a member of. Declared
// rather than observed: a check that did not run is not a check that does not
// exist — an integration-only method skips without ever consulting the drop
// set, and reading that as a phantom would fail a correct suite on a machine
// with the environment unset.
//
//nolint:gochecknoglobals // the emitted check paths, read-only after init.
var compositewriterPaths = map[string]struct{}{}

// phantoms names every CompositeWriterWithout path that declines nothing.
//
// A drop that drops nothing is this option's silent half: the consumer wrote
// it against a check since renamed, or never emitted for that method at all,
// and every run after reads green while asserting one thing more than its
// author believes. Extensions carry their own names — the seam reports under
// paths this file has never heard of — so they count as known too.
func (c *compositewriterConfig) phantoms() []string {
	known := func(p string) bool {
		if _, ok := compositewriterPaths[p]; ok {
			return true
		}
		for _, e := range c.extensions {
			if e.name == p {
				return true
			}
		}
		for _, on := range c.onSet {
			if string(suite.MethodID(compositeWriterSet, on.name)) == p {
				return true
			}
		}
		return false
	}
	var out []string
	for p := range c.without {
		if !known(p) {
			out = append(out, p)
		}
	}
	slices.Sort(out)
	return out
}

// run executes one check unless it was dropped, reporting the drop rather than
// silently omitting it.
func (c *compositewriterConfig) run(t *testing.T, path, name string, fn func(tb testing.TB)) {
	t.Helper()
	if c.declined(path) {
		t.Run(name, func(t *testing.T) {
			t.Skipf("dropped through CompositeWriterWithout(%q)", path)
		})
		return
	}
	t.Run(name, func(t *testing.T) {
		t.Parallel()
		fn(t)
	})
}

// testkit: end of generated content.
// testkit:provenance 6576f5edd9024b3287040b86dc2472f87ea2cf967594a5879cfc2c344254ddcd
