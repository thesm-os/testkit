// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/detector/compositewriter/iface.go
// Plugins:   golang 1.0.0, suite 1.1.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package compositewritertest_test

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/compositewriter"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/compositewriter/compositewritertest"
)

// Every generated check for CompositeWriter, driven against a stand-in built
// to violate it.
//
//	Proved:   4 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertCompositeWriterSetSmokeCanFail holds AssertCompositeWriterSetSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertCompositeWriterSetSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := compositewritertest.DefaultCompositeWriterFixture()
	subject := compositewritertest.NewCompositeWriterStub(t,
		compositewritertest.WithCompositeWriterSet(func(context.Context, string, compositewriter.Value) error {
			panic("CompositeWriter.Set: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			compositewritertest.AssertCompositeWriterSetSmoke(tb, subject, fixture.Key, fixture.V)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertCompositeWriterSetCancelsCanFail holds AssertCompositeWriterSetCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertCompositeWriterSetCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := compositewritertest.DefaultCompositeWriterFixture()
	subject := compositewritertest.NewCompositeWriterStub(t,
		compositewritertest.WithCompositeWriterSet(func(context.Context, string, compositewriter.Value) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			compositewritertest.AssertCompositeWriterSetCancels(tb, subject, fixture.Key, fixture.V)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertCompositeWriterSetHonoursDeadlineCanFail holds AssertCompositeWriterSetHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertCompositeWriterSetHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := compositewritertest.DefaultCompositeWriterFixture()
	subject := compositewritertest.NewCompositeWriterStub(t,
		compositewritertest.WithCompositeWriterSet(func(context.Context, string, compositewriter.Value) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			compositewritertest.AssertCompositeWriterSetHonoursDeadline(tb, subject, fixture.Key, fixture.V)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertCompositeWriterSetToleratesNilContextCanFail holds AssertCompositeWriterSetToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertCompositeWriterSetToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := compositewritertest.DefaultCompositeWriterFixture()
	subject := compositewritertest.NewCompositeWriterStub(t,
		compositewritertest.WithCompositeWriterSet(func(context.Context, string, compositewriter.Value) error {
			panic("CompositeWriter.Set: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			compositewritertest.AssertCompositeWriterSetToleratesNilContext(tb, subject, fixture.Key, fixture.V)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance a1df55b370400c8c3bd66b1d69135bdc56f86b1a2918ce700b56347b4d3bd7f9
