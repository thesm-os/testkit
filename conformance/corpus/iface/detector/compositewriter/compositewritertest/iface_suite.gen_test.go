// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/detector/compositewriter/iface.go
// Plugins:   golang 1.0.0, suite 1.0.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package compositewritertest_test

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/compositewriter"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/compositewriter/compositewritertest"
)

// Every generated check for CompositeWriter, driven against a stand-in built
// to violate it.
//
//	Proved:   5 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertCompositeWriterStoreSmokeCanFail holds AssertCompositeWriterStoreSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertCompositeWriterStoreSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := compositewritertest.DefaultCompositeWriterFixture()
	subject := compositewritertest.NewCompositeWriterStub(t,
		compositewritertest.WithCompositeWriterStore(func(context.Context, compositewriter.Value) (compositewriter.Value, error) {
			panic("CompositeWriter.Store: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			compositewritertest.AssertCompositeWriterStoreSmoke(tb, subject, fixture.V)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertCompositeWriterStoreCancelsCanFail holds AssertCompositeWriterStoreCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertCompositeWriterStoreCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := compositewritertest.DefaultCompositeWriterFixture()
	subject := compositewritertest.NewCompositeWriterStub(t,
		compositewritertest.WithCompositeWriterStore(func(context.Context, compositewriter.Value) (compositewriter.Value, error) {
			var r0 compositewriter.Value
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			compositewritertest.AssertCompositeWriterStoreCancels(tb, subject, fixture.V)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertCompositeWriterStoreHonoursDeadlineCanFail holds AssertCompositeWriterStoreHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertCompositeWriterStoreHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := compositewritertest.DefaultCompositeWriterFixture()
	subject := compositewritertest.NewCompositeWriterStub(t,
		compositewritertest.WithCompositeWriterStore(func(context.Context, compositewriter.Value) (compositewriter.Value, error) {
			var r0 compositewriter.Value
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			compositewritertest.AssertCompositeWriterStoreHonoursDeadline(tb, subject, fixture.V)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertCompositeWriterStoreToleratesNilContextCanFail holds AssertCompositeWriterStoreToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertCompositeWriterStoreToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := compositewritertest.DefaultCompositeWriterFixture()
	subject := compositewritertest.NewCompositeWriterStub(t,
		compositewritertest.WithCompositeWriterStore(func(context.Context, compositewriter.Value) (compositewriter.Value, error) {
			panic("CompositeWriter.Store: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			compositewritertest.AssertCompositeWriterStoreToleratesNilContext(tb, subject, fixture.V)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertCompositeWriterStoreZeroOnErrorCanFail holds AssertCompositeWriterStoreZeroOnError to being able to fail.
//
// The stand-in succeeds for the input the check chose so it would miss. A check
// that let that pass would never reach the comparison it exists to make, and
// would report success without having compared anything.
func TestAssertCompositeWriterStoreZeroOnErrorCanFail(t *testing.T) {
	t.Parallel()

	fixture := compositewritertest.DefaultCompositeWriterFixture()
	subject := compositewritertest.NewCompositeWriterStub(t,
		compositewritertest.WithCompositeWriterStore(func(context.Context, compositewriter.Value) (compositewriter.Value, error) {
			var r0 compositewriter.Value
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that succeeds for an input it should miss",
		func(tb testing.TB) {
			tb.Helper()
			compositewritertest.AssertCompositeWriterStoreZeroOnError(tb, subject, fixture.VOther)
		})
	testkit.Assert(t, got).Contains("supply inputs it misses",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance 23acafefefe263670b589c732295cb85117d45e30b055b916464b1f9276a1bf9
