// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/detector/pointerreader/iface.go
// Plugins:   golang 1.0.0, suite 1.0.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package pointerreadertest_test

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/pointerreader"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/pointerreader/pointerreadertest"
)

// Every generated check for PointerReader, driven against a stand-in built
// to violate it.
//
//	Proved:   2 checks
//	Unproved: 1, named below with what each would take
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertPointerReaderFindSmokeCanFail holds AssertPointerReaderFindSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertPointerReaderFindSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := pointerreadertest.DefaultPointerReaderFixture()
	subject := pointerreadertest.NewPointerReaderStub(t,
		pointerreadertest.WithPointerReaderFind(func(context.Context, string) *pointerreader.Value {
			panic("PointerReader.Find: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			pointerreadertest.AssertPointerReaderFindSmoke(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertPointerReaderFindToleratesNilContextCanFail holds AssertPointerReaderFindToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertPointerReaderFindToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := pointerreadertest.DefaultPointerReaderFixture()
	subject := pointerreadertest.NewPointerReaderStub(t,
		pointerreadertest.WithPointerReaderFind(func(context.Context, string) *pointerreader.Value {
			panic("PointerReader.Find: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			pointerreadertest.AssertPointerReaderFindToleratesNilContext(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// Checks with no proof here yet, and what one would take.
//
// Nothing below is unbreakable — a stand-in answers each call from a closure,
// so it can carry whatever state a violation needs. Each line names the piece
// that is missing.
//
//   - AssertPointerReaderFindReportsAMiss: the stand-in has to answer with a believable value, and no literal can be written for what this method returns

// testkit: end of generated content.
// testkit:provenance 720a9f01760d90b8962b3300b4b3aca45a436414d12d6de1f621fd9d63bd0eba
