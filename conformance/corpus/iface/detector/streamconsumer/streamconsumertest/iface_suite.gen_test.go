// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/detector/streamconsumer/iface.go
// Plugins:   golang 1.0.0, suite 1.15.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package streamconsumertest_test

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/streamconsumer"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/streamconsumer/streamconsumertest"
)

// Every generated check for Source, driven against a stand-in built
// to violate it.
//
//	Proved:   4 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertSourceNextSmokeCanFail holds AssertSourceNextSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertSourceNextSmokeCanFail(t *testing.T) {
	t.Parallel()

	subject := streamconsumertest.NewSourceStub(t,
		streamconsumertest.WithSourceNext(func(context.Context) (streamconsumer.Value, bool, error) {
			panic("Source.Next: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			streamconsumertest.AssertSourceNextSmoke(tb, subject)
		})
	testkit.Assert(t, got).Contains("Next panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertSourceNextCancelsCanFail holds AssertSourceNextCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertSourceNextCancelsCanFail(t *testing.T) {
	t.Parallel()

	subject := streamconsumertest.NewSourceStub(t,
		streamconsumertest.WithSourceNext(func(context.Context) (streamconsumer.Value, bool, error) {
			var r0 streamconsumer.Value
			var r1 bool
			return r0, r1, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			streamconsumertest.AssertSourceNextCancels(tb, subject)
		})
	testkit.Assert(t, got).Contains("Next must report a cancelled context as context.Canceled",
		"and rejects it for the reason the check is about")
}

// TestAssertSourceNextHonoursDeadlineCanFail holds AssertSourceNextHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertSourceNextHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	subject := streamconsumertest.NewSourceStub(t,
		streamconsumertest.WithSourceNext(func(context.Context) (streamconsumer.Value, bool, error) {
			var r0 streamconsumer.Value
			var r1 bool
			return r0, r1, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			streamconsumertest.AssertSourceNextHonoursDeadline(tb, subject)
		})
	testkit.Assert(t, got).Contains("Next must report an expired deadline as context.DeadlineExceeded",
		"and rejects it for the reason the check is about")
}

// TestAssertSourceNextToleratesNilContextCanFail holds AssertSourceNextToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertSourceNextToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	subject := streamconsumertest.NewSourceStub(t,
		streamconsumertest.WithSourceNext(func(context.Context) (streamconsumer.Value, bool, error) {
			panic("Source.Next: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			streamconsumertest.AssertSourceNextToleratesNilContext(tb, subject)
		})
	testkit.Assert(t, got).Contains("Next panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// Every generated check for StreamConsumer, driven against a stand-in built
// to violate it.
//
//	Proved:   4 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertStreamConsumerIngestSmokeCanFail holds AssertStreamConsumerIngestSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertStreamConsumerIngestSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := streamconsumertest.DefaultStreamConsumerFixture()
	subject := streamconsumertest.NewStreamConsumerStub(t,
		streamconsumertest.WithStreamConsumerIngest(func(context.Context, streamconsumer.Source) (int, error) {
			panic("StreamConsumer.Ingest: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			streamconsumertest.AssertStreamConsumerIngestSmoke(tb, subject, fixture.Src)
		})
	testkit.Assert(t, got).Contains("Ingest panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertStreamConsumerIngestCancelsCanFail holds AssertStreamConsumerIngestCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertStreamConsumerIngestCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := streamconsumertest.DefaultStreamConsumerFixture()
	subject := streamconsumertest.NewStreamConsumerStub(t,
		streamconsumertest.WithStreamConsumerIngest(func(context.Context, streamconsumer.Source) (int, error) {
			var r0 int
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			streamconsumertest.AssertStreamConsumerIngestCancels(tb, subject, fixture.Src)
		})
	testkit.Assert(t, got).Contains("Ingest must report a cancelled context as context.Canceled",
		"and rejects it for the reason the check is about")
}

// TestAssertStreamConsumerIngestHonoursDeadlineCanFail holds AssertStreamConsumerIngestHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertStreamConsumerIngestHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := streamconsumertest.DefaultStreamConsumerFixture()
	subject := streamconsumertest.NewStreamConsumerStub(t,
		streamconsumertest.WithStreamConsumerIngest(func(context.Context, streamconsumer.Source) (int, error) {
			var r0 int
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			streamconsumertest.AssertStreamConsumerIngestHonoursDeadline(tb, subject, fixture.Src)
		})
	testkit.Assert(t, got).Contains("Ingest must report an expired deadline as context.DeadlineExceeded",
		"and rejects it for the reason the check is about")
}

// TestAssertStreamConsumerIngestToleratesNilContextCanFail holds AssertStreamConsumerIngestToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertStreamConsumerIngestToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := streamconsumertest.DefaultStreamConsumerFixture()
	subject := streamconsumertest.NewStreamConsumerStub(t,
		streamconsumertest.WithStreamConsumerIngest(func(context.Context, streamconsumer.Source) (int, error) {
			panic("StreamConsumer.Ingest: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			streamconsumertest.AssertStreamConsumerIngestToleratesNilContext(tb, subject, fixture.Src)
		})
	testkit.Assert(t, got).Contains("Ingest panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance 839b5fbda6fe6914e6bae901f7b8a9ae2211c8ff13ffe352b9a3ffbf925ca803
