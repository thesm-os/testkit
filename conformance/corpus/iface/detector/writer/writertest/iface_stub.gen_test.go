// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/detector/writer/iface.go
// Plugins:   golang 1.0.0, stub 1.0.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package writertest_test

import (
	"context"
	"testing"
	"time"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/writer"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/writer/writertest"
	"go.thesmos.sh/testkit/rand"
	"go.thesmos.sh/testkit/stub"
)

// writerStubPutSubject binds Put into the shape
// [stub.Behaviour] drives: how to call it, what it answers with, and how to
// override it.
//
// Each call builds a fresh double, because several of the checks assert on
// failure and need one bound to a failable TB rather than to the running
// test.
func writerStubPutSubject(tb testing.TB) stub.Subject[writertest.WriterPutCall, writertest.WriterPutReturn] {
	tb.Helper()
	s := writertest.NewWriterStub(tb)
	return stub.Subject[writertest.WriterPutCall, writertest.WriterPutReturn]{
		Stub: s.OnPut.MethodStub,
		Call: func() {
			var a0 context.Context
			var a1 writer.Value
			_ = s.Put(a0, a1)
		},
		Result: func() writertest.WriterPutReturn {
			var a0 context.Context
			var a1 writer.Value
			got0 := s.Put(a0, a1)
			return writertest.WriterPutReturn{Err: got0}
		},
		Override: func(mark func()) {
			s.OnPut.Func(func(_ context.Context, _ writer.Value) error {
				mark()
				var z0 error
				return z0
			})
		},
		Fails: func() error {
			var a0 context.Context
			var a1 writer.Value
			r0 := s.Put(a0, a1)
			return r0
		},
	}
}

// TestWriterStubPut pins how Put answers.
//
// Recording, resetting, call-count expectations, strict mode, fault injection
// and zero-value dispatch are the same contract for every method, so they are
// asserted once in [stub.Behaviour] rather than restated here. What remains
// below needs a value this method's signature can tell apart from a zero one.
func TestWriterStubPut(t *testing.T) {
	t.Parallel()

	stub.Behaviour(t, "Put", writerStubPutSubject)

	t.Run("answers with the value pinned by Returns", func(t *testing.T) {
		t.Parallel()
		s := writertest.NewWriterStub(t)
		var want0 error
		s.OnPut.Returns(want0)
		var a0 context.Context
		var a1 writer.Value
		got0 := s.Put(a0, a1)
		testkit.Equal(t, got0, want0, "Put must answer with what Returns pinned")
	})
	t.Run("records what it was called with", func(t *testing.T) {
		t.Parallel()
		// Asserting only that a call happened would pass against a double
		// that recorded the wrong arguments, which is the failure a reader
		// most needs the log to surface.
		s := writertest.NewWriterStub(t)
		var a0 context.Context
		var a1 writer.Value
		_ = s.Put(a0, a1)
		got := s.OnPut.AssertCalledOnce(t, "Put must record the call")
		testkit.Equal(t, got.Ctx, a0, "the recorded call carries Ctx")
		testkit.Equal(t, got.V, a1, "the recorded call carries V")
	})

	t.Run("fires the OnRecord hook for every call", func(t *testing.T) {
		t.Parallel()
		// The hook fires synchronously as each call lands, which is what a
		// concurrency test observes progress with — polling the log instead
		// races the thing under test.
		s := writertest.NewWriterStub(t)
		var seen []writertest.WriterPutCall
		s.OnPut.OnRecord(func(c writertest.WriterPutCall) { seen = append(seen, c) })
		var a0 context.Context
		var a1 writer.Value
		_ = s.Put(a0, a1)
		_ = s.Put(a0, a1)
		testkit.Len(t, seen, 2, "OnRecord must fire once per Put call")
	})

	t.Run("wires WithWriterPut at construction", func(t *testing.T) {
		t.Parallel()
		called := false
		s := writertest.NewWriterStub(t, writertest.WithWriterPut(func(_ context.Context, _ writer.Value) error {
			called = true
			var z0 error
			return z0
		}))
		var a0 context.Context
		var a1 writer.Value
		_ = s.Put(a0, a1)
		testkit.True(t, called, "WithWriterPut must install the override")
	})

	t.Run("keeps the Returns configuration across a reset", func(t *testing.T) {
		t.Parallel()
		// A reset clears what happened, not what was configured. Losing the
		// configuration would make a double answer differently in the second
		// half of a test than in the first, for no reason the reader can see.
		s := writertest.NewWriterStub(t)
		var want0 error
		s.OnPut.Returns(want0)
		var a0 context.Context
		var a1 writer.Value
		_ = s.Put(a0, a1)
		s.ResetCalls()
		got0 := s.Put(a0, a1)
		testkit.Equal(t, got0, want0, "a reset must keep what Returns pinned")
	})
}

// writerStubDouble describes how to build a WriterStub under each
// option whose effect is the same whatever a method's signature.
//
// Put stands in for the double as a whole: what these checks assert
// is that an option reached it at all, and the first method witnesses that as
// well as any other would.
func writerStubDouble() stub.Double[writertest.WriterPutCall] {
	instance := func(s *writertest.WriterStub) stub.Instance[writertest.WriterPutCall] {
		return stub.Instance[writertest.WriterPutCall]{
			Stub: s.OnPut.MethodStub,
			Call: func() {
				var a0 context.Context
				var a1 writer.Value
				_ = s.Put(a0, a1)
			},
			Reset: s.ResetCalls,
		}
	}
	return stub.Double[writertest.WriterPutCall]{
		New: func(tb testing.TB) stub.Instance[writertest.WriterPutCall] {
			return instance(writertest.NewWriterStub(tb))
		},
		WithClock: func(tb testing.TB, clk clock.Clock) stub.Instance[writertest.WriterPutCall] {
			return instance(writertest.NewWriterStub(tb, writertest.WriterStubWithClock(clk)))
		},
		WithRandSource: func(tb testing.TB, src rand.Source) stub.Instance[writertest.WriterPutCall] {
			return instance(writertest.NewWriterStub(tb, writertest.WriterStubWithRandSource(src)))
		},
		BenchMode: func(tb testing.TB) stub.Instance[writertest.WriterPutCall] {
			return instance(writertest.NewWriterStub(tb, writertest.WriterStubBenchMode()))
		},
		Strict: func(tb testing.TB) stub.Instance[writertest.WriterPutCall] {
			return instance(writertest.NewWriterStub(tb, writertest.WriterStubStrict()))
		},
	}
}

// TestWriterStubOptions pins the settings that apply to the double rather
// than to one method: bench mode, the injected clock and random source, and
// what a reset clears.
func TestWriterStubOptions(t *testing.T) {
	t.Parallel()

	stub.DoubleBehaviour(t, writerStubDouble())
}

// TestWriterStubDelegateTo pins that a wrapped implementation is called
// through, and recorded on the way past.
//
// This is what lets one generated suite run against both the double and a
// production type, which is the point of conformance testing.
func TestWriterStubDelegateTo(t *testing.T) {
	t.Parallel()

	inner := writertest.NewWriterStub(t)
	s := writertest.NewWriterStub(t, writertest.WriterStubDelegateTo(inner))

	t.Run("forwards Put to the wrapped implementation", func(t *testing.T) {
		var a0 context.Context
		var a1 writer.Value
		_ = s.Put(a0, a1)
		inner.OnPut.AssertCalledOnce(t, "Put must reach the wrapped implementation")
	})

	t.Run("surfaces what Put answered", func(t *testing.T) {
		// Reaching the wrapped implementation is not enough: a double that
		// called through and then discarded the answer would pass the check
		// above while telling its caller nothing true.
		want := testkit.TestError("Put-delegate")
		inner.OnPut.FaultsFor(time.Hour, want)
		var a0 context.Context
		var a1 writer.Value
		r0 := s.Put(a0, a1)
		testkit.ErrorIs(t, r0, want, "Put must surface the wrapped answer")
	})
}

// testkit: end of generated content.
// testkit:provenance ebbd77366308080023d75854ed010ea64866c15b25fda477c2701ddca0e3e472
