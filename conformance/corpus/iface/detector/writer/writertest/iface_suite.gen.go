// Code generated by testkit. DO NOT EDIT.
//
// Source:    conformance/corpus/iface/detector/writer/iface.go
// Plugins:   golang 1.0.0, suite 1.17.0, backend.golang 1.0.0
// Command:   testkit run ./conformance/corpus/...

package writertest

import (
	"context"
	"fmt"
	"testing"

	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/writer"
	"go.thesmos.sh/testkit/engine/suite"
	"go.thesmos.sh/testkit/engine/suite/prove"
)

// The generated conformance surface for Writer.
//
// A file-level comment rather than a package doc: three generators write
// into this package and a package doc has one owner, so the listing
// lives with the witness it describes.
//
// Entry points:
//
//	RunWriter(t, opts...)      ProveWriter(t, checks)
//
// A harness is a config literal (WriterHarness); your own checks are a
// table (WriterChecks); drops are written against the typed index:
//
//	WriterSuite.Checks.<Method>.<Check>()
//
// Every ID this package emits:
//
//	Put/cancel
//	Put/deadline
//	Put/nilcontext
//	Put/smoke
//
// The compatibility handshake. A breaking change to the check surface
// renames the witness, and every file generated against this one stops
// compiling with the skew named rather than dispatching wrong.
var _ = suite.CompatV2

// WriterFixture holds every input the generated checks run against.
//
// Derived from each method's parameter names and types. Every field has a
// companion holding a second, different value: a check comparing a result
// against a single input passes whenever the subject happened to be seeded with
// it, and a miss check whose key happens to hit asserts nothing.
//
// A field whose type admits no literal — a func, a channel, a type from a
// package this run never read — is declared and left at its zero value, and
// every generated check that needed it was dropped rather than emitted against
// a value nobody could write. Those drops are listed above with what closes
// each; a row you write is handed this fixture either way.
type WriterFixture struct {
	value      writer.Value
	valueOther writer.Value
}

// DefaultWriterFixture is what a run with no config draws.
func DefaultWriterFixture() WriterFixture {
	return writerNewFixture()
}

// writerNewFixture is what a run draws from, given its config.
//
// Read through the accessors below rather than as fields: a roled value
// comes from the config's pool and an unroled one from its derived
// literal, and no check that draws should be able to tell which.
func writerNewFixture() WriterFixture {
	return WriterFixture{
		value:      writer.Value{Key: "test-key", Body: "test-body"},
		valueOther: writer.Value{Key: "other-key", Body: "other-body"},
	}
}

// Value is the canonical value every check draws.
func (f WriterFixture) Value() writer.Value { return f.value }

// ValueOther is a second value, different from the first — which is
// what lets a miss check miss and a comparison mean something.
func (f WriterFixture) ValueOther() writer.Value { return f.valueOther }

// Writer is the witnessed instantiation.
//
// A generic interface aliases with its own parameters, which Go admits
// since 1.24 — so every declaration below names Writer and
// the arguments follow, exactly as they do for a concrete one.
type Writer = writer.Writer

// WriterSubject is the runtime subject a RunWith body receives.
//
// Aliased rather than named through the engine, so a consumer writing a
// row against the subject stays inside this package's vocabulary.
type WriterSubject = suite.Subject[Writer]

// WriterRunOpt is one thing you can pass to RunWriter.
type WriterRunOpt interface {
	applyTo(*writerRunConfig)
}

type writerRunConfig struct {
	suite.Bundle[Writer]

	// rows are the consumer's own checks, bound against the run's
	// fixture once the options are folded — which is why they are held
	// here rather than lowered on arrival: a row bound before the
	// fixture is known would draw from a fixture no run uses.
	rows WriterChecks
}

// WriterHarness declares one Writer implementation under test.
type WriterHarness[T Writer] struct {
	Name string

	// Exactly one constructor.
	New   func() T
	Start func(tb testing.TB) T

	// Excuse lists checks this subject structurally cannot take.
	Excuse []suite.ID

	// Oracle marks this subject as the run's reference. At most one.
	Oracle bool

	// Serial keeps this subject out of the parallel group.
	Serial bool

	// Provide answers open capabilities beyond the named fields — the
	// doors only this interface's checks know. The runner's gate reads
	// it for any capability without a dedicated field.
	Provide map[suite.Capability]any
}

// Subject lowers the declaration into what the runner consumes.
func (h WriterHarness[T]) Subject() (suite.Subject[Writer], error) {
	build, err := suite.OneCtor(h.Name, h.New, h.Start)
	if err != nil {
		return suite.Subject[Writer]{}, err
	}
	return suite.Subject[Writer]{
		Name:     h.Name,
		Provides: h.Provide,
		New:      func(tb testing.TB) Writer { return build(tb) },
		Oracle:   h.Oracle,
		Serial:   h.Serial,
		Excused:  suite.ExcuseSet(h.Excuse),
	}, nil
}

func (h WriterHarness[T]) applyTo(rc *writerRunConfig) {
	rc.AddSubject(h.Subject())
}

type writerDropOpt []suite.ID

func (d writerDropOpt) applyTo(rc *writerRunConfig) {
	rc.AddDrops(d...)
}

// writerWithout drops checks by identity.
func writerWithout(ids ...suite.ID) WriterRunOpt {
	return writerDropOpt(ids)
}

// --- The veneer -------------------------------------------------------------

// WriterSuite is the entry a consumer names checks through.
var WriterSuite = writerVeneer{Checks: writerCheckIndex}

type writerVeneer struct {
	// Checks is the check index — the constants a Without call is
	// written against.
	Checks writerCheckIndexT
}

// Without drops a check by ID.
func (writerVeneer) Without(ids ...suite.ID) WriterRunOpt {
	return writerWithout(ids...)
}

// Suite returns the checks as data, for tooling or a runner of your own.
//
// The way in from outside this package: the assembler and the table it
// builds are unexported, because a caller reaching them is doing tooling
// and tooling wants one name rather than one per interface.
func (writerVeneer) Suite(fx WriterFixture) suite.Suite[Writer] {
	return writerSuite(fx)
}

// --- Drop hints -------------------------------------------------------------

// writerIndexPath spells every emitted ID as the path a
// consumer would write to drop it, so a run that declines something can
// say what to type rather than what failed.
var writerIndexPath = map[suite.ID]string{
	writerCheckIndex.Put.Smoke():      "WriterSuite.Checks.Put.Smoke()",
	writerCheckIndex.Put.Cancels():    "WriterSuite.Checks.Put.Cancels()",
	writerCheckIndex.Put.NilContext(): "WriterSuite.Checks.Put.NilContext()",
	writerCheckIndex.Put.Deadline():   "WriterSuite.Checks.Put.Deadline()",
}

var writerDropHint = suite.DropHinter(
	"WriterSuite", writerIndexPath,
)

// The names this file spells more than once: one home each.
const (
	writerPut = "Put"
)

// writerCheckIndex indexes every check this package emits, by
// method and then by check.
var writerCheckIndex = writerCheckIndexT{
	Put: writerPutChecks{},
}

type writerCheckIndexT struct {
	Put writerPutChecks
}

// All returns every ID this package emits.
func (writerCheckIndexT) All() []suite.ID {
	var out []suite.ID
	out = append(out, writerPutChecks{}.All()...)
	return out
}

type writerPutChecks struct{}

func (writerPutChecks) Smoke() suite.ID {
	return suite.MethodID(writerPut, suite.SegSmoke)
}

func (writerPutChecks) Cancels() suite.ID {
	return suite.MethodID(writerPut, suite.SegCancel)
}

func (writerPutChecks) NilContext() suite.ID {
	return suite.MethodID(writerPut, suite.SegNilContext)
}

func (writerPutChecks) Deadline() suite.ID {
	return suite.MethodID(writerPut, suite.SegDeadline)
}

func (writerPutChecks) All() []suite.ID {
	return []suite.ID{
		writerPutChecks{}.Smoke(),
		writerPutChecks{}.Cancels(),
		writerPutChecks{}.NilContext(),
		writerPutChecks{}.Deadline(),
	}
}

// writerSuite returns the checks as data, drawn from f.
//
// The seam the proofs companion binds to, and the one a runner of your
// own would. It takes the fixture rather than a config because the
// fixture is what a check draws from; a config that derives one is the
// pool work, and until that lands a caller passes what a default run
// passes.
func writerSuite(fx WriterFixture) suite.Suite[Writer] {
	return suite.Suite[Writer]{
		Name:     "Writer",
		DropHint: writerDropHint,
		Checks:   writerSignatureChecks(fx),
	}
}

// writerSignatureChecks builds every check this run derived.
//
// sig is the one shape they share; the literals below state only what
// differs. Falsifiable is Proven wherever a defect rule reached the
// claim AND the companion beside this file spells that defect — the
// parity gate refuses the stamp without the evidence, so a claim this
// run cannot yet plant is argued rather than asserted.
func writerSignatureChecks(fx WriterFixture) []suite.Check[Writer] {
	sig := suite.ProvenCheck[Writer]
	ix := writerCheckIndex
	return []suite.Check[Writer]{
		sig(ix.Put.Smoke(), suite.ClassSmoke,
			"Put survives a call with a derived value",
			func(tb testing.TB, w Writer) {
				writerAssertPutSmoke(tb, w, fx)
			}),
		sig(ix.Put.Cancels(), suite.ClassCancel,
			"Put reports a cancelled context as cancelled",
			func(tb testing.TB, w Writer) {
				writerAssertPutCancels(tb, w, fx)
			}),
		sig(ix.Put.NilContext(), suite.ClassNilContext,
			"Put returns an error rather than panicking on a nil context",
			func(tb testing.TB, w Writer) {
				writerAssertPutToleratesNilContext(tb, w, fx)
			}),
		sig(ix.Put.Deadline(), suite.ClassDeadline,
			"Put reports an expired deadline as exceeded",
			func(tb testing.TB, w Writer) {
				writerAssertPutHonoursDeadline(tb, w, fx)
			}),
	}
}

// writerAssertPutSmoke asserts Put survives a call with a derived value.
func writerAssertPutSmoke(
	tb testing.TB,
	w Writer,
	fx WriterFixture,
) {
	tb.Helper()
	suite.Survives(tb, writerPut, func(ctx context.Context) {
		_ = w.Put(ctx, fx.Value())
	})
}

// writerAssertPutCancels asserts Put reports a cancelled context as cancelled.
func writerAssertPutCancels(
	tb testing.TB,
	w Writer,
	fx WriterFixture,
) {
	tb.Helper()
	suite.ReportsCancelled(tb, writerPut, func(ctx context.Context) error {
		return w.Put(ctx, fx.Value())
	})
}

// writerAssertPutToleratesNilContext asserts Put returns an error rather than panicking on a nil context.
func writerAssertPutToleratesNilContext(
	tb testing.TB,
	w Writer,
	fx WriterFixture,
) {
	tb.Helper()
	suite.ToleratesNilContext(tb, writerPut, func(ctx context.Context) error {
		return w.Put(ctx, fx.Value())
	})
}

// writerAssertPutHonoursDeadline asserts Put reports an expired deadline as exceeded.
func writerAssertPutHonoursDeadline(
	tb testing.TB,
	w Writer,
	fx WriterFixture,
) {
	tb.Helper()
	suite.ReportsDeadlineExceeded(tb, writerPut, func(ctx context.Context) error {
		return w.Put(ctx, fx.Value())
	})
}

// WriterDefect is anything that can stand as a planted defect for a
// row — in practice a WriterHarness over the generated double.
type WriterDefect interface {
	Subject() (WriterSubject, error)
}

// BrokenWriter names a planted defect.
//
// Sugar over the harness, because a defect IS a subject — one built to
// fail exactly one claim. Its own constructor is what keeps a proofs
// table readable beside a run's.
func BrokenWriter[T Writer](
	name string, build func() T,
) WriterHarness[T] {
	return WriterHarness[T]{
		Name: name, New: build,
	}
}

// WriterChecks is the table of this package's own checks.
type WriterChecks []WriterCheck

func (c WriterChecks) applyTo(rc *writerRunConfig) {
	rc.rows = append(rc.rows, c...)
}

// WriterCheck is one row. The body is a plain named function; which
// field it goes in decides what it receives.
type WriterCheck struct {
	// Method scopes Run's ID: "Put" makes it Put/<name>.
	// A body that fixes its own scope leaves this empty.
	Method string

	// Name is the ID's slug — the stable handle a drop and a rerun use.
	Name string

	// Claim is the sentence the manifest records and the report reads
	// out. A claim wider than its assertion is the one failure this
	// surface cannot catch for you.
	Claim string

	// Exactly one body.
	//
	// Run gets one fresh instance, which is nearly every check. RunWith
	// gets the subject itself, for a claim one instance cannot state:
	// build two, move the clock, induce a failure.
	//
	// Both take the fixture whether or not any DERIVED check draws one,
	// so the row seam has one shape across every package and a fixture
	// that later grows a field does not change a signature consumers
	// have already written against.
	Run     func(tb testing.TB, s Writer, fx WriterFixture)
	RunWith func(tb testing.TB, sub WriterSubject, fx WriterFixture)

	// Class buckets the report. Empty means hand-written.
	Class suite.Class

	// Needs declares capabilities. A harness that cannot meet one fails
	// this check by name with the fix in the message, never skips — a
	// check that skipped for want of wiring reads as coverage.
	Needs suite.Caps

	// ProvenBy is the planted defect that fails this check and nothing
	// else. Setting it is the claim AND the evidence: the row reports
	// Proven, and ProveWriter fails unless the defect turns the check
	// red. ProvenReason, when set, is a substring that red must contain,
	// so a defect dying on an incidental guard stops counting as one.
	//
	// Argued records why no defect can be constructed — the honest third
	// answer. At most one of ProvenBy and Argued; neither means unproven,
	// which is what the report says about a check nobody has driven
	// against anything.
	ProvenBy     WriterDefect
	ProvenReason string
	Argued       string
}

// writerMethods is the interface's method set, for naming a Method typo
// at run start.
//
// A misspelled method starts with a capital too, so the ID grammar alone
// would file a check under a method that does not exist and report it
// under a path no drop can name.
var writerMethods = suite.NewNameSet("Writer", writerPut)

// bind lowers one row onto the runtime check type, against the run's own
// fixture — which is what lets a row's body draw what the run draws.
func (c WriterCheck) bind(
	fx WriterFixture,
) (suite.Check[Writer], error) {
	out := suite.Check[Writer]{
		Claim: c.Claim, Class: c.Class, Needs: c.Needs,
	}
	if out.Class == "" {
		out.Class = suite.ClassHandWritten
	}

	var err error
	bodies := 0
	if c.Run != nil {
		bodies++
		if out.ID, err = suite.RowID("Run", c.Method, c.Name, writerMethods); err != nil {
			return out, err
		}
		run := c.Run
		out.Run = func(tb testing.TB, s Writer) {
			run(tb, s, fx)
		}
	}
	if c.RunWith != nil {
		bodies++
		out.ID = suite.HandRowID(c.Name)
		rw := c.RunWith
		out.RunWith = func(tb testing.TB, sub WriterSubject) {
			rw(tb, sub, fx)
		}
	}
	if err := suite.OneBody(c.Name, bodies, "Run, RunWith"); err != nil {
		return out, err
	}
	if c.Method != "" && c.Run == nil {
		return out, fmt.Errorf(
			"check %q sets Method, but its body fixes its own scope; drop Method", c.Name)
	}
	if out.Falsifiable, err = suite.Falsify(c.Name, c.ProvenBy != nil, c.Argued); err != nil {
		return out, err
	}
	return out, nil
}

// RunWriter runs every check — generated and yours — against every
// harness.
//
//	RunWriter(t,
//		WriterHarness[*InMemory]{Name: "in-memory", New: NewInMemory},
//		WriterSuite.Without(WriterSuite.Checks.Put.Smoke()),
//	)
func RunWriter(
	t *testing.T, opts ...WriterRunOpt,
) {
	t.Helper()
	var rc writerRunConfig
	for _, o := range opts {
		o.applyTo(&rc)
	}
	fx := writerNewFixture()
	for _, row := range rc.rows {
		rc.AddCheck(row.bind(fx))
	}
	// Between folding and running: a misused run reports every fix it
	// needs and executes nothing, rather than reporting one and leaving
	// the rest to be found a cycle at a time.
	rc.Fail(t, "RunWriter")
	suite.Run(t,
		writerSuite(fx).With(rc.Extra...).Without(rc.Drops...),
		rc.Subjects...)
}

// ProveWriter drives each of your rows against the defect it names.
//
// A row with no ProvenBy is not skipped — it is held to saying why
// through Argued. The parity gate refuses a Proven claim with no
// evidence and evidence for a claim nobody made, in both directions, so
// this is where "proven able to fail" stops being prose.
func ProveWriter(
	t *testing.T, checks WriterChecks,
) {
	t.Helper()
	// The RUN's config, not the derived one: a check proven at default
	// pools carries no evidence about the pools a run actually uses.
	fx := writerNewFixture()
	bound := make([]suite.Check[Writer], 0, len(checks))
	defects := prove.Defects[Writer]{}
	for _, row := range checks {
		bd, err := row.bind(fx)
		if err != nil {
			t.Fatalf("ProveWriter: %v", err)
		}
		bound = append(bound, bd)
		if row.ProvenBy == nil {
			continue
		}
		sub, err := row.ProvenBy.Subject()
		if err != nil {
			t.Fatalf("ProveWriter: check %q: defect: %v", bd.ID, err)
		}
		defects[bd.ID] = prove.Defect[Writer]{
			Subject: sub, Reason: row.ProvenReason,
		}
	}
	prove.All(t, bound, defects)
}

// testkit: end of generated content.
// testkit:provenance 073a432e802d121973f9a2c672275a222e421b71d5ea73a288cb621e34aec818
