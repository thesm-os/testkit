// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/detector/multireader/iface.go
// Plugins:   golang 1.0.0, suite 1.15.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package multireadertest_test

import (
	"context"
	"errors"
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/multireader"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/multireader/multireadertest"
)

// Every generated check for MultiReader, driven against a stand-in built
// to violate it.
//
//	Proved:   5 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertMultiReaderGetWithMetaSmokeCanFail holds AssertMultiReaderGetWithMetaSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertMultiReaderGetWithMetaSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := multireadertest.DefaultMultiReaderFixture()
	subject := multireadertest.NewMultiReaderStub(t,
		multireadertest.WithMultiReaderGetWithMeta(func(context.Context, string) (multireader.Value, multireader.Meta, error) {
			panic("MultiReader.GetWithMeta: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			multireadertest.AssertMultiReaderGetWithMetaSmoke(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("GetWithMeta panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertMultiReaderGetWithMetaCancelsCanFail holds AssertMultiReaderGetWithMetaCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertMultiReaderGetWithMetaCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := multireadertest.DefaultMultiReaderFixture()
	subject := multireadertest.NewMultiReaderStub(t,
		multireadertest.WithMultiReaderGetWithMeta(func(context.Context, string) (multireader.Value, multireader.Meta, error) {
			var r0 multireader.Value
			var r1 multireader.Meta
			return r0, r1, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			multireadertest.AssertMultiReaderGetWithMetaCancels(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("GetWithMeta must report a cancelled context as context.Canceled",
		"and rejects it for the reason the check is about")
}

// TestAssertMultiReaderGetWithMetaHonoursDeadlineCanFail holds AssertMultiReaderGetWithMetaHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertMultiReaderGetWithMetaHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := multireadertest.DefaultMultiReaderFixture()
	subject := multireadertest.NewMultiReaderStub(t,
		multireadertest.WithMultiReaderGetWithMeta(func(context.Context, string) (multireader.Value, multireader.Meta, error) {
			var r0 multireader.Value
			var r1 multireader.Meta
			return r0, r1, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			multireadertest.AssertMultiReaderGetWithMetaHonoursDeadline(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("GetWithMeta must report an expired deadline as context.DeadlineExceeded",
		"and rejects it for the reason the check is about")
}

// TestAssertMultiReaderGetWithMetaToleratesNilContextCanFail holds AssertMultiReaderGetWithMetaToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertMultiReaderGetWithMetaToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := multireadertest.DefaultMultiReaderFixture()
	subject := multireadertest.NewMultiReaderStub(t,
		multireadertest.WithMultiReaderGetWithMeta(func(context.Context, string) (multireader.Value, multireader.Meta, error) {
			panic("MultiReader.GetWithMeta: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			multireadertest.AssertMultiReaderGetWithMetaToleratesNilContext(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("GetWithMeta panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertMultiReaderGetWithMetaZeroOnErrorCanFail holds AssertMultiReaderGetWithMetaZeroOnError to being able to fail.
//
// The stand-in refuses the call and answers a value that looks like a result
// anyway. A check that let that pass would leave a caller who checks the error
// and a caller who checks the value disagreeing about what happened — the
// claim's own violation, not a stand-in that merely succeeds.
func TestAssertMultiReaderGetWithMetaZeroOnErrorCanFail(t *testing.T) {
	t.Parallel()

	fixture := multireadertest.DefaultMultiReaderFixture()
	subject := multireadertest.NewMultiReaderStub(t,
		multireadertest.WithMultiReaderGetWithMeta(func(context.Context, string) (multireader.Value, multireader.Meta, error) {
			return multireader.Value{Key: "test-r0"}, multireader.Meta{Revision: 42}, errors.New("violate: refused with a believable value")
		}))

	got := testkit.Rejects(t, "a method answering a believable value beside its error",
		func(tb testing.TB) {
			tb.Helper()
			multireadertest.AssertMultiReaderGetWithMetaZeroOnError(tb, subject, fixture.KeyOther)
		})
	testkit.Assert(t, got).Contains("GetWithMeta must return the zero value alongside an error",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance 5a2d703738e2e83a0bf4f7c64c572d113dafba20ef1fe9b9cc999648d4f8a96d
