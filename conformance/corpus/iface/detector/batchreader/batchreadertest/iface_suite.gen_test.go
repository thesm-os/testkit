// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/detector/batchreader/iface.go
// Plugins:   golang 1.0.0, suite 1.1.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package batchreadertest_test

import (
	"context"
	"errors"
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/batchreader"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/batchreader/batchreadertest"
)

// Every generated check for BatchReader, driven against a stand-in built
// to violate it.
//
//	Proved:   6 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertBatchReaderGetAllSmokeCanFail holds AssertBatchReaderGetAllSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertBatchReaderGetAllSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := batchreadertest.DefaultBatchReaderFixture()
	subject := batchreadertest.NewBatchReaderStub(t,
		batchreadertest.WithBatchReaderGetAll(func(context.Context, ...string) ([]batchreader.Value, error) {
			panic("BatchReader.GetAll: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			batchreadertest.AssertBatchReaderGetAllSmoke(tb, subject, fixture.Keys)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertBatchReaderGetAllCancelsCanFail holds AssertBatchReaderGetAllCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertBatchReaderGetAllCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := batchreadertest.DefaultBatchReaderFixture()
	subject := batchreadertest.NewBatchReaderStub(t,
		batchreadertest.WithBatchReaderGetAll(func(context.Context, ...string) ([]batchreader.Value, error) {
			var r0 []batchreader.Value
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			batchreadertest.AssertBatchReaderGetAllCancels(tb, subject, fixture.Keys)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertBatchReaderGetAllHonoursDeadlineCanFail holds AssertBatchReaderGetAllHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertBatchReaderGetAllHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := batchreadertest.DefaultBatchReaderFixture()
	subject := batchreadertest.NewBatchReaderStub(t,
		batchreadertest.WithBatchReaderGetAll(func(context.Context, ...string) ([]batchreader.Value, error) {
			var r0 []batchreader.Value
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			batchreadertest.AssertBatchReaderGetAllHonoursDeadline(tb, subject, fixture.Keys)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertBatchReaderGetAllToleratesNilContextCanFail holds AssertBatchReaderGetAllToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertBatchReaderGetAllToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := batchreadertest.DefaultBatchReaderFixture()
	subject := batchreadertest.NewBatchReaderStub(t,
		batchreadertest.WithBatchReaderGetAll(func(context.Context, ...string) ([]batchreader.Value, error) {
			panic("BatchReader.GetAll: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			batchreadertest.AssertBatchReaderGetAllToleratesNilContext(tb, subject, fixture.Keys)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertBatchReaderGetAllZeroOnErrorCanFail holds AssertBatchReaderGetAllZeroOnError to being able to fail.
//
// The stand-in refuses the call and answers a value that looks like a result
// anyway. A check that let that pass would leave a caller who checks the error
// and a caller who checks the value disagreeing about what happened — the
// claim's own violation, not a stand-in that merely succeeds.
func TestAssertBatchReaderGetAllZeroOnErrorCanFail(t *testing.T) {
	t.Parallel()

	fixture := batchreadertest.DefaultBatchReaderFixture()
	subject := batchreadertest.NewBatchReaderStub(t,
		batchreadertest.WithBatchReaderGetAll(func(context.Context, ...string) ([]batchreader.Value, error) {
			return []batchreader.Value{{Key: "test-r0"}}, errors.New("violate: refused with a believable value")
		}))

	got := testkit.Rejects(t, "a method answering a believable value beside its error",
		func(tb testing.TB) {
			tb.Helper()
			batchreadertest.AssertBatchReaderGetAllZeroOnError(tb, subject, fixture.KeysOther)
		})
	testkit.Assert(t, got).Contains("must return the zero value",
		"and rejects it for the reason the check is about")
}

// TestAssertBatchReaderGetAllAnswersPerKeyCanFail holds AssertBatchReaderGetAllAnswersPerKey to being able to fail.
//
// The stand-in answers once for a batch of two. A check that let that pass
// would report a working batch read for a subject whose results no longer line
// up with the keys that asked for them — and nothing about the value returned
// says so.
func TestAssertBatchReaderGetAllAnswersPerKeyCanFail(t *testing.T) {
	t.Parallel()

	fixture := batchreadertest.DefaultBatchReaderFixture()
	subject := batchreadertest.NewBatchReaderStub(t,
		batchreadertest.WithBatchReaderGetAll(func(context.Context, ...string) ([]batchreader.Value, error) {
			return make([]batchreader.Value, 1), nil
		}))

	got := testkit.Rejects(t, "a reader answering once for every batch",
		func(tb testing.TB) {
			tb.Helper()
			batchreadertest.AssertBatchReaderGetAllAnswersPerKey(tb, subject, fixture.Keys, fixture.KeysOther)
		})
	testkit.Assert(t, got).Contains("once per key",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance ee21c73ce290b2e16bbe5c26ec94d09017bf124b357b5f5c2d525d8f6fcf5b47
