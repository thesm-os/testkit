// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/detector/voidlifecycle/iface.go
// Plugins:   golang 1.0.0, stub 1.0.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package voidlifecycletest

import (
	"testing"

	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/voidlifecycle"
	"go.thesmos.sh/testkit/rand"
	"go.thesmos.sh/testkit/stub"
)

// VoidLifecycleStopCall records one invocation of VoidLifecycle.Stop.
//
// Fields take their names from the source signature — parameters and named
// returns alike — so a failure message names what the author named. A slot
// the source left unnamed or blank falls back to a positional name.
type VoidLifecycleStopCall struct {
}

// --- Per-method configuration ---

// VoidLifecycleStopStub controls how the double answers Stop and records
// what it was asked.
//
// The embedded MethodStub supplies the machinery every method shares: call
// recording, fault injection, latency against a virtual clock, gates,
// call-count expectations, and strict mode.
type VoidLifecycleStopStub struct {
	*stub.MethodStub[VoidLifecycleStopCall]

	fn func()
}

// Func supplies a body for Stop, for when the answer depends on the
// arguments. An injected fault still takes precedence.
func (s *VoidLifecycleStopStub) Func(fn func()) *VoidLifecycleStopStub {
	s.fn = fn
	return s
}

// --- VoidLifecycleStub ---

// VoidLifecycleStubOption configures a [VoidLifecycleStub] at construction time.
type VoidLifecycleStubOption func(*VoidLifecycleStub)

// VoidLifecycleStubStrict makes an unconfigured method fail the test rather than
// return its zero value, which turns a call nobody planned for into a failure
// at the call site instead of a puzzling zero further downstream.
func VoidLifecycleStubStrict() VoidLifecycleStubOption {
	return func(s *VoidLifecycleStub) { s.strict = true }
}

// VoidLifecycleStubDelegateTo wraps a real implementation: every method forwards to
// impl and is recorded on the double on the way through.
//
// This is what lets one generated suite run against both the double and the
// production type, which is the point of conformance testing.
func VoidLifecycleStubDelegateTo(impl voidlifecycle.VoidLifecycle) VoidLifecycleStubOption {
	return func(s *VoidLifecycleStub) {
		s.OnStop.Func(impl.Stop)
	}
}

// VoidLifecycleStubWithClock drives latency and time-windowed faults from clk
// rather than from wall time, so a test asserting on a five-second timeout
// does not take five seconds.
func VoidLifecycleStubWithClock(clk clock.Clock) VoidLifecycleStubOption {
	return func(s *VoidLifecycleStub) {
		for _, m := range s.all {
			m.SetClock(clk)
		}
	}
}

// VoidLifecycleStubWithRandSource makes probabilistic fault injection reproducible.
// A failure nobody can replay is not a finding.
func VoidLifecycleStubWithRandSource(src rand.Source) VoidLifecycleStubOption {
	return func(s *VoidLifecycleStub) {
		for _, m := range s.all {
			m.SetRandSource(src)
		}
	}
}

// VoidLifecycleStubBenchMode disables call recording. Dispatch still works; only
// the accumulating call log is dropped, because its allocations are what a
// benchmark would otherwise be measuring.
func VoidLifecycleStubBenchMode() VoidLifecycleStubOption {
	return func(s *VoidLifecycleStub) {
		for _, m := range s.all {
			m.BenchMode()
		}
	}
}

// WithVoidLifecycleStop sets Stop's body at construction
// time, for the common case of configuring one method and taking the
// defaults for the rest.
func WithVoidLifecycleStop(fn func()) VoidLifecycleStubOption {
	return func(s *VoidLifecycleStub) { s.OnStop.Func(fn) }
}

// VoidLifecycleStub is a recording test double for VoidLifecycle.
//
// Each On<Method> field is that method's configuration point. Left alone, a
// method returns its zero value and records the call.
type VoidLifecycleStub struct {
	OnStop *VoidLifecycleStopStub

	// all is every method stub above, viewed through the surface that does
	// not depend on a signature. It is what lets a setting apply to the whole
	// double in a loop rather than in one line per method.
	all    []stub.Configurable
	strict bool
}

// NewVoidLifecycleStub returns a double bound to tb.
//
// Passing tb registers a cleanup that verifies every method's call-count
// expectations when the test ends, so an unmet Times is reported without the
// caller remembering to check. A nil tb skips that, which is what benchmarks
// and non-test callers want.
func NewVoidLifecycleStub(tb testing.TB, opts ...VoidLifecycleStubOption) *VoidLifecycleStub {
	s := &VoidLifecycleStub{
		OnStop: &VoidLifecycleStopStub{MethodStub: stub.NewMethodStub[VoidLifecycleStopCall](tb, "VoidLifecycle.Stop")},
	}
	s.all = []stub.Configurable{
		s.OnStop.MethodStub,
	}
	for _, opt := range opts {
		opt(s)
	}
	if s.strict {
		for _, m := range s.all {
			m.Strict()
		}
	}
	if tb != nil {
		// A fuzz target reruns its body many times over and registers a
		// cleanup per run, so verifying there would report against the wrong
		// iteration.
		if _, isFuzz := tb.(*testing.F); !isFuzz {
			tb.Cleanup(func() {
				for _, m := range s.all {
					m.Verify()
				}
			})
		}
	}
	return s
}

// ResetCalls clears recorded calls, fault counters, and call-count
// expectations. Func and Returns configuration is deliberately preserved, so
// a test can reuse one double across phases without rebuilding it.
func (s *VoidLifecycleStub) ResetCalls() {
	for _, m := range s.all {
		m.Reset()
	}
}

// --- Interface methods ---

// invokeVoid adapts the Func override to the shape [stub.Answer] consumes.
// A method returning nothing still has an override worth dispatching to and
// a call worth recording; the empty struct is the return tuple of no returns.
func (s *VoidLifecycleStopStub) invokeVoid() func() struct{} {
	if s.fn == nil {
		return nil
	}
	return func() struct{} {
		s.fn()
		return struct{}{}
	}
}

// Stop records the call and answers it.
//
// Which arm answers — injected fault, Func override, Returns fallback, or the
// zero value — is [stub.Answer]'s to decide, so every generated double
// resolves a call the same way and the ordering is tested once rather than
// restated per method.
func (s *VoidLifecycleStub) Stop() {
	call := VoidLifecycleStopCall{}
	stub.Answer(s.OnStop.MethodStub, &call, stub.Arms[VoidLifecycleStopCall, struct{}]{
		Invoke: s.OnStop.invokeVoid(),
	})
}

// testkit: end of generated content.
// testkit:provenance 85151b8b21cc95a57c9fd01f6483165cbac0f0b8e45fa7dc4e95c445ba915d49
