// Code generated by testkit. DO NOT EDIT.
//
// Source:    conformance/corpus/iface/detector/readerwithbool/iface.go
// Plugins:   golang 1.0.0, suite 1.16.0, backend.golang 1.0.0
// Command:   testkit run ./conformance/corpus/...

package readerwithbooltest

import (
	"context"
	"slices"
	"testing"

	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/conformance/corpus/iface/detector/readerwithbool"
	"go.thesmos.sh/testkit/engine/suite"
)

// ReaderWithBoolLoadCheck is one assertion about Load.
//
// Every generated check for Load is a value of it, and so is one you
// write — so they compose, reorder, and each runs standalone. testing.TB rather
// than *testing.T is what lets a stand-in drive it and prove it can fail.
type ReaderWithBoolLoadCheck func(tb testing.TB, subject readerwithbool.ReaderWithBool, key string)

// ReaderWithBoolFixture holds every input the generated checks run against.
//
// Derived from each method's parameter names and types. Every field has a
// companion holding a second, different value: a check comparing a result
// against a single input passes whenever the subject happened to be seeded with
// it, and a miss check whose key happens to hit asserts nothing.
//
// A field whose type admits no literal — a func, a channel, a type from a
// package this run never read — is declared and left at its zero value, and
// every generated check that needed it was dropped rather than emitted against
// a value nobody could write. Supply one through ReaderWithBoolWithFixture
// and the check a consumer writes has something to use.
type ReaderWithBoolFixture struct {
	Key      string
	KeyOther string
}

// DefaultReaderWithBoolFixture is what this run derived. Replace any field through
// ReaderWithBoolWithFixture; the rest keep these values.
func DefaultReaderWithBoolFixture() ReaderWithBoolFixture {
	return ReaderWithBoolFixture{
		Key:      "test-key",
		KeyOther: "other-key",
	}
}

// ReaderWithBool is the witnessed instantiation.
//
// A generic interface aliases with its own parameters, which Go admits
// since 1.24 — so every declaration below names ReaderWithBool and
// the arguments follow, exactly as they do for a concrete one.
type ReaderWithBool = readerwithbool.ReaderWithBool

// ReaderWithBoolRunOpt is one thing you can pass to RunReaderWithBool.
type ReaderWithBoolRunOpt interface {
	applyTo(*readerWithBoolRunConfig)
}

type readerWithBoolRunConfig struct {
	suite.Bundle[ReaderWithBool]
}

// ReaderWithBoolHarness declares one ReaderWithBool implementation under test.
type ReaderWithBoolHarness[T ReaderWithBool] struct {
	Name string

	// Exactly one constructor.
	New   func() T
	Start func(tb testing.TB) T

	// Excuse lists checks this subject structurally cannot take.
	Excuse []suite.ID

	// Oracle marks this subject as the run's reference. At most one.
	Oracle bool

	// Serial keeps this subject out of the parallel group.
	Serial bool

	// Provide answers open capabilities beyond the named fields — the
	// doors only this interface's checks know. The runner's gate reads
	// it for any capability without a dedicated field.
	Provide map[suite.Capability]any
}

// Subject lowers the declaration into what the runner consumes.
func (h ReaderWithBoolHarness[T]) Subject() (suite.Subject[ReaderWithBool], error) {
	build, err := suite.OneCtor(h.Name, h.New, h.Start)
	if err != nil {
		return suite.Subject[ReaderWithBool]{}, err
	}
	return suite.Subject[ReaderWithBool]{
		Name:     h.Name,
		Provides: h.Provide,
		New:      func(tb testing.TB) ReaderWithBool { return build(tb) },
		Oracle:   h.Oracle,
		Serial:   h.Serial,
		Excused:  suite.ExcuseSet(h.Excuse),
	}, nil
}

func (h ReaderWithBoolHarness[T]) applyTo(rc *readerWithBoolRunConfig) {
	rc.AddSubject(h.Subject())
}

type readerWithBoolDropOpt []suite.ID

func (d readerWithBoolDropOpt) applyTo(rc *readerWithBoolRunConfig) {
	rc.AddDrops(d...)
}

// readerWithBoolWithout drops checks by identity.
func readerWithBoolWithout(ids ...suite.ID) ReaderWithBoolRunOpt {
	return readerWithBoolDropOpt(ids)
}

// --- The veneer -------------------------------------------------------------

// ReaderWithBoolSuite is the entry a consumer names checks through.
var ReaderWithBoolSuite = readerWithBoolVeneer{Checks: readerWithBoolCheckIndex}

type readerWithBoolVeneer struct {
	// Checks is the check index — the constants a Without call is
	// written against.
	Checks readerWithBoolCheckIndexT
}

// Without drops a check by ID.
func (readerWithBoolVeneer) Without(ids ...suite.ID) ReaderWithBoolRunOpt {
	return readerWithBoolWithout(ids...)
}

// --- Drop hints -------------------------------------------------------------

// readerWithBoolIndexPath spells every emitted ID as the path a
// consumer would write to drop it, so a run that declines something can
// say what to type rather than what failed.
var readerWithBoolIndexPath = map[suite.ID]string{
	readerWithBoolCheckIndex.Load.Smoke(): "ReaderWithBoolSuite.Checks.Load.Smoke()",
	readerWithBoolCheckIndex.Load.Miss():  "ReaderWithBoolSuite.Checks.Load.Miss()",
	readerWithBoolCheckIndex.Load.Hit():   "ReaderWithBoolSuite.Checks.Load.Hit()",
}

var readerWithBoolDropHint = suite.DropHinter(
	"ReaderWithBoolSuite", readerWithBoolIndexPath,
)

// The names this file spells more than once: one home each.
const (
	readerWithBoolLoad = "Load"
)

// readerWithBoolCheckIndex indexes every check this package emits, by
// method and then by check.
var readerWithBoolCheckIndex = readerWithBoolCheckIndexT{
	Load: readerWithBoolLoadChecks{},
}

type readerWithBoolCheckIndexT struct {
	Load readerWithBoolLoadChecks
}

// All returns every ID this package emits.
func (readerWithBoolCheckIndexT) All() []suite.ID {
	var out []suite.ID
	out = append(out, readerWithBoolLoadChecks{}.All()...)
	return out
}

type readerWithBoolLoadChecks struct{}

func (readerWithBoolLoadChecks) Smoke() suite.ID {
	return suite.MethodID(readerWithBoolLoad, suite.SegSmoke)
}

func (readerWithBoolLoadChecks) Miss() suite.ID {
	return suite.MethodID(readerWithBoolLoad, suite.SegMiss)
}

func (readerWithBoolLoadChecks) Hit() suite.ID {
	return suite.MethodID(readerWithBoolLoad, suite.SegHit)
}

func (readerWithBoolLoadChecks) All() []suite.ID {
	return []suite.ID{
		readerWithBoolLoadChecks{}.Smoke(),
		readerWithBoolLoadChecks{}.Miss(),
		readerWithBoolLoadChecks{}.Hit(),
	}
}

// readerWithBoolSignatureChecks builds every check this run derived.
//
// sig is the one shape they share; the literals below state only what
// differs. Falsifiable is Proven wherever a defect rule reached the
// claim, which the parity gate refuses to stamp without one.
func readerWithBoolSignatureChecks(fx ReaderWithBoolFixture) []suite.Check[ReaderWithBool] {
	sig := suite.ProvenCheck[ReaderWithBool]
	ix := readerWithBoolCheckIndex
	return []suite.Check[ReaderWithBool]{
		sig(ix.Load.Smoke(), suite.ClassSmoke,
			"Load survives a call with a seeded key",
			func(tb testing.TB, r ReaderWithBool) {
				readerWithBoolAssertLoadSmoke(tb, r, fx)
			}),
		sig(ix.Load.Miss(), suite.ClassReader,
			"Load reports zero for a key nothing has seeded",
			func(tb testing.TB, r ReaderWithBool) {
				readerWithBoolAssertLoadMiss(tb, r, fx)
			}),
	}
}

// readerWithBoolAssertLoadSmoke asserts Load survives a call with a seeded key.
func readerWithBoolAssertLoadSmoke(
	tb testing.TB,
	r ReaderWithBool,
	fx ReaderWithBoolFixture,
) {
	tb.Helper()
	suite.Survives(tb, readerWithBoolLoad, func(ctx context.Context) {
		_, _ = r.Load(ctx, fx.Key)
	})
}

// readerWithBoolAssertLoadMiss asserts Load reports zero for a key nothing has seeded.
func readerWithBoolAssertLoadMiss(
	tb testing.TB,
	r ReaderWithBool,
	fx ReaderWithBoolFixture,
) {
	tb.Helper()
	ctx := tb.Context()

	got, _ := r.Load(ctx, fx.KeyOther)

	var zero readerwithbool.Value
	if got != zero {
		tb.Errorf("Load must return the zero value for an input nothing supplied: got %+v, want %+v",
			got, zero)
	}
}

// AssertReaderWithBoolContract runs every generated check against every declared subject.
//
//	Subjects: declare each with ReaderWithBoolSubject
//	Extend:   ReaderWithBoolOnLoad
//	Drop:     ReaderWithBoolWithout, by the path each check reports under
//	Unseeded: every check here runs against a fresh subject, because
//	          no method here is classified as a write.
//	          A read over an empty subject cannot tell a miss from a bug;
//	          ReaderWithBoolSeed is what closes it.
func AssertReaderWithBoolContract(t *testing.T, opts ...ReaderWithBoolOption) {
	t.Helper()
	cfg := newReaderWithBoolConfig(opts...)
	if len(cfg.subjects) == 0 {
		t.Fatalf("AssertReaderWithBoolContract was given no subject; declare one with " +
			"ReaderWithBoolSubject(name, factory)")
	}
	for _, p := range cfg.phantoms() {
		t.Errorf("ReaderWithBoolWithout(%q) declines nothing: no check here reports "+
			"under that path, so this suite asserts one thing more than its "+
			"author believes", p)
	}
	for _, s := range cfg.subjects {
		runReaderWithBoolChecks(t, cfg, s.name, s.factory, nil)
	}
}

// runReaderWithBoolChecks runs the whole contract against one subject.
//
// wrap is applied after the subject is built and seeded, so a wrapper is
// transparent to the seed and every failure it causes is its own.
func runReaderWithBoolChecks(
	t *testing.T,
	cfg *readerwithboolConfig,
	label string,
	factory func() readerwithbool.ReaderWithBool,
	wrap func(tb testing.TB, subject readerwithbool.ReaderWithBool) readerwithbool.ReaderWithBool,
) {
	t.Helper()
	t.Run(label, func(t *testing.T) {
		t.Parallel()

		t.Run(readerWithBoolLoad, func(t *testing.T) {
			t.Parallel()

			for _, c := range cfg.onLoad {
				cfg.run(t, string(suite.MethodID(readerWithBoolLoad, c.name)), c.name, func(tb testing.TB) {
					c.fn(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.Key)
				})
			}
		})

		// The extra bodies of checks this run was given, each reporting under its
		// own name and dropped by it.
		//
		// The plain subject only. The wrapped pass exists to prove the double
		// faithful and the per-method checks above already do that, so running one
		// through the wrapper prices the wrapper and says nothing new about the
		// subject.
		if wrap == nil {
			for _, e := range cfg.extensions {
				if cfg.declined(e.name) {
					t.Run(e.name, func(t *testing.T) {
						t.Skipf("dropped through ReaderWithBoolWithout(%q)", e.name)
					})
					continue
				}
				t.Run(e.name, func(t *testing.T) {
					t.Parallel()
					e.run(t, label, factory, cfg)
				})
			}
		}
	})
}

// ReaderWithBoolOption configures a contract run.
type ReaderWithBoolOption func(*readerwithboolConfig)

// ReaderWithBoolSubject declares an implementation to hold to the contract.
//
// Repeat it for every implementation that claims to satisfy the interface — an
// in-memory one, the one backed by a database, the one behind a network. Every
// check and every option is stated once and runs against all of them, which is
// what a conformance suite is for.
func ReaderWithBoolSubject(name string, factory func() readerwithbool.ReaderWithBool) ReaderWithBoolOption {
	return func(c *readerwithboolConfig) {
		c.subjects = append(c.subjects, namedReaderWithBoolSubject{name, factory})
	}
}

// ReaderWithBoolWithClock supplies the clock every time-reading check measures on.
//
// Defaults to the real one, so an implementation that does not take a clock
// behaves as it would have. Supply a [clock.TestClock] — the same one the
// factory builds the subject with — and a budget becomes a claim about the time
// the implementation means to spend rather than about how loaded the machine
// was.
func ReaderWithBoolWithClock(c clock.Clock) ReaderWithBoolOption {
	return func(cfg *readerwithboolConfig) { cfg.clock = c }
}

// ReaderWithBoolWithFixture replaces the derived inputs.
func ReaderWithBoolWithFixture(f ReaderWithBoolFixture) ReaderWithBoolOption {
	return func(c *readerwithboolConfig) { c.Fixture = f }
}

// ReaderWithBoolSeed replaces the derived seed.
//
// For an interface with no writer to seed itself through, which is the one
// input a conformance run cannot derive: a reader over an empty subject
// asserts nothing.
//
// Returning an error rather than swallowing one. A seed that failed quietly
// leaves every check after it asserting nothing and reporting success.
func ReaderWithBoolSeed(fn func(ctx context.Context, subject readerwithbool.ReaderWithBool) error) ReaderWithBoolOption {
	return func(c *readerwithboolConfig) {
		c.seed = fn
	}
}

// ReaderWithBoolOnLoad adds a named check to Load.
func ReaderWithBoolOnLoad(name string, fn ReaderWithBoolLoadCheck) ReaderWithBoolOption {
	return func(c *readerwithboolConfig) {
		c.onLoad = append(c.onLoad, namedReaderWithBoolLoadCheck{name, fn})
	}
}

// ReaderWithBoolWithout drops generated checks by the path each reports under.
//
// For a subject that legitimately violates one. Without this the only recourse
// is to stop running the suite, which costs every other check in the file — so
// a dropped check is reported as skipped rather than silently omitted.
func ReaderWithBoolWithout(paths ...string) ReaderWithBoolOption {
	return func(c *readerwithboolConfig) {
		for _, p := range paths {
			c.without[p] = struct{}{}
		}
	}
}

type namedReaderWithBoolLoadCheck struct {
	name string
	fn   ReaderWithBoolLoadCheck
}

type namedReaderWithBoolSubject struct {
	name    string
	factory func() readerwithbool.ReaderWithBool
}

// readerwithboolContractExtension is a body of checks a generated sibling adds through an option.
//
// A sibling declares the option and closes over whatever configures it, so
// everything that varies per run is typed and private to the file that owns it
// — this file understands only the name and the call. Nothing arrives except
// through an option, which is what keeps the set fixed before the first subtest
// starts.
type readerwithboolContractExtension struct {
	// name is the path this reports under, and the one ReaderWithBoolWithout drops
	// it by. Anything nested beneath it is the extension's own to name.
	name string

	// run takes the factory rather than a subject. One that drives sequences
	// builds a subject per iteration, and one that compares against a reference
	// needs the unseeded subject a factory returns — which
	// readerwithboolConfig.subject is not.
	//
	// Called once per subject, in parallel, sharing the config. Anything it
	// mutates has to be built inside the call: state captured when the option
	// was declared is state two subjects write at once.
	run func(
		t *testing.T,
		subject string,
		factory func() readerwithbool.ReaderWithBool,
		cfg *readerwithboolConfig,
	)
}

type readerwithboolConfig struct {
	Fixture    ReaderWithBoolFixture
	subjects   []namedReaderWithBoolSubject
	clock      clock.Clock
	seed       func(ctx context.Context, subject readerwithbool.ReaderWithBool) error
	without    map[string]struct{}
	extensions []readerwithboolContractExtension
	onLoad     []namedReaderWithBoolLoadCheck
}

func newReaderWithBoolConfig(opts ...ReaderWithBoolOption) *readerwithboolConfig {
	c := &readerwithboolConfig{
		Fixture: DefaultReaderWithBoolFixture(),
		clock:   clock.RealClock(),
		without: map[string]struct{}{},
	}
	for _, o := range opts {
		o(c)
	}
	return c
}

// subject builds a fresh implementation and seeds it, so every check runs
// against state no other check touched.
func (c *readerwithboolConfig) subject(
	tb testing.TB,
	factory func() readerwithbool.ReaderWithBool,
	wrap func(tb testing.TB, subject readerwithbool.ReaderWithBool) readerwithbool.ReaderWithBool,
) readerwithbool.ReaderWithBool {
	tb.Helper()
	s := factory()
	if c.seed != nil {
		// Reported rather than ignored. A seed that silently failed would leave
		// every check after it running against an empty subject — passing, and
		// asserting nothing.
		//
		// Which seed ran decides what the message can usefully say. The derived
		// one fails for one of two reasons and both are fixable from here, so it
		// names them. A seed the caller supplied is theirs, and telling them to
		// supply one is advice they already took.
		if err := c.seed(tb.Context(), s); err != nil {
			tb.Fatalf("the seed supplied through ReaderWithBoolSeed failed: %v", err)
		}
	}
	if wrap != nil {
		s = wrap(tb, s)
	}
	return s
}

// dropped reports whether a path was declined through ReaderWithBoolWithout.
//
// Named rather than inlined because the drop set is read from two places: here,
// and by whatever registered through the seam — which reports under paths this
// file has never heard of and must answer the same question about them.
func (c *readerwithboolConfig) declined(path string) bool {
	_, ok := c.without[path]
	return ok
}

// readerwithboolPaths is every path this file reports a check under.
//
// The set a ReaderWithBoolWithout argument has to be a member of. Declared
// rather than observed: a check that did not run is not a check that does not
// exist — an integration-only method skips without ever consulting the drop
// set, and reading that as a phantom would fail a correct suite on a machine
// with the environment unset.
//
//nolint:gochecknoglobals // the emitted check paths, read-only after init.
var readerwithboolPaths = map[string]struct{}{}

// phantoms names every ReaderWithBoolWithout path that declines nothing.
//
// A drop that drops nothing is this option's silent half: the consumer wrote
// it against a check since renamed, or never emitted for that method at all,
// and every run after reads green while asserting one thing more than its
// author believes. Extensions carry their own names — the seam reports under
// paths this file has never heard of — so they count as known too.
func (c *readerwithboolConfig) phantoms() []string {
	known := func(p string) bool {
		if _, ok := readerwithboolPaths[p]; ok {
			return true
		}
		for _, e := range c.extensions {
			if e.name == p {
				return true
			}
		}
		for _, on := range c.onLoad {
			if string(suite.MethodID(readerWithBoolLoad, on.name)) == p {
				return true
			}
		}
		return false
	}
	var out []string
	for p := range c.without {
		if !known(p) {
			out = append(out, p)
		}
	}
	slices.Sort(out)
	return out
}

// run executes one check unless it was dropped, reporting the drop rather than
// silently omitting it.
func (c *readerwithboolConfig) run(t *testing.T, path, name string, fn func(tb testing.TB)) {
	t.Helper()
	if c.declined(path) {
		t.Run(name, func(t *testing.T) {
			t.Skipf("dropped through ReaderWithBoolWithout(%q)", path)
		})
		return
	}
	t.Run(name, func(t *testing.T) {
		t.Parallel()
		fn(t)
	})
}

// testkit: end of generated content.
// testkit:provenance 116acb85defdd5c20816d2d73cf8d183c596ea725afc7322031686b3a02d2ff0
