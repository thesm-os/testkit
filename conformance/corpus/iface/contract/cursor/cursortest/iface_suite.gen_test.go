// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/contract/cursor/iface.go
// Plugins:   golang 1.0.0, suite 1.15.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package cursortest_test

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/iface/contract/cursor"
	"go.thesmos.sh/testkit/conformance/corpus/iface/contract/cursor/cursortest"
)

// Every generated check for Contract, driven against a stand-in built
// to violate it.
//
//	Proved:   8 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertContractNextSmokeCanFail holds AssertContractNextSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertContractNextSmokeCanFail(t *testing.T) {
	t.Parallel()

	subject := cursortest.NewContractStub(t,
		cursortest.WithContractNext(func(context.Context) (cursor.Value, bool, error) {
			panic("Contract.Next: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			cursortest.AssertContractNextSmoke(tb, subject)
		})
	testkit.Assert(t, got).Contains("Next panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertContractNextCancelsCanFail holds AssertContractNextCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertContractNextCancelsCanFail(t *testing.T) {
	t.Parallel()

	subject := cursortest.NewContractStub(t,
		cursortest.WithContractNext(func(context.Context) (cursor.Value, bool, error) {
			var r0 cursor.Value
			var r1 bool
			return r0, r1, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			cursortest.AssertContractNextCancels(tb, subject)
		})
	testkit.Assert(t, got).Contains("Next must report a cancelled context as context.Canceled",
		"and rejects it for the reason the check is about")
}

// TestAssertContractNextHonoursDeadlineCanFail holds AssertContractNextHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertContractNextHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	subject := cursortest.NewContractStub(t,
		cursortest.WithContractNext(func(context.Context) (cursor.Value, bool, error) {
			var r0 cursor.Value
			var r1 bool
			return r0, r1, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			cursortest.AssertContractNextHonoursDeadline(tb, subject)
		})
	testkit.Assert(t, got).Contains("Next must report an expired deadline as context.DeadlineExceeded",
		"and rejects it for the reason the check is about")
}

// TestAssertContractNextToleratesNilContextCanFail holds AssertContractNextToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertContractNextToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	subject := cursortest.NewContractStub(t,
		cursortest.WithContractNext(func(context.Context) (cursor.Value, bool, error) {
			panic("Contract.Next: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			cursortest.AssertContractNextToleratesNilContext(tb, subject)
		})
	testkit.Assert(t, got).Contains("Next panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertContractCloseSmokeCanFail holds AssertContractCloseSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertContractCloseSmokeCanFail(t *testing.T) {
	t.Parallel()

	subject := cursortest.NewContractStub(t,
		cursortest.WithContractClose(func(context.Context) error {
			panic("Contract.Close: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			cursortest.AssertContractCloseSmoke(tb, subject)
		})
	testkit.Assert(t, got).Contains("Close panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertContractCloseCancelsCanFail holds AssertContractCloseCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertContractCloseCancelsCanFail(t *testing.T) {
	t.Parallel()

	subject := cursortest.NewContractStub(t,
		cursortest.WithContractClose(func(context.Context) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			cursortest.AssertContractCloseCancels(tb, subject)
		})
	testkit.Assert(t, got).Contains("Close must report a cancelled context as context.Canceled",
		"and rejects it for the reason the check is about")
}

// TestAssertContractCloseHonoursDeadlineCanFail holds AssertContractCloseHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertContractCloseHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	subject := cursortest.NewContractStub(t,
		cursortest.WithContractClose(func(context.Context) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			cursortest.AssertContractCloseHonoursDeadline(tb, subject)
		})
	testkit.Assert(t, got).Contains("Close must report an expired deadline as context.DeadlineExceeded",
		"and rejects it for the reason the check is about")
}

// TestAssertContractCloseToleratesNilContextCanFail holds AssertContractCloseToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertContractCloseToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	subject := cursortest.NewContractStub(t,
		cursortest.WithContractClose(func(context.Context) error {
			panic("Contract.Close: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			cursortest.AssertContractCloseToleratesNilContext(tb, subject)
		})
	testkit.Assert(t, got).Contains("Close panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance bb76afe720ed5d74b1e1b2b68309f1d2f6c9331e4789f4c0321e5b31f0e0de9e
