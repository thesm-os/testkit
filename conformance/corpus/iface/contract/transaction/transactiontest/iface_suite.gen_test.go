// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/contract/transaction/iface.go
// Plugins:   golang 1.0.0, suite 1.7.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package transactiontest_test

import (
	"context"
	"errors"
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/iface/contract/transaction"
	"go.thesmos.sh/testkit/conformance/corpus/iface/contract/transaction/transactiontest"
)

// Every generated check for Contract, driven against a stand-in built
// to violate it.
//
//	Proved:   13 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertContractRunSmokeCanFail holds AssertContractRunSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertContractRunSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := transactiontest.DefaultContractFixture()
	subject := transactiontest.NewContractStub(t,
		transactiontest.WithContractRun(func(context.Context, func(context.Context) error) error {
			panic("Contract.Run: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			transactiontest.AssertContractRunSmoke(tb, subject, fixture.Body)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertContractRunCancelsCanFail holds AssertContractRunCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertContractRunCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := transactiontest.DefaultContractFixture()
	subject := transactiontest.NewContractStub(t,
		transactiontest.WithContractRun(func(context.Context, func(context.Context) error) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			transactiontest.AssertContractRunCancels(tb, subject, fixture.Body)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertContractRunHonoursDeadlineCanFail holds AssertContractRunHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertContractRunHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := transactiontest.DefaultContractFixture()
	subject := transactiontest.NewContractStub(t,
		transactiontest.WithContractRun(func(context.Context, func(context.Context) error) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			transactiontest.AssertContractRunHonoursDeadline(tb, subject, fixture.Body)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertContractRunToleratesNilContextCanFail holds AssertContractRunToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertContractRunToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := transactiontest.DefaultContractFixture()
	subject := transactiontest.NewContractStub(t,
		transactiontest.WithContractRun(func(context.Context, func(context.Context) error) error {
			panic("Contract.Run: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			transactiontest.AssertContractRunToleratesNilContext(tb, subject, fixture.Body)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertContractPutSmokeCanFail holds AssertContractPutSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertContractPutSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := transactiontest.DefaultContractFixture()
	subject := transactiontest.NewContractStub(t,
		transactiontest.WithContractPut(func(context.Context, string, transaction.Value) error {
			panic("Contract.Put: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			transactiontest.AssertContractPutSmoke(tb, subject, fixture.Key, fixture.V)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertContractPutCancelsCanFail holds AssertContractPutCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertContractPutCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := transactiontest.DefaultContractFixture()
	subject := transactiontest.NewContractStub(t,
		transactiontest.WithContractPut(func(context.Context, string, transaction.Value) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			transactiontest.AssertContractPutCancels(tb, subject, fixture.Key, fixture.V)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertContractPutHonoursDeadlineCanFail holds AssertContractPutHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertContractPutHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := transactiontest.DefaultContractFixture()
	subject := transactiontest.NewContractStub(t,
		transactiontest.WithContractPut(func(context.Context, string, transaction.Value) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			transactiontest.AssertContractPutHonoursDeadline(tb, subject, fixture.Key, fixture.V)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertContractPutToleratesNilContextCanFail holds AssertContractPutToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertContractPutToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := transactiontest.DefaultContractFixture()
	subject := transactiontest.NewContractStub(t,
		transactiontest.WithContractPut(func(context.Context, string, transaction.Value) error {
			panic("Contract.Put: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			transactiontest.AssertContractPutToleratesNilContext(tb, subject, fixture.Key, fixture.V)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertContractGetSmokeCanFail holds AssertContractGetSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertContractGetSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := transactiontest.DefaultContractFixture()
	subject := transactiontest.NewContractStub(t,
		transactiontest.WithContractGet(func(context.Context, string) (transaction.Value, error) {
			panic("Contract.Get: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			transactiontest.AssertContractGetSmoke(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertContractGetCancelsCanFail holds AssertContractGetCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertContractGetCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := transactiontest.DefaultContractFixture()
	subject := transactiontest.NewContractStub(t,
		transactiontest.WithContractGet(func(context.Context, string) (transaction.Value, error) {
			var r0 transaction.Value
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			transactiontest.AssertContractGetCancels(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertContractGetHonoursDeadlineCanFail holds AssertContractGetHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertContractGetHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := transactiontest.DefaultContractFixture()
	subject := transactiontest.NewContractStub(t,
		transactiontest.WithContractGet(func(context.Context, string) (transaction.Value, error) {
			var r0 transaction.Value
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			transactiontest.AssertContractGetHonoursDeadline(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertContractGetToleratesNilContextCanFail holds AssertContractGetToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertContractGetToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := transactiontest.DefaultContractFixture()
	subject := transactiontest.NewContractStub(t,
		transactiontest.WithContractGet(func(context.Context, string) (transaction.Value, error) {
			panic("Contract.Get: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			transactiontest.AssertContractGetToleratesNilContext(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertContractGetZeroOnErrorCanFail holds AssertContractGetZeroOnError to being able to fail.
//
// The stand-in refuses the call and answers a value that looks like a result
// anyway. A check that let that pass would leave a caller who checks the error
// and a caller who checks the value disagreeing about what happened — the
// claim's own violation, not a stand-in that merely succeeds.
func TestAssertContractGetZeroOnErrorCanFail(t *testing.T) {
	t.Parallel()

	fixture := transactiontest.DefaultContractFixture()
	subject := transactiontest.NewContractStub(t,
		transactiontest.WithContractGet(func(context.Context, string) (transaction.Value, error) {
			return transaction.Value{Key: "test-r0"}, errors.New("violate: refused with a believable value")
		}))

	got := testkit.Rejects(t, "a method answering a believable value beside its error",
		func(tb testing.TB) {
			tb.Helper()
			transactiontest.AssertContractGetZeroOnError(tb, subject, fixture.KeyOther)
		})
	testkit.Assert(t, got).Contains("must return the zero value",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance 0e469bc05086b22252930d0a9efb273db9fd138b147834516b625283cdbb62df
