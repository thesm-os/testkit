// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/contract/leader-election/iface.go
// Plugins:   golang 1.0.0, suite 1.0.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package leaderelectiontest

import (
	"context"
	"testing"
	"time"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/clock"
	leaderelection "go.thesmos.sh/testkit/conformance/corpus/iface/contract/leader-election"
)

// ContractCampaignCheck is one assertion about Campaign.
//
// Every generated check for Campaign is a value of it, and so is one you
// write — so they compose, reorder, and each runs standalone. testing.TB rather
// than *testing.T is what lets a stand-in drive it and prove it can fail.
type ContractCampaignCheck func(tb testing.TB, subject leaderelection.Contract)

// ContractResignCheck is one assertion about Resign.
//
// Every generated check for Resign is a value of it, and so is one you
// write — so they compose, reorder, and each runs standalone. testing.TB rather
// than *testing.T is what lets a stand-in drive it and prove it can fail.
type ContractResignCheck func(tb testing.TB, subject leaderelection.Contract)

// ContractIsLeaderCheck is one assertion about IsLeader.
//
// Every generated check for IsLeader is a value of it, and so is one you
// write — so they compose, reorder, and each runs standalone. testing.TB rather
// than *testing.T is what lets a stand-in drive it and prove it can fail.
type ContractIsLeaderCheck func(tb testing.TB, subject leaderelection.Contract)

// ContractFixture holds every input the generated checks run against.
//
// Derived from each method's parameter names and types. Every field has a
// companion holding a second, different value: a check comparing a result
// against a single input passes whenever the subject happened to be seeded with
// it, and a miss check whose key happens to hit asserts nothing.
//
// A field whose type admits no literal — a func, a channel, a type from a
// package this run never read — is declared and left at its zero value, and
// every generated check that needed it was dropped rather than emitted against
// a value nobody could write. Supply one through ContractWithFixture
// and the check a consumer writes has something to use.
type ContractFixture struct {
}

// DefaultContractFixture is what this run derived. Replace any field through
// ContractWithFixture; the rest keep these values.
func DefaultContractFixture() ContractFixture {
	return ContractFixture{}
}

// AssertContractContract runs every generated check against every declared subject.
//
//	Checks:   10 across 3 methods, per subject
//	Subjects: declare each with ContractSubject
//	Double:   every subject runs a second time wrapped in ContractStub, so
//	          anything the wrapper fails that the subject passes is the double
//	          lying. ContractWithoutDouble declines it.
//	Extend:   ContractOnCampaign, ContractOnResign, ContractOnIsLeader
//	Drop:     ContractWithout, by the path each check reports under
//
// # What this file does not check
//
// The interface declares these and nothing here asserts them. Each is statable
// against a subject you can build, so the check is yours to write:
//
//   - leader-election, on Campaign, Resign, IsLeader — write it with ContractOnCampaign
//
// # What is checked somewhere else
//
// These need a reference implementation to compare against, which a suite run
// has no way to build. Nothing here asserts them and nothing here should:
//
//   - lifecycle, on Campaign, Resign
//   - aggregator, on IsLeader
func AssertContractContract(t *testing.T, opts ...ContractOption) {
	t.Helper()
	cfg := newContractConfig(opts...)
	if len(cfg.subjects) == 0 {
		t.Fatalf("AssertContractContract was given no subject; declare one with " +
			"ContractSubject(name, factory)")
	}
	for _, s := range cfg.subjects {
		runContractChecks(t, cfg, s.name, s.factory, nil)
		if !cfg.withoutDouble {
			runContractChecks(t, cfg, s.name+" through the double", s.factory,
				func(tb testing.TB, subject leaderelection.Contract) leaderelection.Contract {
					return NewContractStub(tb, ContractStubDelegateTo(subject))
				})
		}
	}
}

// runContractChecks runs the whole contract against one subject.
//
// wrap is applied after the subject is built and seeded, so a wrapper is
// transparent to the seed and every failure it causes is its own.
func runContractChecks(
	t *testing.T,
	cfg *contractConfig,
	label string,
	factory func() leaderelection.Contract,
	wrap func(tb testing.TB, subject leaderelection.Contract) leaderelection.Contract,
) {
	t.Helper()
	t.Run(label, func(t *testing.T) {
		t.Parallel()

		t.Run("Campaign", func(t *testing.T) {
			t.Parallel()
			cfg.run(t, "Campaign/smoke", "smoke", func(tb testing.TB) {
				AssertContractCampaignSmoke(tb, cfg.subject(tb, factory, wrap))
			})
			cfg.run(t, "Campaign/reports a cancelled context", "reports a cancelled context", func(tb testing.TB) {
				AssertContractCampaignCancels(tb, cfg.subject(tb, factory, wrap))
			})
			cfg.run(t, "Campaign/reports an expired deadline", "reports an expired deadline", func(tb testing.TB) {
				AssertContractCampaignHonoursDeadline(tb, cfg.subject(tb, factory, wrap))
			})
			cfg.run(t, "Campaign/tolerates a nil context", "tolerates a nil context", func(tb testing.TB) {
				AssertContractCampaignToleratesNilContext(tb, cfg.subject(tb, factory, wrap))
			})
			for _, c := range cfg.onCampaign {
				cfg.run(t, "Campaign"+"/"+c.name, c.name, func(tb testing.TB) {
					c.fn(tb, cfg.subject(tb, factory, wrap))
				})
			}
		})

		t.Run("Resign", func(t *testing.T) {
			t.Parallel()
			cfg.run(t, "Resign/smoke", "smoke", func(tb testing.TB) {
				AssertContractResignSmoke(tb, cfg.subject(tb, factory, wrap))
			})
			cfg.run(t, "Resign/reports a cancelled context", "reports a cancelled context", func(tb testing.TB) {
				AssertContractResignCancels(tb, cfg.subject(tb, factory, wrap))
			})
			cfg.run(t, "Resign/reports an expired deadline", "reports an expired deadline", func(tb testing.TB) {
				AssertContractResignHonoursDeadline(tb, cfg.subject(tb, factory, wrap))
			})
			cfg.run(t, "Resign/tolerates a nil context", "tolerates a nil context", func(tb testing.TB) {
				AssertContractResignToleratesNilContext(tb, cfg.subject(tb, factory, wrap))
			})
			for _, c := range cfg.onResign {
				cfg.run(t, "Resign"+"/"+c.name, c.name, func(tb testing.TB) {
					c.fn(tb, cfg.subject(tb, factory, wrap))
				})
			}
		})

		t.Run("IsLeader", func(t *testing.T) {
			t.Parallel()
			cfg.run(t, "IsLeader/smoke", "smoke", func(tb testing.TB) {
				AssertContractIsLeaderSmoke(tb, cfg.subject(tb, factory, wrap))
			})
			cfg.run(t, "IsLeader/tolerates a nil context", "tolerates a nil context", func(tb testing.TB) {
				AssertContractIsLeaderToleratesNilContext(tb, cfg.subject(tb, factory, wrap))
			})
			for _, c := range cfg.onIsLeader {
				cfg.run(t, "IsLeader"+"/"+c.name, c.name, func(tb testing.TB) {
					c.fn(tb, cfg.subject(tb, factory, wrap))
				})
			}
		})
	})
}

// AssertContractCampaignSmoke asserts Campaign survives a call with derived inputs.
//
// Fails when: Campaign panics. The weakest check in this file and the one
// that catches the most — a method that panics on a derived value is one no
// other check here reaches.
func AssertContractCampaignSmoke(tb testing.TB, subject leaderelection.Contract) {
	tb.Helper()
	defer func() {
		if r := recover(); r != nil {
			tb.Fatalf("Campaign panicked on a derived value (%v); supply one it "+
				"accepts through ContractWithFixture", r)
		}
	}()
	ctx := tb.Context()
	_ = subject.Campaign(ctx)
}

// AssertContractCampaignCancels asserts Campaign reports a cancelled context as cancelled.
//
// Fails when: Campaign returns nil for a context cancelled before the
// call, or returns an error that does not answer to context.Canceled. Both
// matter and they are different defects: the first is work done that the caller
// asked not to be, the second is a caller who wrote
// `errors.Is(err, context.Canceled)` and gets false for a call that was.
//
// The error rather than merely its presence is what separates this from the
// deadline check. Asserting only that something came back makes the two one
// check written twice.
func AssertContractCampaignCancels(tb testing.TB, subject leaderelection.Contract) {
	tb.Helper()
	ctx, cancel := context.WithCancel(tb.Context())
	cancel()
	err := subject.Campaign(ctx)
	testkit.ErrorIs(tb, err, context.Canceled,
		"Campaign must report a cancelled context as context.Canceled")
}

// AssertContractCampaignHonoursDeadline asserts Campaign reports an expired deadline as exceeded.
//
// Fails when: Campaign returns nil for a context whose deadline has
// passed, or returns an error that does not answer to
// context.DeadlineExceeded. Distinct from cancellation in what the caller
// learns: a cancelled call was called off, an expired one ran out of time, and
// only the second is worth retrying.
//
// The deadline is the zero time, which is unconditionally in the past. No clock
// is read: a generated check that consults the wall clock is one whose subject
// is partly the machine it runs on.
func AssertContractCampaignHonoursDeadline(tb testing.TB, subject leaderelection.Contract) {
	tb.Helper()
	ctx, cancel := context.WithDeadline(tb.Context(), time.Time{})
	defer cancel()
	err := subject.Campaign(ctx)
	testkit.ErrorIs(tb, err, context.DeadlineExceeded,
		"Campaign must report an expired deadline as context.DeadlineExceeded")
}

// AssertContractCampaignToleratesNilContext asserts Campaign does not panic on a nil context.
//
// Fails when: Campaign panics. Returning an error is correct and
// succeeding is correct — a nil context reaches production through a caller
// that forgot one, and a panic turns that into an outage rather than a failed
// request.
func AssertContractCampaignToleratesNilContext(tb testing.TB, subject leaderelection.Contract) {
	tb.Helper()
	defer func() {
		if r := recover(); r != nil {
			tb.Errorf("Campaign panicked on a nil context (%v); return an error instead", r)
		}
	}()
	//nolint:staticcheck // passing nil is the check.
	var ctx context.Context
	_ = subject.Campaign(ctx)
}

// AssertContractResignSmoke asserts Resign survives a call with derived inputs.
//
// Fails when: Resign panics. The weakest check in this file and the one
// that catches the most — a method that panics on a derived value is one no
// other check here reaches.
func AssertContractResignSmoke(tb testing.TB, subject leaderelection.Contract) {
	tb.Helper()
	defer func() {
		if r := recover(); r != nil {
			tb.Fatalf("Resign panicked on a derived value (%v); supply one it "+
				"accepts through ContractWithFixture", r)
		}
	}()
	ctx := tb.Context()
	_ = subject.Resign(ctx)
}

// AssertContractResignCancels asserts Resign reports a cancelled context as cancelled.
//
// Fails when: Resign returns nil for a context cancelled before the
// call, or returns an error that does not answer to context.Canceled. Both
// matter and they are different defects: the first is work done that the caller
// asked not to be, the second is a caller who wrote
// `errors.Is(err, context.Canceled)` and gets false for a call that was.
//
// The error rather than merely its presence is what separates this from the
// deadline check. Asserting only that something came back makes the two one
// check written twice.
func AssertContractResignCancels(tb testing.TB, subject leaderelection.Contract) {
	tb.Helper()
	ctx, cancel := context.WithCancel(tb.Context())
	cancel()
	err := subject.Resign(ctx)
	testkit.ErrorIs(tb, err, context.Canceled,
		"Resign must report a cancelled context as context.Canceled")
}

// AssertContractResignHonoursDeadline asserts Resign reports an expired deadline as exceeded.
//
// Fails when: Resign returns nil for a context whose deadline has
// passed, or returns an error that does not answer to
// context.DeadlineExceeded. Distinct from cancellation in what the caller
// learns: a cancelled call was called off, an expired one ran out of time, and
// only the second is worth retrying.
//
// The deadline is the zero time, which is unconditionally in the past. No clock
// is read: a generated check that consults the wall clock is one whose subject
// is partly the machine it runs on.
func AssertContractResignHonoursDeadline(tb testing.TB, subject leaderelection.Contract) {
	tb.Helper()
	ctx, cancel := context.WithDeadline(tb.Context(), time.Time{})
	defer cancel()
	err := subject.Resign(ctx)
	testkit.ErrorIs(tb, err, context.DeadlineExceeded,
		"Resign must report an expired deadline as context.DeadlineExceeded")
}

// AssertContractResignToleratesNilContext asserts Resign does not panic on a nil context.
//
// Fails when: Resign panics. Returning an error is correct and
// succeeding is correct — a nil context reaches production through a caller
// that forgot one, and a panic turns that into an outage rather than a failed
// request.
func AssertContractResignToleratesNilContext(tb testing.TB, subject leaderelection.Contract) {
	tb.Helper()
	defer func() {
		if r := recover(); r != nil {
			tb.Errorf("Resign panicked on a nil context (%v); return an error instead", r)
		}
	}()
	//nolint:staticcheck // passing nil is the check.
	var ctx context.Context
	_ = subject.Resign(ctx)
}

// AssertContractIsLeaderSmoke asserts IsLeader survives a call with derived inputs.
//
// Fails when: IsLeader panics. The weakest check in this file and the one
// that catches the most — a method that panics on a derived value is one no
// other check here reaches.
func AssertContractIsLeaderSmoke(tb testing.TB, subject leaderelection.Contract) {
	tb.Helper()
	defer func() {
		if r := recover(); r != nil {
			tb.Fatalf("IsLeader panicked on a derived value (%v); supply one it "+
				"accepts through ContractWithFixture", r)
		}
	}()
	ctx := tb.Context()
	_ = subject.IsLeader(ctx)
}

// AssertContractIsLeaderToleratesNilContext asserts IsLeader does not panic on a nil context.
//
// Fails when: IsLeader panics. Returning an error is correct and
// succeeding is correct — a nil context reaches production through a caller
// that forgot one, and a panic turns that into an outage rather than a failed
// request.
func AssertContractIsLeaderToleratesNilContext(tb testing.TB, subject leaderelection.Contract) {
	tb.Helper()
	defer func() {
		if r := recover(); r != nil {
			tb.Errorf("IsLeader panicked on a nil context (%v); return an error instead", r)
		}
	}()
	//nolint:staticcheck // passing nil is the check.
	var ctx context.Context
	_ = subject.IsLeader(ctx)
}

// ContractOption configures a contract run.
type ContractOption func(*contractConfig)

// ContractSubject declares an implementation to hold to the contract.
//
// Repeat it for every implementation that claims to satisfy the interface — an
// in-memory one, the one backed by a database, the one behind a network. Every
// check and every option is stated once and runs against all of them, which is
// what a conformance suite is for.
func ContractSubject(name string, factory func() leaderelection.Contract) ContractOption {
	return func(c *contractConfig) {
		c.subjects = append(c.subjects, namedContractSubject{name, factory})
	}
}

// ContractWithoutDouble declines the second run through ContractStub.
//
// The double run proves the stand-in is faithful, and costs a second pass over
// every check. Decline it where the double is not used.
func ContractWithoutDouble() ContractOption {
	return func(c *contractConfig) { c.withoutDouble = true }
}

// ContractWithClock supplies the clock every time-reading check measures on.
//
// Defaults to the real one, so an implementation that does not take a clock
// behaves as it would have. Supply a [clock.TestClock] — the same one the
// factory builds the subject with — and a budget becomes a claim about the time
// the implementation means to spend rather than about how loaded the machine
// was.
func ContractWithClock(c clock.Clock) ContractOption {
	return func(cfg *contractConfig) { cfg.clock = c }
}

// ContractWithFixture replaces the derived inputs.
func ContractWithFixture(f ContractFixture) ContractOption {
	return func(c *contractConfig) { c.Fixture = f }
}

// ContractSeed replaces the derived seed.
//
// For an interface with no writer to seed itself through, which is the one
// input a conformance run cannot derive: a reader over an empty subject
// asserts nothing.
//
// Returning an error rather than swallowing one. A seed that failed quietly
// leaves every check after it asserting nothing and reporting success.
func ContractSeed(fn func(ctx context.Context, subject leaderelection.Contract) error) ContractOption {
	return func(c *contractConfig) {
		c.seed = fn
	}
}

// ContractOnCampaign adds a named check to Campaign.
func ContractOnCampaign(name string, fn ContractCampaignCheck) ContractOption {
	return func(c *contractConfig) {
		c.onCampaign = append(c.onCampaign, namedContractCampaignCheck{name, fn})
	}
}

// ContractOnResign adds a named check to Resign.
func ContractOnResign(name string, fn ContractResignCheck) ContractOption {
	return func(c *contractConfig) {
		c.onResign = append(c.onResign, namedContractResignCheck{name, fn})
	}
}

// ContractOnIsLeader adds a named check to IsLeader.
func ContractOnIsLeader(name string, fn ContractIsLeaderCheck) ContractOption {
	return func(c *contractConfig) {
		c.onIsLeader = append(c.onIsLeader, namedContractIsLeaderCheck{name, fn})
	}
}

// ContractWithout drops generated checks by the path each reports under.
//
// For a subject that legitimately violates one. Without this the only recourse
// is to stop running the suite, which costs every other check in the file — so
// a dropped check is reported as skipped rather than silently omitted.
func ContractWithout(paths ...string) ContractOption {
	return func(c *contractConfig) {
		for _, p := range paths {
			c.without[p] = struct{}{}
		}
	}
}

type namedContractCampaignCheck struct {
	name string
	fn   ContractCampaignCheck
}

type namedContractResignCheck struct {
	name string
	fn   ContractResignCheck
}

type namedContractIsLeaderCheck struct {
	name string
	fn   ContractIsLeaderCheck
}

type namedContractSubject struct {
	name    string
	factory func() leaderelection.Contract
}

type contractConfig struct {
	Fixture       ContractFixture
	subjects      []namedContractSubject
	withoutDouble bool
	clock         clock.Clock
	seed          func(ctx context.Context, subject leaderelection.Contract) error
	without       map[string]struct{}
	onCampaign    []namedContractCampaignCheck
	onResign      []namedContractResignCheck
	onIsLeader    []namedContractIsLeaderCheck
}

func newContractConfig(opts ...ContractOption) *contractConfig {
	c := &contractConfig{
		Fixture: DefaultContractFixture(),
		clock:   clock.RealClock(),
		without: map[string]struct{}{},
	}
	for _, o := range opts {
		o(c)
	}
	return c
}

// subject builds a fresh implementation and seeds it, so every check runs
// against state no other check touched.
func (c *contractConfig) subject(
	tb testing.TB,
	factory func() leaderelection.Contract,
	wrap func(tb testing.TB, subject leaderelection.Contract) leaderelection.Contract,
) leaderelection.Contract {
	tb.Helper()
	s := factory()
	if c.seed != nil {
		// Reported rather than ignored. A seed that silently failed would leave
		// every check after it running against an empty subject — passing, and
		// asserting nothing.
		//
		// Which seed ran decides what the message can usefully say. The derived
		// one fails for one of two reasons and both are fixable from here, so it
		// names them. A seed the caller supplied is theirs, and telling them to
		// supply one is advice they already took.
		if err := c.seed(tb.Context(), s); err != nil {
			tb.Fatalf("the seed supplied through ContractSeed failed: %v", err)
		}
	}
	if wrap != nil {
		s = wrap(tb, s)
	}
	return s
}

// run executes one check unless it was dropped, reporting the drop rather than
// silently omitting it.
func (c *contractConfig) run(t *testing.T, path, name string, fn func(tb testing.TB)) {
	t.Helper()
	if _, dropped := c.without[path]; dropped {
		t.Run(name, func(t *testing.T) {
			t.Skipf("dropped through ContractWithout(%q)", path)
		})
		return
	}
	t.Run(name, func(t *testing.T) {
		t.Parallel()
		fn(t)
	})
}

// testkit: end of generated content.
// testkit:provenance c08012ca113aa7a1aa64bdc1e4a52d3321c938ab89f678d72cf00f86af58e4d5
