// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/contract/leader-election/iface.go
// Plugins:   golang 1.0.0, suite 1.15.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package leaderelectiontest_test

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/iface/contract/leader-election/leaderelectiontest"
)

// Every generated check for Contract, driven against a stand-in built
// to violate it.
//
//	Proved:   10 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertContractCampaignSmokeCanFail holds AssertContractCampaignSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertContractCampaignSmokeCanFail(t *testing.T) {
	t.Parallel()

	subject := leaderelectiontest.NewContractStub(t,
		leaderelectiontest.WithContractCampaign(func(context.Context) error {
			panic("Contract.Campaign: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			leaderelectiontest.AssertContractCampaignSmoke(tb, subject)
		})
	testkit.Assert(t, got).Contains("Campaign panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertContractCampaignCancelsCanFail holds AssertContractCampaignCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertContractCampaignCancelsCanFail(t *testing.T) {
	t.Parallel()

	subject := leaderelectiontest.NewContractStub(t,
		leaderelectiontest.WithContractCampaign(func(context.Context) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			leaderelectiontest.AssertContractCampaignCancels(tb, subject)
		})
	testkit.Assert(t, got).Contains("Campaign must report a cancelled context as context.Canceled",
		"and rejects it for the reason the check is about")
}

// TestAssertContractCampaignHonoursDeadlineCanFail holds AssertContractCampaignHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertContractCampaignHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	subject := leaderelectiontest.NewContractStub(t,
		leaderelectiontest.WithContractCampaign(func(context.Context) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			leaderelectiontest.AssertContractCampaignHonoursDeadline(tb, subject)
		})
	testkit.Assert(t, got).Contains("Campaign must report an expired deadline as context.DeadlineExceeded",
		"and rejects it for the reason the check is about")
}

// TestAssertContractCampaignToleratesNilContextCanFail holds AssertContractCampaignToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertContractCampaignToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	subject := leaderelectiontest.NewContractStub(t,
		leaderelectiontest.WithContractCampaign(func(context.Context) error {
			panic("Contract.Campaign: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			leaderelectiontest.AssertContractCampaignToleratesNilContext(tb, subject)
		})
	testkit.Assert(t, got).Contains("Campaign panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertContractResignSmokeCanFail holds AssertContractResignSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertContractResignSmokeCanFail(t *testing.T) {
	t.Parallel()

	subject := leaderelectiontest.NewContractStub(t,
		leaderelectiontest.WithContractResign(func(context.Context) error {
			panic("Contract.Resign: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			leaderelectiontest.AssertContractResignSmoke(tb, subject)
		})
	testkit.Assert(t, got).Contains("Resign panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertContractResignCancelsCanFail holds AssertContractResignCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertContractResignCancelsCanFail(t *testing.T) {
	t.Parallel()

	subject := leaderelectiontest.NewContractStub(t,
		leaderelectiontest.WithContractResign(func(context.Context) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			leaderelectiontest.AssertContractResignCancels(tb, subject)
		})
	testkit.Assert(t, got).Contains("Resign must report a cancelled context as context.Canceled",
		"and rejects it for the reason the check is about")
}

// TestAssertContractResignHonoursDeadlineCanFail holds AssertContractResignHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertContractResignHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	subject := leaderelectiontest.NewContractStub(t,
		leaderelectiontest.WithContractResign(func(context.Context) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			leaderelectiontest.AssertContractResignHonoursDeadline(tb, subject)
		})
	testkit.Assert(t, got).Contains("Resign must report an expired deadline as context.DeadlineExceeded",
		"and rejects it for the reason the check is about")
}

// TestAssertContractResignToleratesNilContextCanFail holds AssertContractResignToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertContractResignToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	subject := leaderelectiontest.NewContractStub(t,
		leaderelectiontest.WithContractResign(func(context.Context) error {
			panic("Contract.Resign: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			leaderelectiontest.AssertContractResignToleratesNilContext(tb, subject)
		})
	testkit.Assert(t, got).Contains("Resign panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertContractIsLeaderSmokeCanFail holds AssertContractIsLeaderSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertContractIsLeaderSmokeCanFail(t *testing.T) {
	t.Parallel()

	subject := leaderelectiontest.NewContractStub(t,
		leaderelectiontest.WithContractIsLeader(func(context.Context) bool {
			panic("Contract.IsLeader: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			leaderelectiontest.AssertContractIsLeaderSmoke(tb, subject)
		})
	testkit.Assert(t, got).Contains("IsLeader panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertContractIsLeaderToleratesNilContextCanFail holds AssertContractIsLeaderToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertContractIsLeaderToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	subject := leaderelectiontest.NewContractStub(t,
		leaderelectiontest.WithContractIsLeader(func(context.Context) bool {
			panic("Contract.IsLeader: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			leaderelectiontest.AssertContractIsLeaderToleratesNilContext(tb, subject)
		})
	testkit.Assert(t, got).Contains("IsLeader panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance 15a01f369484362a63a038864daefa654730c23ed2034aa85a2cc76a94f6096e
