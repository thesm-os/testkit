// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/contract/watcher/iface.go
// Plugins:   golang 1.0.0, suite 1.3.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package watchertest_test

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/iface/contract/watcher"
	"go.thesmos.sh/testkit/conformance/corpus/iface/contract/watcher/watchertest"
)

// Every generated check for Contract, driven against a stand-in built
// to violate it.
//
//	Proved:   8 checks
//	Unproved: 1, named below with what each would take
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertContractWatchSmokeCanFail holds AssertContractWatchSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertContractWatchSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := watchertest.DefaultContractFixture()
	subject := watchertest.NewContractStub(t,
		watchertest.WithContractWatch(func(context.Context, string) (watcher.Subscription, error) {
			panic("Contract.Watch: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			watchertest.AssertContractWatchSmoke(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertContractWatchCancelsCanFail holds AssertContractWatchCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertContractWatchCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := watchertest.DefaultContractFixture()
	subject := watchertest.NewContractStub(t,
		watchertest.WithContractWatch(func(context.Context, string) (watcher.Subscription, error) {
			var r0 watcher.Subscription
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			watchertest.AssertContractWatchCancels(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertContractWatchHonoursDeadlineCanFail holds AssertContractWatchHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertContractWatchHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := watchertest.DefaultContractFixture()
	subject := watchertest.NewContractStub(t,
		watchertest.WithContractWatch(func(context.Context, string) (watcher.Subscription, error) {
			var r0 watcher.Subscription
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			watchertest.AssertContractWatchHonoursDeadline(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertContractWatchToleratesNilContextCanFail holds AssertContractWatchToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertContractWatchToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := watchertest.DefaultContractFixture()
	subject := watchertest.NewContractStub(t,
		watchertest.WithContractWatch(func(context.Context, string) (watcher.Subscription, error) {
			panic("Contract.Watch: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			watchertest.AssertContractWatchToleratesNilContext(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertContractTriggerSmokeCanFail holds AssertContractTriggerSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertContractTriggerSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := watchertest.DefaultContractFixture()
	subject := watchertest.NewContractStub(t,
		watchertest.WithContractTrigger(func(context.Context, string, watcher.Value) error {
			panic("Contract.Trigger: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			watchertest.AssertContractTriggerSmoke(tb, subject, fixture.Key, fixture.V)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertContractTriggerCancelsCanFail holds AssertContractTriggerCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertContractTriggerCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := watchertest.DefaultContractFixture()
	subject := watchertest.NewContractStub(t,
		watchertest.WithContractTrigger(func(context.Context, string, watcher.Value) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			watchertest.AssertContractTriggerCancels(tb, subject, fixture.Key, fixture.V)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertContractTriggerHonoursDeadlineCanFail holds AssertContractTriggerHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertContractTriggerHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := watchertest.DefaultContractFixture()
	subject := watchertest.NewContractStub(t,
		watchertest.WithContractTrigger(func(context.Context, string, watcher.Value) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			watchertest.AssertContractTriggerHonoursDeadline(tb, subject, fixture.Key, fixture.V)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertContractTriggerToleratesNilContextCanFail holds AssertContractTriggerToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertContractTriggerToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := watchertest.DefaultContractFixture()
	subject := watchertest.NewContractStub(t,
		watchertest.WithContractTrigger(func(context.Context, string, watcher.Value) error {
			panic("Contract.Trigger: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			watchertest.AssertContractTriggerToleratesNilContext(tb, subject, fixture.Key, fixture.V)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// Checks with no proof here yet, and what one would take.
//
// Nothing below is unbreakable — a stand-in answers each call from a closure,
// so it can carry whatever state a violation needs. Each line names the piece
// that is missing.
//
//   - AssertContractWatchZeroOnError: the stand-in has to answer with a believable value, and no literal can be written for what this method returns

// testkit: end of generated content.
// testkit:provenance 5d1c29174b392b73a2f63d433c4aff2ea400085586b3793d1a38e55a8c96f90f
