// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/contract/chain/iface.go
// Plugins:   golang 1.0.0, suite 1.0.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/iface/contract/...

package chaintest_test

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/iface/contract/chain"
	"go.thesmos.sh/testkit/conformance/corpus/iface/contract/chain/chaintest"
)

// Every generated check for Contract, driven against a stand-in built
// to violate it.
//
//	Proved:   12 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertContractAppendSmokeCanFail holds AssertContractAppendSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertContractAppendSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := chaintest.DefaultContractFixture()
	subject := chaintest.NewContractStub(t,
		chaintest.WithContractAppend(func(context.Context, chain.Entry) error {
			panic("Contract.Append: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			chaintest.AssertContractAppendSmoke(tb, subject, fixture.E)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertContractAppendCancelsCanFail holds AssertContractAppendCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertContractAppendCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := chaintest.DefaultContractFixture()
	subject := chaintest.NewContractStub(t,
		chaintest.WithContractAppend(func(context.Context, chain.Entry) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			chaintest.AssertContractAppendCancels(tb, subject, fixture.E)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertContractAppendHonoursDeadlineCanFail holds AssertContractAppendHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertContractAppendHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := chaintest.DefaultContractFixture()
	subject := chaintest.NewContractStub(t,
		chaintest.WithContractAppend(func(context.Context, chain.Entry) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			chaintest.AssertContractAppendHonoursDeadline(tb, subject, fixture.E)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertContractAppendToleratesNilContextCanFail holds AssertContractAppendToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertContractAppendToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := chaintest.DefaultContractFixture()
	subject := chaintest.NewContractStub(t,
		chaintest.WithContractAppend(func(context.Context, chain.Entry) error {
			panic("Contract.Append: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			chaintest.AssertContractAppendToleratesNilContext(tb, subject, fixture.E)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertContractReplaySmokeCanFail holds AssertContractReplaySmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertContractReplaySmokeCanFail(t *testing.T) {
	t.Parallel()

	subject := chaintest.NewContractStub(t,
		chaintest.WithContractReplay(func(context.Context) ([]chain.Entry, error) {
			panic("Contract.Replay: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			chaintest.AssertContractReplaySmoke(tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertContractReplayCancelsCanFail holds AssertContractReplayCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertContractReplayCancelsCanFail(t *testing.T) {
	t.Parallel()

	subject := chaintest.NewContractStub(t,
		chaintest.WithContractReplay(func(context.Context) ([]chain.Entry, error) {
			var r0 []chain.Entry
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			chaintest.AssertContractReplayCancels(tb, subject)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertContractReplayHonoursDeadlineCanFail holds AssertContractReplayHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertContractReplayHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	subject := chaintest.NewContractStub(t,
		chaintest.WithContractReplay(func(context.Context) ([]chain.Entry, error) {
			var r0 []chain.Entry
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			chaintest.AssertContractReplayHonoursDeadline(tb, subject)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertContractReplayToleratesNilContextCanFail holds AssertContractReplayToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertContractReplayToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	subject := chaintest.NewContractStub(t,
		chaintest.WithContractReplay(func(context.Context) ([]chain.Entry, error) {
			panic("Contract.Replay: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			chaintest.AssertContractReplayToleratesNilContext(tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertContractVerifySmokeCanFail holds AssertContractVerifySmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertContractVerifySmokeCanFail(t *testing.T) {
	t.Parallel()

	subject := chaintest.NewContractStub(t,
		chaintest.WithContractVerify(func(context.Context) error {
			panic("Contract.Verify: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			chaintest.AssertContractVerifySmoke(tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertContractVerifyCancelsCanFail holds AssertContractVerifyCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertContractVerifyCancelsCanFail(t *testing.T) {
	t.Parallel()

	subject := chaintest.NewContractStub(t,
		chaintest.WithContractVerify(func(context.Context) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			chaintest.AssertContractVerifyCancels(tb, subject)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertContractVerifyHonoursDeadlineCanFail holds AssertContractVerifyHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertContractVerifyHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	subject := chaintest.NewContractStub(t,
		chaintest.WithContractVerify(func(context.Context) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			chaintest.AssertContractVerifyHonoursDeadline(tb, subject)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertContractVerifyToleratesNilContextCanFail holds AssertContractVerifyToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertContractVerifyToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	subject := chaintest.NewContractStub(t,
		chaintest.WithContractVerify(func(context.Context) error {
			panic("Contract.Verify: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			chaintest.AssertContractVerifyToleratesNilContext(tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance 099e6d2f181d97ca587924ff541ec0e3ff1f242087096b4711a7a2a8b0c6a8dc
