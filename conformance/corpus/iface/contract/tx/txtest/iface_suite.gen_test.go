// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/contract/tx/iface.go
// Plugins:   golang 1.0.0, suite 1.7.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package txtest_test

import (
	"context"
	"errors"
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/iface/contract/tx"
	"go.thesmos.sh/testkit/conformance/corpus/iface/contract/tx/txtest"
)

// Every generated check for Contract, driven against a stand-in built
// to violate it.
//
//	Proved:   21 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertContractBeginSmokeCanFail holds AssertContractBeginSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertContractBeginSmokeCanFail(t *testing.T) {
	t.Parallel()

	subject := txtest.NewContractStub(t,
		txtest.WithContractBegin(func(context.Context) (tx.Tx, error) {
			panic("Contract.Begin: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			txtest.AssertContractBeginSmoke(tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertContractBeginCancelsCanFail holds AssertContractBeginCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertContractBeginCancelsCanFail(t *testing.T) {
	t.Parallel()

	subject := txtest.NewContractStub(t,
		txtest.WithContractBegin(func(context.Context) (tx.Tx, error) {
			var r0 tx.Tx
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			txtest.AssertContractBeginCancels(tb, subject)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertContractBeginHonoursDeadlineCanFail holds AssertContractBeginHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertContractBeginHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	subject := txtest.NewContractStub(t,
		txtest.WithContractBegin(func(context.Context) (tx.Tx, error) {
			var r0 tx.Tx
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			txtest.AssertContractBeginHonoursDeadline(tb, subject)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertContractBeginToleratesNilContextCanFail holds AssertContractBeginToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertContractBeginToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	subject := txtest.NewContractStub(t,
		txtest.WithContractBegin(func(context.Context) (tx.Tx, error) {
			panic("Contract.Begin: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			txtest.AssertContractBeginToleratesNilContext(tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertContractCommitSmokeCanFail holds AssertContractCommitSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertContractCommitSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := txtest.DefaultContractFixture()
	subject := txtest.NewContractStub(t,
		txtest.WithContractCommit(func(context.Context, tx.Tx) error {
			panic("Contract.Commit: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			txtest.AssertContractCommitSmoke(tb, subject, fixture.Tx)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertContractCommitCancelsCanFail holds AssertContractCommitCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertContractCommitCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := txtest.DefaultContractFixture()
	subject := txtest.NewContractStub(t,
		txtest.WithContractCommit(func(context.Context, tx.Tx) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			txtest.AssertContractCommitCancels(tb, subject, fixture.Tx)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertContractCommitHonoursDeadlineCanFail holds AssertContractCommitHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertContractCommitHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := txtest.DefaultContractFixture()
	subject := txtest.NewContractStub(t,
		txtest.WithContractCommit(func(context.Context, tx.Tx) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			txtest.AssertContractCommitHonoursDeadline(tb, subject, fixture.Tx)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertContractCommitToleratesNilContextCanFail holds AssertContractCommitToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertContractCommitToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := txtest.DefaultContractFixture()
	subject := txtest.NewContractStub(t,
		txtest.WithContractCommit(func(context.Context, tx.Tx) error {
			panic("Contract.Commit: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			txtest.AssertContractCommitToleratesNilContext(tb, subject, fixture.Tx)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertContractRollbackSmokeCanFail holds AssertContractRollbackSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertContractRollbackSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := txtest.DefaultContractFixture()
	subject := txtest.NewContractStub(t,
		txtest.WithContractRollback(func(context.Context, tx.Tx) error {
			panic("Contract.Rollback: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			txtest.AssertContractRollbackSmoke(tb, subject, fixture.Tx)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertContractRollbackCancelsCanFail holds AssertContractRollbackCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertContractRollbackCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := txtest.DefaultContractFixture()
	subject := txtest.NewContractStub(t,
		txtest.WithContractRollback(func(context.Context, tx.Tx) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			txtest.AssertContractRollbackCancels(tb, subject, fixture.Tx)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertContractRollbackHonoursDeadlineCanFail holds AssertContractRollbackHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertContractRollbackHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := txtest.DefaultContractFixture()
	subject := txtest.NewContractStub(t,
		txtest.WithContractRollback(func(context.Context, tx.Tx) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			txtest.AssertContractRollbackHonoursDeadline(tb, subject, fixture.Tx)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertContractRollbackToleratesNilContextCanFail holds AssertContractRollbackToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertContractRollbackToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := txtest.DefaultContractFixture()
	subject := txtest.NewContractStub(t,
		txtest.WithContractRollback(func(context.Context, tx.Tx) error {
			panic("Contract.Rollback: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			txtest.AssertContractRollbackToleratesNilContext(tb, subject, fixture.Tx)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertContractPutSmokeCanFail holds AssertContractPutSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertContractPutSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := txtest.DefaultContractFixture()
	subject := txtest.NewContractStub(t,
		txtest.WithContractPut(func(context.Context, tx.Tx, string, tx.Value) error {
			panic("Contract.Put: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			txtest.AssertContractPutSmoke(tb, subject, fixture.Tx, fixture.Key, fixture.V)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertContractPutCancelsCanFail holds AssertContractPutCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertContractPutCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := txtest.DefaultContractFixture()
	subject := txtest.NewContractStub(t,
		txtest.WithContractPut(func(context.Context, tx.Tx, string, tx.Value) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			txtest.AssertContractPutCancels(tb, subject, fixture.Tx, fixture.Key, fixture.V)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertContractPutHonoursDeadlineCanFail holds AssertContractPutHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertContractPutHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := txtest.DefaultContractFixture()
	subject := txtest.NewContractStub(t,
		txtest.WithContractPut(func(context.Context, tx.Tx, string, tx.Value) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			txtest.AssertContractPutHonoursDeadline(tb, subject, fixture.Tx, fixture.Key, fixture.V)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertContractPutToleratesNilContextCanFail holds AssertContractPutToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertContractPutToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := txtest.DefaultContractFixture()
	subject := txtest.NewContractStub(t,
		txtest.WithContractPut(func(context.Context, tx.Tx, string, tx.Value) error {
			panic("Contract.Put: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			txtest.AssertContractPutToleratesNilContext(tb, subject, fixture.Tx, fixture.Key, fixture.V)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertContractGetSmokeCanFail holds AssertContractGetSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertContractGetSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := txtest.DefaultContractFixture()
	subject := txtest.NewContractStub(t,
		txtest.WithContractGet(func(context.Context, string) (tx.Value, error) {
			panic("Contract.Get: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			txtest.AssertContractGetSmoke(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertContractGetCancelsCanFail holds AssertContractGetCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertContractGetCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := txtest.DefaultContractFixture()
	subject := txtest.NewContractStub(t,
		txtest.WithContractGet(func(context.Context, string) (tx.Value, error) {
			var r0 tx.Value
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			txtest.AssertContractGetCancels(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertContractGetHonoursDeadlineCanFail holds AssertContractGetHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertContractGetHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := txtest.DefaultContractFixture()
	subject := txtest.NewContractStub(t,
		txtest.WithContractGet(func(context.Context, string) (tx.Value, error) {
			var r0 tx.Value
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			txtest.AssertContractGetHonoursDeadline(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertContractGetToleratesNilContextCanFail holds AssertContractGetToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertContractGetToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := txtest.DefaultContractFixture()
	subject := txtest.NewContractStub(t,
		txtest.WithContractGet(func(context.Context, string) (tx.Value, error) {
			panic("Contract.Get: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			txtest.AssertContractGetToleratesNilContext(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertContractGetZeroOnErrorCanFail holds AssertContractGetZeroOnError to being able to fail.
//
// The stand-in refuses the call and answers a value that looks like a result
// anyway. A check that let that pass would leave a caller who checks the error
// and a caller who checks the value disagreeing about what happened — the
// claim's own violation, not a stand-in that merely succeeds.
func TestAssertContractGetZeroOnErrorCanFail(t *testing.T) {
	t.Parallel()

	fixture := txtest.DefaultContractFixture()
	subject := txtest.NewContractStub(t,
		txtest.WithContractGet(func(context.Context, string) (tx.Value, error) {
			return tx.Value{Key: "test-r0"}, errors.New("violate: refused with a believable value")
		}))

	got := testkit.Rejects(t, "a method answering a believable value beside its error",
		func(tb testing.TB) {
			tb.Helper()
			txtest.AssertContractGetZeroOnError(tb, subject, fixture.KeyOther)
		})
	testkit.Assert(t, got).Contains("must return the zero value",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance bec0e63d3c686501a18fe2054561e75d6ce04b88044326b6934307d520dab8de
