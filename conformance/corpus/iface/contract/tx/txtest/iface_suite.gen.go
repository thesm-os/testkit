// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/contract/tx/iface.go
// Plugins:   golang 1.0.0, suite 1.9.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package txtest

import (
	"context"
	"slices"
	"testing"
	"time"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/conformance/corpus/iface/contract/tx"
)

// ContractBeginCheck is one assertion about Begin.
//
// Every generated check for Begin is a value of it, and so is one you
// write — so they compose, reorder, and each runs standalone. testing.TB rather
// than *testing.T is what lets a stand-in drive it and prove it can fail.
//
// Begin returns a value beside its error and takes nothing to vary, so no
// "an error carries the zero value" check is generated for it. That check reaches
// the failure it is about by choosing an input that misses, and this signature
// offers none — a generated one could only demand failure from a correct
// implementation. Write it here against a subject this package can break.
type ContractBeginCheck func(tb testing.TB, subject tx.Contract)

// ContractCommitCheck is one assertion about Commit.
//
// Every generated check for Commit is a value of it, and so is one you
// write — so they compose, reorder, and each runs standalone. testing.TB rather
// than *testing.T is what lets a stand-in drive it and prove it can fail.
type ContractCommitCheck func(tb testing.TB, subject tx.Contract, tx tx.Tx)

// ContractRollbackCheck is one assertion about Rollback.
//
// Every generated check for Rollback is a value of it, and so is one you
// write — so they compose, reorder, and each runs standalone. testing.TB rather
// than *testing.T is what lets a stand-in drive it and prove it can fail.
type ContractRollbackCheck func(tb testing.TB, subject tx.Contract, tx tx.Tx)

// ContractPutCheck is one assertion about Put.
//
// Every generated check for Put is a value of it, and so is one you
// write — so they compose, reorder, and each runs standalone. testing.TB rather
// than *testing.T is what lets a stand-in drive it and prove it can fail.
type ContractPutCheck func(tb testing.TB, subject tx.Contract, tx tx.Tx, key string, v tx.Value)

// ContractGetCheck is one assertion about Get.
//
// Every generated check for Get is a value of it, and so is one you
// write — so they compose, reorder, and each runs standalone. testing.TB rather
// than *testing.T is what lets a stand-in drive it and prove it can fail.
type ContractGetCheck func(tb testing.TB, subject tx.Contract, key string)

// ContractFixture holds every input the generated checks run against.
//
// Derived from each method's parameter names and types. Every field has a
// companion holding a second, different value: a check comparing a result
// against a single input passes whenever the subject happened to be seeded with
// it, and a miss check whose key happens to hit asserts nothing.
//
// A field whose type admits no literal — a func, a channel, a type from a
// package this run never read — is declared and left at its zero value, and
// every generated check that needed it was dropped rather than emitted against
// a value nobody could write. Supply one through ContractWithFixture
// and the check a consumer writes has something to use.
type ContractFixture struct {
	Tx       tx.Tx
	TxOther  tx.Tx
	Key      string
	KeyOther string
	V        tx.Value
	VOther   tx.Value
}

// DefaultContractFixture is what this run derived. Replace any field through
// ContractWithFixture; the rest keep these values.
func DefaultContractFixture() ContractFixture {
	return ContractFixture{
		Tx:       tx.Tx{ID: 1},
		TxOther:  tx.Tx{ID: 2},
		Key:      "test-key",
		KeyOther: "other-key",
		V:        tx.Value{Key: "test-key", Body: "test-body"},
		VOther:   tx.Value{Key: "other-key", Body: "other-body"},
	}
}

// AssertContractContract runs every generated check against every declared subject.
//
//	Checks:   21 across 5 methods, per subject
//	Subjects: declare each with ContractSubject
//	Double:   every subject runs a second time wrapped in ContractStub, so
//	          anything the wrapper fails that the subject passes is the double
//	          lying. ContractWithoutDouble declines it.
//	Extend:   ContractOnBegin, ContractOnCommit, ContractOnRollback, ContractOnPut, ContractOnGet
//	Drop:     ContractWithout, by the path each check reports under
//
// # What is checked somewhere else
//
// These need a reference implementation to compare against, which a suite run
// has no way to build. Nothing here asserts them and nothing here should:
//
//   - aggregator, on Begin
//   - tx, on Begin, Commit, Rollback
//   - writer, on Commit, Rollback
//   - multiargwriter, on Put
//
// //testkit:model on the interface derives that reference, and the
// ContractModel option it generates runs them here. Its own file
// names the path it reports under and the ContractWithout call that
// declines it — this header said "model" for every fixture, and a twinned
// reference registers under "model/twin", so it was wrong wherever the
// oracle could not be derived.
func AssertContractContract(t *testing.T, opts ...ContractOption) {
	t.Helper()
	cfg := newContractConfig(opts...)
	if len(cfg.subjects) == 0 {
		t.Fatalf("AssertContractContract was given no subject; declare one with " +
			"ContractSubject(name, factory)")
	}
	for _, p := range cfg.phantoms() {
		t.Errorf("ContractWithout(%q) declines nothing: no check here reports "+
			"under that path, so this suite asserts one thing more than its "+
			"author believes", p)
	}
	for _, s := range cfg.subjects {
		runContractChecks(t, cfg, s.name, s.factory, nil)
		if !cfg.withoutDouble {
			runContractChecks(t, cfg, s.name+" through the double", s.factory,
				func(tb testing.TB, subject tx.Contract) tx.Contract {
					return NewContractStub(tb, ContractStubDelegateTo(subject))
				})
		}
	}
}

// runContractChecks runs the whole contract against one subject.
//
// wrap is applied after the subject is built and seeded, so a wrapper is
// transparent to the seed and every failure it causes is its own.
func runContractChecks(
	t *testing.T,
	cfg *contractConfig,
	label string,
	factory func() tx.Contract,
	wrap func(tb testing.TB, subject tx.Contract) tx.Contract,
) {
	t.Helper()
	t.Run(label, func(t *testing.T) {
		t.Parallel()

		t.Run("Begin", func(t *testing.T) {
			t.Parallel()
			cfg.run(t, "Begin/smoke", "smoke", func(tb testing.TB) {
				AssertContractBeginSmoke(tb, cfg.subject(tb, factory, wrap))
			})
			cfg.run(t, "Begin/reports a cancelled context", "reports a cancelled context", func(tb testing.TB) {
				AssertContractBeginCancels(tb, cfg.subject(tb, factory, wrap))
			})
			cfg.run(t, "Begin/reports an expired deadline", "reports an expired deadline", func(tb testing.TB) {
				AssertContractBeginHonoursDeadline(tb, cfg.subject(tb, factory, wrap))
			})
			cfg.run(t, "Begin/tolerates a nil context", "tolerates a nil context", func(tb testing.TB) {
				AssertContractBeginToleratesNilContext(tb, cfg.subject(tb, factory, wrap))
			})
			for _, c := range cfg.onBegin {
				cfg.run(t, "Begin"+"/"+c.name, c.name, func(tb testing.TB) {
					c.fn(tb, cfg.subject(tb, factory, wrap))
				})
			}
		})

		t.Run("Commit", func(t *testing.T) {
			t.Parallel()
			cfg.run(t, "Commit/smoke", "smoke", func(tb testing.TB) {
				AssertContractCommitSmoke(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.Tx)
			})
			cfg.run(t, "Commit/reports a cancelled context", "reports a cancelled context", func(tb testing.TB) {
				AssertContractCommitCancels(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.Tx)
			})
			cfg.run(t, "Commit/reports an expired deadline", "reports an expired deadline", func(tb testing.TB) {
				AssertContractCommitHonoursDeadline(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.Tx)
			})
			cfg.run(t, "Commit/tolerates a nil context", "tolerates a nil context", func(tb testing.TB) {
				AssertContractCommitToleratesNilContext(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.Tx)
			})
			for _, c := range cfg.onCommit {
				cfg.run(t, "Commit"+"/"+c.name, c.name, func(tb testing.TB) {
					c.fn(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.Tx)
				})
			}
		})

		t.Run("Rollback", func(t *testing.T) {
			t.Parallel()
			cfg.run(t, "Rollback/smoke", "smoke", func(tb testing.TB) {
				AssertContractRollbackSmoke(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.Tx)
			})
			cfg.run(t, "Rollback/reports a cancelled context", "reports a cancelled context", func(tb testing.TB) {
				AssertContractRollbackCancels(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.Tx)
			})
			cfg.run(t, "Rollback/reports an expired deadline", "reports an expired deadline", func(tb testing.TB) {
				AssertContractRollbackHonoursDeadline(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.Tx)
			})
			cfg.run(t, "Rollback/tolerates a nil context", "tolerates a nil context", func(tb testing.TB) {
				AssertContractRollbackToleratesNilContext(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.Tx)
			})
			for _, c := range cfg.onRollback {
				cfg.run(t, "Rollback"+"/"+c.name, c.name, func(tb testing.TB) {
					c.fn(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.Tx)
				})
			}
		})

		t.Run("Put", func(t *testing.T) {
			t.Parallel()
			cfg.run(t, "Put/smoke", "smoke", func(tb testing.TB) {
				AssertContractPutSmoke(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.Tx, cfg.Fixture.Key, cfg.Fixture.V)
			})
			cfg.run(t, "Put/reports a cancelled context", "reports a cancelled context", func(tb testing.TB) {
				AssertContractPutCancels(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.Tx, cfg.Fixture.Key, cfg.Fixture.V)
			})
			cfg.run(t, "Put/reports an expired deadline", "reports an expired deadline", func(tb testing.TB) {
				AssertContractPutHonoursDeadline(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.Tx, cfg.Fixture.Key, cfg.Fixture.V)
			})
			cfg.run(t, "Put/tolerates a nil context", "tolerates a nil context", func(tb testing.TB) {
				AssertContractPutToleratesNilContext(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.Tx, cfg.Fixture.Key, cfg.Fixture.V)
			})
			for _, c := range cfg.onPut {
				cfg.run(t, "Put"+"/"+c.name, c.name, func(tb testing.TB) {
					c.fn(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.Tx, cfg.Fixture.Key, cfg.Fixture.V)
				})
			}
		})

		t.Run("Get", func(t *testing.T) {
			t.Parallel()
			cfg.run(t, "Get/smoke", "smoke", func(tb testing.TB) {
				AssertContractGetSmoke(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.Key)
			})
			cfg.run(t, "Get/reports a cancelled context", "reports a cancelled context", func(tb testing.TB) {
				AssertContractGetCancels(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.Key)
			})
			cfg.run(t, "Get/reports an expired deadline", "reports an expired deadline", func(tb testing.TB) {
				AssertContractGetHonoursDeadline(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.Key)
			})
			cfg.run(t, "Get/tolerates a nil context", "tolerates a nil context", func(tb testing.TB) {
				AssertContractGetToleratesNilContext(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.Key)
			})
			cfg.run(t, "Get/an error carries the zero value", "an error carries the zero value", func(tb testing.TB) {
				AssertContractGetZeroOnError(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.KeyOther)
			})
			for _, c := range cfg.onGet {
				cfg.run(t, "Get"+"/"+c.name, c.name, func(tb testing.TB) {
					c.fn(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.Key)
				})
			}
		})

		// The extra bodies of checks this run was given, each reporting under its
		// own name and dropped by it.
		//
		// The plain subject only. The wrapped pass exists to prove the double
		// faithful and the per-method checks above already do that, so running one
		// through the wrapper prices the wrapper and says nothing new about the
		// subject.
		if wrap == nil {
			for _, e := range cfg.extensions {
				if cfg.declined(e.name) {
					t.Run(e.name, func(t *testing.T) {
						t.Skipf("dropped through ContractWithout(%q)", e.name)
					})
					continue
				}
				t.Run(e.name, func(t *testing.T) {
					t.Parallel()
					e.run(t, label, factory, cfg)
				})
			}
		}
	})
}

// AssertContractBeginSmoke asserts Begin survives a call with derived inputs.
//
// Fails when: Begin panics. The weakest check in this file and the one
// that catches the most — a method that panics on a derived value is one no
// other check here reaches.
func AssertContractBeginSmoke(tb testing.TB, subject tx.Contract) {
	tb.Helper()
	defer func() {
		if r := recover(); r != nil {
			tb.Fatalf("Begin panicked on a derived value (%v); supply one it "+
				"accepts through ContractWithFixture", r)
		}
	}()
	ctx := tb.Context()
	_, _ = subject.Begin(ctx)
}

// AssertContractBeginCancels asserts Begin reports a cancelled context as cancelled.
//
// Fails when: Begin returns nil for a context cancelled before the
// call, or returns an error that does not answer to context.Canceled. Both
// matter and they are different defects: the first is work done that the caller
// asked not to be, the second is a caller who wrote
// `errors.Is(err, context.Canceled)` and gets false for a call that was.
//
// The error rather than merely its presence is what separates this from the
// deadline check. Asserting only that something came back makes the two one
// check written twice.
func AssertContractBeginCancels(tb testing.TB, subject tx.Contract) {
	tb.Helper()
	ctx, cancel := context.WithCancel(tb.Context())
	cancel()
	_, err := subject.Begin(ctx)
	testkit.ErrorIs(tb, err, context.Canceled,
		"Begin must report a cancelled context as context.Canceled")
}

// AssertContractBeginHonoursDeadline asserts Begin reports an expired deadline as exceeded.
//
// Fails when: Begin returns nil for a context whose deadline has
// passed, or returns an error that does not answer to
// context.DeadlineExceeded. Distinct from cancellation in what the caller
// learns: a cancelled call was called off, an expired one ran out of time, and
// only the second is worth retrying.
//
// The deadline is the zero time, which is unconditionally in the past. No clock
// is read: a generated check that consults the wall clock is one whose subject
// is partly the machine it runs on.
func AssertContractBeginHonoursDeadline(tb testing.TB, subject tx.Contract) {
	tb.Helper()
	ctx, cancel := context.WithDeadline(tb.Context(), time.Time{})
	defer cancel()
	_, err := subject.Begin(ctx)
	testkit.ErrorIs(tb, err, context.DeadlineExceeded,
		"Begin must report an expired deadline as context.DeadlineExceeded")
}

// AssertContractBeginToleratesNilContext asserts Begin does not panic on a nil context.
//
// Fails when: Begin panics. Returning an error is correct and
// succeeding is correct — a nil context reaches production through a caller
// that forgot one, and a panic turns that into an outage rather than a failed
// request.
func AssertContractBeginToleratesNilContext(tb testing.TB, subject tx.Contract) {
	tb.Helper()
	defer func() {
		if r := recover(); r != nil {
			tb.Errorf("Begin panicked on a nil context (%v); return an error instead", r)
		}
	}()
	//nolint:staticcheck // passing nil is the check.
	var ctx context.Context
	_, _ = subject.Begin(ctx)
}

// AssertContractCommitSmoke asserts Commit survives a call with derived inputs.
//
// Fails when: Commit panics. The weakest check in this file and the one
// that catches the most — a method that panics on a derived value is one no
// other check here reaches.
func AssertContractCommitSmoke(tb testing.TB, subject tx.Contract, tx tx.Tx) {
	tb.Helper()
	defer func() {
		if r := recover(); r != nil {
			tb.Fatalf("Commit panicked on a derived value (%v); supply one it "+
				"accepts through ContractWithFixture", r)
		}
	}()
	ctx := tb.Context()
	_ = subject.Commit(ctx, tx)
}

// AssertContractCommitCancels asserts Commit reports a cancelled context as cancelled.
//
// Fails when: Commit returns nil for a context cancelled before the
// call, or returns an error that does not answer to context.Canceled. Both
// matter and they are different defects: the first is work done that the caller
// asked not to be, the second is a caller who wrote
// `errors.Is(err, context.Canceled)` and gets false for a call that was.
//
// The error rather than merely its presence is what separates this from the
// deadline check. Asserting only that something came back makes the two one
// check written twice.
func AssertContractCommitCancels(tb testing.TB, subject tx.Contract, tx tx.Tx) {
	tb.Helper()
	ctx, cancel := context.WithCancel(tb.Context())
	cancel()
	err := subject.Commit(ctx, tx)
	testkit.ErrorIs(tb, err, context.Canceled,
		"Commit must report a cancelled context as context.Canceled")
}

// AssertContractCommitHonoursDeadline asserts Commit reports an expired deadline as exceeded.
//
// Fails when: Commit returns nil for a context whose deadline has
// passed, or returns an error that does not answer to
// context.DeadlineExceeded. Distinct from cancellation in what the caller
// learns: a cancelled call was called off, an expired one ran out of time, and
// only the second is worth retrying.
//
// The deadline is the zero time, which is unconditionally in the past. No clock
// is read: a generated check that consults the wall clock is one whose subject
// is partly the machine it runs on.
func AssertContractCommitHonoursDeadline(tb testing.TB, subject tx.Contract, tx tx.Tx) {
	tb.Helper()
	ctx, cancel := context.WithDeadline(tb.Context(), time.Time{})
	defer cancel()
	err := subject.Commit(ctx, tx)
	testkit.ErrorIs(tb, err, context.DeadlineExceeded,
		"Commit must report an expired deadline as context.DeadlineExceeded")
}

// AssertContractCommitToleratesNilContext asserts Commit does not panic on a nil context.
//
// Fails when: Commit panics. Returning an error is correct and
// succeeding is correct — a nil context reaches production through a caller
// that forgot one, and a panic turns that into an outage rather than a failed
// request.
func AssertContractCommitToleratesNilContext(tb testing.TB, subject tx.Contract, tx tx.Tx) {
	tb.Helper()
	defer func() {
		if r := recover(); r != nil {
			tb.Errorf("Commit panicked on a nil context (%v); return an error instead", r)
		}
	}()
	//nolint:staticcheck // passing nil is the check.
	var ctx context.Context
	_ = subject.Commit(ctx, tx)
}

// AssertContractRollbackSmoke asserts Rollback survives a call with derived inputs.
//
// Fails when: Rollback panics. The weakest check in this file and the one
// that catches the most — a method that panics on a derived value is one no
// other check here reaches.
func AssertContractRollbackSmoke(tb testing.TB, subject tx.Contract, tx tx.Tx) {
	tb.Helper()
	defer func() {
		if r := recover(); r != nil {
			tb.Fatalf("Rollback panicked on a derived value (%v); supply one it "+
				"accepts through ContractWithFixture", r)
		}
	}()
	ctx := tb.Context()
	_ = subject.Rollback(ctx, tx)
}

// AssertContractRollbackCancels asserts Rollback reports a cancelled context as cancelled.
//
// Fails when: Rollback returns nil for a context cancelled before the
// call, or returns an error that does not answer to context.Canceled. Both
// matter and they are different defects: the first is work done that the caller
// asked not to be, the second is a caller who wrote
// `errors.Is(err, context.Canceled)` and gets false for a call that was.
//
// The error rather than merely its presence is what separates this from the
// deadline check. Asserting only that something came back makes the two one
// check written twice.
func AssertContractRollbackCancels(tb testing.TB, subject tx.Contract, tx tx.Tx) {
	tb.Helper()
	ctx, cancel := context.WithCancel(tb.Context())
	cancel()
	err := subject.Rollback(ctx, tx)
	testkit.ErrorIs(tb, err, context.Canceled,
		"Rollback must report a cancelled context as context.Canceled")
}

// AssertContractRollbackHonoursDeadline asserts Rollback reports an expired deadline as exceeded.
//
// Fails when: Rollback returns nil for a context whose deadline has
// passed, or returns an error that does not answer to
// context.DeadlineExceeded. Distinct from cancellation in what the caller
// learns: a cancelled call was called off, an expired one ran out of time, and
// only the second is worth retrying.
//
// The deadline is the zero time, which is unconditionally in the past. No clock
// is read: a generated check that consults the wall clock is one whose subject
// is partly the machine it runs on.
func AssertContractRollbackHonoursDeadline(tb testing.TB, subject tx.Contract, tx tx.Tx) {
	tb.Helper()
	ctx, cancel := context.WithDeadline(tb.Context(), time.Time{})
	defer cancel()
	err := subject.Rollback(ctx, tx)
	testkit.ErrorIs(tb, err, context.DeadlineExceeded,
		"Rollback must report an expired deadline as context.DeadlineExceeded")
}

// AssertContractRollbackToleratesNilContext asserts Rollback does not panic on a nil context.
//
// Fails when: Rollback panics. Returning an error is correct and
// succeeding is correct — a nil context reaches production through a caller
// that forgot one, and a panic turns that into an outage rather than a failed
// request.
func AssertContractRollbackToleratesNilContext(tb testing.TB, subject tx.Contract, tx tx.Tx) {
	tb.Helper()
	defer func() {
		if r := recover(); r != nil {
			tb.Errorf("Rollback panicked on a nil context (%v); return an error instead", r)
		}
	}()
	//nolint:staticcheck // passing nil is the check.
	var ctx context.Context
	_ = subject.Rollback(ctx, tx)
}

// AssertContractPutSmoke asserts Put survives a call with derived inputs.
//
// Fails when: Put panics. The weakest check in this file and the one
// that catches the most — a method that panics on a derived value is one no
// other check here reaches.
func AssertContractPutSmoke(tb testing.TB, subject tx.Contract, tx tx.Tx, key string, v tx.Value) {
	tb.Helper()
	defer func() {
		if r := recover(); r != nil {
			tb.Fatalf("Put panicked on a derived value (%v); supply one it "+
				"accepts through ContractWithFixture", r)
		}
	}()
	ctx := tb.Context()
	_ = subject.Put(ctx, tx, key, v)
}

// AssertContractPutCancels asserts Put reports a cancelled context as cancelled.
//
// Fails when: Put returns nil for a context cancelled before the
// call, or returns an error that does not answer to context.Canceled. Both
// matter and they are different defects: the first is work done that the caller
// asked not to be, the second is a caller who wrote
// `errors.Is(err, context.Canceled)` and gets false for a call that was.
//
// The error rather than merely its presence is what separates this from the
// deadline check. Asserting only that something came back makes the two one
// check written twice.
func AssertContractPutCancels(tb testing.TB, subject tx.Contract, tx tx.Tx, key string, v tx.Value) {
	tb.Helper()
	ctx, cancel := context.WithCancel(tb.Context())
	cancel()
	err := subject.Put(ctx, tx, key, v)
	testkit.ErrorIs(tb, err, context.Canceled,
		"Put must report a cancelled context as context.Canceled")
}

// AssertContractPutHonoursDeadline asserts Put reports an expired deadline as exceeded.
//
// Fails when: Put returns nil for a context whose deadline has
// passed, or returns an error that does not answer to
// context.DeadlineExceeded. Distinct from cancellation in what the caller
// learns: a cancelled call was called off, an expired one ran out of time, and
// only the second is worth retrying.
//
// The deadline is the zero time, which is unconditionally in the past. No clock
// is read: a generated check that consults the wall clock is one whose subject
// is partly the machine it runs on.
func AssertContractPutHonoursDeadline(tb testing.TB, subject tx.Contract, tx tx.Tx, key string, v tx.Value) {
	tb.Helper()
	ctx, cancel := context.WithDeadline(tb.Context(), time.Time{})
	defer cancel()
	err := subject.Put(ctx, tx, key, v)
	testkit.ErrorIs(tb, err, context.DeadlineExceeded,
		"Put must report an expired deadline as context.DeadlineExceeded")
}

// AssertContractPutToleratesNilContext asserts Put does not panic on a nil context.
//
// Fails when: Put panics. Returning an error is correct and
// succeeding is correct — a nil context reaches production through a caller
// that forgot one, and a panic turns that into an outage rather than a failed
// request.
func AssertContractPutToleratesNilContext(tb testing.TB, subject tx.Contract, tx tx.Tx, key string, v tx.Value) {
	tb.Helper()
	defer func() {
		if r := recover(); r != nil {
			tb.Errorf("Put panicked on a nil context (%v); return an error instead", r)
		}
	}()
	//nolint:staticcheck // passing nil is the check.
	var ctx context.Context
	_ = subject.Put(ctx, tx, key, v)
}

// AssertContractGetSmoke asserts Get survives a call with derived inputs.
//
// Fails when: Get panics. The weakest check in this file and the one
// that catches the most — a method that panics on a derived value is one no
// other check here reaches.
func AssertContractGetSmoke(tb testing.TB, subject tx.Contract, key string) {
	tb.Helper()
	defer func() {
		if r := recover(); r != nil {
			tb.Fatalf("Get panicked on a derived value (%v); supply one it "+
				"accepts through ContractWithFixture", r)
		}
	}()
	ctx := tb.Context()
	_, _ = subject.Get(ctx, key)
}

// AssertContractGetCancels asserts Get reports a cancelled context as cancelled.
//
// Fails when: Get returns nil for a context cancelled before the
// call, or returns an error that does not answer to context.Canceled. Both
// matter and they are different defects: the first is work done that the caller
// asked not to be, the second is a caller who wrote
// `errors.Is(err, context.Canceled)` and gets false for a call that was.
//
// The error rather than merely its presence is what separates this from the
// deadline check. Asserting only that something came back makes the two one
// check written twice.
func AssertContractGetCancels(tb testing.TB, subject tx.Contract, key string) {
	tb.Helper()
	ctx, cancel := context.WithCancel(tb.Context())
	cancel()
	_, err := subject.Get(ctx, key)
	testkit.ErrorIs(tb, err, context.Canceled,
		"Get must report a cancelled context as context.Canceled")
}

// AssertContractGetHonoursDeadline asserts Get reports an expired deadline as exceeded.
//
// Fails when: Get returns nil for a context whose deadline has
// passed, or returns an error that does not answer to
// context.DeadlineExceeded. Distinct from cancellation in what the caller
// learns: a cancelled call was called off, an expired one ran out of time, and
// only the second is worth retrying.
//
// The deadline is the zero time, which is unconditionally in the past. No clock
// is read: a generated check that consults the wall clock is one whose subject
// is partly the machine it runs on.
func AssertContractGetHonoursDeadline(tb testing.TB, subject tx.Contract, key string) {
	tb.Helper()
	ctx, cancel := context.WithDeadline(tb.Context(), time.Time{})
	defer cancel()
	_, err := subject.Get(ctx, key)
	testkit.ErrorIs(tb, err, context.DeadlineExceeded,
		"Get must report an expired deadline as context.DeadlineExceeded")
}

// AssertContractGetToleratesNilContext asserts Get does not panic on a nil context.
//
// Fails when: Get panics. Returning an error is correct and
// succeeding is correct — a nil context reaches production through a caller
// that forgot one, and a panic turns that into an outage rather than a failed
// request.
func AssertContractGetToleratesNilContext(tb testing.TB, subject tx.Contract, key string) {
	tb.Helper()
	defer func() {
		if r := recover(); r != nil {
			tb.Errorf("Get panicked on a nil context (%v); return an error instead", r)
		}
	}()
	//nolint:staticcheck // passing nil is the check.
	var ctx context.Context
	_, _ = subject.Get(ctx, key)
}

// AssertContractGetZeroOnError asserts an error is accompanied by the zero value.
//
// Fails when: Get returns a non-zero result alongside a non-nil
// error. A caller who checks the error and a caller who checks the value must
// not disagree about whether the call succeeded.
//
// Skips when the alternate input succeeds: a total method has no failure this
// run can reach, and a skip says so where a green line would claim a
// comparison that never happened.
func AssertContractGetZeroOnError(tb testing.TB, subject tx.Contract, key string) {
	tb.Helper()
	ctx := tb.Context()
	r0, r1 := subject.Get(ctx, key)
	if r1 == nil {
		tb.Skipf("Get accepted the alternate input; nothing this run " +
			"supplies makes it fail, so the zero-on-error claim goes unexercised")
		return
	}
	{
		var zero tx.Value
		testkit.Equal(tb, r0, zero,
			"Get must return the zero value alongside an error")
	}
}

// ContractOption configures a contract run.
type ContractOption func(*contractConfig)

// ContractSubject declares an implementation to hold to the contract.
//
// Repeat it for every implementation that claims to satisfy the interface — an
// in-memory one, the one backed by a database, the one behind a network. Every
// check and every option is stated once and runs against all of them, which is
// what a conformance suite is for.
func ContractSubject(name string, factory func() tx.Contract) ContractOption {
	return func(c *contractConfig) {
		c.subjects = append(c.subjects, namedContractSubject{name, factory})
	}
}

// ContractWithoutDouble declines the second run through ContractStub.
//
// The double run proves the stand-in is faithful, and costs a second pass over
// every check. Decline it where the double is not used.
func ContractWithoutDouble() ContractOption {
	return func(c *contractConfig) { c.withoutDouble = true }
}

// ContractWithClock supplies the clock every time-reading check measures on.
//
// Defaults to the real one, so an implementation that does not take a clock
// behaves as it would have. Supply a [clock.TestClock] — the same one the
// factory builds the subject with — and a budget becomes a claim about the time
// the implementation means to spend rather than about how loaded the machine
// was.
func ContractWithClock(c clock.Clock) ContractOption {
	return func(cfg *contractConfig) { cfg.clock = c }
}

// ContractWithFixture replaces the derived inputs.
func ContractWithFixture(f ContractFixture) ContractOption {
	return func(c *contractConfig) { c.Fixture = f }
}

// ContractSeed replaces the derived seed.
//
// For an interface with no writer to seed itself through, which is the one
// input a conformance run cannot derive: a reader over an empty subject
// asserts nothing.
//
// Returning an error rather than swallowing one. A seed that failed quietly
// leaves every check after it asserting nothing and reporting success.
func ContractSeed(fn func(ctx context.Context, subject tx.Contract) error) ContractOption {
	return func(c *contractConfig) {
		c.seed = fn
		c.seedIsDerived = false
	}
}

// ContractOnBegin adds a named check to Begin.
func ContractOnBegin(name string, fn ContractBeginCheck) ContractOption {
	return func(c *contractConfig) {
		c.onBegin = append(c.onBegin, namedContractBeginCheck{name, fn})
	}
}

// ContractOnCommit adds a named check to Commit.
func ContractOnCommit(name string, fn ContractCommitCheck) ContractOption {
	return func(c *contractConfig) {
		c.onCommit = append(c.onCommit, namedContractCommitCheck{name, fn})
	}
}

// ContractOnRollback adds a named check to Rollback.
func ContractOnRollback(name string, fn ContractRollbackCheck) ContractOption {
	return func(c *contractConfig) {
		c.onRollback = append(c.onRollback, namedContractRollbackCheck{name, fn})
	}
}

// ContractOnPut adds a named check to Put.
func ContractOnPut(name string, fn ContractPutCheck) ContractOption {
	return func(c *contractConfig) {
		c.onPut = append(c.onPut, namedContractPutCheck{name, fn})
	}
}

// ContractOnGet adds a named check to Get.
func ContractOnGet(name string, fn ContractGetCheck) ContractOption {
	return func(c *contractConfig) {
		c.onGet = append(c.onGet, namedContractGetCheck{name, fn})
	}
}

// ContractWithout drops generated checks by the path each reports under.
//
// For a subject that legitimately violates one. Without this the only recourse
// is to stop running the suite, which costs every other check in the file — so
// a dropped check is reported as skipped rather than silently omitted.
func ContractWithout(paths ...string) ContractOption {
	return func(c *contractConfig) {
		for _, p := range paths {
			c.without[p] = struct{}{}
		}
	}
}

type namedContractBeginCheck struct {
	name string
	fn   ContractBeginCheck
}

type namedContractCommitCheck struct {
	name string
	fn   ContractCommitCheck
}

type namedContractRollbackCheck struct {
	name string
	fn   ContractRollbackCheck
}

type namedContractPutCheck struct {
	name string
	fn   ContractPutCheck
}

type namedContractGetCheck struct {
	name string
	fn   ContractGetCheck
}

type namedContractSubject struct {
	name    string
	factory func() tx.Contract
}

// contractContractExtension is a body of checks a generated sibling adds through an option.
//
// A sibling declares the option and closes over whatever configures it, so
// everything that varies per run is typed and private to the file that owns it
// — this file understands only the name and the call. Nothing arrives except
// through an option, which is what keeps the set fixed before the first subtest
// starts.
type contractContractExtension struct {
	// name is the path this reports under, and the one ContractWithout drops
	// it by. Anything nested beneath it is the extension's own to name.
	name string

	// run takes the factory rather than a subject. One that drives sequences
	// builds a subject per iteration, and one that compares against a reference
	// needs the unseeded subject a factory returns — which
	// contractConfig.subject is not.
	//
	// Called once per subject, in parallel, sharing the config. Anything it
	// mutates has to be built inside the call: state captured when the option
	// was declared is state two subjects write at once.
	run func(
		t *testing.T,
		subject string,
		factory func() tx.Contract,
		cfg *contractConfig,
	)
}

type contractConfig struct {
	Fixture       ContractFixture
	subjects      []namedContractSubject
	withoutDouble bool
	clock         clock.Clock
	seed          func(ctx context.Context, subject tx.Contract) error
	// seedIsDerived is what lets a failed seed name the right culprit. The
	// derived seed and a consumer's are the same field, and the advice for one
	// is useless for the other.
	seedIsDerived bool
	without       map[string]struct{}
	extensions    []contractContractExtension
	onBegin       []namedContractBeginCheck
	onCommit      []namedContractCommitCheck
	onRollback    []namedContractRollbackCheck
	onPut         []namedContractPutCheck
	onGet         []namedContractGetCheck
}

func newContractConfig(opts ...ContractOption) *contractConfig {
	c := &contractConfig{
		Fixture: DefaultContractFixture(),
		clock:   clock.RealClock(),
		without: map[string]struct{}{},
	}
	// Derived: Commit is classified writer, so the interface
	// populates itself and a reader has something to read.
	c.seed = func(ctx context.Context, subject tx.Contract) error {
		return subject.Commit(ctx, c.Fixture.Tx)
	}
	c.seedIsDerived = true
	for _, o := range opts {
		o(c)
	}
	return c
}

// subject builds a fresh implementation and seeds it, so every check runs
// against state no other check touched.
func (c *contractConfig) subject(
	tb testing.TB,
	factory func() tx.Contract,
	wrap func(tb testing.TB, subject tx.Contract) tx.Contract,
) tx.Contract {
	tb.Helper()
	s := factory()
	if c.seed != nil {
		// Reported rather than ignored. A seed that silently failed would leave
		// every check after it running against an empty subject — passing, and
		// asserting nothing.
		//
		// Which seed ran decides what the message can usefully say. The derived
		// one fails for one of two reasons and both are fixable from here, so it
		// names them. A seed the caller supplied is theirs, and telling them to
		// supply one is advice they already took.
		if err := c.seed(tb.Context(), s); err != nil {
			if c.seedIsDerived {
				tb.Fatalf("seeding through Commit failed: %v; "+
					"supply a value it accepts through ContractWithFixture, or a "+
					"whole seed through ContractSeed", err)
			} else {
				tb.Fatalf("the seed supplied through ContractSeed failed: %v", err)
			}
		}
	}
	if wrap != nil {
		s = wrap(tb, s)
	}
	return s
}

// dropped reports whether a path was declined through ContractWithout.
//
// Named rather than inlined because the drop set is read from two places: here,
// and by whatever registered through the seam — which reports under paths this
// file has never heard of and must answer the same question about them.
func (c *contractConfig) declined(path string) bool {
	_, ok := c.without[path]
	return ok
}

// contractPaths is every path this file reports a check under.
//
// The set a ContractWithout argument has to be a member of. Declared
// rather than observed: a check that did not run is not a check that does not
// exist — an integration-only method skips without ever consulting the drop
// set, and reading that as a phantom would fail a correct suite on a machine
// with the environment unset.
//
//nolint:gochecknoglobals // the emitted check paths, read-only after init.
var contractPaths = map[string]struct{}{
	"Begin/smoke":                          {},
	"Begin/reports a cancelled context":    {},
	"Begin/reports an expired deadline":    {},
	"Begin/tolerates a nil context":        {},
	"Commit/smoke":                         {},
	"Commit/reports a cancelled context":   {},
	"Commit/reports an expired deadline":   {},
	"Commit/tolerates a nil context":       {},
	"Rollback/smoke":                       {},
	"Rollback/reports a cancelled context": {},
	"Rollback/reports an expired deadline": {},
	"Rollback/tolerates a nil context":     {},
	"Put/smoke":                            {},
	"Put/reports a cancelled context":      {},
	"Put/reports an expired deadline":      {},
	"Put/tolerates a nil context":          {},
	"Get/smoke":                            {},
	"Get/reports a cancelled context":      {},
	"Get/reports an expired deadline":      {},
	"Get/tolerates a nil context":          {},
	"Get/an error carries the zero value":  {},
}

// phantoms names every ContractWithout path that declines nothing.
//
// A drop that drops nothing is this option's silent half: the consumer wrote
// it against a check since renamed, or never emitted for that method at all,
// and every run after reads green while asserting one thing more than its
// author believes. Extensions carry their own names — the seam reports under
// paths this file has never heard of — so they count as known too.
func (c *contractConfig) phantoms() []string {
	known := func(p string) bool {
		if _, ok := contractPaths[p]; ok {
			return true
		}
		for _, e := range c.extensions {
			if e.name == p {
				return true
			}
		}
		for _, on := range c.onBegin {
			if "Begin"+"/"+on.name == p {
				return true
			}
		}
		for _, on := range c.onCommit {
			if "Commit"+"/"+on.name == p {
				return true
			}
		}
		for _, on := range c.onRollback {
			if "Rollback"+"/"+on.name == p {
				return true
			}
		}
		for _, on := range c.onPut {
			if "Put"+"/"+on.name == p {
				return true
			}
		}
		for _, on := range c.onGet {
			if "Get"+"/"+on.name == p {
				return true
			}
		}
		return false
	}
	var out []string
	for p := range c.without {
		if !known(p) {
			out = append(out, p)
		}
	}
	slices.Sort(out)
	return out
}

// run executes one check unless it was dropped, reporting the drop rather than
// silently omitting it.
func (c *contractConfig) run(t *testing.T, path, name string, fn func(tb testing.TB)) {
	t.Helper()
	if c.declined(path) {
		t.Run(name, func(t *testing.T) {
			t.Skipf("dropped through ContractWithout(%q)", path)
		})
		return
	}
	t.Run(name, func(t *testing.T) {
		t.Parallel()
		fn(t)
	})
}

// testkit: end of generated content.
// testkit:provenance 0b9dd4101d7fe270f14cdc2c9a050e65d89e7ae8f3e43e92e1ee23bef2edf057
