// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/contract/singleflight/iface.go
// Plugins:   golang 1.0.0, suite 1.15.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package singleflighttest_test

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/iface/contract/singleflight/singleflighttest"
)

// Every generated check for Contract, driven against a stand-in built
// to violate it.
//
//	Proved:   8 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertContractRunSmokeCanFail holds AssertContractRunSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertContractRunSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := singleflighttest.DefaultContractFixture()
	subject := singleflighttest.NewContractStub(t,
		singleflighttest.WithContractRun(func(context.Context, string, func() string) (string, error) {
			panic("Contract.Run: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			singleflighttest.AssertContractRunSmoke(tb, subject, fixture.Key, fixture.Compute)
		})
	testkit.Assert(t, got).Contains("Run panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertContractRunCancelsCanFail holds AssertContractRunCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertContractRunCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := singleflighttest.DefaultContractFixture()
	subject := singleflighttest.NewContractStub(t,
		singleflighttest.WithContractRun(func(context.Context, string, func() string) (string, error) {
			var r0 string
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			singleflighttest.AssertContractRunCancels(tb, subject, fixture.Key, fixture.Compute)
		})
	testkit.Assert(t, got).Contains("Run must report a cancelled context as context.Canceled",
		"and rejects it for the reason the check is about")
}

// TestAssertContractRunHonoursDeadlineCanFail holds AssertContractRunHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertContractRunHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := singleflighttest.DefaultContractFixture()
	subject := singleflighttest.NewContractStub(t,
		singleflighttest.WithContractRun(func(context.Context, string, func() string) (string, error) {
			var r0 string
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			singleflighttest.AssertContractRunHonoursDeadline(tb, subject, fixture.Key, fixture.Compute)
		})
	testkit.Assert(t, got).Contains("Run must report an expired deadline as context.DeadlineExceeded",
		"and rejects it for the reason the check is about")
}

// TestAssertContractRunToleratesNilContextCanFail holds AssertContractRunToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertContractRunToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := singleflighttest.DefaultContractFixture()
	subject := singleflighttest.NewContractStub(t,
		singleflighttest.WithContractRun(func(context.Context, string, func() string) (string, error) {
			panic("Contract.Run: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			singleflighttest.AssertContractRunToleratesNilContext(tb, subject, fixture.Key, fixture.Compute)
		})
	testkit.Assert(t, got).Contains("Run panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertContractFlightsSmokeCanFail holds AssertContractFlightsSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertContractFlightsSmokeCanFail(t *testing.T) {
	t.Parallel()

	subject := singleflighttest.NewContractStub(t,
		singleflighttest.WithContractFlights(func(context.Context) (int, error) {
			panic("Contract.Flights: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			singleflighttest.AssertContractFlightsSmoke(tb, subject)
		})
	testkit.Assert(t, got).Contains("Flights panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertContractFlightsCancelsCanFail holds AssertContractFlightsCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertContractFlightsCancelsCanFail(t *testing.T) {
	t.Parallel()

	subject := singleflighttest.NewContractStub(t,
		singleflighttest.WithContractFlights(func(context.Context) (int, error) {
			var r0 int
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			singleflighttest.AssertContractFlightsCancels(tb, subject)
		})
	testkit.Assert(t, got).Contains("Flights must report a cancelled context as context.Canceled",
		"and rejects it for the reason the check is about")
}

// TestAssertContractFlightsHonoursDeadlineCanFail holds AssertContractFlightsHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertContractFlightsHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	subject := singleflighttest.NewContractStub(t,
		singleflighttest.WithContractFlights(func(context.Context) (int, error) {
			var r0 int
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			singleflighttest.AssertContractFlightsHonoursDeadline(tb, subject)
		})
	testkit.Assert(t, got).Contains("Flights must report an expired deadline as context.DeadlineExceeded",
		"and rejects it for the reason the check is about")
}

// TestAssertContractFlightsToleratesNilContextCanFail holds AssertContractFlightsToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertContractFlightsToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	subject := singleflighttest.NewContractStub(t,
		singleflighttest.WithContractFlights(func(context.Context) (int, error) {
			panic("Contract.Flights: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			singleflighttest.AssertContractFlightsToleratesNilContext(tb, subject)
		})
	testkit.Assert(t, got).Contains("Flights panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance 83be3c60d7ff6d56169575a94331451e52b42888ec380d1417f3f498b8093b07
