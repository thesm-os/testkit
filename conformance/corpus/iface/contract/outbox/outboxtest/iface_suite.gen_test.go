// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/contract/outbox/iface.go
// Plugins:   golang 1.0.0, suite 1.0.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/iface/contract/outbox/...

package outboxtest_test

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/conformance/corpus/iface/contract/outbox"
	"go.thesmos.sh/testkit/conformance/corpus/iface/contract/outbox/outboxtest"
)

// Every generated check for Contract, driven against a stand-in built
// to violate it.
//
//	Proved:   9 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertContractAppendSmokeCanFail holds AssertContractAppendSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertContractAppendSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := outboxtest.DefaultContractFixture()
	subject := outboxtest.NewContractStub(t,
		outboxtest.WithContractAppend(func(context.Context, outbox.Value) error {
			panic("Contract.Append: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			outboxtest.AssertContractAppendSmoke(tb, subject, fixture.V)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertContractAppendCancelsCanFail holds AssertContractAppendCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertContractAppendCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := outboxtest.DefaultContractFixture()
	subject := outboxtest.NewContractStub(t,
		outboxtest.WithContractAppend(func(context.Context, outbox.Value) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			outboxtest.AssertContractAppendCancels(tb, subject, fixture.V)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertContractAppendHonoursDeadlineCanFail holds AssertContractAppendHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertContractAppendHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := outboxtest.DefaultContractFixture()
	subject := outboxtest.NewContractStub(t,
		outboxtest.WithContractAppend(func(context.Context, outbox.Value) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			outboxtest.AssertContractAppendHonoursDeadline(tb, subject, fixture.V)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertContractAppendToleratesNilContextCanFail holds AssertContractAppendToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertContractAppendToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := outboxtest.DefaultContractFixture()
	subject := outboxtest.NewContractStub(t,
		outboxtest.WithContractAppend(func(context.Context, outbox.Value) error {
			panic("Contract.Append: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			outboxtest.AssertContractAppendToleratesNilContext(tb, subject, fixture.V)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertContractAppendReachesASubscriberCanFail holds AssertContractAppendReachesASubscriber to being able to fail.
//
// The stand-in accepts every record and delivers none. A check that let that
// pass would report durability for a subject that drops what nobody was
// waiting for — which is the one thing separating this contract from
// `publisher`, and which every other check in the harness is blind to.
func TestAssertContractAppendReachesASubscriberCanFail(t *testing.T) {
	t.Parallel()

	fixture := outboxtest.DefaultContractFixture()
	// Open and empty. Closing it would make the receive return at once, which
	// the check would read as a delivered zero rather than as nothing arriving.
	stream := make(chan outbox.Value)

	subject := outboxtest.NewContractStub(t,
		outboxtest.WithContractAppend(func(context.Context, outbox.Value) error {
			return nil
		}),
		outboxtest.WithContractSubscribe(func(context.Context) (<-chan outbox.Value, error) {
			return stream, nil
		}))

	got := testkit.Rejects(t, "an outbox that accepts every record and delivers none",
		func(tb testing.TB) {
			tb.Helper()
			outboxtest.AssertContractAppendReachesASubscriber(tb, clock.RealClock(), subject, fixture.V)
		})
	testkit.Assert(t, got).Contains("never arrived",
		"and rejects it for the reason the check is about")
}

// TestAssertContractSubscribeSmokeCanFail holds AssertContractSubscribeSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertContractSubscribeSmokeCanFail(t *testing.T) {
	t.Parallel()

	subject := outboxtest.NewContractStub(t,
		outboxtest.WithContractSubscribe(func(context.Context) (<-chan outbox.Value, error) {
			panic("Contract.Subscribe: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			outboxtest.AssertContractSubscribeSmoke(tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertContractSubscribeCancelsCanFail holds AssertContractSubscribeCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertContractSubscribeCancelsCanFail(t *testing.T) {
	t.Parallel()

	subject := outboxtest.NewContractStub(t,
		outboxtest.WithContractSubscribe(func(context.Context) (<-chan outbox.Value, error) {
			var r0 <-chan outbox.Value
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			outboxtest.AssertContractSubscribeCancels(tb, subject)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertContractSubscribeHonoursDeadlineCanFail holds AssertContractSubscribeHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertContractSubscribeHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	subject := outboxtest.NewContractStub(t,
		outboxtest.WithContractSubscribe(func(context.Context) (<-chan outbox.Value, error) {
			var r0 <-chan outbox.Value
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			outboxtest.AssertContractSubscribeHonoursDeadline(tb, subject)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertContractSubscribeToleratesNilContextCanFail holds AssertContractSubscribeToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertContractSubscribeToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	subject := outboxtest.NewContractStub(t,
		outboxtest.WithContractSubscribe(func(context.Context) (<-chan outbox.Value, error) {
			panic("Contract.Subscribe: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			outboxtest.AssertContractSubscribeToleratesNilContext(tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance e72543a311d037d751610b88b8ee1208a5b6d794b61ff3d109d403599a08bffd
