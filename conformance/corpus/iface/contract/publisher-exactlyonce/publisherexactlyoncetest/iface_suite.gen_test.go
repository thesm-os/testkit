// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/contract/publisher-exactlyonce/iface.go
// Plugins:   golang 1.0.0, suite 1.3.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package publisherexactlyoncetest_test

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit"
	publisherexactlyonce "go.thesmos.sh/testkit/conformance/corpus/iface/contract/publisher-exactlyonce"
	"go.thesmos.sh/testkit/conformance/corpus/iface/contract/publisher-exactlyonce/publisherexactlyoncetest"
)

// Every generated check for Contract, driven against a stand-in built
// to violate it.
//
//	Proved:   8 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertContractPublishSmokeCanFail holds AssertContractPublishSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertContractPublishSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := publisherexactlyoncetest.DefaultContractFixture()
	subject := publisherexactlyoncetest.NewContractStub(t,
		publisherexactlyoncetest.WithContractPublish(func(context.Context, publisherexactlyonce.Value) error {
			panic("Contract.Publish: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			publisherexactlyoncetest.AssertContractPublishSmoke(tb, subject, fixture.V)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertContractPublishCancelsCanFail holds AssertContractPublishCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertContractPublishCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := publisherexactlyoncetest.DefaultContractFixture()
	subject := publisherexactlyoncetest.NewContractStub(t,
		publisherexactlyoncetest.WithContractPublish(func(context.Context, publisherexactlyonce.Value) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			publisherexactlyoncetest.AssertContractPublishCancels(tb, subject, fixture.V)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertContractPublishHonoursDeadlineCanFail holds AssertContractPublishHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertContractPublishHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := publisherexactlyoncetest.DefaultContractFixture()
	subject := publisherexactlyoncetest.NewContractStub(t,
		publisherexactlyoncetest.WithContractPublish(func(context.Context, publisherexactlyonce.Value) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			publisherexactlyoncetest.AssertContractPublishHonoursDeadline(tb, subject, fixture.V)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertContractPublishToleratesNilContextCanFail holds AssertContractPublishToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertContractPublishToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := publisherexactlyoncetest.DefaultContractFixture()
	subject := publisherexactlyoncetest.NewContractStub(t,
		publisherexactlyoncetest.WithContractPublish(func(context.Context, publisherexactlyonce.Value) error {
			panic("Contract.Publish: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			publisherexactlyoncetest.AssertContractPublishToleratesNilContext(tb, subject, fixture.V)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertContractSubscribeSmokeCanFail holds AssertContractSubscribeSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertContractSubscribeSmokeCanFail(t *testing.T) {
	t.Parallel()

	subject := publisherexactlyoncetest.NewContractStub(t,
		publisherexactlyoncetest.WithContractSubscribe(func(context.Context) (<-chan publisherexactlyonce.Value, error) {
			panic("Contract.Subscribe: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			publisherexactlyoncetest.AssertContractSubscribeSmoke(tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertContractSubscribeCancelsCanFail holds AssertContractSubscribeCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertContractSubscribeCancelsCanFail(t *testing.T) {
	t.Parallel()

	subject := publisherexactlyoncetest.NewContractStub(t,
		publisherexactlyoncetest.WithContractSubscribe(func(context.Context) (<-chan publisherexactlyonce.Value, error) {
			var r0 <-chan publisherexactlyonce.Value
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			publisherexactlyoncetest.AssertContractSubscribeCancels(tb, subject)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertContractSubscribeHonoursDeadlineCanFail holds AssertContractSubscribeHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertContractSubscribeHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	subject := publisherexactlyoncetest.NewContractStub(t,
		publisherexactlyoncetest.WithContractSubscribe(func(context.Context) (<-chan publisherexactlyonce.Value, error) {
			var r0 <-chan publisherexactlyonce.Value
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			publisherexactlyoncetest.AssertContractSubscribeHonoursDeadline(tb, subject)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertContractSubscribeToleratesNilContextCanFail holds AssertContractSubscribeToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertContractSubscribeToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	subject := publisherexactlyoncetest.NewContractStub(t,
		publisherexactlyoncetest.WithContractSubscribe(func(context.Context) (<-chan publisherexactlyonce.Value, error) {
			panic("Contract.Subscribe: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			publisherexactlyoncetest.AssertContractSubscribeToleratesNilContext(tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance f1cd1ae609b1ef414bd166a3cd8a684d98761e64166082070ef20439ede1949d
