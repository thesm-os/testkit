// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/contract/publisher-redeliver/iface.go
// Plugins:   golang 1.0.0, suite 1.15.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package publisherredelivertest_test

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit"
	publisherredeliver "go.thesmos.sh/testkit/conformance/corpus/iface/contract/publisher-redeliver"
	"go.thesmos.sh/testkit/conformance/corpus/iface/contract/publisher-redeliver/publisherredelivertest"
)

// Every generated check for Contract, driven against a stand-in built
// to violate it.
//
//	Proved:   12 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertContractPublishSmokeCanFail holds AssertContractPublishSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertContractPublishSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := publisherredelivertest.DefaultContractFixture()
	subject := publisherredelivertest.NewContractStub(t,
		publisherredelivertest.WithContractPublish(func(context.Context, publisherredeliver.Value) error {
			panic("Contract.Publish: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			publisherredelivertest.AssertContractPublishSmoke(tb, subject, fixture.V)
		})
	testkit.Assert(t, got).Contains("Publish panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertContractPublishCancelsCanFail holds AssertContractPublishCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertContractPublishCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := publisherredelivertest.DefaultContractFixture()
	subject := publisherredelivertest.NewContractStub(t,
		publisherredelivertest.WithContractPublish(func(context.Context, publisherredeliver.Value) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			publisherredelivertest.AssertContractPublishCancels(tb, subject, fixture.V)
		})
	testkit.Assert(t, got).Contains("Publish must report a cancelled context as context.Canceled",
		"and rejects it for the reason the check is about")
}

// TestAssertContractPublishHonoursDeadlineCanFail holds AssertContractPublishHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertContractPublishHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := publisherredelivertest.DefaultContractFixture()
	subject := publisherredelivertest.NewContractStub(t,
		publisherredelivertest.WithContractPublish(func(context.Context, publisherredeliver.Value) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			publisherredelivertest.AssertContractPublishHonoursDeadline(tb, subject, fixture.V)
		})
	testkit.Assert(t, got).Contains("Publish must report an expired deadline as context.DeadlineExceeded",
		"and rejects it for the reason the check is about")
}

// TestAssertContractPublishToleratesNilContextCanFail holds AssertContractPublishToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertContractPublishToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := publisherredelivertest.DefaultContractFixture()
	subject := publisherredelivertest.NewContractStub(t,
		publisherredelivertest.WithContractPublish(func(context.Context, publisherredeliver.Value) error {
			panic("Contract.Publish: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			publisherredelivertest.AssertContractPublishToleratesNilContext(tb, subject, fixture.V)
		})
	testkit.Assert(t, got).Contains("Publish panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertContractRepublishSmokeCanFail holds AssertContractRepublishSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertContractRepublishSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := publisherredelivertest.DefaultContractFixture()
	subject := publisherredelivertest.NewContractStub(t,
		publisherredelivertest.WithContractRepublish(func(context.Context, publisherredeliver.Value) error {
			panic("Contract.Republish: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			publisherredelivertest.AssertContractRepublishSmoke(tb, subject, fixture.V)
		})
	testkit.Assert(t, got).Contains("Republish panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertContractRepublishCancelsCanFail holds AssertContractRepublishCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertContractRepublishCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := publisherredelivertest.DefaultContractFixture()
	subject := publisherredelivertest.NewContractStub(t,
		publisherredelivertest.WithContractRepublish(func(context.Context, publisherredeliver.Value) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			publisherredelivertest.AssertContractRepublishCancels(tb, subject, fixture.V)
		})
	testkit.Assert(t, got).Contains("Republish must report a cancelled context as context.Canceled",
		"and rejects it for the reason the check is about")
}

// TestAssertContractRepublishHonoursDeadlineCanFail holds AssertContractRepublishHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertContractRepublishHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := publisherredelivertest.DefaultContractFixture()
	subject := publisherredelivertest.NewContractStub(t,
		publisherredelivertest.WithContractRepublish(func(context.Context, publisherredeliver.Value) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			publisherredelivertest.AssertContractRepublishHonoursDeadline(tb, subject, fixture.V)
		})
	testkit.Assert(t, got).Contains("Republish must report an expired deadline as context.DeadlineExceeded",
		"and rejects it for the reason the check is about")
}

// TestAssertContractRepublishToleratesNilContextCanFail holds AssertContractRepublishToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertContractRepublishToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := publisherredelivertest.DefaultContractFixture()
	subject := publisherredelivertest.NewContractStub(t,
		publisherredelivertest.WithContractRepublish(func(context.Context, publisherredeliver.Value) error {
			panic("Contract.Republish: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			publisherredelivertest.AssertContractRepublishToleratesNilContext(tb, subject, fixture.V)
		})
	testkit.Assert(t, got).Contains("Republish panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertContractSubscribeSmokeCanFail holds AssertContractSubscribeSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertContractSubscribeSmokeCanFail(t *testing.T) {
	t.Parallel()

	subject := publisherredelivertest.NewContractStub(t,
		publisherredelivertest.WithContractSubscribe(func(context.Context) (<-chan publisherredeliver.Value, error) {
			panic("Contract.Subscribe: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			publisherredelivertest.AssertContractSubscribeSmoke(tb, subject)
		})
	testkit.Assert(t, got).Contains("Subscribe panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertContractSubscribeCancelsCanFail holds AssertContractSubscribeCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertContractSubscribeCancelsCanFail(t *testing.T) {
	t.Parallel()

	subject := publisherredelivertest.NewContractStub(t,
		publisherredelivertest.WithContractSubscribe(func(context.Context) (<-chan publisherredeliver.Value, error) {
			var r0 <-chan publisherredeliver.Value
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			publisherredelivertest.AssertContractSubscribeCancels(tb, subject)
		})
	testkit.Assert(t, got).Contains("Subscribe must report a cancelled context as context.Canceled",
		"and rejects it for the reason the check is about")
}

// TestAssertContractSubscribeHonoursDeadlineCanFail holds AssertContractSubscribeHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertContractSubscribeHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	subject := publisherredelivertest.NewContractStub(t,
		publisherredelivertest.WithContractSubscribe(func(context.Context) (<-chan publisherredeliver.Value, error) {
			var r0 <-chan publisherredeliver.Value
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			publisherredelivertest.AssertContractSubscribeHonoursDeadline(tb, subject)
		})
	testkit.Assert(t, got).Contains("Subscribe must report an expired deadline as context.DeadlineExceeded",
		"and rejects it for the reason the check is about")
}

// TestAssertContractSubscribeToleratesNilContextCanFail holds AssertContractSubscribeToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertContractSubscribeToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	subject := publisherredelivertest.NewContractStub(t,
		publisherredelivertest.WithContractSubscribe(func(context.Context) (<-chan publisherredeliver.Value, error) {
			panic("Contract.Subscribe: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			publisherredelivertest.AssertContractSubscribeToleratesNilContext(tb, subject)
		})
	testkit.Assert(t, got).Contains("Subscribe panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance 7d091c2dbc15cf05fc992a81bcc5a725b8567c79a18e8814be728c7986880933
