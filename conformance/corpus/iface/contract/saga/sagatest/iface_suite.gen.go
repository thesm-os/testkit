// Code generated by testkit. DO NOT EDIT.
//
// Source:    conformance/corpus/iface/contract/saga/iface.go
// Plugins:   golang 1.0.0, suite 1.16.0, backend.golang 1.0.0
// Command:   testkit run ./conformance/corpus/...

package sagatest

import (
	"context"
	"slices"
	"testing"

	"go.thesmos.sh/testkit/clock"
	"go.thesmos.sh/testkit/conformance/corpus/iface/contract/saga"
	"go.thesmos.sh/testkit/engine/suite"
)

// ContractStepCheck is one assertion about Step.
//
// Every generated check for Step is a value of it, and so is one you
// write — so they compose, reorder, and each runs standalone. testing.TB rather
// than *testing.T is what lets a stand-in drive it and prove it can fail.
type ContractStepCheck func(tb testing.TB, subject saga.Contract, v saga.Value)

// ContractCompensateCheck is one assertion about Compensate.
//
// Every generated check for Compensate is a value of it, and so is one you
// write — so they compose, reorder, and each runs standalone. testing.TB rather
// than *testing.T is what lets a stand-in drive it and prove it can fail.
type ContractCompensateCheck func(tb testing.TB, subject saga.Contract, v saga.Value)

// ContractStateCheck is one assertion about State.
//
// Every generated check for State is a value of it, and so is one you
// write — so they compose, reorder, and each runs standalone. testing.TB rather
// than *testing.T is what lets a stand-in drive it and prove it can fail.
//
// State returns a value beside its error and takes nothing to vary, so no
// "an error carries the zero value" check is generated for it. That check reaches
// the failure it is about by choosing an input that misses, and this signature
// offers none — a generated one could only demand failure from a correct
// implementation. Write it here against a subject this package can break.
type ContractStateCheck func(tb testing.TB, subject saga.Contract)

// ContractFixture holds every input the generated checks run against.
//
// Derived from each method's parameter names and types. Every field has a
// companion holding a second, different value: a check comparing a result
// against a single input passes whenever the subject happened to be seeded with
// it, and a miss check whose key happens to hit asserts nothing.
//
// A field whose type admits no literal — a func, a channel, a type from a
// package this run never read — is declared and left at its zero value, and
// every generated check that needed it was dropped rather than emitted against
// a value nobody could write. Supply one through ContractWithFixture
// and the check a consumer writes has something to use.
type ContractFixture struct {
	Value      saga.Value
	ValueOther saga.Value
}

// DefaultContractFixture is what this run derived. Replace any field through
// ContractWithFixture; the rest keep these values.
func DefaultContractFixture() ContractFixture {
	return ContractFixture{
		Value:      saga.Value{Key: "test-key", Body: "test-body"},
		ValueOther: saga.Value{Key: "other-key", Body: "other-body"},
	}
}

// Contract is the witnessed instantiation.
//
// A generic interface aliases with its own parameters, which Go admits
// since 1.24 — so every declaration below names Contract and
// the arguments follow, exactly as they do for a concrete one.
type Contract = saga.Contract

// ContractRunOpt is one thing you can pass to RunContract.
type ContractRunOpt interface {
	applyTo(*contractRunConfig)
}

type contractRunConfig struct {
	suite.Bundle[Contract]
}

// ContractHarness declares one Contract implementation under test.
type ContractHarness[T Contract] struct {
	Name string

	// Exactly one constructor.
	New   func() T
	Start func(tb testing.TB) T

	// Excuse lists checks this subject structurally cannot take.
	Excuse []suite.ID

	// Oracle marks this subject as the run's reference. At most one.
	Oracle bool

	// Serial keeps this subject out of the parallel group.
	Serial bool

	// Provide answers open capabilities beyond the named fields — the
	// doors only this interface's checks know. The runner's gate reads
	// it for any capability without a dedicated field.
	Provide map[suite.Capability]any
}

// Subject lowers the declaration into what the runner consumes.
func (h ContractHarness[T]) Subject() (suite.Subject[Contract], error) {
	build, err := suite.OneCtor(h.Name, h.New, h.Start)
	if err != nil {
		return suite.Subject[Contract]{}, err
	}
	return suite.Subject[Contract]{
		Name:     h.Name,
		Provides: h.Provide,
		New:      func(tb testing.TB) Contract { return build(tb) },
		Oracle:   h.Oracle,
		Serial:   h.Serial,
		Excused:  suite.ExcuseSet(h.Excuse),
	}, nil
}

func (h ContractHarness[T]) applyTo(rc *contractRunConfig) {
	rc.AddSubject(h.Subject())
}

type contractDropOpt []suite.ID

func (d contractDropOpt) applyTo(rc *contractRunConfig) {
	rc.AddDrops(d...)
}

// contractWithout drops checks by identity.
func contractWithout(ids ...suite.ID) ContractRunOpt {
	return contractDropOpt(ids)
}

// --- The veneer -------------------------------------------------------------

// ContractSuite is the entry a consumer names checks through.
var ContractSuite = contractVeneer{Checks: contractCheckIndex}

type contractVeneer struct {
	// Checks is the check index — the constants a Without call is
	// written against.
	Checks contractCheckIndexT
}

// Without drops a check by ID.
func (contractVeneer) Without(ids ...suite.ID) ContractRunOpt {
	return contractWithout(ids...)
}

// --- Drop hints -------------------------------------------------------------

// contractIndexPath spells every emitted ID as the path a
// consumer would write to drop it, so a run that declines something can
// say what to type rather than what failed.
var contractIndexPath = map[suite.ID]string{
	contractCheckIndex.Step.Smoke():            "ContractSuite.Checks.Step.Smoke()",
	contractCheckIndex.Step.Cancels():          "ContractSuite.Checks.Step.Cancels()",
	contractCheckIndex.Step.NilContext():       "ContractSuite.Checks.Step.NilContext()",
	contractCheckIndex.Step.Deadline():         "ContractSuite.Checks.Step.Deadline()",
	contractCheckIndex.Compensate.Smoke():      "ContractSuite.Checks.Compensate.Smoke()",
	contractCheckIndex.Compensate.Cancels():    "ContractSuite.Checks.Compensate.Cancels()",
	contractCheckIndex.Compensate.NilContext(): "ContractSuite.Checks.Compensate.NilContext()",
	contractCheckIndex.Compensate.Deadline():   "ContractSuite.Checks.Compensate.Deadline()",
	contractCheckIndex.State.Smoke():           "ContractSuite.Checks.State.Smoke()",
	contractCheckIndex.State.Cancels():         "ContractSuite.Checks.State.Cancels()",
	contractCheckIndex.State.NilContext():      "ContractSuite.Checks.State.NilContext()",
	contractCheckIndex.State.Deadline():        "ContractSuite.Checks.State.Deadline()",
	contractCheckIndex.State.ZeroOnError():     "ContractSuite.Checks.State.ZeroOnError()",
	contractCheckIndex.Model.Laws():            "ContractSuite.Checks.Model.Laws()",
	contractCheckIndex.Model.Agrees():          "ContractSuite.Checks.Model.Agrees()",
}

var contractDropHint = suite.DropHinter(
	"ContractSuite", contractIndexPath,
)

// The names this file spells more than once: one home each.
const (
	contractStep       = "Step"
	contractCompensate = "Compensate"
	contractState      = "State"

	// The interface's word inside a family-scoped identity.
	contractQualifier = "contract"
)

// contractCheckIndex indexes every check this package emits, by
// method and then by check.
var contractCheckIndex = contractCheckIndexT{
	Step:       contractStepChecks{},
	Compensate: contractCompensateChecks{},
	State:      contractStateChecks{},
	Model:      contractModelChecks{},
}

type contractCheckIndexT struct {
	Step       contractStepChecks
	Compensate contractCompensateChecks
	State      contractStateChecks
	Model      contractModelChecks
}

// All returns every ID this package emits.
func (contractCheckIndexT) All() []suite.ID {
	var out []suite.ID
	out = append(out, contractStepChecks{}.All()...)
	out = append(out, contractCompensateChecks{}.All()...)
	out = append(out, contractStateChecks{}.All()...)
	out = append(out, contractModelChecks{}.All()...)
	return out
}

type contractStepChecks struct{}

func (contractStepChecks) Smoke() suite.ID {
	return suite.MethodID(contractStep, suite.SegSmoke)
}

func (contractStepChecks) Cancels() suite.ID {
	return suite.MethodID(contractStep, suite.SegCancel)
}

func (contractStepChecks) NilContext() suite.ID {
	return suite.MethodID(contractStep, suite.SegNilContext)
}

func (contractStepChecks) Deadline() suite.ID {
	return suite.MethodID(contractStep, suite.SegDeadline)
}

func (contractStepChecks) All() []suite.ID {
	return []suite.ID{
		contractStepChecks{}.Smoke(),
		contractStepChecks{}.Cancels(),
		contractStepChecks{}.NilContext(),
		contractStepChecks{}.Deadline(),
	}
}

type contractCompensateChecks struct{}

func (contractCompensateChecks) Smoke() suite.ID {
	return suite.MethodID(contractCompensate, suite.SegSmoke)
}

func (contractCompensateChecks) Cancels() suite.ID {
	return suite.MethodID(contractCompensate, suite.SegCancel)
}

func (contractCompensateChecks) NilContext() suite.ID {
	return suite.MethodID(contractCompensate, suite.SegNilContext)
}

func (contractCompensateChecks) Deadline() suite.ID {
	return suite.MethodID(contractCompensate, suite.SegDeadline)
}

func (contractCompensateChecks) All() []suite.ID {
	return []suite.ID{
		contractCompensateChecks{}.Smoke(),
		contractCompensateChecks{}.Cancels(),
		contractCompensateChecks{}.NilContext(),
		contractCompensateChecks{}.Deadline(),
	}
}

type contractStateChecks struct{}

func (contractStateChecks) Smoke() suite.ID {
	return suite.MethodID(contractState, suite.SegSmoke)
}

func (contractStateChecks) Cancels() suite.ID {
	return suite.MethodID(contractState, suite.SegCancel)
}

func (contractStateChecks) NilContext() suite.ID {
	return suite.MethodID(contractState, suite.SegNilContext)
}

func (contractStateChecks) Deadline() suite.ID {
	return suite.MethodID(contractState, suite.SegDeadline)
}

func (contractStateChecks) ZeroOnError() suite.ID {
	return suite.MethodID(contractState, suite.SegZeroValue)
}

func (contractStateChecks) All() []suite.ID {
	return []suite.ID{
		contractStateChecks{}.Smoke(),
		contractStateChecks{}.Cancels(),
		contractStateChecks{}.NilContext(),
		contractStateChecks{}.Deadline(),
		contractStateChecks{}.ZeroOnError(),
	}
}

type contractModelChecks struct{}

func (contractModelChecks) Laws() suite.ID {
	return suite.FamilyID(suite.FamilyModel, contractQualifier, suite.SegLaws)
}

func (contractModelChecks) Agrees() suite.ID {
	return suite.FamilyID(suite.FamilyModel, contractQualifier, suite.SegDifferential)
}

func (contractModelChecks) All() []suite.ID {
	return []suite.ID{
		contractModelChecks{}.Laws(),
		contractModelChecks{}.Agrees(),
	}
}

// contractSignatureChecks builds every check this run derived.
//
// sig is the one shape they share; the literals below state only what
// differs. Falsifiable is Proven wherever a defect rule reached the
// claim, which the parity gate refuses to stamp without one.
func contractSignatureChecks(fx ContractFixture) []suite.Check[Contract] {
	sig := suite.ProvenCheck[Contract]
	ix := contractCheckIndex
	return []suite.Check[Contract]{
		sig(ix.Step.Smoke(), suite.ClassSmoke,
			"Step survives a call with a derived value",
			func(tb testing.TB, c Contract) {
				contractAssertStepSmoke(tb, c, fx)
			}),
		sig(ix.Step.Cancels(), suite.ClassCancel,
			"Step reports a cancelled context as cancelled",
			func(tb testing.TB, c Contract) {
				contractAssertStepCancels(tb, c, fx)
			}),
		sig(ix.Step.NilContext(), suite.ClassNilContext,
			"Step returns an error rather than panicking on a nil context",
			func(tb testing.TB, c Contract) {
				contractAssertStepToleratesNilContext(tb, c, fx)
			}),
		sig(ix.Step.Deadline(), suite.ClassDeadline,
			"Step reports an expired deadline as exceeded",
			func(tb testing.TB, c Contract) {
				contractAssertStepHonoursDeadline(tb, c, fx)
			}),
		sig(ix.Compensate.Smoke(), suite.ClassSmoke,
			"Compensate survives a call with a derived value",
			func(tb testing.TB, c Contract) {
				contractAssertCompensateSmoke(tb, c, fx)
			}),
		sig(ix.Compensate.Cancels(), suite.ClassCancel,
			"Compensate reports a cancelled context as cancelled",
			func(tb testing.TB, c Contract) {
				contractAssertCompensateCancels(tb, c, fx)
			}),
		sig(ix.Compensate.NilContext(), suite.ClassNilContext,
			"Compensate returns an error rather than panicking on a nil context",
			func(tb testing.TB, c Contract) {
				contractAssertCompensateToleratesNilContext(tb, c, fx)
			}),
		sig(ix.Compensate.Deadline(), suite.ClassDeadline,
			"Compensate reports an expired deadline as exceeded",
			func(tb testing.TB, c Contract) {
				contractAssertCompensateHonoursDeadline(tb, c, fx)
			}),
		sig(ix.State.Smoke(), suite.ClassSmoke,
			"State survives a call",
			func(tb testing.TB, c Contract) {
				contractAssertStateSmoke(tb, c)
			}),
		sig(ix.State.Cancels(), suite.ClassCancel,
			"State reports a cancelled context as cancelled",
			func(tb testing.TB, c Contract) {
				contractAssertStateCancels(tb, c)
			}),
		sig(ix.State.NilContext(), suite.ClassNilContext,
			"State returns an error rather than panicking on a nil context",
			func(tb testing.TB, c Contract) {
				contractAssertStateToleratesNilContext(tb, c)
			}),
		sig(ix.State.Deadline(), suite.ClassDeadline,
			"State reports an expired deadline as exceeded",
			func(tb testing.TB, c Contract) {
				contractAssertStateHonoursDeadline(tb, c)
			}),
		sig(ix.State.ZeroOnError(), suite.ClassZeroValue,
			"State returns zero alongside any error",
			func(tb testing.TB, c Contract) {
				contractAssertStateZeroOnError(tb, c)
			}),
	}
}

// contractAssertStepSmoke asserts Step survives a call with a derived value.
func contractAssertStepSmoke(
	tb testing.TB,
	c Contract,
	fx ContractFixture,
) {
	tb.Helper()
	suite.Survives(tb, contractStep, func(ctx context.Context) {
		_ = c.Step(ctx, fx.Value)
	})
}

// contractAssertStepCancels asserts Step reports a cancelled context as cancelled.
func contractAssertStepCancels(
	tb testing.TB,
	c Contract,
	fx ContractFixture,
) {
	tb.Helper()
	suite.ReportsCancelled(tb, contractStep, func(ctx context.Context) error {
		return c.Step(ctx, fx.Value)
	})
}

// contractAssertStepToleratesNilContext asserts Step returns an error rather than panicking on a nil context.
func contractAssertStepToleratesNilContext(
	tb testing.TB,
	c Contract,
	fx ContractFixture,
) {
	tb.Helper()
	suite.ToleratesNilContext(tb, contractStep, func(ctx context.Context) error {
		return c.Step(ctx, fx.Value)
	})
}

// contractAssertStepHonoursDeadline asserts Step reports an expired deadline as exceeded.
func contractAssertStepHonoursDeadline(
	tb testing.TB,
	c Contract,
	fx ContractFixture,
) {
	tb.Helper()
	suite.ReportsDeadlineExceeded(tb, contractStep, func(ctx context.Context) error {
		return c.Step(ctx, fx.Value)
	})
}

// contractAssertCompensateSmoke asserts Compensate survives a call with a derived value.
func contractAssertCompensateSmoke(
	tb testing.TB,
	c Contract,
	fx ContractFixture,
) {
	tb.Helper()
	suite.Survives(tb, contractCompensate, func(ctx context.Context) {
		_ = c.Compensate(ctx, fx.Value)
	})
}

// contractAssertCompensateCancels asserts Compensate reports a cancelled context as cancelled.
func contractAssertCompensateCancels(
	tb testing.TB,
	c Contract,
	fx ContractFixture,
) {
	tb.Helper()
	suite.ReportsCancelled(tb, contractCompensate, func(ctx context.Context) error {
		return c.Compensate(ctx, fx.Value)
	})
}

// contractAssertCompensateToleratesNilContext asserts Compensate returns an error rather than panicking on a nil context.
func contractAssertCompensateToleratesNilContext(
	tb testing.TB,
	c Contract,
	fx ContractFixture,
) {
	tb.Helper()
	suite.ToleratesNilContext(tb, contractCompensate, func(ctx context.Context) error {
		return c.Compensate(ctx, fx.Value)
	})
}

// contractAssertCompensateHonoursDeadline asserts Compensate reports an expired deadline as exceeded.
func contractAssertCompensateHonoursDeadline(
	tb testing.TB,
	c Contract,
	fx ContractFixture,
) {
	tb.Helper()
	suite.ReportsDeadlineExceeded(tb, contractCompensate, func(ctx context.Context) error {
		return c.Compensate(ctx, fx.Value)
	})
}

// contractAssertStateSmoke asserts State survives a call.
func contractAssertStateSmoke(
	tb testing.TB,
	c Contract,
) {
	tb.Helper()
	suite.Survives(tb, contractState, func(ctx context.Context) {
		_, _ = c.State(ctx)
	})
}

// contractAssertStateCancels asserts State reports a cancelled context as cancelled.
func contractAssertStateCancels(
	tb testing.TB,
	c Contract,
) {
	tb.Helper()
	suite.ReportsCancelled(tb, contractState, func(ctx context.Context) error {
		_, err := c.State(ctx)
		return err
	})
}

// contractAssertStateToleratesNilContext asserts State returns an error rather than panicking on a nil context.
func contractAssertStateToleratesNilContext(
	tb testing.TB,
	c Contract,
) {
	tb.Helper()
	suite.ToleratesNilContext(tb, contractState, func(ctx context.Context) error {
		_, err := c.State(ctx)
		return err
	})
}

// contractAssertStateHonoursDeadline asserts State reports an expired deadline as exceeded.
func contractAssertStateHonoursDeadline(
	tb testing.TB,
	c Contract,
) {
	tb.Helper()
	suite.ReportsDeadlineExceeded(tb, contractState, func(ctx context.Context) error {
		_, err := c.State(ctx)
		return err
	})
}

// contractAssertStateZeroOnError asserts State returns zero alongside any error.
func contractAssertStateZeroOnError(
	tb testing.TB,
	c Contract,
) {
	tb.Helper()
	ctx, cancel := context.WithCancel(tb.Context())
	cancel()
	got, err := c.State(ctx)
	if err == nil {
		tb.Skip("State answered for a cancelled context, so this check has no error to inspect")
	}

	var zero string
	if got != zero {
		tb.Errorf("State must return the zero value alongside an error: got %+v, want %+v (err %v)",
			got, zero, err)
	}
}

// AssertContractContract runs every generated check against every declared subject.
//
//	Subjects: declare each with ContractSubject
//	Extend:   ContractOnStep, ContractOnCompensate, ContractOnState
//	Drop:     ContractWithout, by the path each check reports under
func AssertContractContract(t *testing.T, opts ...ContractOption) {
	t.Helper()
	cfg := newContractConfig(opts...)
	if len(cfg.subjects) == 0 {
		t.Fatalf("AssertContractContract was given no subject; declare one with " +
			"ContractSubject(name, factory)")
	}
	for _, p := range cfg.phantoms() {
		t.Errorf("ContractWithout(%q) declines nothing: no check here reports "+
			"under that path, so this suite asserts one thing more than its "+
			"author believes", p)
	}
	for _, s := range cfg.subjects {
		runContractChecks(t, cfg, s.name, s.factory, nil)
	}
}

// runContractChecks runs the whole contract against one subject.
//
// wrap is applied after the subject is built and seeded, so a wrapper is
// transparent to the seed and every failure it causes is its own.
func runContractChecks(
	t *testing.T,
	cfg *contractConfig,
	label string,
	factory func() saga.Contract,
	wrap func(tb testing.TB, subject saga.Contract) saga.Contract,
) {
	t.Helper()
	t.Run(label, func(t *testing.T) {
		t.Parallel()

		t.Run(contractStep, func(t *testing.T) {
			t.Parallel()

			for _, c := range cfg.onStep {
				cfg.run(t, string(suite.MethodID(contractStep, c.name)), c.name, func(tb testing.TB) {
					c.fn(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.Value)
				})
			}
		})

		t.Run(contractCompensate, func(t *testing.T) {
			t.Parallel()

			for _, c := range cfg.onCompensate {
				cfg.run(t, string(suite.MethodID(contractCompensate, c.name)), c.name, func(tb testing.TB) {
					c.fn(tb, cfg.subject(tb, factory, wrap), cfg.Fixture.Value)
				})
			}
		})

		t.Run(contractState, func(t *testing.T) {
			t.Parallel()

			for _, c := range cfg.onState {
				cfg.run(t, string(suite.MethodID(contractState, c.name)), c.name, func(tb testing.TB) {
					c.fn(tb, cfg.subject(tb, factory, wrap))
				})
			}
		})

		// The extra bodies of checks this run was given, each reporting under its
		// own name and dropped by it.
		//
		// The plain subject only. The wrapped pass exists to prove the double
		// faithful and the per-method checks above already do that, so running one
		// through the wrapper prices the wrapper and says nothing new about the
		// subject.
		if wrap == nil {
			for _, e := range cfg.extensions {
				if cfg.declined(e.name) {
					t.Run(e.name, func(t *testing.T) {
						t.Skipf("dropped through ContractWithout(%q)", e.name)
					})
					continue
				}
				t.Run(e.name, func(t *testing.T) {
					t.Parallel()
					e.run(t, label, factory, cfg)
				})
			}
		}
	})
}

// ContractOption configures a contract run.
type ContractOption func(*contractConfig)

// ContractSubject declares an implementation to hold to the contract.
//
// Repeat it for every implementation that claims to satisfy the interface — an
// in-memory one, the one backed by a database, the one behind a network. Every
// check and every option is stated once and runs against all of them, which is
// what a conformance suite is for.
func ContractSubject(name string, factory func() saga.Contract) ContractOption {
	return func(c *contractConfig) {
		c.subjects = append(c.subjects, namedContractSubject{name, factory})
	}
}

// ContractWithClock supplies the clock every time-reading check measures on.
//
// Defaults to the real one, so an implementation that does not take a clock
// behaves as it would have. Supply a [clock.TestClock] — the same one the
// factory builds the subject with — and a budget becomes a claim about the time
// the implementation means to spend rather than about how loaded the machine
// was.
func ContractWithClock(c clock.Clock) ContractOption {
	return func(cfg *contractConfig) { cfg.clock = c }
}

// ContractWithFixture replaces the derived inputs.
func ContractWithFixture(f ContractFixture) ContractOption {
	return func(c *contractConfig) { c.Fixture = f }
}

// ContractSeed replaces the derived seed.
//
// For an interface with no writer to seed itself through, which is the one
// input a conformance run cannot derive: a reader over an empty subject
// asserts nothing.
//
// Returning an error rather than swallowing one. A seed that failed quietly
// leaves every check after it asserting nothing and reporting success.
func ContractSeed(fn func(ctx context.Context, subject saga.Contract) error) ContractOption {
	return func(c *contractConfig) {
		c.seed = fn
		c.seedIsDerived = false
	}
}

// ContractOnStep adds a named check to Step.
func ContractOnStep(name string, fn ContractStepCheck) ContractOption {
	return func(c *contractConfig) {
		c.onStep = append(c.onStep, namedContractStepCheck{name, fn})
	}
}

// ContractOnCompensate adds a named check to Compensate.
func ContractOnCompensate(name string, fn ContractCompensateCheck) ContractOption {
	return func(c *contractConfig) {
		c.onCompensate = append(c.onCompensate, namedContractCompensateCheck{name, fn})
	}
}

// ContractOnState adds a named check to State.
func ContractOnState(name string, fn ContractStateCheck) ContractOption {
	return func(c *contractConfig) {
		c.onState = append(c.onState, namedContractStateCheck{name, fn})
	}
}

// ContractWithout drops generated checks by the path each reports under.
//
// For a subject that legitimately violates one. Without this the only recourse
// is to stop running the suite, which costs every other check in the file — so
// a dropped check is reported as skipped rather than silently omitted.
func ContractWithout(paths ...string) ContractOption {
	return func(c *contractConfig) {
		for _, p := range paths {
			c.without[p] = struct{}{}
		}
	}
}

type namedContractStepCheck struct {
	name string
	fn   ContractStepCheck
}

type namedContractCompensateCheck struct {
	name string
	fn   ContractCompensateCheck
}

type namedContractStateCheck struct {
	name string
	fn   ContractStateCheck
}

type namedContractSubject struct {
	name    string
	factory func() saga.Contract
}

// contractContractExtension is a body of checks a generated sibling adds through an option.
//
// A sibling declares the option and closes over whatever configures it, so
// everything that varies per run is typed and private to the file that owns it
// — this file understands only the name and the call. Nothing arrives except
// through an option, which is what keeps the set fixed before the first subtest
// starts.
type contractContractExtension struct {
	// name is the path this reports under, and the one ContractWithout drops
	// it by. Anything nested beneath it is the extension's own to name.
	name string

	// run takes the factory rather than a subject. One that drives sequences
	// builds a subject per iteration, and one that compares against a reference
	// needs the unseeded subject a factory returns — which
	// contractConfig.subject is not.
	//
	// Called once per subject, in parallel, sharing the config. Anything it
	// mutates has to be built inside the call: state captured when the option
	// was declared is state two subjects write at once.
	run func(
		t *testing.T,
		subject string,
		factory func() saga.Contract,
		cfg *contractConfig,
	)
}

type contractConfig struct {
	Fixture  ContractFixture
	subjects []namedContractSubject
	clock    clock.Clock
	seed     func(ctx context.Context, subject saga.Contract) error
	// seedIsDerived is what lets a failed seed name the right culprit. The
	// derived seed and a consumer's are the same field, and the advice for one
	// is useless for the other.
	seedIsDerived bool
	without       map[string]struct{}
	extensions    []contractContractExtension
	onStep        []namedContractStepCheck
	onCompensate  []namedContractCompensateCheck
	onState       []namedContractStateCheck
}

func newContractConfig(opts ...ContractOption) *contractConfig {
	c := &contractConfig{
		Fixture: DefaultContractFixture(),
		clock:   clock.RealClock(),
		without: map[string]struct{}{},
	}
	// Derived: Step is classified writer, so the interface
	// populates itself and a reader has something to read.
	c.seed = func(ctx context.Context, subject saga.Contract) error {
		return subject.Step(ctx, c.Fixture.Value)
	}
	c.seedIsDerived = true
	for _, o := range opts {
		o(c)
	}
	return c
}

// subject builds a fresh implementation and seeds it, so every check runs
// against state no other check touched.
func (c *contractConfig) subject(
	tb testing.TB,
	factory func() saga.Contract,
	wrap func(tb testing.TB, subject saga.Contract) saga.Contract,
) saga.Contract {
	tb.Helper()
	s := factory()
	if c.seed != nil {
		// Reported rather than ignored. A seed that silently failed would leave
		// every check after it running against an empty subject — passing, and
		// asserting nothing.
		//
		// Which seed ran decides what the message can usefully say. The derived
		// one fails for one of two reasons and both are fixable from here, so it
		// names them. A seed the caller supplied is theirs, and telling them to
		// supply one is advice they already took.
		if err := c.seed(tb.Context(), s); err != nil {
			if c.seedIsDerived {
				tb.Fatalf("seeding through Step failed: %v; "+
					"supply a value it accepts through ContractWithFixture, or a "+
					"whole seed through ContractSeed", err)
			} else {
				tb.Fatalf("the seed supplied through ContractSeed failed: %v", err)
			}
		}
	}
	if wrap != nil {
		s = wrap(tb, s)
	}
	return s
}

// dropped reports whether a path was declined through ContractWithout.
//
// Named rather than inlined because the drop set is read from two places: here,
// and by whatever registered through the seam — which reports under paths this
// file has never heard of and must answer the same question about them.
func (c *contractConfig) declined(path string) bool {
	_, ok := c.without[path]
	return ok
}

// contractPaths is every path this file reports a check under.
//
// The set a ContractWithout argument has to be a member of. Declared
// rather than observed: a check that did not run is not a check that does not
// exist — an integration-only method skips without ever consulting the drop
// set, and reading that as a phantom would fail a correct suite on a machine
// with the environment unset.
//
//nolint:gochecknoglobals // the emitted check paths, read-only after init.
var contractPaths = map[string]struct{}{}

// phantoms names every ContractWithout path that declines nothing.
//
// A drop that drops nothing is this option's silent half: the consumer wrote
// it against a check since renamed, or never emitted for that method at all,
// and every run after reads green while asserting one thing more than its
// author believes. Extensions carry their own names — the seam reports under
// paths this file has never heard of — so they count as known too.
func (c *contractConfig) phantoms() []string {
	known := func(p string) bool {
		if _, ok := contractPaths[p]; ok {
			return true
		}
		for _, e := range c.extensions {
			if e.name == p {
				return true
			}
		}
		for _, on := range c.onStep {
			if string(suite.MethodID(contractStep, on.name)) == p {
				return true
			}
		}
		for _, on := range c.onCompensate {
			if string(suite.MethodID(contractCompensate, on.name)) == p {
				return true
			}
		}
		for _, on := range c.onState {
			if string(suite.MethodID(contractState, on.name)) == p {
				return true
			}
		}
		return false
	}
	var out []string
	for p := range c.without {
		if !known(p) {
			out = append(out, p)
		}
	}
	slices.Sort(out)
	return out
}

// run executes one check unless it was dropped, reporting the drop rather than
// silently omitting it.
func (c *contractConfig) run(t *testing.T, path, name string, fn func(tb testing.TB)) {
	t.Helper()
	if c.declined(path) {
		t.Run(name, func(t *testing.T) {
			t.Skipf("dropped through ContractWithout(%q)", path)
		})
		return
	}
	t.Run(name, func(t *testing.T) {
		t.Parallel()
		fn(t)
	})
}

// testkit: end of generated content.
// testkit:provenance 81f836df5895e4077ec4e305a3e0ae119c2f8d4bb38ed2cfefc96a994496c9c3
