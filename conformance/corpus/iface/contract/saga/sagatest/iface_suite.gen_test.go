// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/contract/saga/iface.go
// Plugins:   golang 1.0.0, suite 1.15.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package sagatest_test

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/iface/contract/saga"
	"go.thesmos.sh/testkit/conformance/corpus/iface/contract/saga/sagatest"
)

// Every generated check for Contract, driven against a stand-in built
// to violate it.
//
//	Proved:   12 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertContractStepSmokeCanFail holds AssertContractStepSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertContractStepSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := sagatest.DefaultContractFixture()
	subject := sagatest.NewContractStub(t,
		sagatest.WithContractStep(func(context.Context, saga.Value) error {
			panic("Contract.Step: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			sagatest.AssertContractStepSmoke(tb, subject, fixture.V)
		})
	testkit.Assert(t, got).Contains("Step panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertContractStepCancelsCanFail holds AssertContractStepCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertContractStepCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := sagatest.DefaultContractFixture()
	subject := sagatest.NewContractStub(t,
		sagatest.WithContractStep(func(context.Context, saga.Value) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			sagatest.AssertContractStepCancels(tb, subject, fixture.V)
		})
	testkit.Assert(t, got).Contains("Step must report a cancelled context as context.Canceled",
		"and rejects it for the reason the check is about")
}

// TestAssertContractStepHonoursDeadlineCanFail holds AssertContractStepHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertContractStepHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := sagatest.DefaultContractFixture()
	subject := sagatest.NewContractStub(t,
		sagatest.WithContractStep(func(context.Context, saga.Value) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			sagatest.AssertContractStepHonoursDeadline(tb, subject, fixture.V)
		})
	testkit.Assert(t, got).Contains("Step must report an expired deadline as context.DeadlineExceeded",
		"and rejects it for the reason the check is about")
}

// TestAssertContractStepToleratesNilContextCanFail holds AssertContractStepToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertContractStepToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := sagatest.DefaultContractFixture()
	subject := sagatest.NewContractStub(t,
		sagatest.WithContractStep(func(context.Context, saga.Value) error {
			panic("Contract.Step: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			sagatest.AssertContractStepToleratesNilContext(tb, subject, fixture.V)
		})
	testkit.Assert(t, got).Contains("Step panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertContractCompensateSmokeCanFail holds AssertContractCompensateSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertContractCompensateSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := sagatest.DefaultContractFixture()
	subject := sagatest.NewContractStub(t,
		sagatest.WithContractCompensate(func(context.Context, saga.Value) error {
			panic("Contract.Compensate: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			sagatest.AssertContractCompensateSmoke(tb, subject, fixture.V)
		})
	testkit.Assert(t, got).Contains("Compensate panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertContractCompensateCancelsCanFail holds AssertContractCompensateCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertContractCompensateCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := sagatest.DefaultContractFixture()
	subject := sagatest.NewContractStub(t,
		sagatest.WithContractCompensate(func(context.Context, saga.Value) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			sagatest.AssertContractCompensateCancels(tb, subject, fixture.V)
		})
	testkit.Assert(t, got).Contains("Compensate must report a cancelled context as context.Canceled",
		"and rejects it for the reason the check is about")
}

// TestAssertContractCompensateHonoursDeadlineCanFail holds AssertContractCompensateHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertContractCompensateHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := sagatest.DefaultContractFixture()
	subject := sagatest.NewContractStub(t,
		sagatest.WithContractCompensate(func(context.Context, saga.Value) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			sagatest.AssertContractCompensateHonoursDeadline(tb, subject, fixture.V)
		})
	testkit.Assert(t, got).Contains("Compensate must report an expired deadline as context.DeadlineExceeded",
		"and rejects it for the reason the check is about")
}

// TestAssertContractCompensateToleratesNilContextCanFail holds AssertContractCompensateToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertContractCompensateToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := sagatest.DefaultContractFixture()
	subject := sagatest.NewContractStub(t,
		sagatest.WithContractCompensate(func(context.Context, saga.Value) error {
			panic("Contract.Compensate: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			sagatest.AssertContractCompensateToleratesNilContext(tb, subject, fixture.V)
		})
	testkit.Assert(t, got).Contains("Compensate panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertContractStateSmokeCanFail holds AssertContractStateSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertContractStateSmokeCanFail(t *testing.T) {
	t.Parallel()

	subject := sagatest.NewContractStub(t,
		sagatest.WithContractState(func(context.Context) (string, error) {
			panic("Contract.State: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			sagatest.AssertContractStateSmoke(tb, subject)
		})
	testkit.Assert(t, got).Contains("State panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertContractStateCancelsCanFail holds AssertContractStateCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertContractStateCancelsCanFail(t *testing.T) {
	t.Parallel()

	subject := sagatest.NewContractStub(t,
		sagatest.WithContractState(func(context.Context) (string, error) {
			var r0 string
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			sagatest.AssertContractStateCancels(tb, subject)
		})
	testkit.Assert(t, got).Contains("State must report a cancelled context as context.Canceled",
		"and rejects it for the reason the check is about")
}

// TestAssertContractStateHonoursDeadlineCanFail holds AssertContractStateHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertContractStateHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	subject := sagatest.NewContractStub(t,
		sagatest.WithContractState(func(context.Context) (string, error) {
			var r0 string
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			sagatest.AssertContractStateHonoursDeadline(tb, subject)
		})
	testkit.Assert(t, got).Contains("State must report an expired deadline as context.DeadlineExceeded",
		"and rejects it for the reason the check is about")
}

// TestAssertContractStateToleratesNilContextCanFail holds AssertContractStateToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertContractStateToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	subject := sagatest.NewContractStub(t,
		sagatest.WithContractState(func(context.Context) (string, error) {
			panic("Contract.State: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			sagatest.AssertContractStateToleratesNilContext(tb, subject)
		})
	testkit.Assert(t, got).Contains("State panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance 3abafc91678d1fbb5d783a634d860901d6a78ebe1076aea2074a5e48290a3c5d
