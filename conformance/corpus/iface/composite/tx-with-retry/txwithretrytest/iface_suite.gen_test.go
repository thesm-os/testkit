// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/composite/tx-with-retry/iface.go
// Plugins:   golang 1.0.0, suite 1.15.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package txwithretrytest_test

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/iface/composite/tx-with-retry/txwithretrytest"
)

// Every generated check for TxWithRetry, driven against a stand-in built
// to violate it.
//
//	Proved:   12 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertTxWithRetryBeginSmokeCanFail holds AssertTxWithRetryBeginSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertTxWithRetryBeginSmokeCanFail(t *testing.T) {
	t.Parallel()

	subject := txwithretrytest.NewTxWithRetryStub(t,
		txwithretrytest.WithTxWithRetryBegin(func(context.Context) error {
			panic("TxWithRetry.Begin: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			txwithretrytest.AssertTxWithRetryBeginSmoke(tb, subject)
		})
	testkit.Assert(t, got).Contains("Begin panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertTxWithRetryBeginCancelsCanFail holds AssertTxWithRetryBeginCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertTxWithRetryBeginCancelsCanFail(t *testing.T) {
	t.Parallel()

	subject := txwithretrytest.NewTxWithRetryStub(t,
		txwithretrytest.WithTxWithRetryBegin(func(context.Context) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			txwithretrytest.AssertTxWithRetryBeginCancels(tb, subject)
		})
	testkit.Assert(t, got).Contains("Begin must report a cancelled context as context.Canceled",
		"and rejects it for the reason the check is about")
}

// TestAssertTxWithRetryBeginHonoursDeadlineCanFail holds AssertTxWithRetryBeginHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertTxWithRetryBeginHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	subject := txwithretrytest.NewTxWithRetryStub(t,
		txwithretrytest.WithTxWithRetryBegin(func(context.Context) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			txwithretrytest.AssertTxWithRetryBeginHonoursDeadline(tb, subject)
		})
	testkit.Assert(t, got).Contains("Begin must report an expired deadline as context.DeadlineExceeded",
		"and rejects it for the reason the check is about")
}

// TestAssertTxWithRetryBeginToleratesNilContextCanFail holds AssertTxWithRetryBeginToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertTxWithRetryBeginToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	subject := txwithretrytest.NewTxWithRetryStub(t,
		txwithretrytest.WithTxWithRetryBegin(func(context.Context) error {
			panic("TxWithRetry.Begin: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			txwithretrytest.AssertTxWithRetryBeginToleratesNilContext(tb, subject)
		})
	testkit.Assert(t, got).Contains("Begin panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertTxWithRetryCommitSmokeCanFail holds AssertTxWithRetryCommitSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertTxWithRetryCommitSmokeCanFail(t *testing.T) {
	t.Parallel()

	subject := txwithretrytest.NewTxWithRetryStub(t,
		txwithretrytest.WithTxWithRetryCommit(func(context.Context) error {
			panic("TxWithRetry.Commit: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			txwithretrytest.AssertTxWithRetryCommitSmoke(tb, subject)
		})
	testkit.Assert(t, got).Contains("Commit panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertTxWithRetryCommitCancelsCanFail holds AssertTxWithRetryCommitCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertTxWithRetryCommitCancelsCanFail(t *testing.T) {
	t.Parallel()

	subject := txwithretrytest.NewTxWithRetryStub(t,
		txwithretrytest.WithTxWithRetryCommit(func(context.Context) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			txwithretrytest.AssertTxWithRetryCommitCancels(tb, subject)
		})
	testkit.Assert(t, got).Contains("Commit must report a cancelled context as context.Canceled",
		"and rejects it for the reason the check is about")
}

// TestAssertTxWithRetryCommitHonoursDeadlineCanFail holds AssertTxWithRetryCommitHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertTxWithRetryCommitHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	subject := txwithretrytest.NewTxWithRetryStub(t,
		txwithretrytest.WithTxWithRetryCommit(func(context.Context) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			txwithretrytest.AssertTxWithRetryCommitHonoursDeadline(tb, subject)
		})
	testkit.Assert(t, got).Contains("Commit must report an expired deadline as context.DeadlineExceeded",
		"and rejects it for the reason the check is about")
}

// TestAssertTxWithRetryCommitToleratesNilContextCanFail holds AssertTxWithRetryCommitToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertTxWithRetryCommitToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	subject := txwithretrytest.NewTxWithRetryStub(t,
		txwithretrytest.WithTxWithRetryCommit(func(context.Context) error {
			panic("TxWithRetry.Commit: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			txwithretrytest.AssertTxWithRetryCommitToleratesNilContext(tb, subject)
		})
	testkit.Assert(t, got).Contains("Commit panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertTxWithRetryRollbackSmokeCanFail holds AssertTxWithRetryRollbackSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertTxWithRetryRollbackSmokeCanFail(t *testing.T) {
	t.Parallel()

	subject := txwithretrytest.NewTxWithRetryStub(t,
		txwithretrytest.WithTxWithRetryRollback(func(context.Context) error {
			panic("TxWithRetry.Rollback: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			txwithretrytest.AssertTxWithRetryRollbackSmoke(tb, subject)
		})
	testkit.Assert(t, got).Contains("Rollback panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertTxWithRetryRollbackCancelsCanFail holds AssertTxWithRetryRollbackCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertTxWithRetryRollbackCancelsCanFail(t *testing.T) {
	t.Parallel()

	subject := txwithretrytest.NewTxWithRetryStub(t,
		txwithretrytest.WithTxWithRetryRollback(func(context.Context) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			txwithretrytest.AssertTxWithRetryRollbackCancels(tb, subject)
		})
	testkit.Assert(t, got).Contains("Rollback must report a cancelled context as context.Canceled",
		"and rejects it for the reason the check is about")
}

// TestAssertTxWithRetryRollbackHonoursDeadlineCanFail holds AssertTxWithRetryRollbackHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertTxWithRetryRollbackHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	subject := txwithretrytest.NewTxWithRetryStub(t,
		txwithretrytest.WithTxWithRetryRollback(func(context.Context) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			txwithretrytest.AssertTxWithRetryRollbackHonoursDeadline(tb, subject)
		})
	testkit.Assert(t, got).Contains("Rollback must report an expired deadline as context.DeadlineExceeded",
		"and rejects it for the reason the check is about")
}

// TestAssertTxWithRetryRollbackToleratesNilContextCanFail holds AssertTxWithRetryRollbackToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertTxWithRetryRollbackToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	subject := txwithretrytest.NewTxWithRetryStub(t,
		txwithretrytest.WithTxWithRetryRollback(func(context.Context) error {
			panic("TxWithRetry.Rollback: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			txwithretrytest.AssertTxWithRetryRollbackToleratesNilContext(tb, subject)
		})
	testkit.Assert(t, got).Contains("Rollback panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance f47a2ba0d8cecfc253e09d7fcf81d970df1efd961e1282c68b2850eea74b932b
