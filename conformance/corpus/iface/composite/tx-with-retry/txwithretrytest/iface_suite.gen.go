// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/composite/tx-with-retry/iface.go
// Plugins:   golang 1.0.0, suite 1.0.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package txwithretrytest

import (
	"context"
	"testing"
	"time"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/clock"
	txwithretry "go.thesmos.sh/testkit/conformance/corpus/iface/composite/tx-with-retry"
)

// TxWithRetryBeginCheck is one assertion about Begin.
//
// Every generated check for Begin is a value of it, and so is one you
// write — so they compose, reorder, and each runs standalone. testing.TB rather
// than *testing.T is what lets a stand-in drive it and prove it can fail.
type TxWithRetryBeginCheck func(tb testing.TB, subject txwithretry.TxWithRetry)

// TxWithRetryCommitCheck is one assertion about Commit.
//
// Every generated check for Commit is a value of it, and so is one you
// write — so they compose, reorder, and each runs standalone. testing.TB rather
// than *testing.T is what lets a stand-in drive it and prove it can fail.
type TxWithRetryCommitCheck func(tb testing.TB, subject txwithretry.TxWithRetry)

// TxWithRetryRollbackCheck is one assertion about Rollback.
//
// Every generated check for Rollback is a value of it, and so is one you
// write — so they compose, reorder, and each runs standalone. testing.TB rather
// than *testing.T is what lets a stand-in drive it and prove it can fail.
type TxWithRetryRollbackCheck func(tb testing.TB, subject txwithretry.TxWithRetry)

// TxWithRetryFixture holds every input the generated checks run against.
//
// Derived from each method's parameter names and types. Every field has a
// companion holding a second, different value: a check comparing a result
// against a single input passes whenever the subject happened to be seeded with
// it, and a miss check whose key happens to hit asserts nothing.
//
// A field whose type admits no literal — a func, a channel, a type from a
// package this run never read — is declared and left at its zero value, and
// every generated check that needed it was dropped rather than emitted against
// a value nobody could write. Supply one through TxWithRetryWithFixture
// and the check a consumer writes has something to use.
type TxWithRetryFixture struct {
}

// DefaultTxWithRetryFixture is what this run derived. Replace any field through
// TxWithRetryWithFixture; the rest keep these values.
func DefaultTxWithRetryFixture() TxWithRetryFixture {
	return TxWithRetryFixture{}
}

// AssertTxWithRetryContract runs every generated check against every declared subject.
//
//	Checks:   12 across 3 methods, per subject
//	Subjects: declare each with TxWithRetrySubject
//	Double:   every subject runs a second time wrapped in TxWithRetryStub, so
//	          anything the wrapper fails that the subject passes is the double
//	          lying. TxWithRetryWithoutDouble declines it.
//	Extend:   TxWithRetryOnBegin, TxWithRetryOnCommit, TxWithRetryOnRollback
//	Drop:     TxWithRetryWithout, by the path each check reports under
//
// # What this file does not check
//
// The interface declares these and nothing here asserts them. Each is statable
// against a subject you can build, so the check is yours to write:
//
//   - retrysucceeds, on Commit — write it with TxWithRetryOnCommit
//
// # What is checked somewhere else
//
// These need a reference implementation to compare against, which a suite run
// has no way to build. Nothing here asserts them and nothing here should:
//
//   - lifecycle, on Begin, Commit, Rollback
//   - tx, on Begin, Commit, Rollback
func AssertTxWithRetryContract(t *testing.T, opts ...TxWithRetryOption) {
	t.Helper()
	cfg := newTxWithRetryConfig(opts...)
	if len(cfg.subjects) == 0 {
		t.Fatalf("AssertTxWithRetryContract was given no subject; declare one with " +
			"TxWithRetrySubject(name, factory)")
	}
	for _, s := range cfg.subjects {
		runTxWithRetryChecks(t, cfg, s.name, s.factory, nil)
		if !cfg.withoutDouble {
			runTxWithRetryChecks(t, cfg, s.name+" through the double", s.factory,
				func(tb testing.TB, subject txwithretry.TxWithRetry) txwithretry.TxWithRetry {
					return NewTxWithRetryStub(tb, TxWithRetryStubDelegateTo(subject))
				})
		}
	}
}

// runTxWithRetryChecks runs the whole contract against one subject.
//
// wrap is applied after the subject is built and seeded, so a wrapper is
// transparent to the seed and every failure it causes is its own.
func runTxWithRetryChecks(
	t *testing.T,
	cfg *txwithretryConfig,
	label string,
	factory func() txwithretry.TxWithRetry,
	wrap func(tb testing.TB, subject txwithretry.TxWithRetry) txwithretry.TxWithRetry,
) {
	t.Helper()
	t.Run(label, func(t *testing.T) {
		t.Parallel()

		t.Run("Begin", func(t *testing.T) {
			t.Parallel()
			cfg.run(t, "Begin/smoke", "smoke", func(tb testing.TB) {
				AssertTxWithRetryBeginSmoke(tb, cfg.subject(tb, factory, wrap))
			})
			cfg.run(t, "Begin/reports a cancelled context", "reports a cancelled context", func(tb testing.TB) {
				AssertTxWithRetryBeginCancels(tb, cfg.subject(tb, factory, wrap))
			})
			cfg.run(t, "Begin/reports an expired deadline", "reports an expired deadline", func(tb testing.TB) {
				AssertTxWithRetryBeginHonoursDeadline(tb, cfg.subject(tb, factory, wrap))
			})
			cfg.run(t, "Begin/tolerates a nil context", "tolerates a nil context", func(tb testing.TB) {
				AssertTxWithRetryBeginToleratesNilContext(tb, cfg.subject(tb, factory, wrap))
			})
			for _, c := range cfg.onBegin {
				cfg.run(t, "Begin"+"/"+c.name, c.name, func(tb testing.TB) {
					c.fn(tb, cfg.subject(tb, factory, wrap))
				})
			}
		})

		t.Run("Commit", func(t *testing.T) {
			t.Parallel()
			cfg.run(t, "Commit/smoke", "smoke", func(tb testing.TB) {
				AssertTxWithRetryCommitSmoke(tb, cfg.subject(tb, factory, wrap))
			})
			cfg.run(t, "Commit/reports a cancelled context", "reports a cancelled context", func(tb testing.TB) {
				AssertTxWithRetryCommitCancels(tb, cfg.subject(tb, factory, wrap))
			})
			cfg.run(t, "Commit/reports an expired deadline", "reports an expired deadline", func(tb testing.TB) {
				AssertTxWithRetryCommitHonoursDeadline(tb, cfg.subject(tb, factory, wrap))
			})
			cfg.run(t, "Commit/tolerates a nil context", "tolerates a nil context", func(tb testing.TB) {
				AssertTxWithRetryCommitToleratesNilContext(tb, cfg.subject(tb, factory, wrap))
			})
			for _, c := range cfg.onCommit {
				cfg.run(t, "Commit"+"/"+c.name, c.name, func(tb testing.TB) {
					c.fn(tb, cfg.subject(tb, factory, wrap))
				})
			}
		})

		t.Run("Rollback", func(t *testing.T) {
			t.Parallel()
			cfg.run(t, "Rollback/smoke", "smoke", func(tb testing.TB) {
				AssertTxWithRetryRollbackSmoke(tb, cfg.subject(tb, factory, wrap))
			})
			cfg.run(t, "Rollback/reports a cancelled context", "reports a cancelled context", func(tb testing.TB) {
				AssertTxWithRetryRollbackCancels(tb, cfg.subject(tb, factory, wrap))
			})
			cfg.run(t, "Rollback/reports an expired deadline", "reports an expired deadline", func(tb testing.TB) {
				AssertTxWithRetryRollbackHonoursDeadline(tb, cfg.subject(tb, factory, wrap))
			})
			cfg.run(t, "Rollback/tolerates a nil context", "tolerates a nil context", func(tb testing.TB) {
				AssertTxWithRetryRollbackToleratesNilContext(tb, cfg.subject(tb, factory, wrap))
			})
			for _, c := range cfg.onRollback {
				cfg.run(t, "Rollback"+"/"+c.name, c.name, func(tb testing.TB) {
					c.fn(tb, cfg.subject(tb, factory, wrap))
				})
			}
		})
	})
}

// AssertTxWithRetryBeginSmoke asserts Begin survives a call with derived inputs.
//
// Fails when: Begin panics. The weakest check in this file and the one
// that catches the most — a method that panics on a derived value is one no
// other check here reaches.
func AssertTxWithRetryBeginSmoke(tb testing.TB, subject txwithretry.TxWithRetry) {
	tb.Helper()
	defer func() {
		if r := recover(); r != nil {
			tb.Fatalf("Begin panicked on a derived value (%v); supply one it "+
				"accepts through TxWithRetryWithFixture", r)
		}
	}()
	ctx := tb.Context()
	_ = subject.Begin(ctx)
}

// AssertTxWithRetryBeginCancels asserts Begin reports a cancelled context as cancelled.
//
// Fails when: Begin returns nil for a context cancelled before the
// call, or returns an error that does not answer to context.Canceled. Both
// matter and they are different defects: the first is work done that the caller
// asked not to be, the second is a caller who wrote
// `errors.Is(err, context.Canceled)` and gets false for a call that was.
//
// The error rather than merely its presence is what separates this from the
// deadline check. Asserting only that something came back makes the two one
// check written twice.
func AssertTxWithRetryBeginCancels(tb testing.TB, subject txwithretry.TxWithRetry) {
	tb.Helper()
	ctx, cancel := context.WithCancel(tb.Context())
	cancel()
	err := subject.Begin(ctx)
	testkit.ErrorIs(tb, err, context.Canceled,
		"Begin must report a cancelled context as context.Canceled")
}

// AssertTxWithRetryBeginHonoursDeadline asserts Begin reports an expired deadline as exceeded.
//
// Fails when: Begin returns nil for a context whose deadline has
// passed, or returns an error that does not answer to
// context.DeadlineExceeded. Distinct from cancellation in what the caller
// learns: a cancelled call was called off, an expired one ran out of time, and
// only the second is worth retrying.
//
// The deadline is the zero time, which is unconditionally in the past. No clock
// is read: a generated check that consults the wall clock is one whose subject
// is partly the machine it runs on.
func AssertTxWithRetryBeginHonoursDeadline(tb testing.TB, subject txwithretry.TxWithRetry) {
	tb.Helper()
	ctx, cancel := context.WithDeadline(tb.Context(), time.Time{})
	defer cancel()
	err := subject.Begin(ctx)
	testkit.ErrorIs(tb, err, context.DeadlineExceeded,
		"Begin must report an expired deadline as context.DeadlineExceeded")
}

// AssertTxWithRetryBeginToleratesNilContext asserts Begin does not panic on a nil context.
//
// Fails when: Begin panics. Returning an error is correct and
// succeeding is correct — a nil context reaches production through a caller
// that forgot one, and a panic turns that into an outage rather than a failed
// request.
func AssertTxWithRetryBeginToleratesNilContext(tb testing.TB, subject txwithretry.TxWithRetry) {
	tb.Helper()
	defer func() {
		if r := recover(); r != nil {
			tb.Errorf("Begin panicked on a nil context (%v); return an error instead", r)
		}
	}()
	//nolint:staticcheck // passing nil is the check.
	var ctx context.Context
	_ = subject.Begin(ctx)
}

// AssertTxWithRetryCommitSmoke asserts Commit survives a call with derived inputs.
//
// Fails when: Commit panics. The weakest check in this file and the one
// that catches the most — a method that panics on a derived value is one no
// other check here reaches.
func AssertTxWithRetryCommitSmoke(tb testing.TB, subject txwithretry.TxWithRetry) {
	tb.Helper()
	defer func() {
		if r := recover(); r != nil {
			tb.Fatalf("Commit panicked on a derived value (%v); supply one it "+
				"accepts through TxWithRetryWithFixture", r)
		}
	}()
	ctx := tb.Context()
	_ = subject.Commit(ctx)
}

// AssertTxWithRetryCommitCancels asserts Commit reports a cancelled context as cancelled.
//
// Fails when: Commit returns nil for a context cancelled before the
// call, or returns an error that does not answer to context.Canceled. Both
// matter and they are different defects: the first is work done that the caller
// asked not to be, the second is a caller who wrote
// `errors.Is(err, context.Canceled)` and gets false for a call that was.
//
// The error rather than merely its presence is what separates this from the
// deadline check. Asserting only that something came back makes the two one
// check written twice.
func AssertTxWithRetryCommitCancels(tb testing.TB, subject txwithretry.TxWithRetry) {
	tb.Helper()
	ctx, cancel := context.WithCancel(tb.Context())
	cancel()
	err := subject.Commit(ctx)
	testkit.ErrorIs(tb, err, context.Canceled,
		"Commit must report a cancelled context as context.Canceled")
}

// AssertTxWithRetryCommitHonoursDeadline asserts Commit reports an expired deadline as exceeded.
//
// Fails when: Commit returns nil for a context whose deadline has
// passed, or returns an error that does not answer to
// context.DeadlineExceeded. Distinct from cancellation in what the caller
// learns: a cancelled call was called off, an expired one ran out of time, and
// only the second is worth retrying.
//
// The deadline is the zero time, which is unconditionally in the past. No clock
// is read: a generated check that consults the wall clock is one whose subject
// is partly the machine it runs on.
func AssertTxWithRetryCommitHonoursDeadline(tb testing.TB, subject txwithretry.TxWithRetry) {
	tb.Helper()
	ctx, cancel := context.WithDeadline(tb.Context(), time.Time{})
	defer cancel()
	err := subject.Commit(ctx)
	testkit.ErrorIs(tb, err, context.DeadlineExceeded,
		"Commit must report an expired deadline as context.DeadlineExceeded")
}

// AssertTxWithRetryCommitToleratesNilContext asserts Commit does not panic on a nil context.
//
// Fails when: Commit panics. Returning an error is correct and
// succeeding is correct — a nil context reaches production through a caller
// that forgot one, and a panic turns that into an outage rather than a failed
// request.
func AssertTxWithRetryCommitToleratesNilContext(tb testing.TB, subject txwithretry.TxWithRetry) {
	tb.Helper()
	defer func() {
		if r := recover(); r != nil {
			tb.Errorf("Commit panicked on a nil context (%v); return an error instead", r)
		}
	}()
	//nolint:staticcheck // passing nil is the check.
	var ctx context.Context
	_ = subject.Commit(ctx)
}

// AssertTxWithRetryRollbackSmoke asserts Rollback survives a call with derived inputs.
//
// Fails when: Rollback panics. The weakest check in this file and the one
// that catches the most — a method that panics on a derived value is one no
// other check here reaches.
func AssertTxWithRetryRollbackSmoke(tb testing.TB, subject txwithretry.TxWithRetry) {
	tb.Helper()
	defer func() {
		if r := recover(); r != nil {
			tb.Fatalf("Rollback panicked on a derived value (%v); supply one it "+
				"accepts through TxWithRetryWithFixture", r)
		}
	}()
	ctx := tb.Context()
	_ = subject.Rollback(ctx)
}

// AssertTxWithRetryRollbackCancels asserts Rollback reports a cancelled context as cancelled.
//
// Fails when: Rollback returns nil for a context cancelled before the
// call, or returns an error that does not answer to context.Canceled. Both
// matter and they are different defects: the first is work done that the caller
// asked not to be, the second is a caller who wrote
// `errors.Is(err, context.Canceled)` and gets false for a call that was.
//
// The error rather than merely its presence is what separates this from the
// deadline check. Asserting only that something came back makes the two one
// check written twice.
func AssertTxWithRetryRollbackCancels(tb testing.TB, subject txwithretry.TxWithRetry) {
	tb.Helper()
	ctx, cancel := context.WithCancel(tb.Context())
	cancel()
	err := subject.Rollback(ctx)
	testkit.ErrorIs(tb, err, context.Canceled,
		"Rollback must report a cancelled context as context.Canceled")
}

// AssertTxWithRetryRollbackHonoursDeadline asserts Rollback reports an expired deadline as exceeded.
//
// Fails when: Rollback returns nil for a context whose deadline has
// passed, or returns an error that does not answer to
// context.DeadlineExceeded. Distinct from cancellation in what the caller
// learns: a cancelled call was called off, an expired one ran out of time, and
// only the second is worth retrying.
//
// The deadline is the zero time, which is unconditionally in the past. No clock
// is read: a generated check that consults the wall clock is one whose subject
// is partly the machine it runs on.
func AssertTxWithRetryRollbackHonoursDeadline(tb testing.TB, subject txwithretry.TxWithRetry) {
	tb.Helper()
	ctx, cancel := context.WithDeadline(tb.Context(), time.Time{})
	defer cancel()
	err := subject.Rollback(ctx)
	testkit.ErrorIs(tb, err, context.DeadlineExceeded,
		"Rollback must report an expired deadline as context.DeadlineExceeded")
}

// AssertTxWithRetryRollbackToleratesNilContext asserts Rollback does not panic on a nil context.
//
// Fails when: Rollback panics. Returning an error is correct and
// succeeding is correct — a nil context reaches production through a caller
// that forgot one, and a panic turns that into an outage rather than a failed
// request.
func AssertTxWithRetryRollbackToleratesNilContext(tb testing.TB, subject txwithretry.TxWithRetry) {
	tb.Helper()
	defer func() {
		if r := recover(); r != nil {
			tb.Errorf("Rollback panicked on a nil context (%v); return an error instead", r)
		}
	}()
	//nolint:staticcheck // passing nil is the check.
	var ctx context.Context
	_ = subject.Rollback(ctx)
}

// TxWithRetryOption configures a contract run.
type TxWithRetryOption func(*txwithretryConfig)

// TxWithRetrySubject declares an implementation to hold to the contract.
//
// Repeat it for every implementation that claims to satisfy the interface — an
// in-memory one, the one backed by a database, the one behind a network. Every
// check and every option is stated once and runs against all of them, which is
// what a conformance suite is for.
func TxWithRetrySubject(name string, factory func() txwithretry.TxWithRetry) TxWithRetryOption {
	return func(c *txwithretryConfig) {
		c.subjects = append(c.subjects, namedTxWithRetrySubject{name, factory})
	}
}

// TxWithRetryWithoutDouble declines the second run through TxWithRetryStub.
//
// The double run proves the stand-in is faithful, and costs a second pass over
// every check. Decline it where the double is not used.
func TxWithRetryWithoutDouble() TxWithRetryOption {
	return func(c *txwithretryConfig) { c.withoutDouble = true }
}

// TxWithRetryWithClock supplies the clock every time-reading check measures on.
//
// Defaults to the real one, so an implementation that does not take a clock
// behaves as it would have. Supply a [clock.TestClock] — the same one the
// factory builds the subject with — and a budget becomes a claim about the time
// the implementation means to spend rather than about how loaded the machine
// was.
func TxWithRetryWithClock(c clock.Clock) TxWithRetryOption {
	return func(cfg *txwithretryConfig) { cfg.clock = c }
}

// TxWithRetryWithFixture replaces the derived inputs.
func TxWithRetryWithFixture(f TxWithRetryFixture) TxWithRetryOption {
	return func(c *txwithretryConfig) { c.Fixture = f }
}

// TxWithRetrySeed replaces the derived seed.
//
// For an interface with no writer to seed itself through, which is the one
// input a conformance run cannot derive: a reader over an empty subject
// asserts nothing.
//
// Returning an error rather than swallowing one. A seed that failed quietly
// leaves every check after it asserting nothing and reporting success.
func TxWithRetrySeed(fn func(ctx context.Context, subject txwithretry.TxWithRetry) error) TxWithRetryOption {
	return func(c *txwithretryConfig) {
		c.seed = fn
	}
}

// TxWithRetryOnBegin adds a named check to Begin.
func TxWithRetryOnBegin(name string, fn TxWithRetryBeginCheck) TxWithRetryOption {
	return func(c *txwithretryConfig) {
		c.onBegin = append(c.onBegin, namedTxWithRetryBeginCheck{name, fn})
	}
}

// TxWithRetryOnCommit adds a named check to Commit.
func TxWithRetryOnCommit(name string, fn TxWithRetryCommitCheck) TxWithRetryOption {
	return func(c *txwithretryConfig) {
		c.onCommit = append(c.onCommit, namedTxWithRetryCommitCheck{name, fn})
	}
}

// TxWithRetryOnRollback adds a named check to Rollback.
func TxWithRetryOnRollback(name string, fn TxWithRetryRollbackCheck) TxWithRetryOption {
	return func(c *txwithretryConfig) {
		c.onRollback = append(c.onRollback, namedTxWithRetryRollbackCheck{name, fn})
	}
}

// TxWithRetryWithout drops generated checks by the path each reports under.
//
// For a subject that legitimately violates one. Without this the only recourse
// is to stop running the suite, which costs every other check in the file — so
// a dropped check is reported as skipped rather than silently omitted.
func TxWithRetryWithout(paths ...string) TxWithRetryOption {
	return func(c *txwithretryConfig) {
		for _, p := range paths {
			c.without[p] = struct{}{}
		}
	}
}

type namedTxWithRetryBeginCheck struct {
	name string
	fn   TxWithRetryBeginCheck
}

type namedTxWithRetryCommitCheck struct {
	name string
	fn   TxWithRetryCommitCheck
}

type namedTxWithRetryRollbackCheck struct {
	name string
	fn   TxWithRetryRollbackCheck
}

type namedTxWithRetrySubject struct {
	name    string
	factory func() txwithretry.TxWithRetry
}

type txwithretryConfig struct {
	Fixture       TxWithRetryFixture
	subjects      []namedTxWithRetrySubject
	withoutDouble bool
	clock         clock.Clock
	seed          func(ctx context.Context, subject txwithretry.TxWithRetry) error
	without       map[string]struct{}
	onBegin       []namedTxWithRetryBeginCheck
	onCommit      []namedTxWithRetryCommitCheck
	onRollback    []namedTxWithRetryRollbackCheck
}

func newTxWithRetryConfig(opts ...TxWithRetryOption) *txwithretryConfig {
	c := &txwithretryConfig{
		Fixture: DefaultTxWithRetryFixture(),
		clock:   clock.RealClock(),
		without: map[string]struct{}{},
	}
	for _, o := range opts {
		o(c)
	}
	return c
}

// subject builds a fresh implementation and seeds it, so every check runs
// against state no other check touched.
func (c *txwithretryConfig) subject(
	tb testing.TB,
	factory func() txwithretry.TxWithRetry,
	wrap func(tb testing.TB, subject txwithretry.TxWithRetry) txwithretry.TxWithRetry,
) txwithretry.TxWithRetry {
	tb.Helper()
	s := factory()
	if c.seed != nil {
		// Reported rather than ignored. A seed that silently failed would leave
		// every check after it running against an empty subject — passing, and
		// asserting nothing.
		//
		// Which seed ran decides what the message can usefully say. The derived
		// one fails for one of two reasons and both are fixable from here, so it
		// names them. A seed the caller supplied is theirs, and telling them to
		// supply one is advice they already took.
		if err := c.seed(tb.Context(), s); err != nil {
			tb.Fatalf("the seed supplied through TxWithRetrySeed failed: %v", err)
		}
	}
	if wrap != nil {
		s = wrap(tb, s)
	}
	return s
}

// run executes one check unless it was dropped, reporting the drop rather than
// silently omitting it.
func (c *txwithretryConfig) run(t *testing.T, path, name string, fn func(tb testing.TB)) {
	t.Helper()
	if _, dropped := c.without[path]; dropped {
		t.Run(name, func(t *testing.T) {
			t.Skipf("dropped through TxWithRetryWithout(%q)", path)
		})
		return
	}
	t.Run(name, func(t *testing.T) {
		t.Parallel()
		fn(t)
	})
}

// testkit: end of generated content.
// testkit:provenance d85b907c60ef88e947120eb9967f394667c5f8c4c24df234d9ee78375bd679fb
