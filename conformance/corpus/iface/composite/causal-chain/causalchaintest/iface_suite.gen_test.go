// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/composite/causal-chain/iface.go
// Plugins:   golang 1.0.0, suite 1.3.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package causalchaintest_test

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit"
	causalchain "go.thesmos.sh/testkit/conformance/corpus/iface/composite/causal-chain"
	"go.thesmos.sh/testkit/conformance/corpus/iface/composite/causal-chain/causalchaintest"
)

// Every generated check for Log, driven against a stand-in built
// to violate it.
//
//	Proved:   8 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertLogAppendSmokeCanFail holds AssertLogAppendSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertLogAppendSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := causalchaintest.DefaultLogFixture()
	subject := causalchaintest.NewLogStub(t,
		causalchaintest.WithLogAppend(func(context.Context, causalchain.Entry) error {
			panic("Log.Append: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			causalchaintest.AssertLogAppendSmoke(tb, subject, fixture.E)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertLogAppendCancelsCanFail holds AssertLogAppendCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertLogAppendCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := causalchaintest.DefaultLogFixture()
	subject := causalchaintest.NewLogStub(t,
		causalchaintest.WithLogAppend(func(context.Context, causalchain.Entry) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			causalchaintest.AssertLogAppendCancels(tb, subject, fixture.E)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertLogAppendHonoursDeadlineCanFail holds AssertLogAppendHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertLogAppendHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := causalchaintest.DefaultLogFixture()
	subject := causalchaintest.NewLogStub(t,
		causalchaintest.WithLogAppend(func(context.Context, causalchain.Entry) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			causalchaintest.AssertLogAppendHonoursDeadline(tb, subject, fixture.E)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertLogAppendToleratesNilContextCanFail holds AssertLogAppendToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertLogAppendToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := causalchaintest.DefaultLogFixture()
	subject := causalchaintest.NewLogStub(t,
		causalchaintest.WithLogAppend(func(context.Context, causalchain.Entry) error {
			panic("Log.Append: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			causalchaintest.AssertLogAppendToleratesNilContext(tb, subject, fixture.E)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertLogReplaySmokeCanFail holds AssertLogReplaySmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertLogReplaySmokeCanFail(t *testing.T) {
	t.Parallel()

	subject := causalchaintest.NewLogStub(t,
		causalchaintest.WithLogReplay(func(context.Context) ([]causalchain.Entry, error) {
			panic("Log.Replay: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			causalchaintest.AssertLogReplaySmoke(tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertLogReplayCancelsCanFail holds AssertLogReplayCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertLogReplayCancelsCanFail(t *testing.T) {
	t.Parallel()

	subject := causalchaintest.NewLogStub(t,
		causalchaintest.WithLogReplay(func(context.Context) ([]causalchain.Entry, error) {
			var r0 []causalchain.Entry
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			causalchaintest.AssertLogReplayCancels(tb, subject)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertLogReplayHonoursDeadlineCanFail holds AssertLogReplayHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertLogReplayHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	subject := causalchaintest.NewLogStub(t,
		causalchaintest.WithLogReplay(func(context.Context) ([]causalchain.Entry, error) {
			var r0 []causalchain.Entry
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			causalchaintest.AssertLogReplayHonoursDeadline(tb, subject)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertLogReplayToleratesNilContextCanFail holds AssertLogReplayToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertLogReplayToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	subject := causalchaintest.NewLogStub(t,
		causalchaintest.WithLogReplay(func(context.Context) ([]causalchain.Entry, error) {
			panic("Log.Replay: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			causalchaintest.AssertLogReplayToleratesNilContext(tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance 3e16c7bc71bce1507b8ab1aa1c1cbcaf54e1831546c13dbb187b570d64737c95
