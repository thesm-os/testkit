// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/composite/paginated-reader/iface.go
// Plugins:   golang 1.0.0, suite 1.0.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package paginatedreadertest_test

import (
	"context"
	"testing"

	"go.thesmos.sh/testkit"
	paginatedreader "go.thesmos.sh/testkit/conformance/corpus/iface/composite/paginated-reader"
	"go.thesmos.sh/testkit/conformance/corpus/iface/composite/paginated-reader/paginatedreadertest"
)

// Every generated check for PaginatedReader, driven against a stand-in built
// to violate it.
//
//	Proved:   5 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertPaginatedReaderPageSmokeCanFail holds AssertPaginatedReaderPageSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertPaginatedReaderPageSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := paginatedreadertest.DefaultPaginatedReaderFixture()
	subject := paginatedreadertest.NewPaginatedReaderStub(t,
		paginatedreadertest.WithPaginatedReaderPage(func(context.Context, int) ([]paginatedreader.Value, int, error) {
			panic("PaginatedReader.Page: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			paginatedreadertest.AssertPaginatedReaderPageSmoke(tb, subject, fixture.Cursor)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertPaginatedReaderPageCancelsCanFail holds AssertPaginatedReaderPageCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertPaginatedReaderPageCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := paginatedreadertest.DefaultPaginatedReaderFixture()
	subject := paginatedreadertest.NewPaginatedReaderStub(t,
		paginatedreadertest.WithPaginatedReaderPage(func(context.Context, int) ([]paginatedreader.Value, int, error) {
			var items []paginatedreader.Value
			var next int
			return items, next, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			paginatedreadertest.AssertPaginatedReaderPageCancels(tb, subject, fixture.Cursor)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertPaginatedReaderPageHonoursDeadlineCanFail holds AssertPaginatedReaderPageHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertPaginatedReaderPageHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := paginatedreadertest.DefaultPaginatedReaderFixture()
	subject := paginatedreadertest.NewPaginatedReaderStub(t,
		paginatedreadertest.WithPaginatedReaderPage(func(context.Context, int) ([]paginatedreader.Value, int, error) {
			var items []paginatedreader.Value
			var next int
			return items, next, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			paginatedreadertest.AssertPaginatedReaderPageHonoursDeadline(tb, subject, fixture.Cursor)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertPaginatedReaderPageToleratesNilContextCanFail holds AssertPaginatedReaderPageToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertPaginatedReaderPageToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := paginatedreadertest.DefaultPaginatedReaderFixture()
	subject := paginatedreadertest.NewPaginatedReaderStub(t,
		paginatedreadertest.WithPaginatedReaderPage(func(context.Context, int) ([]paginatedreader.Value, int, error) {
			panic("PaginatedReader.Page: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			paginatedreadertest.AssertPaginatedReaderPageToleratesNilContext(tb, subject, fixture.Cursor)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertPaginatedReaderPageZeroOnErrorCanFail holds AssertPaginatedReaderPageZeroOnError to being able to fail.
//
// The stand-in succeeds for the input the check chose so it would miss. A check
// that let that pass would never reach the comparison it exists to make, and
// would report success without having compared anything.
func TestAssertPaginatedReaderPageZeroOnErrorCanFail(t *testing.T) {
	t.Parallel()

	fixture := paginatedreadertest.DefaultPaginatedReaderFixture()
	subject := paginatedreadertest.NewPaginatedReaderStub(t,
		paginatedreadertest.WithPaginatedReaderPage(func(context.Context, int) ([]paginatedreader.Value, int, error) {
			var items []paginatedreader.Value
			var next int
			return items, next, nil
		}))

	got := testkit.Rejects(t, "a method that succeeds for an input it should miss",
		func(tb testing.TB) {
			tb.Helper()
			paginatedreadertest.AssertPaginatedReaderPageZeroOnError(tb, subject, fixture.CursorOther)
		})
	testkit.Assert(t, got).Contains("supply inputs it misses",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance c0882f2403c835edb581025ad503ab65c57bb537838c90857c2ba4cbd9370c4b
