// Code generated by testkit. DO NOT EDIT.
//
// Source:    corpus/iface/composite/batched-mixins/iface.go
// Plugins:   golang 1.0.0, suite 1.1.0, backend.golang 1.0.0
// Command:   testkit run ./corpus/...

package batchedmixinstest_test

import (
	"context"
	"errors"
	"testing"

	"go.thesmos.sh/testkit"
	"go.thesmos.sh/testkit/conformance/corpus/iface/composite/batched-mixins/batchedmixinstest"
)

// Every generated check for Batched, driven against a stand-in built
// to violate it.
//
//	Proved:   14 checks
//
// Each guard asserts the *reason* the check rejected, not merely that it did. A
// stand-in that failed for some unrelated reason — a nil map, a closed channel
// — would satisfy a guard reading only the fact, which is the vacuity this file
// exists to catch, one level up.

// TestAssertBatchedPutSmokeCanFail holds AssertBatchedPutSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertBatchedPutSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := batchedmixinstest.DefaultBatchedFixture()
	subject := batchedmixinstest.NewBatchedStub(t,
		batchedmixinstest.WithBatchedPut(func(context.Context, string, string) error {
			panic("Batched.Put: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			batchedmixinstest.AssertBatchedPutSmoke(tb, subject, fixture.Key, fixture.Value)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertBatchedPutCancelsCanFail holds AssertBatchedPutCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertBatchedPutCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := batchedmixinstest.DefaultBatchedFixture()
	subject := batchedmixinstest.NewBatchedStub(t,
		batchedmixinstest.WithBatchedPut(func(context.Context, string, string) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			batchedmixinstest.AssertBatchedPutCancels(tb, subject, fixture.Key, fixture.Value)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertBatchedPutHonoursDeadlineCanFail holds AssertBatchedPutHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertBatchedPutHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := batchedmixinstest.DefaultBatchedFixture()
	subject := batchedmixinstest.NewBatchedStub(t,
		batchedmixinstest.WithBatchedPut(func(context.Context, string, string) error {
			return nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			batchedmixinstest.AssertBatchedPutHonoursDeadline(tb, subject, fixture.Key, fixture.Value)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertBatchedPutToleratesNilContextCanFail holds AssertBatchedPutToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertBatchedPutToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := batchedmixinstest.DefaultBatchedFixture()
	subject := batchedmixinstest.NewBatchedStub(t,
		batchedmixinstest.WithBatchedPut(func(context.Context, string, string) error {
			panic("Batched.Put: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			batchedmixinstest.AssertBatchedPutToleratesNilContext(tb, subject, fixture.Key, fixture.Value)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertBatchedPutSurvivesConcurrentCallersCanFail holds AssertBatchedPutSurvivesConcurrentCallers to being able to fail.
//
// The stand-in panics under any caller, so every one of the check's
// goroutines meets the recover arm this proves.
func TestAssertBatchedPutSurvivesConcurrentCallersCanFail(t *testing.T) {
	t.Parallel()

	fixture := batchedmixinstest.DefaultBatchedFixture()
	subject := batchedmixinstest.NewBatchedStub(t,
		batchedmixinstest.WithBatchedPut(func(context.Context, string, string) error {
			panic("violate: unsynchronized state")
		}))

	got := testkit.Rejects(t, "a method that panics the moment callers overlap",
		func(tb testing.TB) {
			tb.Helper()
			batchedmixinstest.AssertBatchedPutSurvivesConcurrentCallers(tb, subject, fixture.Key, fixture.Value)
		})
	testkit.Assert(t, got).Contains("panicked under concurrent callers",
		"and rejects it for the reason the check is about")
}

// TestAssertBatchedReadSmokeCanFail holds AssertBatchedReadSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertBatchedReadSmokeCanFail(t *testing.T) {
	t.Parallel()

	fixture := batchedmixinstest.DefaultBatchedFixture()
	subject := batchedmixinstest.NewBatchedStub(t,
		batchedmixinstest.WithBatchedRead(func(context.Context, string) (string, error) {
			panic("Batched.Read: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			batchedmixinstest.AssertBatchedReadSmoke(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertBatchedReadCancelsCanFail holds AssertBatchedReadCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertBatchedReadCancelsCanFail(t *testing.T) {
	t.Parallel()

	fixture := batchedmixinstest.DefaultBatchedFixture()
	subject := batchedmixinstest.NewBatchedStub(t,
		batchedmixinstest.WithBatchedRead(func(context.Context, string) (string, error) {
			var r0 string
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			batchedmixinstest.AssertBatchedReadCancels(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertBatchedReadHonoursDeadlineCanFail holds AssertBatchedReadHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertBatchedReadHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	fixture := batchedmixinstest.DefaultBatchedFixture()
	subject := batchedmixinstest.NewBatchedStub(t,
		batchedmixinstest.WithBatchedRead(func(context.Context, string) (string, error) {
			var r0 string
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			batchedmixinstest.AssertBatchedReadHonoursDeadline(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertBatchedReadToleratesNilContextCanFail holds AssertBatchedReadToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertBatchedReadToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	fixture := batchedmixinstest.DefaultBatchedFixture()
	subject := batchedmixinstest.NewBatchedStub(t,
		batchedmixinstest.WithBatchedRead(func(context.Context, string) (string, error) {
			panic("Batched.Read: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			batchedmixinstest.AssertBatchedReadToleratesNilContext(tb, subject, fixture.Key)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// TestAssertBatchedReadZeroOnErrorCanFail holds AssertBatchedReadZeroOnError to being able to fail.
//
// The stand-in refuses the call and answers a value that looks like a result
// anyway. A check that let that pass would leave a caller who checks the error
// and a caller who checks the value disagreeing about what happened — the
// claim's own violation, not a stand-in that merely succeeds.
func TestAssertBatchedReadZeroOnErrorCanFail(t *testing.T) {
	t.Parallel()

	fixture := batchedmixinstest.DefaultBatchedFixture()
	subject := batchedmixinstest.NewBatchedStub(t,
		batchedmixinstest.WithBatchedRead(func(context.Context, string) (string, error) {
			return "test-r0", errors.New("violate: refused with a believable value")
		}))

	got := testkit.Rejects(t, "a method answering a believable value beside its error",
		func(tb testing.TB) {
			tb.Helper()
			batchedmixinstest.AssertBatchedReadZeroOnError(tb, subject, fixture.KeyOther)
		})
	testkit.Assert(t, got).Contains("must return the zero value",
		"and rejects it for the reason the check is about")
}

// TestAssertBatchedListSmokeCanFail holds AssertBatchedListSmoke to being able to fail.
//
// The stand-in panics on the value the fixture derived. A check that let that
// pass would leave a panicking method reported as a crashed run rather than as
// a failed assertion — with no line naming the method that did it.
func TestAssertBatchedListSmokeCanFail(t *testing.T) {
	t.Parallel()

	subject := batchedmixinstest.NewBatchedStub(t,
		batchedmixinstest.WithBatchedList(func(context.Context) ([]string, error) {
			panic("Batched.List: violating the smoke check")
		}))

	got := testkit.Rejects(t, "a method that panics on a derived value",
		func(tb testing.TB) {
			tb.Helper()
			batchedmixinstest.AssertBatchedListSmoke(tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a derived value",
		"and rejects it for the reason the check is about")
}

// TestAssertBatchedListCancelsCanFail holds AssertBatchedListCancels to being able to fail.
//
// The stand-in answers a cancelled context as though nothing were wrong. A
// check that let that pass would report success for a subject doing work its
// caller had already abandoned.
func TestAssertBatchedListCancelsCanFail(t *testing.T) {
	t.Parallel()

	subject := batchedmixinstest.NewBatchedStub(t,
		batchedmixinstest.WithBatchedList(func(context.Context) ([]string, error) {
			var r0 []string
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for a cancelled context",
		func(tb testing.TB) {
			tb.Helper()
			batchedmixinstest.AssertBatchedListCancels(tb, subject)
		})
	testkit.Assert(t, got).Contains("must report a cancelled context",
		"and rejects it for the reason the check is about")
}

// TestAssertBatchedListHonoursDeadlineCanFail holds AssertBatchedListHonoursDeadline to being able to fail.
//
// The stand-in answers an expired deadline as though nothing were wrong. A
// check that let that pass would report success for a subject that keeps
// working past the point its caller stopped waiting.
func TestAssertBatchedListHonoursDeadlineCanFail(t *testing.T) {
	t.Parallel()

	subject := batchedmixinstest.NewBatchedStub(t,
		batchedmixinstest.WithBatchedList(func(context.Context) ([]string, error) {
			var r0 []string
			return r0, nil
		}))

	got := testkit.Rejects(t, "a method that reports nothing for an expired deadline",
		func(tb testing.TB) {
			tb.Helper()
			batchedmixinstest.AssertBatchedListHonoursDeadline(tb, subject)
		})
	testkit.Assert(t, got).Contains("must report an expired deadline",
		"and rejects it for the reason the check is about")
}

// TestAssertBatchedListToleratesNilContextCanFail holds AssertBatchedListToleratesNilContext to being able to fail.
//
// The stand-in panics rather than reporting. A check that let that pass would
// leave a caller who forgot a context taking the process down instead of
// getting an error back.
func TestAssertBatchedListToleratesNilContextCanFail(t *testing.T) {
	t.Parallel()

	subject := batchedmixinstest.NewBatchedStub(t,
		batchedmixinstest.WithBatchedList(func(context.Context) ([]string, error) {
			panic("Batched.List: violating the nil-context check")
		}))

	got := testkit.Rejects(t, "a method that panics on a nil context",
		func(tb testing.TB) {
			tb.Helper()
			batchedmixinstest.AssertBatchedListToleratesNilContext(tb, subject)
		})
	testkit.Assert(t, got).Contains("panicked on a nil context",
		"and rejects it for the reason the check is about")
}

// testkit: end of generated content.
// testkit:provenance 75c1baed695581ca0b460e6cc1d285891d27edcb892367b0c59cd3bee9c52346
